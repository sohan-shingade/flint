# Codemap: `flint.api`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.api.__init__`

### `flint.api.main`
_Flint FastAPI application._
- imports: `..collector.service`, `..config`, `..paper.engine`, `..paper.price_sources`, `..paper.price_ticker`, `..store`, `.routes`, `.routes.backtest`, `.routes.live`, `.routes.system`, `.websocket`, `flint.migration`
- `async def lifespan(app: FastAPI)` — Startup: init shared store and collector. Shutdown: clean up.
- `def health()`
- `def capabilities_root(request: Request)` — Phase 4 T4.6 — alias for GET /api/v1/system/capabilities at root.
- `async def websocket_paper(websocket: WebSocket, session_id: str)` — D-4.3-websocket: per-session paper-trading stream.
- `async def websocket_live(websocket: WebSocket, session_id: str)` — D-4.3-websocket: per-session live-trading stream.
- `async def websocket_endpoint(websocket: WebSocket, channel: str='all')` — Legacy generic-channel endpoint. New clients should use the

### `flint.api.websocket`
_WebSocket connection manager for real-time streaming._
- `class ConnectionManager` — Manages WebSocket connections, broadcasts events by channel,
    - `def __init__(self)`
    - `async def connect(self, websocket: WebSocket, channel: str='all', since_seq: Optional[int]=None) -> None` — Accept a WebSocket connection. When `since_seq` is set, replay
    - `def disconnect(self, websocket: WebSocket, channel: str='all') -> None`
    - `async def broadcast(self, channel: str, data: dict) -> int` — Broadcast a message to all connections on a channel and the
    - `async def ping(self, channel: str) -> int` — Send a heartbeat to all subscribers of `channel`. Returns
    - `def connection_count(self) -> int`
    - `def channels(self) -> Set[str]`
    - `def buffered_count(self, channel: str) -> int`
