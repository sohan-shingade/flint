# Codemap: `flint.api`

> source git hash: `ed058a8f1c5615fb91d76512b05ee6bcf7203bdd`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.api.__init__`
_api — FastAPI REST + WebSocket surface; delegates to services/ only (§12, §17)._
- imports: `.app`

### `flint.api.app`
_The REST + WebSocket API — the only channel between any UI and the backend (§12)._
- imports: `.schemas`, `.security`, `flint.adapters`, `flint.data`, `flint.data.ranges`, `flint.ports`, `flint.services`, `flint.strategy`, `flint.venues`, `flint.venues.hyperliquid`
- `class Deps` — The composed services + security context an app instance serves.
- `def create_app(*, user_data: UserDataPort | None=None, data: DataManager | None=None, job_runner: JobRunnerPort | None=None, tenant: TenantContext | None=None, token: str | None=None, allowed_origins: frozenset[str] | None=None, secrets: SecretsPort | None=None, live_client_factory=None) -> FastAPI` — Build the API app with its services composed and security wired.

### `flint.api.schemas`
_Request bodies for the REST surface (§12)._
- `class RangeModel(BaseModel)`
- `class BacktestBody(BaseModel)`
- `class SourceBacktestBody(BaseModel)` — A user-source backtest (§13.2): ``source`` replaces the template name.
- `class DataPullBody(BaseModel)`
- `class AlertBody(BaseModel)`

### `flint.api.security`
_Local API security — this server executes strategy code (§12)._
- imports: `flint.services.errors`
- `def check_token(conn: HTTPConnection, expected: str) -> None` — Raise :class:`AuthError` unless the connection carries the session token.
- `def check_origin(conn: HTTPConnection, allowed: frozenset[str]) -> None` — Raise :class:`OriginError` for a disallowed browser Origin (else allow).
- `def generate_token() -> str` — A fresh per-session bearer token (URL-safe, ~256 bits).
