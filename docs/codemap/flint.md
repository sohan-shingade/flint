# Codemap: `flint`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.__init__`
_Flint — Solana backtesting framework._

### `flint.cli`
_Flint CLI — command-line interface for backtesting, optimization, and trading._
- imports: `flint.api.routes.backtest`, `flint.backtest.calibration`, `flint.backtest.engine`, `flint.backtest.parity`, `flint.config`, `flint.execution.drift_live`, `flint.models`, `flint.optimization.optimizer`, `flint.providers.birdeye`, `flint.providers.ccxt_provider`, `flint.providers.drift_candles`, `flint.providers.drift_s3`, `flint.providers.helius`, `flint.providers.open_interest`, `flint.providers.orca`, `flint.providers.pyth`, `flint.providers.pyth_candles`, `flint.providers.raydium`, `flint.providers.registry`, `flint.store`, `flint.strategy`, `flint.strategy.base`, `flint.strategy.loader`
- `def init(days: int=typer.Option(90, help='Days of data to backfill'), market: str=typer.Option('SOL-PERP', help='Primary market to backfill'), skip_demo: bool=typer.Option(False, '--skip-demo', help='Skip running the demo backtest'))` — Scaffold a Flint project — create config, backfill data, run sample backtest.
- `def backtest(strategy_file: str=typer.Argument(..., help='Path to strategy .py file'), market: str=typer.Option('SOL-PERP', '--market', '-m', help='Market to backtest'), resolution: int=typer.Option(3600, '--resolution', '-r', help='Candle resolution in seconds'), start: Optional[str]=typer.Option(None, '--start', '-s', help='Start date YYYY-MM-DD'), end: Optional[str]=typer.Option(None, '--end', '-e', help='End date YYYY-MM-DD'), period: Optional[str]=typer.Option(None, '--period', '-p', help='Period like 30d, 90d, 1y'), capital: float=typer.Option(10000, '--capital', '-c', help='Initial capital'), fee_rate: float=typer.Option(0.0005, '--fee', help='Fee rate (e.g., 0.0005 for 5bps)'))` — Run a backtest on a strategy file.
- `def optimize(strategy_file: str=typer.Argument(..., help='Path to strategy .py file'), market: str=typer.Option('SOL-PERP', '--market', '-m'), resolution: int=typer.Option(3600, '--resolution', '-r'), start: Optional[str]=typer.Option(None, '--start', '-s'), end: Optional[str]=typer.Option(None, '--end', '-e'), period: Optional[str]=typer.Option('90d', '--period', '-p'), metric: str=typer.Option('sharpe_ratio', '--metric', help='Metric to optimize'), trials: int=typer.Option(50, '--trials', '-n', help='Number of optimization trials'), capital: float=typer.Option(10000, '--capital', '-c'))` — Optimize strategy parameters using Optuna.
- `def serve(host: str=typer.Option('127.0.0.1', '--host'), port: int=typer.Option(8000, '--port'), dev: bool=typer.Option(False, '--dev', help='Run in dev mode (API only, no UI build)'))` — Start the Flint API server + UI.
- `def data_download(market: Optional[List[str]]=typer.Option(None, '--market', '-m', help='Market(s) to download (repeatable)'), days: int=typer.Option(365, '--days', '-d'), resolution: int=typer.Option(3600, '--resolution', '-r'), end_date: Optional[str]=typer.Option(None, '--end-date', help='End date YYYY-MM-DD'))` — Download historical data from Drift S3.
- `def data_status()` — Show data coverage in the local database.
- `def data_exchanges()` — List supported exchanges (via CCXT).
- `def data_markets(exchange: str=typer.Argument(..., help='Exchange name (e.g. binance, bybit, okx)'), market_type: str=typer.Option('swap', '--type', '-t', help='Market type: swap, spot, future, option'), quote: str=typer.Option('USDT', '--quote', '-q', help='Quote currency filter'), limit: int=typer.Option(50, '--limit', '-n', help='Max markets to display'))` — List available markets on an exchange (via CCXT).
- `def new_strategy(name: str=typer.Argument('my_strategy', help='Strategy name'), v2: bool=typer.Option(False, '--v2', help='Use v2 ExecutionContext API'))` — Scaffold a new strategy file.
- `def live(strategy_file: str=typer.Argument(..., help='Path to strategy .py file'), market: str=typer.Option('SOL-PERP', '--market', '-m'), paper: bool=typer.Option(True, '--paper/--real', help='Paper trading (default) or real'), capital: float=typer.Option(10000, '--capital', '-c'), private_key: Optional[str]=typer.Option(None, '--key', help='Base58 private key (or set FLINT_PRIVATE_KEY)'), rpc_url: Optional[str]=typer.Option(None, '--rpc', help='Solana RPC URL'))` — Run a strategy live (paper or real trading on Drift).
- `def provider_status()` — Show status of all data providers.
- `def provider_list()` — List all data providers (alias for status).
- `def provider_enable(name: str=typer.Argument(..., help='Provider name to enable'), api_key: Optional[str]=typer.Option(None, '--api-key', help='API key (appended to .env)'))` — Enable a data provider in flint.yaml.
- `def provider_disable(name: str=typer.Argument(..., help='Provider name to disable'))` — Disable a data provider in flint.yaml.
- `def parity(strategy: str=typer.Argument(..., help='Strategy name (e.g. momentum)'), market: str=typer.Option('SOL-PERP', help='Market to test'), start: str=typer.Option(..., help='Start date (YYYY-MM-DD)'), end: str=typer.Option(..., help='End date (YYYY-MM-DD)'), capital: float=typer.Option(10000.0, help='Initial capital'), fee_rate: float=typer.Option(0.0005, help='Fee rate'))` — Run backtest-vs-paper parity test.
- `def calibrate(venue: str=typer.Argument(..., help='Venue to calibrate (e.g. drift, hyperliquid)'), market: str=typer.Option('SOL-PERP', help='Market to calibrate'), lookback: int=typer.Option(30, help='Days of fill data to use'), dry_run: bool=typer.Option(False, '--dry-run', help='Print report without writing to config'))` — Calibrate slippage impact model from live fill data.
- `def main()`

### `flint.config`
_Centralized configuration for Flint._
- `class FlintConfig(BaseSettings)` — Flint platform configuration.
- `def load_config() -> FlintConfig` — Load config with YAML settings as initial values, overridden by env vars.
- `def default_venue() -> str` — Resolve the default trading venue.

### `flint.exceptions`
_Flint exception hierarchy._
- `class FlintError(Exception)` — Base exception for all Flint errors.
- `class StoreError(FlintError)` — Database/store operation failed.
- `class ProviderError(FlintError)` — Data provider operation failed.
- `class StrategyError(FlintError)` — Strategy loading or execution error.
- `class ConfigError(FlintError)` — Configuration validation error.
- `class BacktestError(FlintError)` — Backtest execution error.

### `flint.indicators`
_Technical indicators for strategy development._
- imports: `.models`
- `def closes(history: List[Candle], period: Optional[int]=None) -> np.ndarray` — Extract close prices as numpy array.
- `def highs(history: List[Candle], period: Optional[int]=None) -> np.ndarray` — Extract high prices as numpy array.
- `def lows(history: List[Candle], period: Optional[int]=None) -> np.ndarray` — Extract low prices as numpy array.
- `def volumes(history: List[Candle], period: Optional[int]=None) -> np.ndarray` — Extract volumes as numpy array.
- `def sma(history: List[Candle], period: int) -> float` — Simple Moving Average of close prices.
- `def ema(history: List[Candle], period: int) -> float` — Exponential Moving Average of close prices.
- `def wma(history: List[Candle], period: int) -> float` — Weighted Moving Average — recent prices weighted more.
- `def rsi(history: List[Candle], period: int=14) -> float` — Relative Strength Index (0-100).
- `def stochastic(history: List[Candle], period: int=14) -> Tuple[float, float]` — Stochastic oscillator. Returns (%K, %D).
- `def macd(history: List[Candle], fast: int=12, slow: int=26, signal: int=9) -> Tuple[float, float, float]` — MACD indicator. Returns (macd_line, signal_line, histogram).
- `def bollinger(history: List[Candle], period: int=20, num_std: float=2.0) -> Tuple[float, float, float]` — Bollinger Bands. Returns (upper, middle, lower).
- `def bollinger_width(history: List[Candle], period: int=20, num_std: float=2.0) -> float` — Bollinger Band width as percentage of middle band.
- `def atr(history: List[Candle], period: int=14) -> float` — Average True Range — measures volatility.
- `def volatility(history: List[Candle], period: int=20) -> float` — Annualized volatility of returns.
- `def vwap(history: List[Candle], period: int=20) -> float` — Volume-Weighted Average Price.
- `def volume_ratio(history: List[Candle], period: int=20) -> float` — Current volume / average volume ratio. > 1.0 = above average.
- `def roc(history: List[Candle], period: int=12) -> float` — Rate of Change (percentage).
- `def adx(history: List[Candle], period: int=14) -> float` — Average Directional Index — trend strength (0-100).
- `def z_score(history: List[Candle], period: int=20) -> float` — Z-score of current price vs rolling mean.
- `def highest_high(history: List[Candle], period: int) -> float` — Highest high in the last N candles.
- `def lowest_low(history: List[Candle], period: int) -> float` — Lowest low in the last N candles.

### `flint.mcp_server`
_Flint MCP Server — exposes Flint's trading tools to AI models._
- imports: `flint.analytics.correlation`, `flint.collector.tasks`, `flint.config`, `flint.optimization.optimizer`, `flint.portfolio.event_log`, `flint.portfolio.replay`, `flint.portfolio.snapshots`, `flint.providers.coingecko`, `flint.providers.drift_candles`, `flint.providers.drift_s3`, `flint.services`, `flint.services.backtest`, `flint.services.data`, `flint.store`, `flint.strategy`
- `def run_backtest(market: str='SOL-PERP', strategy: str='ma_crossover', start_date: str='2025-01-01', end_date: str='2025-06-01', initial_capital: float=10000.0, fee_rate: float=0.001, resolution_s: int=3600, fast_period: int=10, slow_period: int=30, code: str='') -> str` — Run a backtest on Solana market data. Returns PnL, Sharpe ratio, win rate, and trade details.
- `def list_strategies() -> str` — List all 20 built-in trading strategies with their parameters and categories.
- `def start_paper_trading(market: str='SOL-PERP', strategy: str='rsi_macd_combo', initial_capital: float=10000.0, code: str='', max_drawdown_pct: float=0.15) -> str` — Start a paper trading session. The strategy runs live on real market data with simulated fills.
- `def stop_paper_trading(session_id: str) -> str` — Stop a paper trading session gracefully. Closes positions and saves state.
- `def get_paper_sessions() -> str` — List all paper trading sessions with their current status, equity, and trade count.
- `def get_paper_status(session_id: str) -> str` — Get detailed status of a paper trading session including positions and equity history.
- `def list_journal_runs(limit: int=20) -> str` — List past backtest runs saved in the journal. Shows strategy, market, PnL, Sharpe, and more.
- `def compare_runs(run_ids: str) -> str` — Compare multiple backtest runs side-by-side. Shows metrics diff.
- `def replay_summary(session_id: str) -> str` — Show replay-log summary for a backtest or paper session: total
- `def replay_state(session_id: str, target_ts: int, initial_capital: float=10000.0) -> str` — Replay-fold the portfolio event log up to `target_ts` and
- `def list_replay_events(session_id: str, since: int=-1, limit: int=50) -> str` — Page through portfolio events for a session in seq order.
- `def get_candles(market: str='SOL-PERP', resolution_s: int=3600, start_date: str='', end_date: str='', limit: int=100) -> str` — Get OHLCV candle data for a market. Returns price and volume history.
- `def download_market_data(market: str='SOL-PERP', days: int=90, resolution_s: int=3600, funding_venues: str='hyperliquid,okx,bybit,dydx,gateio,bitget') -> str` — Download historical market data from Hyperliquid and funding rates from multiple venues.
- `def list_available_markets() -> str` — List all markets available for download — Solana perpetuals, spot, and CoinGecko spot.
- `def list_local_markets() -> str` — Show what market data is cached in the local database.
- `def get_funding_rates(market: str='SOL-PERP', venue: str='', limit: int=50) -> str` — Get funding rate history for a perpetual market, grouped by venue.
- `def get_open_interest(market: str='SOL-PERP') -> str` — Get open interest data for a perpetual market.
- `def get_correlation(markets: str='SOL-PERP,BTC-PERP,ETH-PERP', resolution_s: int=3600) -> str` — Compute cross-market correlation matrix from historical returns.
- `def get_data_freshness() -> str` — Check how fresh the data is across all providers and markets.
- `def optimize_strategy(market: str='SOL-PERP', strategy: str='ma_crossover', start_date: str='2025-01-01', end_date: str='2025-06-01', metric: str='sharpe_ratio', trials: int=30, resolution_s: int=3600) -> str` — Run hyperparameter optimization using Optuna to find the best strategy parameters.
- `def flint_guide() -> str` — Flint platform overview and usage guide for AI models.
- `def flint_markets() -> str` — Current list of available markets with types.

### `flint.migration`
_Auto-migration for Pyth price data._
- imports: `.providers.pyth_candles`
- `def run_pyth_migration(store) -> dict` — Migrate existing markets to Pyth candle data.

### `flint.models`
_Core data models._
- `class Signal(enum.Enum)`
- `class Side(enum.Enum)`
- `class OrderType(enum.Enum)`
- `class OrderStatus(enum.Enum)`
- `class OrderState(enum.Enum)` — Full lifecycle state for live/on-chain orders.
- `class TimeInForce(str, enum.Enum)`
- `class Candle`
- `class Trade`
- `class Position`
    - `def side(self) -> Side`
    - `def close(self, price: float, ts: int) -> float`
- `class BacktestResult`
- `class AccountState` — Snapshot of account value.
- `class PositionInfo` — Read-only view of an open position.
- `class OrderLeg` — A single leg of a cross-venue order group.
- `class LegGroup` — A group of paired orders across venues.
- `class LegGroupResult` — Result of a paired leg group submission.
- `class FundingRate`
- `class BorrowSnapshot` — Jupiter Perps borrow rate snapshot.
- `class OrderbookLevel`
- `class OrderbookSnapshot`
- `class MarketInfo` — Protocol-level market metadata.
- `class PoolState` — AMM pool snapshot for arb detection.
    - `def price_a_in_b(self) -> float`
    - `def price_b_in_a(self) -> float`
- `class Order`
- `class Fill`
- `class ArbRoute` — A profitable arbitrage path through AMM pools.
    - `def hops(self) -> int`
- `class LiquidationOpportunity` — A position nearing liquidation on Drift/Mango.
- `class OraclePrice`
- `class OpenInterest`
    - `def net_oi(self) -> float`
    - `def total_oi(self) -> float`
- `class Liquidation`
- `class WhaleTransfer`
- `class DexVolume`
- `class TokenUnlock`
- `class SyncMetadata`
- `class CollectorStatus`

### `flint.precision`
_Token precision, order size management, and Drift integer math._
- `def amount_to_precision(size: float, step_size: float) -> float` — Truncate order size to valid step size (always round DOWN).
- `def price_to_precision(price: float, tick_size: float) -> float` — Round price to nearest valid tick (round half-up).
- `def round_pnl(value: float, decimals: int=8) -> float` — Round PnL/equity values to prevent float drift accumulation.
- `def to_drift_base(size: float) -> int` — Convert human-readable size to Drift base_asset_amount (1e9).
- `def from_drift_base(raw: int) -> float` — Convert Drift base_asset_amount to human-readable size.
- `def to_drift_price(price: float) -> int` — Convert human-readable price to Drift price integer (1e6).
- `def from_drift_price(raw: int) -> float` — Convert Drift price integer to human-readable price.
- `def to_drift_funding(rate: float) -> int` — Convert human funding rate to Drift integer (1e9).
- `def from_drift_funding(raw: int) -> float` — Convert Drift funding integer to human rate.
- `def to_base_units(amount: float, token: str) -> int` — Convert human token amount to on-chain base units.
- `def from_base_units(raw: int, token: str) -> float` — Convert on-chain base units to human token amount.
- `def validate_order_size(size: float, market: str, price: Optional[float]=None, min_notional: float=0.0) -> float` — Validate and truncate order size for a Drift market.
- `def get_market_specs(market: str) -> Optional[Tuple[float, float, float]]` — Get (tick_size, min_order_size, step_size) for a market, or None.

### `flint.regimes`
_Market regime definitions for multi-regime backtesting._
- `class Regime` — A named market regime with a date range.
    - `def start_date(self) -> str`
    - `def end_date(self) -> str`
    - `def to_dict(self) -> dict`
- `def get_regime(regime_id: str) -> Optional[Regime]` — Look up a regime by ID.
- `def get_all_regime_ids() -> List[str]` — Return all regime IDs in chronological order.
- `def get_regimes_by_type(regime_type: str) -> List[Regime]` — Return all regimes of a given type.
