# Codemap: `flint.services`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.services.__init__`
_flint/services — in-process service layer._

### `flint.services.backtest`
_Backtest service — synchronous one-shot runner for in-process callers._
- imports: `..analytics.tearsheet`, `..backtest.engine`, `..execution.fill_models`, `..models`, `..store`, `.strategies`
- `class BacktestRunRequest`
- `def run_backtest_sync(req: BacktestRunRequest, store: FlintStore) -> Dict[str, Any]` — Run a backtest end-to-end and return the tearsheet dict.

### `flint.services.data`
_Data service — store-backed read APIs + download/provider business logic._
- imports: `..collector.tasks`, `..models`, `..providers.ccxt_provider`, `..providers.coingecko`, `..providers.drift_candles`, `..providers.drift_s3`, `..providers.funding_rates`, `..providers.hyperliquid_candles`, `..providers.jupiter_borrow`, `..providers.pyth_candles`, `..store`
- `def get_ohlcv(store: FlintStore, market: str, resolution_s: int=3600, start_ts: Optional[int]=None, end_ts: Optional[int]=None, limit: int=1000, venue: Optional[str]=None) -> Dict[str, Any]` — Return OHLCV candles. When `venue` is None, deduplicates across
- `def get_funding(store: FlintStore, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> Dict[str, Any]` — Return funding rates grouped by venue. Defaults to last 30 days.
- `def get_borrow_rates(store: FlintStore, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> Dict[str, Any]`
- `def list_markets(store: FlintStore) -> List[Dict[str, Any]]`
- `def delete_market_data(store: FlintStore, market: str) -> Dict[str, Any]`
- `def dl_set(dl_id: str, *, status: str=None, progress: dict=None, result: dict=None)`
- `def dl_get(dl_id: str) -> dict` — Return download state for a given ID, or None if not found.
- `def check_single_market(store: Optional[FlintStore], market: str, resolution_s: int, start_ts: int, end_ts: int) -> dict` — Check data availability for a single market. Returns a dict.
- `def download_venue_volume(store, market, resolution_s, start_ts, end_ts, logger, warnings)` — Download candles from multiple venues and store per-venue.
- `def download_pyth_candles(market: str, resolution_s: int, start_ts: int, end_ts: int)` — Download candles from Pyth Benchmarks API.
- `def download_range(market: str, resolution_s: int, start_ts: int, end_ts: int, logger)` — Try all providers in order for a specific time range.
- `def download_range_for_venue(market: str, resolution_s: int, start_ts: int, end_ts: int, venue: str, logger)` — Download candles from a specific venue.
- `def forward_fill_to_hourly(snapshots: list) -> list` — Forward-fill funding snapshots to hourly resolution.
- `def fetch_historical_mark_prices(market: str, venue: str, start_ts: int, end_ts: int, logger) -> Dict[int, tuple]` — Fetch historical mark price candles for a venue.
- `def enrich_funding_with_mark_prices(snapshots: list, mark_prices: Dict[int, tuple]) -> list` — Replace static mark/index prices with historical per-timestamp prices.
- `def fetch_venue_open_interest(store, market: str, venue: str, start_ts: int, end_ts: int, logger) -> int` — Fetch historical open interest for a specific venue and store it.
- `def download_funding_all_venues(store, market: str, start_ts: int, end_ts: int, logger, venues=None, warnings=None) -> int` — Download funding rates for a market from selected venues.
- `def list_available_markets() -> dict` — List all markets available for download from Drift (perp + spot) + CoinGecko.

### `flint.services.journal`
_Journal service — past-run browsing and comparison._
- imports: `..journal.storage`, `..store`
- `def list_runs(store: FlintStore, limit: int=50) -> List[Dict[str, Any]]`
- `def get_run(store: FlintStore, run_id: str) -> Optional[Dict[str, Any]]`
- `def delete_run(store: FlintStore, run_id: str) -> None`
- `def compare_runs(store: FlintStore, run_ids: List[str]) -> List[Dict[str, Any]]`

### `flint.services.paper`
_Paper service — wrappers around `PaperTradingEngine`._
- imports: `..paper.engine`, `..paper.session_store`, `..store`
- `def list_sessions(engine: PaperTradingEngine) -> List[Dict[str, Any]]`
- `def get_session_status(engine: PaperTradingEngine, session_id: str) -> Optional[Dict[str, Any]]`
- `def stop_session(engine: PaperTradingEngine, session_id: str) -> bool`
- `def kill_session(engine: PaperTradingEngine, session_id: str) -> bool`
- `def list_sessions_from_store(store: FlintStore) -> List[Dict[str, Any]]` — Read persisted session metadata without needing a running engine.
- `def get_equity_history_from_store(store: FlintStore, session_id: str) -> List[Dict[str, Any]]`

### `flint.services.strategies`
_Shared strategy builder — single source of truth._
- imports: `..strategy`, `..strategy.loader`
- `def list_builtin_strategies() -> list[str]` — Names of every built-in strategy template.
- `def build_strategy(name: str, params: Optional[Dict]=None, code: Optional[str]=None)` — Resolve a strategy reference into a live Strategy instance.
