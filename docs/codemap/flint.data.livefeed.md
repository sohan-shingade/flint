# Codemap: `flint.data.livefeed`

> source git hash: `119d05d5f4a4f397f5c0c76fc906602b7b1c391c`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.data.livefeed.__init__`
_data.livefeed — live market-data feed for paper/live; lake used for reconnect gap-replay only (§6.7)._
- imports: `.aggregator`, `.clock`, `.feed`, `.gap`

### `flint.data.livefeed.aggregator`
_Bar aggregation from the normalized WS record stream (§6.7)._
- imports: `flint.core.models`, `flint.core.time`, `flint.data.normalize`, `flint.engine.context`
- `class LiveBar` — One closed bar plus every sidecar observed within it (§6.7).
- `class CandleAggregator` — Aggregates normalized records into closed :class:`LiveBar`s (§6.7).
    - `def __init__(self, market: str, venue: str, resolution_s: int, *, funding_interval_s: int=3600) -> None`
    - `def add_trade(self, p: TradePrint) -> list[LiveBar]`
    - `def add_book(self, book: OrderbookSnapshot) -> list[LiveBar]`
    - `def add_context(self, ctx: AssetContext) -> list[LiveBar]` — Fold an ``activeAssetCtx`` snapshot into mark / OI / predicted-funding.
    - `def flush(self) -> list[LiveBar]` — Close the final open bar (graceful session end, not a disconnect).

### `flint.data.livefeed.clock`
_The paper/live clock — driven by venue event time, never the wall clock (§6.7)._
- imports: `flint.core.time`
- `class PaperClock` — Tracks the latest venue event timestamp and the bar it falls in (§6.7).
    - `def __init__(self, resolution_s: int) -> None`
    - `def resolution_s(self) -> int`
    - `def now(self) -> int | None` — Latest venue event ts observed, or ``None`` before the first event.
    - `def observe(self, event_ts: int) -> None` — Advance the session clock to a venue event ts (forward-only).
    - `def current_bar_start(self) -> int | None` — Start ts of the bar the session clock currently sits in, or ``None``.

### `flint.data.livefeed.feed`
_The LiveFeed — venue WS in, closed bars out, gap-replay across reconnects (§6.7)._
- imports: `.aggregator`, `.clock`, `.gap`, `flint.core.models`, `flint.core.time`, `flint.data.ingest.recorders`, `flint.data.normalize`, `flint.engine.context`
- `class EngineInputs` — The per-market feeds a bar stream lowers to, ready for ``engine.run`` (§6.7).
    - `def run_kwargs(self) -> dict`
- `def assemble_engine_inputs(bars: Iterable[LiveBar]) -> EngineInputs` — Lower an emitted :class:`LiveBar` stream into :class:`EngineInputs`.
- `class LiveFeed` — Subscribes a venue WS and emits closed bars with reconnect gap replay (§6.7).
    - `def __init__(self, market: str, *, venue: str=VENUE_HYPERLIQUID, resolution_s: int, gap_source: GapSource | None=None, funding_interval_s: int=3600, resume_bar_start: int | None=None) -> None`
    - `def connect(self, source: WsMessageSource) -> list[LiveBar]` — Process one WS connection; return the bars it closed, in time order.
    - `def close_session(self) -> list[LiveBar]` — Flush the final open bar on a graceful (non-disconnect) session end.

### `flint.data.livefeed.gap`
_Reconnect gap replay — the lake as a cold backfill, never the hot path (§6.7)._
- imports: `flint.core.models`, `flint.engine.context`
- `class GapData` — What the lake could return for a reconnect window, keyed by nothing but ts.
- `class GapSource(Protocol)` — Reads closed bars + funding for a reconnect window from the lake (§6.7).
    - `def fetch(self, venue: str, market: str, start_ms: int, end_ms: int, resolution_s: int) -> GapData` — Return whatever the lake holds for bars with start in ``[start, end)``.
- `class GapRecovery` — The record of one reconnect: what was missed and how much came back (§6.7).
    - `def gap_minutes(self) -> float`
    - `def degraded_fidelity(self) -> bool`
    - `def message(self) -> str`
- `class InMemoryGapSource` — A ``GapSource`` over pre-loaded fragments — for tests and the runner's seam.
    - `def fetch(self, venue: str, market: str, start_ms: int, end_ms: int, resolution_s: int) -> GapData`
