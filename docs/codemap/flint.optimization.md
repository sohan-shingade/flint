# Codemap: `flint.optimization`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.optimization.__init__`
_Hyperparameter optimization for strategies._
- imports: `.ga_optimizer`, `.optimizer`

### `flint.optimization.ga_optimizer`
_Genetic algorithm optimizer — evolutionary hyperparameter search._
- imports: `..backtest.engine`, `..execution.fill_models`, `..models`, `..strategy.base`, `.optimizer`
- `class GAConfig`
- `class GAOptimizer` — Optimizes strategy parameters using a genetic algorithm.
    - `def __init__(self, strategy_cls: Type[Strategy], candles: Union[List[Candle], Dict[str, List[Candle]]], metric: str='sharpe_ratio', ga_config: Optional[GAConfig]=None, initial_capital: float=10000.0, fee_rate: float=0.0005, train_ratio: float=1.0, fill_model: Optional[FillModel]=None, funding_rates: Optional[List]=None, orderbook_snapshots: Optional[List]=None, open_interest: Optional[List]=None, margin_engine=None, capital_allocator=None)`
    - `def optimize(self) -> OptimizationResult` — Run GA optimization. Returns best params and all evaluated trials.

### `flint.optimization.optimizer`
_Strategy optimizer — Optuna-based hyperparameter search._
- imports: `..backtest.engine`, `..execution.fill_models`, `..models`, `..strategy.base`
- `class TrialResult`
- `class OptimizationResult`
- `class StrategyOptimizer` — Optimizes strategy parameters using Optuna.
    - `def __init__(self, strategy_cls: Type[Strategy], candles: Union[List[Candle], Dict[str, List[Candle]]], metric: str='sharpe_ratio', n_trials: int=50, initial_capital: float=10000.0, fee_rate: float=0.0005, train_ratio: float=1.0, fill_model: Optional[FillModel]=None, funding_rates: Optional[List]=None, orderbook_snapshots: Optional[List]=None, open_interest: Optional[List]=None, margin_engine=None, capital_allocator=None)`
    - `def optimize(self) -> OptimizationResult` — Run optimization. Returns best params and all trial results.
    - `def walk_forward(self, n_splits: int=5, train_pct: float=0.7, trials_per_split: int=30) -> Dict[str, Any]` — Walk-forward optimization with rolling train/test windows.

### `flint.optimization.portfolio_objective`
_Portfolio-level Optuna objective — Phase 6 T6.3._
- `def pairwise_correlations(equity_curves: Sequence[Sequence[float]]) -> float` — Mean pairwise correlation of per-strategy equity-curve returns.
- `def portfolio_objective(trial, strategies_fn: Callable[[object], List], runner: Callable[[List], Dict], penalty_lambda: float=0.5, correlation_floor: float=0.3) -> float` — Optuna objective — `portfolio_sharpe - penalty_lambda * excess_corr`.
- `def score(portfolio_sharpe: float, equity_curves: Sequence[Sequence[float]], penalty_lambda: float=0.5, correlation_floor: float=0.3) -> float` — Standalone scoring helper — same formula as `portfolio_objective`

### `flint.optimization.walk_forward`
_Walk-forward analysis — rolling train/test optimization with overfitting detection._
- imports: `..backtest.engine`, `..models`, `..strategy.base`, `.optimizer`
- `class WindowResult`
- `class WalkForwardResult`
- `def run_walk_forward(strategy_cls: Type[Strategy], candles: List[Candle], n_windows: int=5, train_pct: float=0.7, metric: str='sharpe_ratio', trials_per_window: int=30, initial_capital: float=10000.0, fee_rate: float=0.0005) -> WalkForwardResult` — Run walk-forward analysis.
