# Codemap: `flint.paper`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.paper.__init__`
_Paper trading engine — live strategy execution with simulated orders._
- imports: `.engine`

### `flint.paper.context`
_PaperContext — unified state owner + strategy-facing API for paper trading._
- imports: `..config`, `..execution._position`, `..execution.borrow_ledger`, `..execution.capital`, `..execution.cash_manager`, `..execution.context`, `..execution.fee_models`, `..execution.fill_models`, `..execution.fill_recorder`, `..execution.funding_ledger`, `..execution.market_data_feed`, `..execution.order_queue`, `..execution.position_manager`, `..execution.venue_config`, `..models`
- `class PaperContext(ExecutionContext)` — ExecutionContext for paper trading — state owner + strategy API.
    - `def __init__(self, initial_capital: float=10000.0, venue: Optional[str]=None, capital_allocation: Optional[dict]=None, fill_model: Optional[FillModel]=None, fee_model: Optional[FeeModel]=None, latency_enabled: bool=False, store=None, resolution_s: int=3600, session_id: str='')`
    - `def account(self) -> AccountState`
    - `def positions(self) -> List[PositionInfo]` — Strategy-facing positions list. Compatible with the
    - `def pending_orders(self) -> List[Order]`
    - `def current_candle(self) -> Optional[Candle]`
    - `def timestamp(self) -> int`
    - `def set_candle(self, candle: Candle) -> None`
    - `def market_order(self, market, side, size, reduce_only=False, tag='', venue='default')`
    - `def limit_order(self, market, side, size, price, reduce_only=False, tag='', venue='default')`
    - `def stop_order(self, market, side, size, trigger_price, tag='', venue='default')`
    - `def take_profit_order(self, market, side, size, trigger_price, tag='', venue='default')`
    - `def cancel(self, order_id)`
    - `def log(self, message: str) -> None`
    - `def cash(self) -> float`
    - `def cash(self, value: float) -> None`
    - `def unrealized_pnl_total(self) -> float`
    - `def equity(self) -> float`
    - `def margin_used(self) -> float`
    - `def free_margin(self) -> float`
    - `def leverage(self) -> float`
    - `def margin_ratio(self) -> float`
    - `def total_fees(self) -> float`
    - `def total_funding(self) -> float`
    - `def total_funding(self, value: float) -> None`
    - `def total_tx_costs(self) -> float`
    - `def total_borrow_paid(self) -> float`
    - `def fills(self) -> List[Fill]`
    - `def closed_trades(self) -> List[dict]`
    - `def venue(self) -> str`
    - `def venue_balance(self, venue: str) -> float`
    - `def venue_balances(self) -> dict`
    - `def transfer(self, from_venue: str, to_venue: str, amount: float) -> bool`
    - `def venue_positions(self, venue: str) -> List[PositionInfo]`
    - `def can_open_position(self, size: float, price: float) -> bool`
    - `def get_liquidation_price(self, market: str) -> float`
    - `def submit_order(self, order: Order) -> str`
    - `def cancel_order(self, order_id: str) -> bool`
    - `def cancel_all(self, market: Optional[str]=None) -> int`
    - `def process_candle(self, candle: Candle) -> List[Fill]` — Expire timed-out pending orders, fill remaining ones against
    - `def apply_funding(self, market: str, rate: float, mark_price: float, venue: Optional[str]=None) -> float` — Apply funding payment to all open legs on `market`.
    - `def close_all_positions(self, mark_prices: dict) -> None` — Force-close all positions at their per-market mark prices.
    - `def get_funding_rate(self, market=None)`
    - `def get_funding_rates(self, market=None, lookback=24)`
    - `def get_funding_by_venue(self, market=None, lookback=24)`
    - `def get_orderbook(self, market=None)`
    - `def get_candles(self, market, lookback=50)`
    - `def position_at(self, venue: str, market: str) -> Optional[_Position]` — Look up a single leg by (venue, market). Returns None if no
    - `def positions_for_market(self, market: str) -> List[_Position]` — All legs on `market` regardless of venue. Multi-venue arb
    - `def positions_dict(self) -> Dict[Tuple[str, str], dict]` — Legacy dict view, keyed by (venue, market). Used by
    - `def equity_history(self) -> List[float]`
    - `def equity_history(self, value: List[float]) -> None`

### `flint.paper.engine`
_PaperTradingEngine — runs a strategy against live data with simulated execution._
- imports: `..backtest.engine`, `..config`, `..execution._position`, `..models`, `..providers.hyperliquid_candles`, `..store`, `..strategy.base`, `..strategy.loader`, `.context`, `.risk_guard`, `.session_store`
- `def backfill_candle_gap(store: FlintStore, market: str, from_ts: int, to_ts: int) -> int` — Download candles for a gap period so resumed sessions can catch up.
- `class PaperSession` — Represents a single paper trading session.
    - `def __init__(self, session_id: str, strategy: Strategy, market: str, resolution_s: int, ctx: PaperContext, venue: Optional[str]=None)`
    - `def to_dict(self) -> dict`
- `class PaperTradingEngine` — Manages paper trading sessions.
    - `def __init__(self, store: FlintStore)`
    - `def set_event_loop(self, loop: asyncio.AbstractEventLoop) -> None` — Store reference to the main event loop for scheduling tasks from sync threads.
    - `def start_session(self, strategy: Strategy, market: str='SOL-PERP', resolution_s: int=3600, initial_capital: float=10000.0, venue: Optional[str]=None, strategy_code: str='', strategy_params: dict=None, risk_config: dict=None) -> str` — Start a new paper trading session with replay. Returns session_id.
    - `def stop_session(self, session_id: str) -> bool` — Stop a paper trading session.
    - `def kill_session(self, session_id: str) -> bool` — Emergency stop — close all positions and stop.
    - `def get_status(self, session_id: str) -> Optional[dict]`
    - `def get_trades(self, session_id: str) -> List[dict]`
    - `def list_sessions(self) -> List[dict]`
    - `def resume_sessions(self) -> int` — Resume all active sessions from DuckDB after server restart.
    - `def deploy_session(self, strategy: Strategy, strategy_code: str, strategy_params: dict, market: str='SOL-PERP', resolution_s: int=3600, initial_capital: float=10000.0, replay_start_ts: int=0, risk_config: Optional[dict]=None, capital_allocation: Optional[dict]=None, venue: Optional[str]=None) -> str` — Deploy a strategy with replay-forward execution.
    - `def redeploy_session(self, session_id: str, replay_start_ts: int) -> Optional[str]` — Kill an existing session and redeploy it from a new start date.

### `flint.paper.price_ticker`
_Live PnL price poller — walks a fallback chain of price sources._
- imports: `.price_sources`
- `class PriceTicker` — Polls a fallback chain of price sources at a fixed interval.
    - `def __init__(self, markets: List[str], interval_s: float=5.0, sources: Optional[Sequence[PriceSource]]=None)`
    - `def prices(self) -> Dict[str, float]` — Back-compat shim: callers that used `ticker.prices[mkt] = …`
    - `async def run(self) -> None` — Main async loop — poll until cancelled.
    - `def stop(self) -> None`
    - `def add_market(self, market: str) -> None`
    - `def get_price(self, market: str) -> Optional[float]`
    - `def get_quote(self, market: str) -> Optional[PriceQuote]`
    - `def get_all_prices(self) -> Dict[str, float]`
    - `def get_health(self) -> List[dict]` — Per-source health snapshot for `/api/v1/system/price-sources`.

### `flint.paper.risk_guard`
_Risk guard for paper trading — checks limits after each candle._
- `class RiskConfig`
- `class RiskGuard` — Checks risk limits after each candle.
    - `def __init__(self, config: RiskConfig)`
    - `def check(self, broker, initial_capital: float, mark_prices: dict) -> Optional[str]` — Returns breach reason string, or None if all limits OK.
    - `def margin_ratio(self, broker, mark_prices: dict) -> float`
    - `def liquidation_distance_pct(self, broker, mark_prices: dict) -> float`
    - `def risk_status(self, broker, initial_capital: float, mark_prices: dict) -> dict`

### `flint.paper.session_store`
_Persistence layer for paper trading sessions._
- imports: `..store`
- `class PaperSessionStore` — CRUD operations for paper trading persistence.
    - `def __init__(self, store: FlintStore)`
    - `def save_session(self, *, session_id: str, strategy_name: str, strategy_code: str, strategy_params: dict, market: str, initial_capital: float, replay_start_ts: int, started_at: int, status: str, risk_config: dict, live_start_ts: int=0) -> None`
    - `def load_session(self, session_id: str) -> Optional[Dict[str, Any]]`
    - `def update_status(self, session_id: str, status: str, *, live_start_ts: int=0, stopped_at: int=0, stop_reason: str='') -> None`
    - `def update_risk_config(self, session_id: str, risk_config: dict) -> None`
    - `def save_equity_snapshots(self, session_id: str, snapshots: List[dict]) -> None`
    - `def get_equity_history(self, session_id: str) -> List[dict]`
    - `def save_trades(self, session_id: str, trades: List[dict]) -> None`
    - `def get_trades(self, session_id: str) -> List[dict]`
    - `def save_positions(self, session_id: str, positions: List[dict]) -> None` — Persist all open legs. Each entry must include `venue` —
    - `def load_positions(self, session_id: str) -> List[dict]`
    - `def list_active_sessions(self) -> List[dict]`
    - `def save_funding_payment(self, session_id: str, ts: int, market: str, rate: float, payment: float, position_size: float, mark_price: float, venue: str='unknown') -> None`
    - `def get_funding_payments(self, session_id: str) -> list`
    - `def clear_session_data(self, session_id: str) -> None` — Wipe all live data for a session (equity, trades, positions, funding).
    - `def list_all_sessions(self) -> List[dict]`
