# Codemap: `flint.data`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.data.__init__`
_Data management — quality checks, export/import._

### `flint.data.export`
_Parquet export/import for DuckDB data. Thread-safe via store lock._
- imports: `..store`
- `def export_table_parquet(store: FlintStore, table: str, path: str) -> int` — Export a DuckDB table to a Parquet file. Returns row count.
- `def import_parquet(store: FlintStore, path: str, table: str) -> int` — Import a Parquet file into a DuckDB table. Returns row count.

### `flint.data.quality`
_Data quality checks — gap detection, outlier detection, completeness._
- imports: `..models`
- `class DataQualityReport` — Results of a data quality check.
- `def check_candle_quality(candles: List[Candle], resolution_s: int, outlier_std_threshold: float=5.0) -> DataQualityReport` — Run quality checks on a list of candles.
