# Codemap: `flint.engine.funding`

> source git hash: `ce3be0a0a5438e0d9a86a25e980d43c2909b821c`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.engine.funding.__init__`
_engine.funding — venue-specific funding accrual and ledger; predicted/final divergence (§6.4)._
- imports: `.settlement`

### `flint.engine.funding.settlement`
_Funding settlement — the payment one settlement moves, priced honestly (§6.4)._
- imports: `..money`, `flint.core.models`, `flint.core.time`
- `class FundingCoverageError(Exception)` — A funding settlement cannot be settled/priced honestly (§6.4, §16).
- `def clamp_funding_rate(rate_hourly: float, cap_hourly: float) -> tuple[float, bool]` — Clamp ``rate_hourly`` to ``±cap_hourly``; return (rate, whether it bit).
- `def settlement_price_for(rate: FundingRate, marks: list[MarkSnapshot]) -> float` — The price a settlement is charged on, per its ``price_basis`` (§6.4, §2.2).
- `def settlement_payment(position: Position, rate_hourly: float, settlement_price: float, interval_s: int=3600) -> Money` — The signed ``Decimal`` funding payment to ``position`` for one settlement.
