# Codemap: `flint.adapters`

> source git hash: `17ac165215ad4e0abbe45a8f44ef1683807e0487`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.adapters.__init__`
_adapters — concrete port implementations (§2.7, §17)._
- imports: `.local`

### `flint.adapters.local`
_Local reference adapters — the Phase-1 walking-skeleton implementations (§2.7)._
- imports: `flint.ports`
- `class InMemoryMarketData(MarketDataPort)` — Tenant-agnostic market store backed by per-``(venue, market)`` Arrow tables.
    - `def __init__(self) -> None`
    - `def save_candles(self, venue: str, market: str, candles: pa.Table) -> int`
    - `def load_candles(self, venue: str, market: str, start_ts: int, end_ts: int) -> pa.Table`
- `class InMemoryUserData(UserDataPort)` — Per-tenant run store — the concrete proof of the cross-leak seam (§2.7).
    - `def __init__(self) -> None`
    - `def save_run(self, tenant: TenantContext, run: RunRecord) -> str`
    - `def load_run(self, tenant: TenantContext, run_id: str) -> RunRecord`
    - `def list_runs(self, tenant: TenantContext) -> list[RunRecord]`
- `class InProcessJobRunner(JobRunnerPort)` — Run jobs synchronously in the current process (laptop default).
    - `def __init__(self) -> None`
    - `def submit(self, tenant: TenantContext, fn: Callable[[], T], quota: ResourceQuota) -> T`
- `class EnvSecrets(SecretsPort)` — Resolve secrets from the process environment (server-side ``.env``).
    - `def get_secret(self, tenant: TenantContext, name: str) -> str | None`
- `class InMemoryEventBus(EventBusPort)` — Ephemeral pub/sub keyed by ``(tenant_id, topic)`` — an isolation seam.
    - `def __init__(self) -> None`
    - `def publish(self, tenant: TenantContext, topic: str, payload: Mapping[str, Any]) -> None`
    - `def subscribe(self, tenant: TenantContext, topic: str, handler: EventHandler) -> None`
- `class LocalIdentity(Identity)` — The single implicit tenant on a self-hosted install.
    - `def current(self) -> TenantContext`
