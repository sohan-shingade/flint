# Codemap: `flint.engine.nautilus`

> source git hash: `86bd23d30c1fe6ef31b2dd29c08a253fb29c7cbb`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.engine.nautilus.__init__`
_``flint.engine.nautilus`` — the Nautilus-backed simulation substrate (§A3, D29)._

### `flint.engine.nautilus._compat`
_The churn firewall — every ``nautilus_trader`` import Flint uses, in one place (§A3)._

### `flint.engine.nautilus.dataconv`
_Flint market-data models → Nautilus objects (the engine-side wrangler, §A3, cross-seam 1)._
- imports: `.`, `._compat`, `flint.core.models`, `flint.core.time`, `flint.engine.fills`, `flint.venues`
- `class FlintFundingRate(Data)` — A predicted funding observation carried losslessly through Nautilus (§6.4, §A4).
    - `def __init__(self, *, instrument_id: InstrumentId, market: str, venue: str, rate_hourly: float, interval_s: int, price_basis: str, rate_type: str, settlement_ts: int, ts_event: int, ts_init: int) -> None`
    - `def ts_event(self) -> int`
    - `def ts_init(self) -> int`
- `def predicted_funding_to_data(fr: FundingRate, instrument_id: InstrumentId) -> FlintFundingRate | None` — Convert one **predicted** :class:`FundingRate` to :class:`FlintFundingRate`.
- `def predicted_funding_custom_data(fr: FundingRate, instrument_id: InstrumentId) -> CustomData | None` — Wrap a predicted :class:`FlintFundingRate` as Nautilus ``CustomData`` (§6.4).
- `def resolution_to_bar_spec(resolution_s: int) -> BarSpecification` — Map a Flint ``resolution_s`` to a Nautilus ``BarSpecification`` (LAST price).
- `def bar_type_for(instrument_id: InstrumentId, resolution_s: int, *, aggregation_source: AggregationSource=AggregationSource.EXTERNAL) -> BarType` — The ``BarType`` at ``resolution_s`` for ``instrument_id``.
- `def candle_to_bar(candle: Candle, bar_type: BarType, *, price_precision: int, size_precision: int) -> Bar` — Convert a Flint ``Candle`` (START ms) to a Nautilus ``Bar`` (CLOSE ns).
- `def mark_to_updates(mark: MarkSnapshot, instrument_id: InstrumentId, *, price_precision: int) -> list[MarkPriceUpdate | IndexPriceUpdate]` — Split a Flint ``MarkSnapshot`` into a ``MarkPriceUpdate`` (+ ``IndexPriceUpdate``).
- `def trade_to_tick(tp: TradePrint, instrument_id: InstrumentId, *, price_precision: int, size_precision: int, seq: int) -> TradeTick` — Convert a Flint ``TradePrint`` to a Nautilus ``TradeTick``.
- `def quote_to_tick(q: QuoteTick, instrument_id: InstrumentId, *, price_precision: int, size_precision: int) -> NautilusQuoteTick` — Convert a Flint :class:`~flint.core.models.QuoteTick` to a Nautilus quote.
- `def tick_to_trade_print(tick: TradeTick) -> TradePrint` — Recover a Flint :class:`TradePrint` from a Nautilus ``TradeTick`` (tick lane).
- `def orderbook_to_deltas(book: OrderbookSnapshot, instrument_id: InstrumentId, *, price_precision: int, size_precision: int, seq_start: int=0) -> OrderBookDeltas` — An ``OrderbookSnapshot`` → an initial-book ``OrderBookDeltas`` (CLEAR + levels).
- `def book_delta_to_delta(bd: BookDelta, instrument_id: InstrumentId, *, price_precision: int, size_precision: int) -> OrderBookDelta` — Convert one Flint :class:`BookDelta` row to a Nautilus ``OrderBookDelta``.
- `def book_delta_clear(instrument_id: InstrumentId, ts_ms: int, seq: int) -> OrderBookDelta` — A standalone ``CLEAR`` delta — emitted before a snapshot-resync group.
- `def build_instrument(venue_spec: VenueSpec, market: str, venue: str, *, price_precision: int=2) -> CryptoPerpetual` — Build a Nautilus ``CryptoPerpetual`` for one HL market from a ``VenueSpec``.

### `flint.engine.nautilus.engine`
_``NautilusEngine`` — the Nautilus-backed :class:`SimulationEngine` (§A3, §6.0)._
- imports: `.`, `._compat`, `.execmodels`, `.funding`, `.liquidation`, `.shim`, `.ticklane`, `.translate`, `flint.engine.api`, `flint.engine.money`, `flint.engine.portfolio`, `flint.engine.state`
- `class NautilusEngine` — Run a Flint backtest on a Nautilus core (§6.0, D29).
    - `def run(self, feed: EngineFeed, strategy: object, *, event_log: EventLog, spec: EngineRunSpec) -> PortfolioState` — Dispatch to the bar or tick lane by the strategy's ``lane`` marker (§8.6).

### `flint.engine.nautilus.execmodels`
_Making Nautilus record a fill at *Flint's* price and fee (§A7, §6.3)._
- imports: `._compat`
- `def encode_fill_tag(payload: dict[str, Any]) -> str` — Encode a Flint fill payload as the ``flint_fill=<json>`` order tag.
- `def decode_fill_tag(order: Any) -> dict[str, Any] | None` — Recover the Flint fill payload from an order's tags, or ``None`` if absent.
- `class FlintPriceFillModel(FillModel)` — Fill every order at the exact price Flint computed (bar lane, §A7).
    - `def fill_limit_inside_spread(self) -> bool`
    - `def get_orderbook_for_fill_simulation(self, instrument, order, best_bid, best_ask)`
- `class FlintTagFeeModel(FeeModel)` — Charge exactly the fee Flint computed for the fill (bar lane, §A7).
    - `def get_commission(self, order, fill_qty, fill_px, instrument)`
- `class TickFeeModel(FeeModel)` — The tick lane's fee model: native maker/taker, zero for liquidation closes (§A7/N8).
    - `def __init__(self) -> None`
    - `def get_commission(self, order, fill_qty, fill_px, instrument)`

### `flint.engine.nautilus.funding`
_``FlintFundingModule`` — final-only funding settlement on the Nautilus lane (§A4, §6.4)._
- imports: `.`, `._compat`, `flint.core.models`, `flint.engine.funding.settlement`, `flint.venues`
- `class FlintFundingModule(SimulationModule)` — Settle §6.4 funding on the Nautilus core, byte-exact with the legacy loop (§A4).
    - `def __init__(self, config: SimulationModuleConfig, *, recorder, shadow, feed, venue_spec: VenueSpec, lane: str='bar') -> None`
    - `def settle_bar(self, candle, end: int) -> None` — Settle every final row whose ts falls in ``[candle.ts, end)``, in ts order.
    - `def process(self, ts_now: int) -> None` — Tick-lane driver — settle every final row now due (``ts <= ts_now``), in ts order.
    - `def log_diagnostics(self, logger) -> None` — Abstract on ``SimulationModule``; Flint routes settlement to the EventLog.
    - `def reset(self) -> None` — Abstract on ``SimulationModule``; a Flint run is constructed per backtest.
- `def build_funding_module(*, recorder, shadow, feed, venue_spec: VenueSpec, lane: str='bar') -> FlintFundingModule | None` — Construct the module iff the feed carries at least one final funding row.

### `flint.engine.nautilus.liquidation`
_``FlintLiquidationModule`` — §6.5 mark liquidation on the Nautilus lane (§A5)._
- imports: `.`, `._compat`, `flint.core.models`, `flint.engine.liquidation.cascade`, `flint.engine.money`, `flint.engine.state`, `flint.venues`
- `class LiquidationClose` — A Nautilus-position flatten the shim submits for one applied liquidation.
- `class FlintLiquidationModule(SimulationModule)` — Check §6.5 liquidation on the Nautilus core, byte-exact with the legacy loop (§A5).
    - `def __init__(self, config: SimulationModuleConfig, *, recorder, shadow: PortfolioState, venue_spec: VenueSpec, lane: str='bar') -> None`
    - `def set_close_fn(self, close_fn) -> None` — Wire the tick-lane forced-close callback (the host's ``flatten``).
    - `def check_bar(self, candle, bar_marks) -> list[LiquidationClose]` — Plan + apply this bar's liquidations for ``candle``'s venue (§6.5).
    - `def process(self, ts_now: int) -> None` — Tick-lane driver — check + apply §6.5 liquidation on the recorded marks.
    - `def log_diagnostics(self, logger) -> None` — Abstract on ``SimulationModule``; Flint routes liquidations to the EventLog.
    - `def reset(self) -> None` — Abstract on ``SimulationModule``; a Flint run is constructed per backtest.
- `def build_liquidation_module(*, recorder, shadow: PortfolioState, venue_spec: VenueSpec, lane: str='bar') -> FlintLiquidationModule` — Construct the liquidation module (always — any open position can liquidate).

### `flint.engine.nautilus.shim`
_``BarLaneStrategy`` — unmodified Flint bar strategies on the Nautilus core (§A7, §6.1)._
- imports: `.`, `._compat`, `.execmodels`, `flint.core.models`, `flint.core.time`, `flint.engine.api`, `flint.engine.fills`, `flint.engine.fills.latency`, `flint.engine.loop`, `flint.engine.orders`, `flint.engine.signals`, `flint.engine.state`
- `class BarLaneStrategy(Strategy)` — Host an unmodified Flint bar strategy on the Nautilus core (§A7).
    - `def __init__(self, *, flint_strategy: object, feed: EngineFeed, recorder: object, shadow: PortfolioState, spec: EngineRunSpec, instruments: dict[str, object], resolution_s: int, funding: object=None, liquidation: object=None) -> None`
    - `def on_start(self) -> None`
    - `def on_bar(self, bar) -> None` — Drive one locked per-bar sequence for the fed candle at this bar close.
    - `def finalize_run(self) -> None` — Cancel every still-working order at run end (mirrors the legacy loop).

### `flint.engine.nautilus.ticklane`
_``TickLaneStrategy`` — event-driven Flint strategies on the Nautilus core (§8.6, §A7b)._
- imports: `.`, `._compat`, `.dataconv`, `.execmodels`, `flint.core.models`, `flint.engine.api`, `flint.engine.loop`, `flint.engine.signals`, `flint.engine.state`
- `class TickLaneStrategy(Strategy)` — Host an event-driven Flint tick strategy on the Nautilus core (§8.6, §A7b).
    - `def __init__(self, *, tick_strategy: object, feed: EngineFeed, recorder: object, shadow: PortfolioState, spec: EngineRunSpec, instruments: dict[str, object], resolution_s: int, venue: str) -> None`
    - `def on_start(self) -> None`
    - `def on_trade_tick(self, tick) -> None`
    - `def on_quote_tick(self, quote) -> None`
    - `def on_order_book_deltas(self, deltas) -> None`
    - `def on_bar(self, bar) -> None`
    - `def on_data(self, data) -> None`
    - `def flatten(self, close) -> None` — Flatten one liquidated position on Nautilus at its entry price (§6.5).

### `flint.engine.nautilus.timeconv`
_Time-base conversion between Flint and Nautilus (§A3, §5)._
- `def ms_to_ns(ts_ms: int) -> int` — Convert integer unix-ms to integer unix-ns (exact).
- `def ns_to_ms(ts_ns: int) -> int` — Convert integer unix-ns to integer unix-ms, flooring sub-ms remainder.
- `def candle_start_ms_to_bar_close_ns(start_ms: int, resolution_s: int) -> int` — Nautilus ``Bar.ts_event`` (bar CLOSE, ns) for a Flint candle START (ms).
- `def bar_close_ns_to_candle_start_ms(close_ns: int, resolution_s: int) -> int` — Flint candle START (ms) for a Nautilus ``Bar.ts_event`` (bar CLOSE, ns).

### `flint.engine.nautilus.translate`
_``FlintRecorder`` — the single writer to the Flint EventLog (§A6)._
- imports: `.`, `._compat`, `.execmodels`, `flint.core.models`, `flint.engine.money`, `flint.engine.orders`, `flint.engine.portfolio`, `flint.engine.portfolio.events`, `flint.engine.state`, `flint.engine.tearsheet`
- `class FlintRecorder(Actor)` — Single EventLog writer + shadow book + order state machine (§A6).
    - `def __init__(self, *, event_log, shadow: PortfolioState, engine_name: str, equity_interval_s: int | None=None, mark_keys: dict | None=None) -> None`
    - `def on_start(self) -> None`
    - `def emit_run_finished(self) -> None` — Emit RUN_FINISHED — called by the engine after run-end order cancels.
    - `def order_record(self, client_order_id: str) -> OrderRecord | None` — The live state-machine record for ``client_order_id`` (or ``None``).
    - `def record_placed(self, order, **placed_extra) -> OrderRecord` — Accept an order onto the machine: pending → placed, ORDER_PLACED emitted.
    - `def record_rejected(self, order, *, reason: str) -> None` — Carry ``order`` to the terminal ``rejected`` state and emit (§6.2).
    - `def record_cancelled(self, order, *, reason: str) -> None` — Carry ``order`` to the terminal ``cancelled`` state and emit (§6.2).
    - `def note_native_order(self, naut_coid: str, *, flint_coid: str, venue: str, market: str, margin_mode: str, submit_mid: float) -> None` — Record the metadata a native L2 fill needs but the event doesn't carry (N8).
    - `def on_mark_price(self, update) -> None` — Carry the latest tick-lane mark for its (venue, market) key (§6.0/§6.5).
    - `def finalize_native(self) -> None` — Cancel every still-working tick-lane order at run end (mirrors §6.2).
    - `def record_funding(self, *, venue: str, market: str, rate_hourly: float, settled_rate_hourly: float, rate_capped: bool, interval_s: int, price_basis: str, rate_type: str, oracle_price: float, size: float, amount: Decimal, ts: int) -> None` — Apply one settled funding payment and emit FUNDING — byte-identical to ``loop._settle_funding``.
    - `def record_liquidation(self, decision) -> None` — Apply one planned liquidation and emit LIQUIDATION — byte-identical to ``loop._apply_liquidation``.
    - `def carried_marks(self) -> dict[tuple[str, str], float]` — The carried bar-close marks (per venue/market) for cross-pool valuation.
    - `def set_mark(self, key: tuple[str, str], mark: float) -> None` — Carry the bar-closing mark for ``(venue, market)`` (equity valuation).
    - `def emit_equity(self, ts: int) -> None` — Portfolio-wide EQUITY from the shadow book — byte-identical to ``loop._emit_equity``.
    - `def reconcile(self, *, portfolio, cache, venue, settlement_currency, seed_positions=None) -> None` — Assert the shadow book agrees with Nautilus's own account/positions (§19.4).
