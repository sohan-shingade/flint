# Codemap: `flint.ports`

> source git hash: `0e879ab91e50645db7eaf7a0ac65556918ec7dd7`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.ports.__init__`
_ports — the interfaces the domain depends on instead of concrete infra (§2.7)._
- imports: `.events`, `.identity`, `.jobs`, `.records`, `.secrets`, `.storage`, `.tenant`

### `flint.ports.events`
_``EventBusPort`` — publish "a thing happened" to interested subscribers (§2.7)._
- imports: `.tenant`
- `class EventBusPort(ABC)` — Publish/subscribe to tenant-scoped topics. Ephemeral; not the event log.
    - `def publish(self, tenant: TenantContext, topic: str, payload: Mapping[str, Any]) -> None` — Deliver ``payload`` to every handler subscribed to ``(tenant, topic)``.
    - `def subscribe(self, tenant: TenantContext, topic: str, handler: EventHandler) -> None` — Register ``handler`` for ``(tenant, topic)``. Sees only this tenant.

### `flint.ports.identity`
_``Identity`` — resolve who the current user is (§2.7)._
- imports: `.tenant`
- `class Identity(ABC)` — Resolve the acting user into a ``TenantContext``.
    - `def current(self) -> TenantContext` — Return the ``TenantContext`` for the acting user.

### `flint.ports.jobs`
_``JobRunnerPort`` — run a job under CPU/memory/wall quotas (§2.7)._
- imports: `.tenant`
- `class ResourceQuota` — The CPU/memory/wall ceiling a job runs under. Enforced by the runner.
    - `def default(cls) -> 'ResourceQuota'` — A conservative single-backtest ceiling (30s CPU, 2 GiB, 60s wall).
    - `def default_nautilus(cls) -> 'ResourceQuota'` — The ceiling for a sandbox run on the Nautilus substrate (§8.3, §19.4).
- `class JobRunnerPort(ABC)` — Run ``fn`` for ``tenant`` under ``quota`` and return its result.
    - `def submit(self, tenant: TenantContext, fn: Callable[[], T], quota: ResourceQuota) -> T` — Execute ``fn`` under ``quota`` and return its result.

### `flint.ports.records`
_Persistence DTOs carried across the user-data port (§2.7)._
- `class RunRecord` — The persisted head of a backtest/paper/live run (§6.2).

### `flint.ports.secrets`
_``SecretsPort`` — fetch an API key without knowing where it is stored (§2.7)._
- imports: `.tenant`
- `class SecretsPort(ABC)` — Resolve a named secret for a tenant, or ``None`` if it is unset.
    - `def get_secret(self, tenant: TenantContext, name: str) -> str | None` — Return the secret ``name`` for ``tenant``, or ``None`` if absent.

### `flint.ports.storage`
_Storage ports — the two halves of "where data lives" (§2.7)._
- imports: `.records`, `.tenant`
- `class MarketDataPort(ABC)` — Save/fetch market data, keyed ``(venue, market, ts)``. Shared namespace.
    - `def save_candles(self, venue: str, market: str, candles: pa.Table) -> int` — Persist candles for ``(venue, market)``; return the row count written.
    - `def load_candles(self, venue: str, market: str, start_ts: int, end_ts: int) -> pa.Table` — Return candles for ``(venue, market)`` with ``start_ts <= ts < end_ts``.
- `class UserDataPort(ABC)` — Save/fetch per-user data (runs, orders, positions, models) — §2.7.
    - `def save_run(self, tenant: TenantContext, run: RunRecord) -> str` — Persist ``run`` under ``tenant``; return its ``run_id``.
    - `def load_run(self, tenant: TenantContext, run_id: str) -> RunRecord` — Return ``tenant``'s run ``run_id``, or raise ``KeyError`` if absent.
    - `def list_runs(self, tenant: TenantContext) -> list[RunRecord]` — Return every run owned by ``tenant`` (never another tenant's).
    - `def append_events(self, tenant: TenantContext, run_id: str, rows: Sequence[Mapping[str, Any]]) -> int` — Append event rows to ``tenant``'s run ``run_id``; return the new total.
    - `def load_events(self, tenant: TenantContext, run_id: str) -> list[dict[str, Any]]` — Return ``tenant``'s events for ``run_id`` in append order.

### `flint.ports.tenant`
_``TenantContext`` — the identity threaded through every user-scoped call (§2.8)._
- `class TenantContext` — Who a user-scoped call belongs to. Filters every ``UserDataPort`` query.
    - `def local(cls) -> 'TenantContext'` — The single implicit tenant on a self-hosted/laptop install.
