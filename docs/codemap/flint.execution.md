# Codemap: `flint.execution`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.execution.__init__`
_Execution layer — the bridge between strategies and order execution._
- imports: `.backtest_context`, `.context`, `.fee_models`, `.fill_drift`, `.fill_jupiter`, `.fill_models`, `.fill_registry`, `.impact`, `.latency`, `.partial_fill`

### `flint.execution._position`
_Internal mutable position tracker shared by BacktestContext + PaperContext._
- imports: `..models`

### `flint.execution.backtest_context`
_BacktestContext — ExecutionContext implementation for simulated backtesting._
- imports: `..models`, `..portfolio.replay`, `..risk.portfolio_orchestrator`, `._position`, `.borrow_ledger`, `.cash_manager`, `.context`, `.fee_models`, `.fill_models`, `.fill_recorder`, `.funding_ledger`, `.market_data_feed`, `.order_queue`, `.position_manager`
- `class BacktestContext(ExecutionContext)` — ExecutionContext backed by simulated execution for backtesting.
    - `def __init__(self, initial_capital: float, fill_model: Optional[FillModel]=None, fee_model: Optional[FeeModel]=None, position_size_pct: float=1.0, risk_manager=None, margin_engine=None, capital_allocator=None, venue_fill_models: Optional[Dict[str, FillModel]]=None, portfolio_risk=None, event_log_writer=None, event_session_id: Optional[str]=None, snapshot_store=None, snapshot_every: int=10000)`
    - `def account(self) -> AccountState`
    - `def positions(self) -> List[PositionInfo]`
    - `def pending_orders(self) -> List[Order]`
    - `def current_candle(self) -> Optional[Candle]`
    - `def timestamp(self) -> int`
    - `def market_order(self, market: str, side: Side, size: float, reduce_only: bool=False, tag: str='', venue: str='default', time_in_force=None) -> str`
    - `def limit_order(self, market: str, side: Side, size: float, price: float, reduce_only: bool=False, tag: str='', venue: str='default') -> str`
    - `def stop_order(self, market: str, side: Side, size: float, trigger_price: float, tag: str='', venue: str='default') -> str`
    - `def take_profit_order(self, market: str, side: Side, size: float, trigger_price: float, tag: str='', venue: str='default') -> str`
    - `def cancel(self, order_id: str) -> bool`
    - `def cancel_all(self, market: Optional[str]=None) -> int`
    - `def log(self, message: str) -> None`
    - `def set_candle(self, candle: Candle) -> None` — Called by engine before strategy runs.
    - `def set_market_histories(self, histories: Dict[str, List[Candle]]) -> None` — Set multi-market candle data for cross-market access.
    - `def get_candles(self, market: str, lookback: int=50) -> List[Candle]` — Get recent candles for any available market.
    - `def markets(self) -> List[str]` — List of available markets.
    - `def add_funding_rate(self, fr: FundingRate) -> None` — Record a funding rate for strategy access. Called by engine.
    - `def get_funding_rate(self, market: Optional[str]=None) -> Optional[float]` — Get the most recent funding rate for a market.
    - `def get_funding_rates(self, market: Optional[str]=None, lookback: int=24) -> list` — Get recent funding rate history as [(ts, rate), ...].
    - `def get_funding_by_venue(self, market: Optional[str]=None, lookback: int=24) -> dict` — Get recent funding rates grouped by venue: {venue: [(ts, rate), ...]}.
    - `def get_venue_snapshots(self, market: Optional[str]=None, lookback: int=24) -> dict` — Get full FundingRate objects grouped by venue (includes mark/oracle prices).
    - `def add_borrow_rate(self, bs: BorrowSnapshot) -> None` — Record a borrow rate snapshot for strategy access.
    - `def get_borrow_rate(self, market: str=None, venue: str=None) -> Optional[float]` — Get the most recent hourly borrow rate for a market.
    - `def get_borrow_rates(self, market: str=None, venue: str=None, lookback: int=24) -> list` — Get recent borrow rate history as [(ts, rate_hourly), ...].
    - `def get_borrow_cumulative_at(self, market: str, ts: int) -> Optional[float]` — Get the cumulative borrow rate at or before a timestamp. Returns None if no data.
    - `def add_orderbook_snapshot(self, snapshot) -> None` — Record an orderbook snapshot. Called by engine.
    - `def get_orderbook(self, market: Optional[str]=None)` — Get the most recent orderbook snapshot for a market.
    - `def get_impact_price(self, market: Optional[str]=None, side=None, size: float=0) -> Optional[float]` — Estimate fill price for a given size, using the same model as the fill pipeline.
    - `def add_open_interest(self, oi) -> None` — Record an OI snapshot. Called by engine.
    - `def get_open_interest(self, market=None)` — Get most recent (long_oi, short_oi) for a market.
    - `def get_open_interest_history(self, market=None, lookback: int=24) -> list` — Get recent OI as [(ts, long_oi, short_oi), ...].
    - `def check_liquidations(self, candle: Candle) -> list` — Check all positions for liquidation. Called by engine each bar.
    - `def process_pending_orders(self, candle: Candle) -> List[Fill]` — Process stop/limit orders against current candles BEFORE strategy runs.
    - `def process_market_orders(self, candle: Candle) -> List[Fill]` — Process market orders placed during this bar AFTER strategy runs.
    - `def apply_funding(self, funding_rate: FundingRate) -> float` — Apply funding payment to positions on the funding_rate's
    - `def close_all_positions(self, candle: Candle) -> List[Fill]` — Force-close all positions at candle close. Used at end of backtest.
    - `def all_fills(self) -> List[Fill]`
    - `def closed_trades(self) -> List[dict]`
    - `def total_fees(self) -> float`
    - `def total_funding(self) -> float`
    - `def total_tx_costs(self) -> float`
    - `def total_borrow_paid(self) -> float`
    - `def borrow_payments(self) -> List[dict]` — Per-trade Jupiter borrow attribution rows. Used by
    - `def log_messages(self) -> List[str]`
    - `def venue_balance(self, venue: str) -> float` — Get available cash on a specific venue.
    - `def venue_balances(self) -> dict` — Get all venue balances.
    - `def transfer(self, from_venue: str, to_venue: str, amount: float) -> bool` — Transfer capital between venues. Returns True if initiated.
    - `def process_transfers(self, current_ts: int) -> int` — Process arrived transfers. Called by engine each bar. Returns count.
    - `def capital_allocator(self)` — Access the capital allocator (for metrics at end of backtest).

### `flint.execution.borrow_ledger`
_BorrowLedger — Jupiter Perps borrow history + paid-borrow ledger._
- imports: `..models`
- `class BorrowLedger`
    - `def __init__(self) -> None`
    - `def record(self, snapshot: BorrowSnapshot) -> None`
    - `def record_payment(self, payment: Dict[str, Any]) -> None`
    - `def add_paid(self, amount: float) -> None`
    - `def latest(self, market: str) -> Optional[float]`
    - `def recent(self, market: str, lookback: int=24) -> List[Tuple[int, float]]`
    - `def cumulative_at(self, market: str, ts: int) -> Optional[float]` — Cumulative-rate value at or before `ts`. Snapshots in
    - `def history(self) -> Dict[str, List[BorrowSnapshot]]`
    - `def payments(self) -> List[Dict[str, Any]]`

### `flint.execution.capital`
_Capital fragmentation — per-venue balance tracking with transfer mechanics._
- `class Transfer` — A capital transfer between venues.
- `class TransferConfig` — Transfer parameters between venues.
    - `def get_time(self, from_venue: str, to_venue: str) -> int`
    - `def get_cost(self, from_venue: str, to_venue: str) -> float`
- `class FragmentationMetrics` — Summary of capital fragmentation during a backtest.
- `class VenueAllocator` — Manages per-venue capital balances with transfer delays.
    - `def __init__(self, initial_balances: Dict[str, float], transfer_config: Optional[TransferConfig]=None)`
    - `def total_cash(self) -> float` — Total cash across all venues + in-transit.
    - `def balances(self) -> Dict[str, float]` — Current per-venue balances (read-only copy).
    - `def available(self, venue: str) -> float` — Cash available to trade on this venue right now.
    - `def debit(self, venue: str, amount: float) -> bool` — Deduct cash from a venue (for fees, margin, etc). Returns False if insufficient.
    - `def credit(self, venue: str, amount: float) -> None` — Add cash to a venue (from PnL, position close, etc).
    - `def track_pnl(self, venue: str, pnl: float) -> None` — Record PnL attribution for a venue.
    - `def transfer(self, from_venue: str, to_venue: str, amount: float, current_ts: int) -> Optional[Transfer]` — Initiate a transfer. Deducts immediately, arrives later.
    - `def process_arrivals(self, current_ts: int) -> List[Transfer]` — Credit arrived transfers. Called each bar by engine.
    - `def pending_transfers(self) -> List[Transfer]`
    - `def compute_metrics(self, final_equity: float) -> FragmentationMetrics` — Compute fragmentation metrics at end of backtest.

### `flint.execution.cash_manager`
_CashManager — owns cash, allocator, and running cost counters._
- `class CashManager` — Single-ledger cash + counters. Allocator-aware when multi-venue.
    - `def __init__(self, initial_capital: float, allocator=None) -> None`
    - `def cash(self) -> float`
    - `def cash(self, value: float) -> None`
    - `def allocator(self)`
    - `def debit(self, amount: float, venue: str='default') -> bool` — Deduct cash. Returns True on success; False when allocator
    - `def credit(self, amount: float, venue: str='default') -> None` — Add cash; routes through allocator when present. `track_pnl`
    - `def add_fee(self, amount: float) -> None`
    - `def add_tx_cost(self, amount: float) -> None`
    - `def add_funding(self, amount: float) -> None`
    - `def available(self, venue: str='default') -> float`
    - `def balances(self) -> dict`

### `flint.execution.ccxt_live`
_LiveCCXTContext — ExecutionContext for live trading on CEX venues via CCXT._
- imports: `..models`, `.live_base`
- `class LiveCCXTContext(LiveExecutionContext)` — ExecutionContext that submits real orders to CEX exchanges via CCXT.
    - `def __init__(self, exchange: Optional[str]=None, api_key: Optional[str]=None, secret: Optional[str]=None, password: Optional[str]=None, sandbox: Optional[bool]=None, market_order_slippage: float=0.002, initial_capital: float=0, risk_manager=None, store=None, session_id: str='', max_retries: int=3, on_failure: str='drop', **kwargs)`

### `flint.execution.context`
_ExecutionContext — the core abstraction for backtest-live symmetry._
- imports: `..models`
- `class ExecutionContext(abc.ABC)` — Interface between strategy logic and order execution.
    - `def account(self) -> AccountState` — Current account state: equity, cash, unrealized PnL.
    - `def positions(self) -> List[PositionInfo]` — All open positions.
    - `def pending_orders(self) -> List[Order]` — All unfilled orders.
    - `def current_candle(self) -> Optional[Candle]` — The candle currently being processed.
    - `def timestamp(self) -> int` — Current simulation or live timestamp (unix seconds).
    - `def market_order(self, market: str, side: Side, size: float, reduce_only: bool=False, tag: str='', venue: str='default') -> str` — Submit a market order. Returns order_id.
    - `def limit_order(self, market: str, side: Side, size: float, price: float, reduce_only: bool=False, tag: str='', venue: str='default') -> str` — Submit a limit order. Returns order_id.
    - `def stop_order(self, market: str, side: Side, size: float, trigger_price: float, tag: str='', venue: str='default') -> str` — Submit a stop-loss order. Triggers a market sell at trigger_price. Returns order_id.
    - `def take_profit_order(self, market: str, side: Side, size: float, trigger_price: float, tag: str='', venue: str='default') -> str` — Submit a take-profit order. Returns order_id.
    - `def cancel(self, order_id: str) -> bool` — Cancel a pending order. Returns True if cancelled.
    - `def cancel_all(self, market: Optional[str]=None) -> int` — Cancel all pending orders, optionally filtered by market. Returns count cancelled.
    - `def close_position(self, market: str, venue: str='default') -> Optional[str]` — Close the entire position for a market+venue. Returns order_id or None.
    - `def position(self, market: str, venue: str='default') -> Optional[PositionInfo]` — Get position for a specific market+venue, or None.
    - `def venue_positions(self, venue: str) -> list` — Get all positions for a specific venue.
    - `def get_candles(self, market: str, lookback: int=50) -> list` — Get recent candles for any market (multi-market backtesting).
    - `def markets(self) -> list` — List of available markets in this context. Default: empty.
    - `def get_funding_rate(self, market: Optional[str]=None) -> Optional[float]` — Get the most recent funding rate for a market.
    - `def get_funding_rates(self, market: Optional[str]=None, lookback: int=24) -> list` — Get recent funding rate history for a market.
    - `def get_funding_by_venue(self, market: Optional[str]=None, lookback: int=24) -> dict` — Get recent funding rates grouped by venue.
    - `def get_borrow_rate(self, market: str=None, venue: str=None)` — Current hourly borrow rate. Returns None for venues using funding rates.
    - `def get_borrow_rates(self, market: str=None, venue: str=None, lookback: int=24) -> list` — Historical borrow rates as [(ts, rate), ...]. Empty for non-borrow venues.
    - `def get_orderbook(self, market: Optional[str]=None)` — Get the most recent orderbook snapshot for a market.
    - `def get_impact_price(self, market: Optional[str]=None, side: Optional[Side]=None, size: float=0) -> Optional[float]` — Estimate the average fill price for a given order size.
    - `def venue_balance(self, venue: str) -> float` — Get available cash on a specific venue. Returns total cash if no allocator.
    - `def transfer(self, from_venue: str, to_venue: str, amount: float) -> bool` — Transfer capital between venues. Returns True if initiated.
    - `def venue_balances(self) -> dict` — Get all venue balances. Returns {"default": total_cash} if no allocator.
    - `def get_open_interest(self, market: Optional[str]=None) -> Optional[tuple]` — Get the most recent open interest for a market.
    - `def get_open_interest_history(self, market: Optional[str]=None, lookback: int=24) -> list` — Get recent OI history as [(ts, long_oi, short_oi), ...].
    - `def get_venue_snapshots(self, market: Optional[str]=None, lookback: int=24) -> dict` — Get full funding rate snapshots grouped by venue.
    - `def get_oracle_price(self, market: Optional[str]=None) -> Optional[tuple]` — Get latest oracle price for a market. Returns (price, timestamp) or None.
    - `def log(self, message: str) -> None` — Log a message from the strategy. Default: no-op. Override in subclasses.
    - `def estimate_cost(self, market: str, size: float, venue: str='default')` — Estimate total execution cost. Returns CostEstimate or None.

### `flint.execution.drift_live`
_LiveDriftContext — ExecutionContext for real order execution on Drift Protocol._
- imports: `..models`, `..precision`, `.live_base`, `.wallet`
- `class LiveDriftContext(LiveExecutionContext)` — ExecutionContext that submits real orders to Drift Protocol.
    - `def __init__(self, private_key: Optional[str]=None, rpc_url: Optional[str]=None, network: str='devnet', initial_capital: float=0, risk_manager=None, store=None, session_id: str='', max_retries: int=3, on_failure: str='drop')`

### `flint.execution.fee_models`
_Pluggable fee models for trade cost simulation._
- imports: `..models`
- `class FeeModel(abc.ABC)` — Computes the fee for a fill.
    - `def compute_fee(self, fill: Fill) -> float` — Return the fee in quote currency for this fill.
- `class FlatFeeModel(FeeModel)` — v0.1 behavior: flat basis-point fee on notional.
    - `def __init__(self, fee_bps: float=5.0)`
    - `def compute_fee(self, fill: Fill) -> float`
- `class DriftFeeModel(FeeModel)` — Drift protocol tiered fees: maker rebate, taker fee.
    - `def __init__(self, maker_fee: float=-0.0002, taker_fee: float=0.001)`
    - `def compute_fee(self, fill: Fill, is_maker: bool=False) -> float`
- `class ZeroFeeModel(FeeModel)` — No fees — useful for testing.
    - `def compute_fee(self, fill: Fill) -> float`
- `class MakerTakerFeeModel(FeeModel)` — Generic maker/taker fee model. Callers pass `is_maker=True` to the
    - `def __init__(self, maker_bps: float=1.0, taker_bps: float=5.0)`
    - `def compute_fee(self, fill: Fill, is_maker: bool=False) -> float`
- `class HyperliquidFeeModel(MakerTakerFeeModel)` — Hyperliquid standard perps fee schedule: 3.5 bps taker / 1 bp maker.
    - `def __init__(self, maker_bps: float=1.0, taker_bps: float=3.5)`

### `flint.execution.fill_cex`
_CEX fill models for Binance, OKX, and Bybit._
- imports: `..models`, `.fill_models`, `.synthetic_depth`
- `class CexFillModel(FillModel)` — Base CEX fill model.  Walks L2 orderbook or falls back to synthetic depth.
    - `def __init__(self, venue: str, taker_fee_bps: float) -> None`
    - `def set_orderbook(self, book: Optional[OrderbookSnapshot]) -> None` — Store the current orderbook snapshot (called by the engine each bar).
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill a market order, walking the book or using synthetic depth.
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill a limit order using simple price-cross logic.
- `class BinanceFillModel(CexFillModel)` — Binance perpetuals fill model.
    - `def __init__(self) -> None`
- `class OkxFillModel(CexFillModel)` — OKX perpetuals fill model.
    - `def __init__(self) -> None`
- `class BybitFillModel(CexFillModel)` — Bybit perpetuals fill model with IOC price-band semantics.
    - `def __init__(self) -> None`
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill market order capped at a 1% price band (IOC conversion).
- `class CoinbaseFillModel(CexFillModel)` — Coinbase perpetuals fill model.
    - `def __init__(self) -> None`
- `class KrakenFillModel(CexFillModel)` — Kraken perpetuals fill model.
    - `def __init__(self) -> None`
- `class KucoinFillModel(CexFillModel)` — KuCoin perpetuals fill model.
    - `def __init__(self) -> None`
- `class GateFillModel(CexFillModel)` — Gate.io perpetuals fill model.
    - `def __init__(self) -> None`
- `class BitgetFillModel(CexFillModel)` — Bitget perpetuals fill model.
    - `def __init__(self) -> None`
- `class MexcFillModel(CexFillModel)` — MEXC perpetuals fill model.
    - `def __init__(self) -> None`
- `class HtxFillModel(CexFillModel)` — HTX (Huobi) perpetuals fill model.
    - `def __init__(self) -> None`

### `flint.execution.fill_drift`
_DriftFillModel — 3-tier execution pipeline for Drift protocol._
- imports: `..models`, `.fill_models`, `.synthetic_depth`, `.vamm`
- `class DriftFillModel(FillModel)` — Simulate Drift's 3-tier execution: JIT auction -> DLOB -> vAMM.
    - `def __init__(self, jit_fill_probability: float=0.6, auction_slots: int=20, auction_price_improvement_bps: float=2.0, seed: Optional[int]=None) -> None`
    - `def set_orderbook(self, book: Optional[OrderbookSnapshot]) -> None` — Provide a real DLOB snapshot for Tier 2.  Called by the engine
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill a market order through the 3-tier Drift pipeline.
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]` — Limit orders: fill when price crosses the limit (passive maker).

### `flint.execution.fill_hyperliquid`
_Hyperliquid-specific fill model._
- imports: `..models`, `.fill_models`, `.synthetic_depth`
- `class HyperliquidFillModel(FillModel)` — Walk the Hyperliquid CLOB; HLP vault backstops any unfilled remainder.
    - `def __init__(self, impact_coefficient: float=0.005) -> None`
    - `def set_orderbook(self, book: Optional[OrderbookSnapshot]) -> None` — Called by the engine/context to update the current orderbook state.
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill a market order by walking CLOB levels, then HLP backstop.
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill a limit order if the candle's range crosses the limit price.

### `flint.execution.fill_jupiter`
_Jupiter Perps fill model — keeper-delay-aware pool-based execution._
- imports: `..models`, `.fill_models`
- `class JupiterFillModel(FillModel)` — Fill model for Jupiter Perps: pool-based, keeper-delay-aware.
    - `def __init__(self, base_latency_s: float=12.0, latency_jitter_s: float=8.0, seed: Optional[int]=None) -> None`
    - `def set_orderbook(self, book: Optional[OrderbookSnapshot]) -> None` — No-op: Jupiter Perps is pool-based and does not use an orderbook.
    - `def set_candle_buffer(self, candles: List[Candle]) -> None` — Provide lookahead candles used for cross-bar keeper delay interpolation.
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill a market order at the keeper-delayed oracle price plus impact.
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]` — Fill a limit order if the candle's range crosses the limit price.

### `flint.execution.fill_models`
_Pluggable fill models for order execution simulation._
- imports: `..models`, `.impact`, `.latency`, `.partial_fill`
- `class FillModel(abc.ABC)` — Determines execution price for an order.
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]` — Attempt to fill a market order against a candle.
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]` — Attempt to fill a limit order if price crosses the limit.
    - `def check_stop_trigger(self, order: Order, candle: Candle) -> bool` — Check if a stop order's trigger price was hit during this candle.
- `class ClosePriceFill(FillModel)` — v0.1 behavior: fill market orders at candle close price.
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]`
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]`
- `class NextBarOpenFill(FillModel)` — Fill market orders at the next bar's open price (more realistic).
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]`
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]`
- `class SlippageFill(FillModel)` — Apply basis-point slippage to market orders.
    - `def __init__(self, slippage_bps: float=5.0)`
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]`
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]`
- `class OrderbookFillModel(FillModel)` — Walk the orderbook to compute realistic fill prices.
    - `def __init__(self, fallback_slippage_bps: float=5.0, reject_on_insufficient_depth: bool=True)` — Args:
    - `def set_orderbook(self, book: Optional[OrderbookSnapshot]) -> None` — Called by the engine/context to set the current orderbook state.
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]`
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]`
- `class FillPipeline(FillModel)` — Composable fill pipeline: latency -> impact -> partial fill.
    - `def __init__(self, impact_coefficient: float=0.005, fallback_bps: float=5.0, base_latency_s: float=1.0, latency_jitter_s: float=0.5, latency_seed: Optional[int]=None, latency_enabled: bool=True, tx_cost_model=None, vamm_configs=None)`
    - `def set_orderbook(self, book: Optional[OrderbookSnapshot]) -> None` — Called by the engine to set the current orderbook state.
    - `def pending_resting_orders(self) -> list` — GTC resting orders created during the last fill_market call.
    - `def drain_resting_orders(self) -> list` — Pop and return resting orders. Called by context to add to pending.
    - `def fill_market(self, order: Order, candle: Candle) -> Optional[Fill]`
    - `def fill_limit(self, order: Order, candle: Candle) -> Optional[Fill]` — Limit orders use simple price-cross logic (same as legacy).

### `flint.execution.fill_recorder`
_FillRecorder — owns the recorded-fill list and the diagnostic log._
- imports: `..models`
- `class FillRecorder` — Append-only ledgers for recorded fills + diagnostic log messages.
    - `def __init__(self) -> None`
    - `def fills(self) -> List[Fill]` — Direct list access — used by `_apply_fill` etc. Treat as
    - `def record(self, fill: Fill) -> None`
    - `def all_fills(self) -> List[Fill]` — Snapshot copy — what `BacktestContext.all_fills` returns.
    - `def logs(self) -> List[str]`
    - `def log(self, message: str) -> None`
    - `def messages(self) -> List[str]` — Snapshot copy — what `BacktestContext.log_messages` returns.

### `flint.execution.fill_registry`
_Registry for creating venue-specific fill models._
- imports: `.fill_models`
- `def create_fill_model(venue: str, **kwargs) -> FillModel` — Create a fill model for a specific venue.
- `def create_venue_fill_models(venues: list, **kwargs) -> Dict[str, FillModel]` — Create fill models for a list of venues.

### `flint.execution.funding_ledger`
_FundingLedger — recorded funding rates per (market, venue)._
- imports: `..models`
- `class FundingLedger` — Append-only per-market and per-venue funding history.
    - `def __init__(self) -> None`
    - `def add(self, fr: FundingRate) -> None`
    - `def latest(self, market: str) -> Optional[float]`
    - `def recent(self, market: str, lookback: int=24) -> List[Tuple[int, float]]`
    - `def by_venue(self, market: str, lookback: int=24) -> Dict[str, List[Tuple[int, float]]]`
    - `def venue_snapshots(self, market: str, lookback: int=24) -> Dict[str, List[FundingRate]]`
    - `def history(self) -> Dict[str, List[FundingRate]]`
    - `def venue_history(self) -> Dict[str, Dict[str, List[FundingRate]]]`

### `flint.execution.holding_cost`
_Holding cost models for different venue fee structures._
- `class HoldingCostModel(ABC)`
    - `def cost_at_bar(self, **kwargs) -> float` — Unrealized holding cost at a bar for margin/liquidation checks.
    - `def cost_at_close(self, **kwargs) -> float` — Realized holding cost when position is closed.
- `class FundingCostModel(HoldingCostModel)`
    - `def cost_at_bar(self, *, side: str, size_usd: float, rate: float) -> float`
    - `def cost_at_close(self, *, side: str, size_usd: float, rate: float) -> float`
- `class BorrowCostModel(HoldingCostModel)`
    - `def cost_at_bar(self, *, cumulative_entry: float, cumulative_now: float, size_usd: float) -> float`
    - `def cost_at_close(self, *, cumulative_entry: float, cumulative_close: float, size_usd: float) -> float`

### `flint.execution.hyperliquid_live`
_LiveHyperliquidContext — ExecutionContext for live trading on Hyperliquid._
- imports: `..connectors.hyperliquid`, `..models`, `.live_base`
- `class LiveHyperliquidContext(LiveExecutionContext)` — ExecutionContext that submits real orders to Hyperliquid.
    - `def __init__(self, private_key: Optional[str]=None, network: str='testnet', market_order_slippage: float=0.003, initial_capital: float=0, risk_manager=None, store=None, session_id: str='', max_retries: int=3, on_failure: str='drop', **kwargs)`

### `flint.execution.impact`
_ImpactStage — computes fill price using vAMM, orderbook, sqrt model, or flat bps._
- imports: `..models`, `.vamm`
- `class ImpactResult` — Output of the impact stage.
- `class ImpactStage` — Determines fill price via four-tier fallback.
    - `def __init__(self, impact_coefficient: float=0.005, fallback_bps: float=5.0, vamm_configs: Optional[Dict[str, 'VammCurve']]=None)`
    - `def compute(self, order: Order, candle: Candle, book: Optional[OrderbookSnapshot]) -> ImpactResult` — Compute fill price and available liquidity for an order.

### `flint.execution.jupiter_costs`
_Jupiter Perps transaction cost model._
- `class JupiterCostEstimate`
- `class JupiterTxCostModel`
    - `def __init__(self, fee_rate: float=_FEE_RATE, impact_k: float=_IMPACT_K) -> None`
    - `def estimate_borrow(self, size_usd: float, hours: float, rate_hourly: float) -> float`
    - `def estimate_round_trip(self, size_usd: float, hold_hours: float, rate_hourly: float) -> JupiterCostEstimate`

### `flint.execution.jupiter_live`
_Live execution context for Jupiter Perps._
- imports: `..models`
- `class LiveJupiterContext`
    - `def __init__(self, sidecar_url='http://127.0.0.1:8401', store=None)`
    - `def close(self)`
    - `def get_mark_price(self, market: str) -> float`
    - `def get_positions(self) -> List[PositionInfo]`
    - `def get_balances(self) -> dict`
    - `def market_order(self, market: str, side: str, size: float, **kwargs) -> str`
    - `def get_borrow_rate(self, market=None, venue=None)`
    - `def get_borrow_rates(self, market=None, venue=None, lookback=24)`

### `flint.execution.jupiter_sidecar`
_Jupiter Perps sidecar process manager._
- `class JupiterSidecar`
    - `def __init__(self, port=8401, rpc_url='', wallet_path='', max_restarts=3)`
    - `def is_running(self) -> bool`
    - `def base_url(self) -> str`
    - `def start(self) -> bool`
    - `def stop(self)`

### `flint.execution.latency`
_LatencyStage — models venue-specific execution delays._
- imports: `..models`
- `class LatencyStage` — Computes execution delay for orders based on venue latency.
    - `def __init__(self, base_latency_s: float=1.0, latency_jitter_s: float=0.5, seed: Optional[int]=None, enabled: bool=True)`
    - `def compute_eligible_ts(self, order: Order) -> int` — Compute the earliest timestamp at which this order can fill.
    - `def is_eligible(eligible_ts: int, current_ts: int) -> bool` — Check if an order's delay has elapsed.

### `flint.execution.live_base`
_LiveExecutionContext — abstract base class for all live venue implementations._
- imports: `..models`, `..notifications.base`, `..risk.guards`, `.borrow_ledger`, `.cash_manager`, `.context`, `.fill_recorder`, `.funding_ledger`, `.market_data_feed`, `.order_queue`, `.order_tracker`, `.position_manager`
- `class LiveExecutionContext(ExecutionContext, abc.ABC)` — Base class for live venue execution contexts.
    - `def __init__(self, venue: str='default', initial_capital: float=0.0, risk_manager: Optional[RiskManager]=None, store=None, session_id: str='', max_retries: int=3, on_failure: str='drop', max_orders_per_sec: int=10, max_concurrent_tx: int=2, tick_interval_s: int=60, position_sync_interval: int=5, limit_order_timeout_bars: int=10, tick_mode: str='on_candle_close', tick_markets: Optional[List[str]]=None, dry_run: bool=False, notification_manager=None, tx_cost_model=None)`
    - `async def connect(self) -> None`
    - `async def disconnect(self) -> None`
    - `async def run(self, strategy, market: str, feeds=None, fetch_candle=None) -> None` — Run the strategy tick loop.
    - `async def stop(self) -> None` — Stop the tick loop gracefully.
    - `def account(self) -> AccountState`
    - `def positions(self) -> List[PositionInfo]`
    - `def pending_orders(self) -> List[Order]`
    - `def current_candle(self) -> Optional[Candle]`
    - `def timestamp(self) -> int`
    - `def market_order(self, market, side, size, reduce_only=False, tag='', venue='default')`
    - `def limit_order(self, market, side, size, price, reduce_only=False, tag='', venue='default')`
    - `def stop_order(self, market, side, size, trigger_price, tag='', venue='default')`
    - `def take_profit_order(self, market, side, size, trigger_price, tag='', venue='default')`
    - `def cancel(self, order_id)`
    - `def cancel_all(self, market=None)`
    - `def modify_order(self, order_id: str, new_size: Optional[float]=None, new_price: Optional[float]=None) -> str` — Modify an order by cancelling and replacing it.
    - `async def submit_pending_orders(self) -> List[Fill]`
    - `def log(self, message: str) -> None`
    - `def estimate_cost(self, market: str, size: float, venue: str='default')`
    - `def get_oracle_price(self, market: Optional[str]=None) -> Optional[Tuple[float, int]]` — Get latest oracle price. Returns (price, ts) or None.

### `flint.execution.margin`
_Margin and liquidation tracking for backtesting._
- imports: `..models`, `.venue_config`
- `class LiquidationEvent` — Record of a forced liquidation during backtest.
- `class MarginState` — Snapshot of margin utilization.
- `class MarginStats` — Accumulated margin statistics over a backtest.
    - `def avg_leverage(self) -> float`
- `def compute_liquidation_price(entry_price: float, size: float, collateral: float, maintenance_margin: float=0.05, is_long: bool=True) -> float` — Compute oracle price at which a position gets liquidated.
- `class MarginEngine` — Enforces margin requirements and tracks liquidation risk.
    - `def __init__(self, venue_configs: Optional[Dict[str, VenueConfig]]=None)`
    - `def get_config(self, venue: str) -> VenueConfig`
    - `def check_can_open(self, order: Order, cash: float, positions: List[PositionInfo], current_price: float) -> Tuple[bool, str]` — Check if this order can be opened given current margin.
    - `def compute_position_liq_price(self, entry_price: float, size: float, venue: str, side: Side, cash: float) -> float` — Compute liquidation price for a new position.
    - `def check_liquidations(self, positions: Dict, current_prices: Dict[str, float], current_ts: int) -> List[LiquidationEvent]` — Check all positions for liquidation.
    - `def update_stats(self, positions: List[PositionInfo], cash: float) -> None` — Update accumulated stats. Called once per bar.
    - `def compute_margin_state(self, cash: float, positions: List[PositionInfo]) -> MarginState` — Compute current margin utilization snapshot.
    - `def liquidation_events(self) -> List[LiquidationEvent]`
    - `def reset(self) -> None`

### `flint.execution.market_data_feed`
_MarketDataFeed — orderbook + OI + cross-market candle history._
- imports: `..models`
- `class MarketDataFeed`
    - `def __init__(self) -> None`
    - `def set_histories(self, histories: Dict[str, List[Candle]]) -> None`
    - `def candles(self, market: str, lookback: int=50) -> List[Candle]`
    - `def history_for(self, market: str) -> List[Candle]` — Full history for a market — used by `set_candle` to look up
    - `def markets(self) -> List[str]`
    - `def market_histories(self) -> Dict[str, List[Candle]]`
    - `def market_histories(self, value: Dict[str, List[Candle]]) -> None`
    - `def add_orderbook(self, snapshot) -> None`
    - `def latest_orderbook(self, market: str)`
    - `def orderbook_history(self) -> Dict[str, List[Any]]`
    - `def add_open_interest(self, oi) -> None`
    - `def latest_oi(self, market: str) -> Optional[Tuple[float, float]]`
    - `def oi_recent(self, market: str, lookback: int=24) -> List[Tuple[int, float, float]]`
    - `def oi_history(self) -> Dict[str, List[Any]]`

### `flint.execution.multi_venue_live`
_MultiVenueLiveContext — wraps multiple venue contexts for cross-venue strategies._
- imports: `..models`, `.context`, `.live_base`
- `class MultiVenueLiveContext(ExecutionContext)` — Wraps multiple LiveExecutionContext instances for cross-venue trading.
    - `def __init__(self, contexts: Dict[str, LiveExecutionContext], primary_venue: str='', tick_mode: str='primary', leg_timeout_s: float=30.0, auto_unwind_failed_legs: bool=False)`
    - `def account(self) -> AccountState`
    - `def positions(self) -> List[PositionInfo]`
    - `def pending_orders(self) -> List[Order]`
    - `def current_candle(self) -> Optional[Candle]`
    - `def timestamp(self) -> int`
    - `def venue_account(self, venue: str) -> AccountState`
    - `def total_exposure(self, market: str) -> float`
    - `def per_venue_pnl(self) -> Dict[str, float]`
    - `def market_order(self, market, side, size, reduce_only=False, tag='', venue='default')`
    - `def limit_order(self, market, side, size, price, reduce_only=False, tag='', venue='default')`
    - `def stop_order(self, market, side, size, trigger_price, tag='', venue='default')`
    - `def take_profit_order(self, market, side, size, trigger_price, tag='', venue='default')`
    - `def cancel(self, order_id)`
    - `def cancel_all(self, market=None)`
    - `def log(self, message: str) -> None`
    - `def estimate_cost(self, market: str, size: float, venue: str='default')`
    - `async def submit_leg_group(self, legs: List[OrderLeg]) -> LegGroupResult` — Submit a group of paired orders across venues.
    - `async def run(self, strategy, market: str, feeds=None, fetch_candle=None) -> None` — Run the multi-venue tick loop.
    - `async def stop(self) -> None`
    - `def close_position(self, market, venue='default')`
    - `def position(self, market, venue='default')`
    - `def get_candles(self, market, lookback=50)`
    - `def get_oracle_price(self, market=None)`
    - `def get_funding_rate(self, market=None)`
    - `def get_funding_by_venue(self, market=None, lookback=24)`

### `flint.execution.order_queue`
_OrderQueue — owns the two order queues used by BacktestContext._
- imports: `..models`
- `class OrderQueue`
    - `def __init__(self, pending_cap: int=DEFAULT_PENDING_CAP) -> None`
    - `def pending(self) -> List[Order]`
    - `def pending(self, new_list: List[Order]) -> None`
    - `def add_pending(self, order: Order) -> bool` — Append to the resting queue, returning False when capped.
    - `def cancel(self, order_id: str) -> bool`
    - `def cancel_all(self, market: Optional[str]=None) -> int`
    - `def is_full(self) -> bool`
    - `def snapshot(self) -> List[Order]` — Read-only copy — what `BacktestContext.pending_orders` returns.
    - `def market_queue(self) -> List[Order]`
    - `def market_queue(self, new_list: List[Order]) -> None`
    - `def add_market(self, order: Order) -> None`
    - `def drain_market(self) -> List[Order]` — Atomically swap out the market queue and return the old list.

### `flint.execution.order_tracker`
_OrderTracker — manages the full lifecycle of live orders._
- imports: `..models`
- `class TrackedOrder` — Wraps a Flint Order with lifecycle tracking metadata.
    - `def flint_order_id(self) -> str`
    - `def is_terminal(self) -> bool`
    - `def filled_size(self) -> float`
    - `def transition(self, new_state: OrderState, tx_sig: Optional[str]=None, venue_order_id: Optional[int]=None) -> None`
    - `def add_fill(self, fill: Fill) -> None`
    - `def to_state_history_json(self) -> str`
- `class OrderTracker` — Manages active and completed orders with rate limiting.
    - `def __init__(self, max_retries: int=3, on_failure: str='drop', max_orders_per_sec: int=10, max_concurrent_tx: int=2, on_fill: Optional[Callable]=None, on_fail: Optional[Callable]=None, on_cancel: Optional[Callable]=None, on_state_change: Optional[Callable]=None)`
    - `def submit(self, order: Order) -> TrackedOrder`
    - `def get(self, order_id: str) -> Optional[TrackedOrder]`
    - `def get_pending(self) -> List[TrackedOrder]`
    - `def in_flight_count(self) -> int`
    - `def can_submit(self) -> bool`
    - `def record_submission(self) -> None`
    - `def mark_submitted(self, order_id: str, tx_sig: str) -> None`
    - `def mark_confirmed(self, order_id: str, venue_order_id: int, bar_count: int=0) -> None`
    - `def mark_filled(self, order_id: str, fill: Fill) -> None`
    - `def mark_cancelled(self, order_id: str) -> None`
    - `def mark_expired(self, order_id: str) -> None`
    - `def mark_failed(self, order_id: str, reason: str) -> None`
    - `def increment_retry(self, order_id: str) -> bool`
    - `def get_submitted(self) -> List[TrackedOrder]` — Get all orders in SUBMITTED state (awaiting on-chain confirmation).
    - `def get_confirmed(self) -> List[TrackedOrder]` — Get all orders in CONFIRMED or PARTIALLY_FILLED state (awaiting fill).
    - `def check_timeouts(self, submission_timeout_s: int=30, current_bar: int=0, limit_timeout_bars: int=10) -> List[str]` — Check for timed-out orders. Returns list of order_ids that timed out.

### `flint.execution.partial_fill`
_PartialFillStage — applies time-in-force semantics to determine fill size._
- imports: `..models`, `.impact`
- `class FillDecision` — Output of the partial fill stage.
- `class PartialFillStage` — Applies time-in-force semantics to determine actual fill size.
    - `def decide(self, order: Order, impact: ImpactResult) -> FillDecision`

### `flint.execution.position_manager`
_PositionManager — owns the open + closed position dicts._
- `class PositionManager` — Container around the (venue, market) → position dict + closed log.
    - `def __init__(self) -> None`
    - `def get(self, key: Tuple[str, str])`
    - `def set(self, key: Tuple[str, str], position) -> None`
    - `def delete(self, key: Tuple[str, str]) -> None`
    - `def keys(self)`
    - `def values(self)`
    - `def items(self)`
    - `def positions(self) -> Dict[Tuple[str, str], Any]` — Direct dict access — used where the margin engine needs to
    - `def update_pnl_for_market(self, market: str, price: float) -> None` — Mark every position in `market` to `price` and refresh
    - `def record_close(self, record: Dict[str, Any]) -> None`
    - `def closed(self) -> List[Dict[str, Any]]`

### `flint.execution.synthetic_depth`
_Synthetic orderbook depth model._
- imports: `..models`
- `class DepthProfile` — Venue depth characteristics for BTC-PERP baseline.
- `def generate_synthetic_book(mid_price: float, profile: DepthProfile, levels: int=20, market: str='', ts: int=0) -> OrderbookSnapshot` — Generate a synthetic orderbook from a depth profile.

### `flint.execution.tx_costs`
_Transaction cost models for Solana and cross-venue trading._
- `class CostEstimate` — Breakdown of all costs for a single trade execution.
    - `def total(self) -> float` — Sum of all cost components.
    - `def to_dict(self) -> Dict[str, float]`
- `class TxCostModel(ABC)` — Abstract base class for transaction cost models.
    - `def estimate(self, market: str, size: float, price: float, urgency: str='normal') -> CostEstimate` — Estimate the full cost of executing a trade.
    - `def venue(self) -> str` — Venue identifier, e.g. "drift", "hyperliquid", "binance".
- `class SolanaTxCostModel(TxCostModel)` — Cost model for Solana-based venues (e.g. Drift).
    - `def venue(self) -> str`
    - `def estimate(self, market: str, size: float, price: float, urgency: str='normal') -> CostEstimate`
- `class HyperliquidTxCostModel(TxCostModel)` — Cost model for Hyperliquid (EVM L1, negligible network costs).
    - `def venue(self) -> str`
    - `def estimate(self, market: str, size: float, price: float, urgency: str='normal') -> CostEstimate`
- `class CexTxCostModel(TxCostModel)` — Cost model for centralised exchanges (no on-chain fees).
    - `def venue(self) -> str`
    - `def estimate(self, market: str, size: float, price: float, urgency: str='normal') -> CostEstimate`
- `def get_tx_cost_model(venue: str) -> TxCostModel` — Return the appropriate TxCostModel for a given venue name.

### `flint.execution.vamm`
_VammCurve — constant-product AMM model for Drift fill price estimation._
- `class VammCurve` — Constant-product virtual AMM with peg multiplier.
    - `def __init__(self, sqrt_k: float, peg_multiplier: float)`
    - `def from_oracle_price(cls, sqrt_k: float, oracle_price: float) -> 'VammCurve'`
    - `def reserve_price(self) -> float`
    - `def fill_price(self, base_amount: float, direction: str) -> float`
    - `def impact_bps(self, base_amount: float, direction: str, oracle_price: float) -> float`
- `class VammAccuracyReport`
    - `def summary(self) -> str`
    - `def to_dict(self) -> dict`

### `flint.execution.venue_config`
_Venue configuration for multi-venue backtesting._
- `class VenueConfig` — Trading parameters for a single venue.
    - `def taker_fee_rate(self) -> float`
    - `def maker_fee_rate(self) -> float`
- `def get_venue_config(venue: str) -> VenueConfig` — Get venue config by name, falling back to default.
- `def load_venue_configs(yaml_config: Optional[dict]=None) -> Dict[str, VenueConfig]` — Load venue configs from YAML config, merged with defaults.

### `flint.execution.wallet`
_WalletAdapter — signing abstraction for live trading._
- `class WalletAdapter(abc.ABC)` — Abstract base for transaction signing.
    - `async def sign_and_send(self, tx, connection) -> str` — Sign a transaction and send it. Returns tx signature string.
    - `def public_key(self)` — Return the wallet's public key.
- `class KeypairAdapter(WalletAdapter)` — Signs transactions locally using a base58-encoded private key.
    - `def __init__(self, private_key: str | None=None)`
    - `def public_key(self)`
    - `def keypair(self)` — Access the underlying Keypair for driftpy DriftClient.
    - `async def sign_and_send(self, tx, connection) -> str` — Sign and send a transaction via the RPC connection.
- `class BrowserWalletAdapter(WalletAdapter)` — Placeholder for browser extension wallet signing (Phantom, Brave, etc.).
    - `async def sign_and_send(self, tx, connection) -> str`
    - `def public_key(self)`
