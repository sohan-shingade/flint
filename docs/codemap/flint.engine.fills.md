# Codemap: `flint.engine.fills`

> source git hash: `a4a80d9fd30c790134780969a2a392088c28d23d`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.engine.fills.__init__`
_engine.fills — FillModel interface plus CLOB and oracle-pool fill models (§6.3)._
- imports: `.arrow`, `.assemble`, `.base`, `.clob`, `.latency`, `.naive`, `.oracle_pool`, `flint.venues`
- `def fill_model_for(structure: MarketStructure) -> FillModel` — Return the fill model a venue of this ``structure`` uses (§6.3).

### `flint.engine.fills.arrow`
_Arrow-native Tier-A taker book-walk — the §19.4 depth-replay hot path._
- imports: `..money`, `.base`, `.clob`, `flint.core.models`
- `class TakerWalk` — Vectorized book-walk result for a batch of snapshots (one order each).
- `def depth_level_arrays(depth: pa.Table, side: Side) -> tuple[np.ndarray, np.ndarray, np.ndarray]` — Zero-copy flat level arrays for the side a taker crosses (asks buy, bids sell).
- `def taker_walk_batch(px: np.ndarray, sz: np.ndarray, level_offsets: np.ndarray, *, buy: bool, order_size: float, band_hi: np.ndarray | float | None=None, band_lo: np.ndarray | float | None=None, eps: float=_EPS) -> TakerWalk` — Walk every snapshot's book for a fixed-size taker order, vectorized (§6.3).
- `def materialize(walk: TakerWalk, *, order: Order, ts: np.ndarray, price_sig_figs: int, taker_fee_rate: float, tier: str='A') -> list[FillResult | None]` — Turn walk arrays into per-snapshot ``FillResult``\ s, bit-identical to scalar.
- `def arrow_taker_fills(depth: pa.Table, order: Order, *, taker_fee_rate: float, price_sig_figs: int=0, oracle_price: np.ndarray | float=0.0, oracle_band_bps: float=0.0, tier: str='A') -> list[FillResult | None]` — Fill a fixed taker ``order`` against every snapshot in ``depth`` (§6.3).

### `flint.engine.fills.assemble`
_Fill-context assembly — the one builder both engines share (§6.0, §6.3, D29)._
- imports: `.base`, `.latency`, `flint.core.models`, `flint.core.time`, `flint.venues`
- `def assemble_fill_context(order: Order, *, reference_price: float, candle: Candle, market_marks: list[MarkSnapshot], spec: VenueSpec, latency: LatencyModel, rng: Random, books: dict[str, list[OrderbookSnapshot]], trades: dict[str, list[TradePrint]]) -> FillContext` — Assemble the :class:`FillContext` at the order's effective time (§6.3).

### `flint.engine.fills.base`
_The ``FillModel`` seam — one interface, tiered implementations (§6.3)._
- imports: `flint.core.models`
- `class TradePrint` — One executed trade on the tape — the evidence a maker queue cleared (§6.3).
- `class FillContext` — What a ``FillModel`` sees at the effective time of a fill.
- `class FillResult` — A ``Fill`` plus attribution flags the loop records on the FILL event.
- `class FillModel(Protocol)` — Turn an ``Order`` into a ``FillResult`` at its effective time, or reject it.
    - `def fill(self, order: Order, ctx: FillContext) -> FillResult | None` — Return the resulting ``FillResult``, or ``None`` if rejected.

### `flint.engine.fills.clob`
_``ClobFillModel`` — the honest central-limit-order-book fill (§6.3)._
- imports: `..money`, `.base`, `flint.core.models`
- `class ClobFillModel(FillModel)` — CLOB fills across fidelity tiers A/B/C, chosen by available data (§6.3).
    - `def fill(self, order: Order, ctx: FillContext) -> FillResult | None`

### `flint.engine.fills.latency`
_Latency — the one-way decision→eligible delay, seeded and reproducible (§6.3)._
- imports: `flint.venues`
- `class LatencyModel` — Lognormal one-way latency in ms with a given median and 95th percentile.
    - `def __init__(self, profile: LatencyProfile) -> None`
    - `def draw_ms(self, rng: Random) -> float` — Draw one latency in milliseconds from the seeded ``rng``.

### `flint.engine.fills.naive`
_``NaiveFillModel`` — a Tier-C placeholder fill for loop-mechanics tests (§6.3)._
- imports: `..money`, `.base`, `flint.core.models`
- `class NaiveFillModel(FillModel)` — Full fill at the reference price, taker fee, fidelity tier C.
    - `def fill(self, order: Order, ctx: FillContext) -> FillResult | None`

### `flint.engine.fills.oracle_pool`
_``OraclePoolFillModel`` — the oracle-pool seam, stub until Jupiter (D28)._
- imports: `.base`, `flint.core.models`
- `class OraclePoolFillModel(FillModel)` — Interface stub for oracle-pool fills — implemented in v1.x with Jupiter.
    - `def fill(self, order: Order, ctx: FillContext) -> FillResult | None`
