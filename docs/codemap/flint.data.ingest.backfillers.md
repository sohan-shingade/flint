# Codemap: `flint.data.ingest.backfillers`

> source git hash: `ed058a8f1c5615fb91d76512b05ee6bcf7203bdd`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.data.ingest.backfillers.__init__`
_data.ingest.backfillers — REST + bulk CSV/S3 backfill (§9.1 class 2, §10)._
- imports: `.cex_funding`, `.hyperliquid`

### `flint.data.ingest.backfillers.cex_funding`
_Read-only CEX funding (+OI) ingestion via CCXT (§10, D28)._
- imports: `...normalize`, `...ranges`, `..quality`
- `class CcxtExchange(Protocol)` — The slice of a ``ccxt`` exchange this module drives (funding + OI history).
    - `def fetch_funding_rate_history(self, symbol: str, since: int | None=None, limit: int | None=None, params: Mapping[str, Any]=...) -> list[Mapping[str, Any]]`
    - `def fetch_open_interest_history(self, symbol: str, timeframe: str=..., since: int | None=None, limit: int | None=None, params: Mapping[str, Any]=...) -> list[Mapping[str, Any]]`
- `class CcxtExchangeFactory(Protocol)` — Builds a ``CcxtExchange`` for a venue id — the injectable transport seam.
    - `def create(self, venue: str) -> CcxtExchange`
- `class LazyCcxtFactory` — Production factory — constructs ``ccxt.<venue>()`` with ``ccxt`` imported lazily.
    - `def __init__(self, *, options: Mapping[str, Any] | None=None) -> None`
    - `def create(self, venue: str) -> CcxtExchange`
- `def cex_symbol(market: str, *, quote: str='USDT') -> str` — Map a Flint market to a CCXT unified swap symbol: ``SOL-PERP`` -> ``SOL/USDT:USDT``.
- `def funding_rate_to_hourly(per_interval_rate: float, interval_s: int) -> float` — Divide a per-settlement rate by the interval in hours to a comparable hourly rate.
- `class CexFundingProvider` — Read-only CEX funding (+OI) provider for one venue — a ``VenueProvider`` (§10).
    - `def __init__(self, venue: str, factory: CcxtExchangeFactory, *, symbol_map: Mapping[str, str] | None=None, quote: str='USDT', coverage_floor_ms: int | None=None, page_limit: int=1000, oi_timeframe: str='1h') -> None`
    - `def supports(self, market: str, kind: Kind) -> bool`
    - `def coverage_floor(self, market: str, kind: Kind) -> int | None`
    - `def fetch_range(self, market: str, kind: Kind, span: TimeRange) -> pa.Table`
    - `def backfill(self, market: str, kind: Kind, span: TimeRange, sink: Any) -> BackfillResult` — Fetch ``kind`` over ``span``, run the pre-write bars, upsert into ``sink``.

### `flint.data.ingest.backfillers.hyperliquid`
_Hyperliquid REST provider + S3 archive backfiller (§9.1, D28)._
- imports: `...normalize`, `...ranges`, `..quality`, `..transport`
- `def coin_of(market: str) -> str` — Map a Flint market symbol to HL's coin id: ``SOL-PERP`` -> ``SOL``.
- `def interval_of(resolution_s: int) -> str` — Map a bar width in seconds to HL's interval string (e.g. 3600 -> ``1h``).
- `class UpsertSink(Protocol)` — The write side a backfiller targets — matches ``InMemoryCacheSource.store``.
    - `def store(self, venue: str, market: str, kind: Kind, table: pa.Table) -> None`
- `class HyperliquidRestProvider` — HL free-venue provider for candles + funding — a ``VenueProvider`` (§9.1).
    - `def __init__(self, transport: HttpTransport, *, resolution_s: int=60, url: str=HL_INFO_URL, coverage_floor_ms: int=HL_INCEPTION_MS) -> None`
    - `def supports(self, market: str, kind: Kind) -> bool`
    - `def coverage_floor(self, market: str, kind: Kind) -> int | None`
    - `def fetch_range(self, market: str, kind: Kind, span: TimeRange) -> pa.Table`
- `class HyperliquidS3Backfiller` — Backfill HL depth + OI from the free public S3 archive (§9.1 class 2).
    - `def __init__(self, object_store: ObjectStore, sink: UpsertSink, *, codec: str='raw', base_hint: str=HL_ARCHIVE_BASE) -> None`
    - `def l2book_key(market: str, date: str, hour: int) -> str` — Archive key for an hour of ``l2Book`` depth: ``.../{H}/l2Book/{coin}``.
    - `def asset_ctxs_key(date: str) -> str` — Archive key for a day of ``asset_ctxs`` (all coins in one file).
    - `def backfill_l2book(self, market: str, date: str, hours: Sequence[int]) -> BackfillResult` — Backfill ``l2Book`` depth for ``market`` on ``date`` over ``hours``.
    - `def backfill_asset_ctxs(self, markets: Sequence[str], date: str) -> dict[str, BackfillResult]` — Backfill daily OI/mark/oracle for ``markets`` from one ``asset_ctxs`` file.
