# Codemap: `flint.connectors.jupiter`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.connectors.jupiter.__init__`
- imports: `.client`

### `flint.connectors.jupiter.client`
_Jupiter connector — quote and swap via Jupiter's Aggregator API._
- imports: `...models`, `...providers.jupiter`, `..base`
- `class JupiterConnector(BaseConnector)` — Jupiter DEX aggregator connector.
    - `def __init__(self, client: Optional[httpx.Client]=None) -> None`
    - `def protocol(self) -> str`
    - `async def get_markets(self) -> List[MarketInfo]`
    - `async def get_orderbook(self, market: str) -> OrderbookSnapshot`
    - `async def get_price(self, market: str) -> float` — Get price by executing a small quote.
    - `async def get_quote(self, input_mint: str, output_mint: str, amount: int, slippage_bps: int=50) -> Dict`
    - `async def get_routes(self, input_mint: str, output_mint: str, amount: int) -> List[Dict]`
    - `async def place_order(self, order: Order) -> Order`
    - `async def cancel_order(self, order_id: str) -> bool`
    - `async def get_positions(self) -> List[Dict]`
    - `async def get_balances(self) -> Dict[str, float]`
    - `async def close(self) -> None`
