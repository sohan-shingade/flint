# Codemap: `flint.venues.hyperliquid`

> source git hash: `9fe1a265db36816d686c7ebbcab65babb447a47f`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.venues.hyperliquid.__init__`
_venues.hyperliquid — DEX CLOB adapter + specs (native API); the only executable v1 venue (D28)._
- imports: `.client`

### `flint.venues.hyperliquid.client`
_venues.hyperliquid.client — the live execution boundary (D20, §3.6)._
- imports: `flint.core.models`
- `class ExecReport` — The venue's response to a submitted order (§6.2 fill semantics).
- `class VenueOrder` — One resting order as the venue reports it (reconciliation input).
- `class VenuePosition` — One open position as the venue reports it (reconciliation input).
    - `def signed_size(self) -> float`
- `class ClearinghouseState` — A snapshot of the account on the venue — the truth to reconcile against.
- `class LiveVenueClient(Protocol)` — The narrow seam the live executor uses to reach the exchange (§3.6).
    - `def place_order(self, order: Order) -> ExecReport` — Sign and submit ``order``; return what the venue did with it.
    - `def cancel_all(self, market: str | None=None) -> list[str]` — Cancel all resting orders (optionally one market); return their ids.
    - `def clearinghouse_state(self) -> ClearinghouseState` — Read back the account's positions + open orders for reconciliation.
- `class LiveVenueUnavailable(Exception)` — A venue/network fault (WS drop, REST 5xx, rate limit) — retryable (§19.1).
- `class HyperliquidLiveClient` — The production HL client — holds the signing key, owns the transport.
    - `def place_order(self, order: Order) -> ExecReport`
    - `def cancel_all(self, market: str | None=None) -> list[str]`
    - `def clearinghouse_state(self) -> ClearinghouseState`
- `def hyperliquid_client_factory(signing_key: str) -> HyperliquidLiveClient` — Build a key-bound production client — the executor's default factory.
