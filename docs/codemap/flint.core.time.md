# Codemap: `flint.core.time`

> source git hash: `17ac165215ad4e0abbe45a8f44ef1683807e0487`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.core.time.__init__`
_core.time — bar alignment and the no-look-ahead guarantees ('as of t = last value with ts strictly < t')._
- imports: `.align`

### `flint.core.time.align`
_Bar-time alignment and the no-look-ahead 'as of t' rule (§5, §8.2)._
- `def bar_start(ts_ms: int, resolution_s: int) -> int` — Floor ``ts_ms`` to the START of its epoch-aligned bar (unix ms).
- `def bar_end(bar_start_ms: int, resolution_s: int) -> int` — The exclusive END of the bar that starts at ``bar_start_ms`` (unix ms).
- `def is_bar_closed(bar_start_ms: int, resolution_s: int, as_of_ms: int) -> bool` — True iff the bar is fully closed as of ``as_of_ms``: bar_end <= as_of (§8.2).
- `def last_before(items: Sequence[T], t_ms: int, key: Callable[[T], int] | None=None) -> T | None` — The last item with timestamp STRICTLY < ``t_ms``, or None (§8.2).
