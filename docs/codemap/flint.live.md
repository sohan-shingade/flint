# Codemap: `flint.live`

> source git hash: `94ecc280684030330ed470ccd79edd74b5dfc5d4`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.live.__init__`
_live — the minimal v1 live executor (D20): Hyperliquid only, caps, kill switch; same code path as paper (§17)._
- imports: `.alerts`, `.drift`, `.executor`, `.session`

### `flint.live.alerts`
_Alerts + heartbeat — the unattended contract (§6.7)._
- imports: `.drift`
- `class Alert` — One fired alert — persisted and dispatched to the channel (§6.7).
- `class AlertContext` — The per-evaluation snapshot the rules read (assembled by the runner).
- `class AlertRule(Protocol)`
    - `def evaluate(self, ctx: AlertContext) -> list[Alert]`
- `class LiqDistanceRule` — Fire when any position sits within ``threshold_pct`` of liquidation (§6.7).
    - `def evaluate(self, ctx: AlertContext) -> list[Alert]`
- `class DriftBreachRule` — Fire when structural drift breaches — the simulator is wrong (§6.7).
    - `def evaluate(self, ctx: AlertContext) -> list[Alert]`
- `class FundingSpreadFlipRule` — Fire when the funding spread a strategy harvests reverses sign (§6.7).
    - `def evaluate(self, ctx: AlertContext) -> list[Alert]`
- `class HeartbeatRule` — Fire when the venue has been silent longer than ``multiple ×`` a bar (§6.7).
    - `def evaluate(self, ctx: AlertContext) -> list[Alert]`
- `def default_rules(*, liq_threshold_pct: float=10.0, heartbeat_multiple: float=2.0) -> list[AlertRule]` — The four v1 rules with their default thresholds (§6.7).
- `class AlertChannel(ABC)` — Where a fired alert goes. One implementation per delivery mechanism.
    - `def send(self, alert: Alert) -> None`
- `class CollectingChannel(AlertChannel)` — An in-memory channel — the test/UI seam; no network.
    - `def __init__(self) -> None`
    - `def send(self, alert: Alert) -> None`
- `class WebhookChannel(AlertChannel)` — Posts an alert as JSON to a webhook (Discord/Telegram/Slack) (§6.7).
    - `def __init__(self, url: str, poster: Callable[[str, Mapping[str, Any]], None]) -> None`
    - `def send(self, alert: Alert) -> None`
- `class AlertEngine` — Runs the rule set over a context, dispatches, and retains fired alerts.
    - `def __init__(self, rules: Sequence[AlertRule], channel: AlertChannel) -> None`
    - `def evaluate(self, ctx: AlertContext) -> list[Alert]` — Evaluate every rule, dispatch each fired alert, and record it.

### `flint.live.drift`
_Drift — the parity promise with numbers, not vibes (§6.7)._
- imports: `flint.engine.money`, `flint.engine.portfolio`, `flint.engine.portfolio.events`
- `class SlippageBaseline` — The backtest's fill-slippage distribution, in basis points (§6.7).
    - `def from_fills(cls, slippages_bps: Sequence[float]) -> 'SlippageBaseline'`
- `class StructuralDrift` — The 'simulator is wrong' signals — each one is alert-worthy (§6.7).
    - `def breached(self) -> bool`
    - `def reasons(self) -> list[str]`
- `class MarketDrift` — Performance decay vs the backtest — chart data, never an alarm (§6.7).
- `class AttributionRow` — One line of the per-component attribution table (§6.7).
- `class DriftReport`
    - `def structural_breaches(self) -> list[str]` — The structural reasons an alert should fire on (empty = in parity).
- `def build_drift_report(events: Sequence[Event], *, slippage_baseline: SlippageBaseline, expected_funding: Money | None=None, latency_p50_ms: float | None=None, modeled_latency_ms: float | None=None, sharpe_window: int=20) -> DriftReport` — Fold the event log into a structural + market + attribution drift report.

### `flint.live.executor`
_The HL live executor — same order state machine as paper, real fills (D20, §3.6)._
- imports: `flint.core.models`, `flint.engine.money`, `flint.engine.orders`, `flint.engine.portfolio`, `flint.engine.state`, `flint.ports`, `flint.ports.records`, `flint.venues`, `flint.venues.hyperliquid.client`
- `class LiveStartRefused(Exception)` — A D20 pre-flight refusal — caps missing or no venue key. The run never starts.
    - `def __init__(self, code: str, message: str, *, hint: str='') -> None`
    - `def to_payload(self) -> dict[str, object]`
- `class LiveCaps` — The D20 risk caps a live run is fenced by.
- `class LiveReject` — A pre-trade refusal returned to the caller — data, not an exception (§19.1).
    - `def to_payload(self) -> dict[str, object]`
- `class DriftAlert` — One reconciliation mismatch between local and venue state (never adopted).
    - `def to_payload(self) -> dict[str, object]`
- `class StopReport` — The result of a kill-switch stop — what was cancelled and flattened.
    - `def to_payload(self) -> dict[str, object]`
- `class LiveExecutor` — Submits real orders under the D20 caps, on the paper/backtest order machine.
    - `def __init__(self, *, tenant: TenantContext, store: UserDataPort, run_id: str, client: LiveVenueClient, caps: LiveCaps, market: str, venue: str=HYPERLIQUID.name, venue_spec: VenueSpec=HYPERLIQUID, initial_capital: Money | str | float='100000') -> None`
    - `def start(cls, *, tenant: TenantContext, store: UserDataPort, secrets: SecretsPort, run_id: str, market: str, caps: LiveCaps, client_factory: ClientFactory=hyperliquid_client_factory, key_name: str=DEFAULT_KEY_NAME, venue: str=HYPERLIQUID.name, venue_spec: VenueSpec=HYPERLIQUID, initial_capital: Money | str | float='100000') -> 'LiveExecutor'` — Pre-flight the D20 contract, resolve the key, persist the head, start.
    - `def resume(cls, *, tenant: TenantContext, store: UserDataPort, secrets: SecretsPort, run_id: str, client_factory: ClientFactory=hyperliquid_client_factory, key_name: str=DEFAULT_KEY_NAME, venue_spec: VenueSpec=HYPERLIQUID) -> 'LiveExecutor'` — Reattach to a persisted live run (kill switch / reconnect entry).
    - `def submit(self, order: Order, *, ref_price: float) -> ExecReport | LiveReject` — Gate ``order`` on the D20 caps, then sign+submit it and record the fill.
    - `def mark_to_market(self, marks: dict[str, float]) -> LiveReject | None` — Feed fresh marks; trip the daily-loss halt if session loss crosses the cap.
    - `def reconcile(self) -> list[DriftAlert]` — Compare the folded local state to the venue clearinghouse (§3.6).
    - `def stop(self, *, flatten: bool=False, reason: str='operator') -> StopReport` — Pull the plug: cancel resting orders, optionally flatten, halt the run.
    - `def halted(self) -> bool`
- `def stop_all_live(tenant: TenantContext, *, store: UserDataPort, secrets: SecretsPort, client_factory: ClientFactory=hyperliquid_client_factory, flatten: bool=False) -> list[StopReport]` — Kill every running live run for ``tenant`` — the ``flint live --stop --all`` path.

### `flint.live.session`
_The paper session runner — the same engine, fed live, that survives restart (§6.7)._
- imports: `.alerts`, `.drift`, `flint.data.ingest.recorders`, `flint.data.livefeed`, `flint.engine`, `flint.engine.api`, `flint.engine.liquidation`, `flint.engine.money`, `flint.engine.portfolio`, `flint.engine.portfolio.events`, `flint.engine.select`, `flint.ports`, `flint.ports.records`, `flint.strategy.base`, `flint.strategy.ml`, `flint.strategy.model_store`, `flint.strategy.templates.registry`, `flint.strategy.tick`, `flint.venues`
- `def build_adapter(template: TemplateSpec | str, tenant: TenantContext, *, seed: int=0, strategy_id: str | None=None, overrides: dict | None=None, model_backend=None) -> EngineStrategy` — Resolve a template to the engine adapter, per the runner contract (§8.4).
- `class RunResult` — What one ``feed`` produced — bars, drains, drift, alerts, final state.
- `class PaperSession` — A restart-safe paper run of one strategy over a LiveFeed (§6.7).
    - `def __init__(self, *, tenant: TenantContext, store: UserDataPort, run_id: str, adapter: EngineStrategy, market: str, resolution_s: int, venue: str=HYPERLIQUID.name, venue_spec: VenueSpec=HYPERLIQUID, initial_capital: Money, gap_source: GapSource | None=None, slippage_baseline: SlippageBaseline | None=None, expected_funding: Money | None=None, alert_rules: Sequence[AlertRule] | None=None, channel: AlertChannel | None=None, resume_bar_start: int | None=None) -> None`
    - `def create(cls, *, tenant: TenantContext, store: UserDataPort, run_id: str, template: TemplateSpec | str, market: str, resolution_s: int, initial_capital: Money | str='100000', seed: int=0, overrides: dict | None=None, **kwargs) -> 'PaperSession'` — Start a fresh paper session and persist its head (§6.2).
    - `def resume(cls, *, tenant: TenantContext, store: UserDataPort, run_id: str, adapter: EngineStrategy, market: str, resolution_s: int, **kwargs) -> 'PaperSession'` — Pick up a killed session: read its head + cursor from the event log.
    - `def feed(self, source: WsMessageSource) -> RunResult` — Consume one WS connection: aggregate → run the engine → drift + alerts.
    - `def heartbeat(self, now_ts: int) -> list[Alert]` — Watchdog seam: fire the heartbeat rule if the venue has gone silent.
