# Codemap: `flint.notifications`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.notifications.__init__`
_Notification system — alerts via Telegram, Discord, webhooks._
- imports: `.base`

### `flint.notifications.base`
_Notification abstractions and event types._
- `class TradingEvent` — An event that can trigger a notification.
- `class Notifier(abc.ABC)` — Base class for notification channels.
    - `async def send(self, event: TradingEvent) -> bool` — Send a notification. Returns True on success.
- `class NotificationManager` — Fan-out notifications to multiple channels.
    - `def __init__(self, notifiers: Optional[List[Notifier]]=None)`
    - `def add_notifier(self, notifier: Notifier) -> None`
    - `async def notify(self, event: TradingEvent) -> int` — Send event to all notifiers. Returns count of successful sends.
    - `def event_log(self) -> List[TradingEvent]`

### `flint.notifications.discord`
_Discord webhook notification channel._
- imports: `.base`
- `class DiscordNotifier(Notifier)` — Send notifications via Discord webhook.
    - `def __init__(self, webhook_url: str)`
    - `async def send(self, event: TradingEvent) -> bool`

### `flint.notifications.telegram`
_Telegram notification channel._
- imports: `.base`
- `class TelegramNotifier(Notifier)` — Send notifications via Telegram Bot API.
    - `def __init__(self, bot_token: str, chat_id: str)`
    - `async def send(self, event: TradingEvent) -> bool`

### `flint.notifications.webhook`
_Generic webhook notification channel._
- imports: `.base`
- `class WebhookNotifier(Notifier)` — Send notifications as JSON POST to any URL.
    - `def __init__(self, url: str, headers: dict=None)`
    - `async def send(self, event: TradingEvent) -> bool`
