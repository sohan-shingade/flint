# Codemap: `flint.journal`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.journal.__init__`
_Trade journal — persistent backtest history._
- imports: `.storage`

### `flint.journal.storage`
_Journal storage — save/load/list/delete backtest runs in DuckDB._
- imports: `..store`
- `class JournalStorage` — Save and query backtest runs. Thread-safe via store lock.
    - `def __init__(self, store: FlintStore)`
    - `def save_run(self, run_id, strategy_name, market, resolution_s, start_ts, end_ts, initial_capital, params=None, result=None)`
    - `def list_runs(self, limit: int=50) -> List[Dict[str, Any]]`
    - `def get_run(self, run_id: str) -> Optional[Dict[str, Any]]`
    - `def get_equity_curve(self, run_id: str) -> list` — Retrieve saved equity curve for a backtest run.
    - `def delete_run(self, run_id: str) -> bool`
    - `def compare_runs(self, run_ids: List[str]) -> List[Dict[str, Any]]`
