# Codemap: `flint.collector`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.collector.__init__`
_Data collection service for automated market data ingestion._

### `flint.collector.service`
_Main collector service -- async loop with scheduling and status tracking._
- imports: `..store`, `.tasks`
- `class CollectorService`
    - `def __init__(self, store: FlintStore, config=None)`
    - `def update_status(self, market: str, data_type: str, state: str, error_message: Optional[str]=None, progress_pct: Optional[float]=None) -> None`
    - `def get_status(self) -> List`
    - `async def backfill(self) -> None`
    - `async def run(self) -> None`
    - `def stop(self) -> None`

### `flint.collector.tasks`
_Individual data collection tasks._
- imports: `..models`, `..providers.drift_api`, `..providers.drift_candles`, `..providers.drift_s3`, `..providers.funding_rates`, `..store`
- `class CollectorConfig`
- `def collect_oracle_prices(store: FlintStore, market: str) -> int`
- `def collect_funding_rates(store: FlintStore, market: str, market_index: int) -> int` — Collect recent funding rates from Drift into venue_funding_rates table.
- `def collect_orderbook(store: FlintStore, market: str) -> int`
- `def collect_candles_latest(store: FlintStore, market: str) -> int` — Fetch the latest few hours of candles to keep the DB current.
- `def collect_candles_backfill(store: FlintStore, market: str, days: int=90) -> int`
