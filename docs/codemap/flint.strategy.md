# Codemap: `flint.strategy`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.strategy.__init__`
- imports: `..execution.context`, `.atr_breakout`, `.base`, `.basis_trade`, `.bollinger`, `.breakout_momentum`, `.crypto_evo`, `.dual_timeframe`, `.ema_crossover`, `.funding_arb`, `.funding_harvest`, `.funding_mean_reversion`, `.grid_trader`, `.ma_crossover`, `.macd_divergence`, `.mean_reversion`, `.mev_arb_monitor`, `.momentum`, `.momentum_breakout`, `.multi_venue_funding`, `.rsi`, `.rsi_macd_combo`, `.vwap_reversion`

### `flint.strategy.atr_breakout`
_ATR channel breakout strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class ATRBreakoutStrategy(Strategy)` — Break out of ATR-based channels around the SMA.
    - `def __init__(self, period: int=20, atr_period: int=14, multiplier: float=2.0) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.base`
_Strategy abstract base class._
- imports: `..execution.context`, `..models`
- `class Strategy(abc.ABC)`
    - `def name(self) -> str`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal` — Process a new candle and return a trading signal.
    - `def reset(self) -> None` — Reset internal state for a fresh run.
    - `def parameters(cls) -> Dict[str, dict]` — Define optimizable parameters for hyperparameter search.

### `flint.strategy.basis_trade`
_BasisTradeStrategy — cross-venue basis arbitrage._
- imports: `..models`, `.base`
- `class BasisTradeStrategy(Strategy)` — Cross-venue basis arbitrage strategy (v2).
    - `def __init__(self, entry_basis_bps: float=30.0, exit_basis_bps: float=5.0, max_hold_hours: int=12, position_size_usd: float=1000.0, venues: Optional[List[str]]=None, candle_resolution_s: int=3600) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx=None) -> Signal`

### `flint.strategy.bollinger`
_Bollinger Bands mean-reversion strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class BollingerStrategy(Strategy)` — Buy when price touches lower band, sell when it touches upper band.
    - `def __init__(self, period: int=20, num_std: float=2.0) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.breakout_momentum`
_Breakout momentum strategy with volume confirmation and trailing stop._
- imports: `..execution.context`, `..models`, `.base`
- `class BreakoutMomentumStrategy(Strategy)` — Enter on high breakout with volume, trail with ATR stop.
    - `def __init__(self, lookback: int=20, volume_mult: float=1.5, atr_mult: float=2.0)`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.crypto_evo`
_CryptoEvo — MACD + Bollinger Band hybrid with regime detection._
- imports: `..execution.context`, `..models`, `.base`
- `class CryptoEvoStrategy(Strategy)`
    - `def __init__(self, fast_ema: int=12, slow_ema: int=26, signal_period: int=9, bb_period: int=20, bb_std_dev: float=2.5, signal_weight: float=0.75, vol_filter_on: int=0, vol_filter_regime: int=0, atr_filter_on: int=0, atr_filter_regime: int=0, trend_filter_on: int=1, trend_filter_regime: int=1, max_position_size: float=0.6) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.dual_timeframe`
_Dual-timeframe strategy — uses higher timeframe trend + lower timeframe entry._
- imports: `..execution.context`, `..models`, `.base`
- `class DualTimeframeStrategy(Strategy)` — Trend on long lookback, entry on short lookback.
    - `def __init__(self, trend_period: int=50, entry_period: int=10, entry_threshold: float=0.02)`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.ema_crossover`
_EMA (Exponential Moving Average) crossover strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class EMACrossoverStrategy(Strategy)` — Go long when fast EMA crosses above slow EMA, exit on cross below.
    - `def __init__(self, fast_period: int=12, slow_period: int=26) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.funding_arb`
_FundingArbStrategy — delta-neutral cross-venue funding rate arbitrage._
- imports: `..models`, `.base`
- `class FundingArbStrategy(Strategy)`
    - `def __init__(self, min_spread_bps: float=5.0, exit_spread_bps: float=1.0, max_hold_hours: int=24, position_size_usd: float=1000.0, min_spread_duration: int=1, venues: Optional[List[str]]=None, candle_resolution_s: int=60)`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx=None) -> Signal`
    - `def parameters(cls) -> Dict[str, dict]`

### `flint.strategy.funding_arb_v2`
_CrossVenueFundingArb — multi-asset delta-neutral funding rate arbitrage._
- imports: `..models`, `.base`
- `class CrossVenueFundingArb(Strategy)` — Delta-neutral cross-venue funding rate arbitrage.
    - `def __init__(self, min_spread_bps: float=4.0, z_score_entry: float=1.5, min_persistence: int=2, exit_spread_bps: float=0.5, max_hold_hours: int=48, position_size_usd: float=1000.0, max_positions: int=5, max_total_notional: float=25000.0, fee_bps: float=3.5, lookback_hours: int=168, venues: Optional[List[str]]=None, max_rate_vol_bps: float=15.0)`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx=None) -> Signal`
    - `def parameters(cls) -> Dict[str, dict]`

### `flint.strategy.funding_dislocation_arb`
_Funding Dislocation Arb — Phase 6 T6.6 reference implementation._
- imports: `..models`, `.base`
- `class FundingDislocationArbStrategy(Strategy)` — Cross-venue funding dislocation capture.
    - `def __init__(self, min_spread_bps: float=10.0, entry_z_threshold: float=1.5, exit_spread_bps: float=2.0, zscore_window: int=48, max_position_usd: float=2000.0, venues: Optional[List[str]]=None)`
    - `def name(self) -> str`
    - `def parameters(cls) -> dict`
    - `def reset(self) -> None`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx=None) -> Signal`

### `flint.strategy.funding_harvest`
_Funding rate harvest strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class FundingHarvestStrategy(Strategy)` — Harvest funding rate payments by taking the opposite side.
    - `def __init__(self, entry_threshold: float=3e-05, exit_threshold: float=5e-06, stop_loss_pct: float=0.05, lookback: int=8)`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.funding_mean_reversion`
_FundingMeanReversionStrategy — Bollinger bands on funding rates._
- imports: `..execution.context`, `..models`, `.base`
- `class FundingMeanReversionStrategy(Strategy)` — Mean-reversion strategy based on Bollinger Bands applied to funding rates.
    - `def __init__(self, bb_lookback: int=24, bb_std: float=2.0, max_hold_hours: int=12, position_size_pct: float=0.5, candle_resolution_s: int=3600) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.grid_trader`
_Grid trading strategy — places limit orders at fixed intervals._
- imports: `..execution.context`, `..models`, `.base`
- `class GridTraderStrategy(Strategy)` — Place limit orders in a grid around current price.
    - `def __init__(self, grid_spacing_pct: float=2.0, grid_levels: int=3, position_size_pct: float=0.1)`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.loader`
_Dynamic strategy loader with AST validation._
- imports: `.base`
- `class StrategyLoadError(Exception)`
- `def validate_strategy_code(code: str) -> Dict[str, Any]` — Validate strategy code via AST analysis.
- `def load_user_strategy(code: str, params: Dict[str, Any]=None) -> Strategy` — Load a user strategy from source code.

### `flint.strategy.ma_crossover`
_Simple moving-average crossover strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class MACrossoverStrategy(Strategy)` — Go long when fast SMA crosses above slow SMA, exit when it crosses below.
    - `def __init__(self, fast_period: int=10, slow_period: int=30) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.macd_divergence`
_MACD signal crossover strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class MACDDivergenceStrategy(Strategy)` — Trade MACD / signal line crossovers.
    - `def __init__(self, fast: int=12, slow: int=26, signal: int=9) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.mean_reversion`
_Z-score mean reversion strategy with stop-loss._
- imports: `..execution.context`, `..models`, `.base`
- `class MeanReversionStrategy(Strategy)` — Buy oversold, sell overbought based on Z-score.
    - `def __init__(self, period: int=20, entry_z: float=2.0, exit_z: float=0.5, stop_loss_pct: float=0.05)`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.mev_arb_monitor`
_MevArbMonitor — DEX arbitrage opportunity scanner (monitoring only)._
- imports: `..execution.context`, `..models`, `.base`
- `class MevArbMonitor(Strategy)` — Monitoring strategy that scans for DEX arbitrage opportunities.
    - `def __init__(self, min_profit_bps: float=10.0, max_hops: int=3, alert_enabled: int=0, candle_resolution_s: int=60) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal` — Scan for arb opportunities and log them. Always returns HOLD.

### `flint.strategy.momentum`
_Momentum / Rate-of-Change strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class MomentumStrategy(Strategy)` — Buy when price is up X% over lookback period, sell when down X%.
    - `def __init__(self, lookback: int=24, threshold_pct: float=5.0) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.momentum_breakout`
_MomentumBreakoutStrategy — price breakout with optional oracle confirmation._
- imports: `..execution.context`, `..models`, `.base`
- `class MomentumBreakoutStrategy(Strategy)` — Buy on N-bar high breakout, sell on N-bar low breakdown.
    - `def __init__(self, breakout_lookback: int=20, trailing_stop_pct: float=0.02, oracle_confirmation: int=1, candle_resolution_s: int=3600) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.multi_venue_funding`
_Cross-venue funding rate arbitrage strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class MultiVenueFundingStrategy(Strategy)` — Cross-venue funding rate arbitrage.
    - `def __init__(self, entry_threshold: float=3e-05, exit_threshold: float=5e-06, lookback: int=12, min_venues: int=2) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.rsi`
_RSI (Relative Strength Index) mean-reversion strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class RSIStrategy(Strategy)` — Buy when RSI drops below oversold, sell when it rises above overbought.
    - `def __init__(self, period: int=14, oversold: float=30, overbought: float=70) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.rsi_macd_combo`
_RSI + MACD confluence strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class RSIMACDComboStrategy(Strategy)` — Trade only when RSI and MACD both confirm the signal.
    - `def __init__(self, rsi_period: int=14, macd_fast: int=12, macd_slow: int=26, macd_signal: int=9, rsi_oversold: float=30, rsi_overbought: float=70) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`

### `flint.strategy.sandbox`
_User-strategy sandbox — subprocess isolation with wall-clock + memory limits._
- imports: `flint.backtest.engine`, `flint.models`, `flint.strategy.loader`
- `class StrategyTimeoutError(Exception)` — Raised when a sandboxed strategy exceeds its wall-clock budget.
- `class StrategyMemoryError(Exception)` — Raised when a sandboxed strategy trips the memory limit.
- `class StrategyExecutionError(Exception)` — Raised when a sandboxed strategy crashes or exits without a result.
- `class SandboxConfig`
- `def run_strategy_in_sandbox(code: str, candles: List[Any], initial_capital: float=10000.0, fee_rate: float=0.0005, params: Optional[Dict[str, Any]]=None, seed: Optional[int]=None, config: Optional[SandboxConfig]=None)` — Run user strategy code against `candles` inside an isolated subprocess.

### `flint.strategy.vwap_reversion`
_VWAP reversion strategy._
- imports: `..execution.context`, `..models`, `.base`
- `class VWAPReversionStrategy(Strategy)` — Buy below VWAP, sell when price reverts back toward VWAP.
    - `def __init__(self, period: int=20, entry_pct: float=2.0, exit_pct: float=0.5) -> None`
    - `def name(self) -> str`
    - `def reset(self) -> None`
    - `def parameters(cls) -> Dict[str, dict]`
    - `def on_candle(self, candle: Candle, history: List[Candle], ctx: Optional['ExecutionContext']=None) -> Signal`
