# Codemap: `flint.strategy`

> source git hash: `345edbf3f655613b6f269510c179c472b10d74ec`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.strategy.__init__`
_strategy — the user's surface: Strategy base class, Signal model, read-only ctx, the OS-isolated sandbox, and templates (§8, §17)._
- imports: `.base`, `.ml`, `.model_store`, `.templates`, `flint.core.models`

### `flint.strategy.base`
_The public ``Strategy`` base — what a user actually writes (§8.1)._
- imports: `flint.core.models`, `flint.venues`
- `class UnknownParamError(ValueError)` — A param override named a knob the strategy never declared (§8.1).
- `class StrategyRejection` — A signal the strategy surface refused *before* it reached the engine.
    - `def venue_not_executable(cls, signal: Signal, ts: int) -> 'StrategyRejection'`
- `class Strategy` — Base class for a user strategy (§8.1).
    - `def __init__(self, **overrides: Any) -> None`
    - `def name(self) -> str` — The strategy's display name (its class name by default).
    - `def param_spec(cls) -> dict[str, Any]` — The declared knobs (name → default) for the optimizer/UI (§3.3).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list[Signal] | Signal | None` — React to the just-closed ``candle`` and return signals (§8.1).
- `def normalize_signals(result: list[Signal] | Signal | None) -> list[Signal]` — Coerce an ``on_candle`` return into a ``list[Signal]`` (§8.1).
- `class EngineStrategy` — Adapts a public :class:`Strategy` to the engine's ``on_candle(candle, ctx)`` seam.
    - `def __init__(self, strategy: Strategy) -> None`
    - `def name(self) -> str`
    - `def on_candle(self, candle: 'Candle', ctx: Any) -> list[Signal]` — The engine-facing seam: derive history, delegate, gate venues (§8.1, D28).
    - `def drain_rejections(self) -> list[StrategyRejection]` — Return and clear the structured rejections recorded so far (§19.1).
    - `def tearsheet_notes(self) -> list[str]` — Notes the run should surface on its tearsheet (§8.1).

### `flint.strategy.ml`
_The declarative ML strategy surface — features/target/train, not a training loop (§8.5, D19)._
- imports: `.base`, `.model_store`, `flint.core.models`
- `class FutureWindow` — The forward label window handed to :meth:`MLStrategy.target` (§8.5).
    - `def return_pct(self) -> float` — Close-to-close return over the window, in percent (0 if empty).
    - `def max_return_pct(self) -> float` — Best high reached in the window vs the anchor close, in percent.
    - `def min_return_pct(self) -> float` — Worst low reached in the window vs the anchor close, in percent.
- `class MLContext` — The read-only ctx an ML strategy sees: engine ctx + ``model_store`` + ``seed``.
    - `def __init__(self, ctx: Any, model_store: ModelStore, seed: int) -> None`
- `def build_training_set(strategy: 'MLStrategy', market: str, closed_history: list['Candle'], ctx: Any, *, label_horizon: int, warmup: int=0) -> tuple[list[dict[str, Any]], list[Any]]` — Build ``(X, y)`` causally from closed history, purging the right edge (§8.5).
- `class MLStrategy(Strategy)` — Base for a declarative ML strategy (§8.5, D19).
    - `def model_key(self) -> str` — The managed-store key this strategy trains and predicts against.
    - `def features(self, market: str, history: list['Candle'], ctx: Any) -> dict[str, Any]` — Return one feature row from data **at or before now** (§8.5).
    - `def target(self, market: str, history: list['Candle'], future_window: FutureWindow) -> Any` — Return the label for the anchor bar from its forward window (§8.5).
    - `def train(self, X: list[dict[str, Any]], y: list[Any], ctx: Any) -> None` — Fit a model on ``(X, y)`` and persist it via ``ctx.model_store`` (§8.5).
    - `def decide(self, candle: 'Candle', features: dict[str, Any], model: Any, ctx: Any) -> list['Signal'] | 'Signal' | None` — Turn one prediction into signals (the declarative trade hook, §8.5).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal'] | 'Signal' | None` — Default declarative loop: no-op until a model exists, then :meth:`decide`.
- `class MLEngineStrategy(EngineStrategy)` — Adapts an :class:`MLStrategy` to the engine seam, owning the ML lifecycle (§8.5).
    - `def __init__(self, strategy: MLStrategy, model_store: ModelStore, *, seed: int=0) -> None`
    - `def on_candle(self, candle: 'Candle', ctx: Any) -> list['Signal']` — Engine seam: retrain-if-due, then delegate the decision, then gate (§8.5, D28).
    - `def tearsheet_notes(self) -> list[str]` — Carry the base notes plus the ML retrain count (§8.1, §8.5).

### `flint.strategy.model_store`
_The managed model store — the ONLY way an ML strategy persists a model (§8.5, D19)._
- imports: `flint.ports`
- `class ModelStoreError(Exception)` — Base for every managed-model-store failure (§8.5).
- `class UnsafeModelError(ModelStoreError)` — Refused: the object has no safe serialisation and would need raw pickle (§8.5).
- `class ModelStoreQuotaError(ModelStoreError)` — Refused: the write would exceed the store's key-count or byte quota (§8.5).
- `class ModelNotFound(ModelStoreError, KeyError)` — No model under this key **for this tenant/strategy** (a scoped miss, §8.5).
- `class SerializedModel` — A model at rest: the safe-format name plus the inert bytes it serialised to.
- `class ModelCodec` — One safe (de)serialiser: ``matches`` decides, ``serialize``/``deserialize`` act.
- `class ModelStoreQuota` — The ceiling a store enforces on every ``save`` (§8.5 quota-limited).
    - `def default(cls) -> 'ModelStoreQuota'`
- `def default_codecs() -> list[ModelCodec]` — The built-in safe codecs, in match order (specific vendors before sklearn).
- `class ModelStore` — A tenant/strategy-scoped, quota'd, safe-format model KV (``ctx.model_store``).
    - `def __init__(self, tenant: TenantContext, strategy_id: str, *, backend: MutableMapping[_ScopeKey, SerializedModel] | None=None, quota: ModelStoreQuota | None=None, codecs: list[ModelCodec] | None=None) -> None`
    - `def save(self, key: str, obj: Any) -> None` — Serialise ``obj`` to a safe format and persist it under ``key`` (§8.5).
    - `def load(self, key: str) -> Any` — Deserialise and return the model under ``key`` (this scope only).
    - `def exists(self, key: str) -> bool` — True iff a model is stored under ``key`` for this tenant/strategy.
    - `def keys(self) -> list[str]` — Every model key in this scope, sorted (never another scope's).
    - `def delete(self, key: str) -> None` — Remove ``key`` from this scope; raise :class:`ModelNotFound` if absent.
