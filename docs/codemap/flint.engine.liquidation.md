# Codemap: `flint.engine.liquidation`

> source git hash: `4c1a7847d40f7325a7bab1bde7d123552ad23452`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.engine.liquidation.__init__`
_engine.liquidation — mark-based liquidation and size-tiered maintenance (§6.5)._
- imports: `.check`

### `flint.engine.liquidation.cascade`
_The liquidation cascade — pure orchestration over the mark-based check (§6.5)._
- imports: `..money`, `.check`, `flint.core.models`, `flint.venues`
- `class LiquidationDecision` — One forced close: the realized amount to credit + the LIQUIDATION payload.
- `def adverse_mark(side: Side, candle: Candle, bar_marks: list[MarkSnapshot]) -> tuple[float, bool]` — The bar's adverse mark for ``side`` + whether the path was assumed.
- `def plan_liquidations(positions: dict[tuple[str, str], Position], cash: Money, marks: dict[tuple[str, str], tuple[float, bool]], spec: VenueSpec, venue: str, ts: int) -> list[LiquidationDecision]` — Plan every liquidation this bar for ``venue``, in application order (§6.5).

### `flint.engine.liquidation.check`
_Liquidation trigger + prices — mark-based, size-tiered maintenance (§6.5)._
- imports: `flint.core.models`, `flint.venues`
- `def maintenance_requirement(position: Position, mark_price: float, maint_frac: float) -> float` — The maintenance margin the position must keep, at a flat ``maint_frac``.
- `def tiered_maintenance(position: Position, mark_price: float, spec: LiquidationSpec) -> float` — The size-tiered maintenance margin at ``mark_price`` (§6.5, D14).
- `def liquidation_price(position: Position, backing_cash: float, maint_frac: float) -> float` — The mark at which ``position`` hits maintenance — the HL formula (§6.5).
- `def bankruptcy_price(position: Position, cash: float) -> float` — The price at which the position's margin is exactly zero (§6.5).
- `def is_liquidated(equity: float, maintenance: float) -> bool` — True iff ``equity`` has fallen below the maintenance requirement.
