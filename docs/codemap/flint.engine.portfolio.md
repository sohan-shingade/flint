# Codemap: `flint.engine.portfolio`

> source git hash: `86ecf366b00f1f7bc9effe0a1b46aea659f13878`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.engine.portfolio.__init__`
_engine.portfolio — cross-position risk plus the event log, replay/fold, and snapshots (§2.10, §6.6)._
- imports: `.event_log`, `.events`, `.upcasters`

### `flint.engine.portfolio.event_log`
_``EventLog`` — the emit seam + upcast-on-read reader (§2.10)._
- imports: `.events`, `.upcasters`, `flint.ports`
- `class EventLog` — Append-only writer + upcast-on-read reader for one run's events.
    - `def __init__(self, store: UserDataPort, tenant: TenantContext, run_id: str, *, registry: UpcasterRegistry=REGISTRY) -> None`
    - `def emit(self, kind: str, payload: Mapping[str, Any] | None=None, *, ts: int=0, event_version: int=1) -> Event` — Append one event; assign its ``seq``; persist it via ``UserDataPort``.
    - `def read(self) -> list[Event]` — Load all events, upcast each to its latest version, return typed events.
    - `def read_raw(self) -> list[dict[str, Any]]` — The stored rows exactly as written (no upcasting) — proves immutability.

### `flint.engine.portfolio.events`
_The event envelope — the atom of the append-only log (§2.10)._
- `class Event` — One immutable entry in a run's event log.
    - `def to_row(self) -> dict[str, Any]` — Render to the primitive on-disk/wire form (a plain dict).
    - `def from_row(cls, row: Mapping[str, Any]) -> 'Event'` — Rebuild a typed event from a stored row.

### `flint.engine.portfolio.upcasters`
_The upcaster registry — how old stored events stay replayable (§2.10)._
- `class UpcasterRegistry` — A registry of ``(kind, from_version) -> payload transform``.
    - `def __init__(self) -> None`
    - `def register(self, kind: str, from_version: int) -> Callable[[PayloadUpcaster], PayloadUpcaster]` — Decorator: register a ``vN -> v(N+1)`` payload transform for ``kind``.
    - `def latest_version(self, kind: str, from_version: int=1) -> int` — Highest version reachable for ``kind`` by following the upcaster chain.
    - `def upcast_to_latest(self, row: Mapping[str, Any]) -> dict[str, Any]` — Return ``row`` walked up to the latest version (a new dict; input untouched).
