# Codemap: `flint.backtest`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.backtest.__init__`
- imports: `.engine`

### `flint.backtest.calibration`
_CalibrationEngine — fits market impact models from live fill data._
- imports: `..execution.venue_config`
- `class CalibrationReport` — Result of impact model calibration.
    - `def to_dict(self) -> dict`
    - `def summary(self) -> str`
- `class DriftReport` — Result of model drift detection.
    - `def summary(self) -> str`
- `class CalibrationEngine` — Fits market impact models from live fill data.
    - `def __init__(self, store)`
    - `def calibrate(self, venue: str, market: str, lookback_days: int=30, min_fills: int=100) -> CalibrationReport` — Fit impact model from historical fills.
    - `def detect_drift(self, venue: str, market: str, window_days: int=7, threshold_pct: float=15.0) -> DriftReport` — Compare recent fills against current impact model.

### `flint.backtest.config`
_BacktestConfig — canonical entry point for running a backtest._
- `class FillConfig` — Fill-model parameters. Covers the common FillPipeline knobs; callers
- `class MarginConfig` — Per-venue margin + liquidation settings.
- `class AllocatorConfig` — Multi-strategy capital allocator.
- `class VenueConfig` — Per-venue fee + constraint overrides.
- `class BacktestConfig` — Canonical backtest configuration.
    - `def to_json(self) -> Dict[str, Any]` — Plain-dict representation. Stable field order for reproducible
    - `def to_json_str(self) -> str`
    - `def checksum(self) -> str` — SHA-256 of the canonical JSON form. Used by proof notebooks and
    - `def from_dict(cls, data: Dict[str, Any]) -> 'BacktestConfig'` — Reconstruct from a plain dict (e.g. JSON, YAML). Tolerant of
    - `def from_yaml(cls, path: str | Path) -> 'BacktestConfig'`
    - `def from_legacy_kwargs(cls, **kwargs) -> 'BacktestConfig'` — Build a config from the old BacktestEngine kwarg flavor.

### `flint.backtest.drift_sim`
_Drift-specific simulation extensions._
- imports: `..models`
- `class DriftMarketConfig` — Drift perp market simulation parameters.
- `class DriftFeeModelV2` — Drift protocol fee model with maker/taker distinction and volume tiers.
    - `def __init__(self, thirty_day_volume: float=0, is_maker: bool=False)`
    - `def compute_fee(self, fill: Fill) -> float`
- `class MevCostModel` — Estimates MEV-related costs for Solana transactions.
    - `def __init__(self, jito_tip_lamports: int=10000, sol_price_usd: float=150.0, front_run_bps: float=0.0)`
    - `def jito_tip_usd(self) -> float`
    - `def base_tx_fee_usd(self) -> float`
    - `def estimate_cost(self, notional_usd: float) -> float` — Total MEV-related cost for a trade.

### `flint.backtest.engine`
_Backtest engine — feeds candles through a strategy and tracks performance._
- imports: `..execution.backtest_context`, `..execution.fee_models`, `..execution.fill_models`, `..models`, `..strategy.base`, `.config`
- `class RustRequiredError(RuntimeError)` — Raised when BacktestEngine(rust_required=True) would have fallen back
- `class BacktestCancelled(RuntimeError)` — Raised mid-run when the caller-supplied cancel_check returns True.
- `class BacktestEngine` — Event-driven backtest engine with pluggable execution models.
    - `def from_config(cls, config, strategy: Strategy, **overrides)` — Construct from a `BacktestConfig` (Phase 2 T2.3).
    - `def __init__(self, strategy: Strategy, initial_capital: float=10000.0, fee_rate: float=0.0005, position_size: float=1.0, fill_model: Optional[FillModel]=None, fee_model: Optional[FeeModel]=None, funding_rates: Optional[List[FundingRate]]=None, orderbook_snapshots: Optional[list]=None, open_interest: Optional[list]=None, borrow_snapshots: Optional[list]=None, risk_manager=None, margin_engine=None, capital_allocator=None, max_runtime_s: float=300, venue_fill_models: Optional[Dict[str, FillModel]]=None, seed: Optional[int]=None, rust_required: bool=False, cancel_check=None) -> None`
    - `def run(self, candles, extra_markets: dict=None) -> BacktestResult` — Run backtest.

### `flint.backtest.parity`
_ParityTest -- compare backtest engine vs paper broker on same data._
- imports: `..execution.fee_models`, `..execution.fill_models`, `..models`, `..paper.context`, `.engine`
- `class ParityReport`
    - `def to_dict(self) -> dict`
    - `def summary(self) -> str`
- `class ParityTest` — Run backtest engine and paper broker on identical data and compare.
    - `def __init__(self, strategy, market: str, candles: List[Candle], initial_capital: float=10000.0, fee_rate: float=0.0005, funding_rates: Optional[List[FundingRate]]=None, threshold_pct: float=2.0)`
    - `def run(self) -> ParityReport`

### `flint.backtest.rust_capabilities`
_Rust engine capability discovery — Phase 3 T3.2._
- `def rust_capabilities() -> Dict[str, Any]` — Return the Rust engine capability dict, or a zero-support stub if
- `def python_capabilities() -> Dict[str, Any]` — Return what the pure-Python engine supports — always the full superset.
- `def rust_supports(feature: str) -> bool` — Boolean flag accessor with a safe default of False.
