# Codemap: `flint.research`

> source git hash: `f13dfa3f559b4475f76ab675287feb8823533a30`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.research.__init__`
_research — trust tooling: walk-forward (+ purge/embargo), Deflated Sharpe, look-ahead linter, Optuna optimize, tearsheet, Run Library (§11, §17)._
- imports: `.deflated`, `.funding_lab`, `.lookahead`, `.runlib`, `.tearsheet`, `.walkforward`

### `flint.research.deflated`
_Deflated Sharpe Ratio — correcting for selection across many trials (D22, §11.1)._
- imports: `.tearsheet`, `.walkforward`
- `def skewness(returns: Sequence[float]) -> float` — Population skewness ``m3 / m2**1.5`` (0.0 for a degenerate, zero-variance series).
- `def kurtosis(returns: Sequence[float], *, excess: bool=False) -> float` — Population kurtosis ``m4 / m2**2``; non-excess by default (normal = 3.0).
- `def expected_max_sharpe(trial_sharpes: Sequence[float]) -> float` — The Sharpe you would expect to find by luck alone across ``N`` trials (SR0).
- `def probabilistic_sharpe(observed_sharpe: float, benchmark_sharpe: float, n_returns: int, skew: float, kurt: float) -> float` — PSR: probability the true Sharpe exceeds ``benchmark_sharpe`` (Bailey/LdP 2014).
- `class DeflatedSharpe` — The DSR and every input that produced it — so the number is auditable, not a
- `def deflated_sharpe(returns: Sequence[float], trial_sharpes: Sequence[float]) -> DeflatedSharpe` — Deflate the selected strategy's Sharpe by the trials that produced it (D22).
- `def trial_sharpes(result: WalkForwardResult) -> tuple[float, ...]` — Pool every trial's score across all walk-forward windows — the ``N`` trials and

### `flint.research.funding_lab`
_Cross-venue funding & basis lab (§10, D6/D28) — the wedge, as analytics._
- imports: `flint.core.models`
- `def settlements_per_day(interval_s: int) -> float` — How many funding settlements fall in a day at a venue's native interval (§10).
- `def settlements_per_year(interval_s: int) -> float` — Settlements per day × 365 (§10) — the linear annualization multiplier count.
- `def annualize_hourly(rate_hourly: float) -> float` — Annualize an already-hourly-normalized rate **linearly** (× 24 × 365, §10).
- `def annualized_rate(rate: FundingRate) -> float` — Annualize one funding row via its native settlement structure (§10).
- `class VenueFundingStat` — One venue's normalized funding carry for a market over a window (§10).
- `def venue_funding_stats(rates: Sequence[FundingRate], *, rate_type: str | None=PREDICTED) -> list[VenueFundingStat]` — Per-venue normalized funding carry for a single market (§10).
- `class DislocationPoint` — At one timestamp: the cross-venue benchmark and each venue's deviation from it.
- `class DislocationSeries` — The dislocation time series for one market across venues (§10).
    - `def venue_series(self, venue: str) -> list[tuple[int, float]]` — ``(ts, dislocation)`` pairs for one venue, only where it had an as-of rate.
    - `def widest(self) -> tuple[int, str, float] | None` — The single largest-magnitude dislocation as ``(ts, venue, signed value)``.
    - `def describe(self) -> str`
- `def cross_venue_dislocation(rates: Sequence[FundingRate], *, rate_type: str | None=PREDICTED) -> DislocationSeries` — Cross-venue benchmark and per-venue dislocation for one market (§10).
- `class BasisPoint` — Perp premium/discount vs index at one timestamp (§10, §5.MarkSnapshot).
- `def basis_series(marks: Sequence[MarkSnapshot]) -> list[BasisPoint]` — Basis time series from recorded mark snapshots, ascending by ts (§10).
- `def mean_basis_bps(marks: Sequence[MarkSnapshot]) -> float` — Mean basis in bps over the recorded snapshots (undefined-index rows skipped).

### `flint.research.lookahead`
_Look-ahead / leakage linter — a FreqAI-style causality check (§8.5, §11, D27)._
- `class LeakFinding` — One detected (or suspected) leak: a category, a human message, and — for static
- `class LookaheadResult` — The linter's verdict. ``findings`` is what fired; ``checks_run`` is what was
    - `def leak_detected(self) -> bool`
    - `def categories(self) -> tuple[str, ...]`
    - `def summary(self) -> str`
- `def lint_source(source: str, *, check_universe: bool=True) -> list[LeakFinding]` — Run the static AST pass over ``source`` and return the raw findings.
- `def truncation_probe(feature_fn: Callable[[Sequence], Sequence], frame: Sequence, *, truncate: int=1, atol: float=1e-09) -> list[LeakFinding]` — Differential re-run: compute features on the full ``frame`` and on ``frame`` with
- `def check_label_horizon(horizon: int) -> LeakFinding | None` — Flag a non-positive label horizon known at call time (a degenerate label that
- `def analyze(source: str | None=None, *, check_universe: bool=True, label_horizon: int | None=None, feature_fn: Callable[[Sequence], Sequence] | None=None, frame: Sequence | None=None, truncate: int=1, atol: float=1e-09) -> LookaheadResult` — Run every applicable check and return a single result carrying the blind spots.

### `flint.research.runlib`
_Run Library + reproducibility export — research memory for humans and agents (§11.2)._
- imports: `flint.ports`
- `def content_hash(source: str) -> str` — SHA-256 hex digest of strategy source — the run's content-addressed identity.
- `def engine_version() -> str` — The installed Flint engine version, stamped on every run (§17, repro bundle).
- `class DataSource` — One entry in a run's data manifest: which recorded series it read, over what
    - `def to_dict(self) -> dict[str, Any]`
    - `def from_dict(cls, d: Mapping[str, Any]) -> 'DataSource'`
- `class RunManifest` — The full §11.2 head record for one run — everything needed to find it later,
    - `def strategy_hash(self) -> str` — Content hash of the strategy source (§11.2). Derived, never stored twice.
    - `def to_summary(self) -> dict[str, Any]` — Reduce to the JSON-safe blob stored in ``RunRecord.summary``.
    - `def from_record(cls, record: RunRecord) -> 'RunManifest'` — Reconstruct a manifest from a persisted ``RunRecord`` (its ``summary`` blob).
    - `def to_record(self, *, status: str='done') -> RunRecord` — Build the persisted ``RunRecord`` head that carries this manifest.
- `def persist_run(tenant: TenantContext, port: UserDataPort, manifest: RunManifest, *, events: Sequence[Mapping[str, Any]]=(), status: str='done') -> str` — Persist a run's head record (and optional event stream) via ``UserDataPort``.
- `def load_run(tenant: TenantContext, port: UserDataPort, run_id: str) -> RunManifest` — Load one run's manifest for ``tenant``; raises ``KeyError`` if absent for it.
- `def list_runs(tenant: TenantContext, port: UserDataPort, *, strategy: str | None=None) -> list[RunManifest]` — List ``tenant``'s run manifests, optionally filtered to one strategy name.
- `class RunComparison` — Side-by-side comparison of two-or-more runs with honesty warnings.
    - `def metric_table(self) -> dict[str, list[Any]]` — One row per metric key seen, one column per run (missing → ``None``).
    - `def describe(self) -> str`
- `def compare(tenant: TenantContext, port: UserDataPort, run_ids: Sequence[str]) -> RunComparison` — Compare runs by ``run_id`` for ``tenant``, surfacing not-comparable warnings.
- `class ReproBundle` — A self-contained reproducibility bundle: everything needed to re-run bit-for-bit.
    - `def verify_integrity(self) -> bool` — True when the carried source still hashes to the carried hash (§11.2).
    - `def to_json(self) -> str`
    - `def from_json(cls, text: str) -> 'ReproBundle'`
- `def export_bundle(tenant: TenantContext, port: UserDataPort, run_id: str) -> ReproBundle` — Assemble the reproducibility bundle for ``tenant``'s run ``run_id`` (§11.2).
- `class BundleRunner(Protocol)` — Re-executes a bundle deterministically, returning the emitted event stream.
- `class ReproductionResult` — Outcome of re-executing a bundle: is the event stream bit-for-bit identical?
    - `def describe(self) -> str`
- `def reproduce(bundle: ReproBundle, runner: BundleRunner) -> ReproductionResult` — Re-execute ``bundle`` via ``runner`` and check the event stream is identical.
- `def legacy_run_manifests(*, strategy_rows: Sequence[Mapping[str, Any]]=(), journal_rows: Sequence[Mapping[str, Any]]=()) -> list[RunManifest]` — Map legacy ``strategies`` / ``journal_equity`` rows into ``RunManifest``s (§19.6).
- `def import_legacy_runs(tenant: TenantContext, port: UserDataPort, *, strategy_rows: Sequence[Mapping[str, Any]]=(), journal_rows: Sequence[Mapping[str, Any]]=()) -> list[str]` — Persist legacy run metadata into ``tenant``'s Run Library; return the run_ids.

### `flint.research.tearsheet`
_Performance metrics — the §11.1 definitions, stated once and locked by goldens._
- imports: `.deflated`, `flint.engine.portfolio`, `flint.engine.portfolio.events`, `flint.engine.tearsheet`
- `def bar_returns(equity: Sequence[float]) -> list[float]` — Per-bar arithmetic returns of an equity curve: ``(E_t - E_{t-1}) / E_{t-1}``.
- `def sharpe(returns: Sequence[float], *, rf: float=0.0) -> float` — Per-bar Sharpe: ``mean(r - rf) / std(r)``, sample std, rf=0 by default (§11.1).
- `def downside_deviation(returns: Sequence[float], *, target: float=0.0) -> float` — Root-mean-square of the below-target returns (over all n), vs ``target`` (§11.1).
- `def sortino(returns: Sequence[float], *, target: float=0.0) -> float` — Per-bar Sortino: ``mean(r - target) / downside_deviation`` (§11.1).
- `def max_drawdown(equity: Sequence[float]) -> float` — Largest peak-to-trough decline of ``equity`` as a non-negative fraction (§11.1).
- `def bars_per_year(resolution_s: int) -> float` — How many bars of ``resolution_s`` fit in a 365-day year (§11.1).
- `def annualization_factor(resolution_s: int) -> float` — The √(bars-per-year) factor printed on the tearsheet (§11.1).
- `class MetricSummary` — The §11.1 risk/return numbers with their annualization factor and the effective
- `def summarize_equity(equity: Sequence[float], *, resolution_s: int, rf: float=0.0, evaluated_start_ts: int | None=None, evaluated_end_ts: int | None=None) -> MetricSummary` — Fold a total-equity curve into the §11.1 metric summary.
- `def win_rate_from_events(events: Iterable[Event]) -> float | None` — Fraction of closing trades that realized a positive PnL (§11 tearsheet).
- `def equity_series_from_events(events: Iterable[Event]) -> list[float]` — Fold the per-bar EQUITY event stream into a total-equity curve (§6.1/§11.1).
- `def equity_points_from_events(events: Iterable[Event]) -> list[list[float]]` — The EQUITY stream as ``[ts_ms, value]`` pairs — the wire equity curve (§B7).
- `class PerformanceReport` — The trust report: §11.1 metrics + the engine's full-cost decomposition + the
    - `def describe(self) -> str`
- `def build_report(equity: Sequence[float], *, resolution_s: int, events: Iterable[Event] | None=None, deflated: 'DeflatedSharpe | None'=None, rf: float=0.0, evaluated_start_ts: int | None=None, evaluated_end_ts: int | None=None) -> PerformanceReport` — Assemble the full trust report (§11).

### `flint.research.walkforward`
_Walk-forward optimization — the v1 anti-overfit backbone (§3.3, §11)._
- `class WalkForwardWarning(UserWarning)` — Raised (as a warning) when a walk-forward config weakens a leak guard.
- `class BacktestRunner(Protocol)` — Runs one backtest over the half-open bar range ``[start, end)``.
- `class ParamSpace` — One searchable parameter: a closed ``[low, high]`` range, optional step.
    - `def parse(cls, spec: str) -> 'ParamSpace'` — Parse ``name=lo:hi`` or ``name=lo:hi:step`` (the CLI ``--param`` form).
- `class WalkForwardConfig` — How to split, guard, and search.
- `class Window` — One fold: expanding train ``[train_start, train_end)`` then OOS ``[oos_start,
- `class WindowResult` — The tuned outcome for one fold: the best params and their in-sample score,
- `class WalkForwardResult` — The whole run: per-window results, the OOS series that is the honest report,
    - `def mean_oos(self) -> float` — Equal-weighted mean OOS score across windows (0.0 if none).
    - `def describe(self) -> str` — A human-readable summary — trial count is displayed, not just recorded.
- `def plan_windows(n_bars: int, config: WalkForwardConfig) -> list[Window]` — Carve ``n_bars`` into an expanding-train / forward-OOS walk (§3.3, pure).
- `def run_walk_forward(n_bars: int, space: Iterable[ParamSpace], runner: BacktestRunner, config: WalkForwardConfig) -> WalkForwardResult` — Tune → OOS-test across every window and report the out-of-sample series.
