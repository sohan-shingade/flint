# Codemap: `flint.mev`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.mev.__init__`
- imports: `.arb`, `.bundle`, `.jit`, `.liquidation`, `.simulator`

### `flint.mev.arb`
_Multi-hop arbitrage detection across AMM pools._
- imports: `..mev.clmm`, `..models`
- `class ArbDetector` — Detects arbitrage opportunities across a set of AMM pools.
    - `def __init__(self, min_profit_bps: float=10.0) -> None`
    - `def update_pools(self, pools: List[PoolState], clmm_pools: Optional[Dict[str, 'CLMMPool']]=None) -> None` — Rebuild the graph from current pool states.
    - `def find_arb_routes(self, start_token: str, amount: float=1.0, max_hops: int=4) -> List[ArbRoute]` — Find profitable circular routes starting and ending at ``start_token``.
    - `def estimate_profit(self, route: ArbRoute, amount: float) -> float` — Re-simulate a known route with a different input amount.

### `flint.mev.bundle`
_Jito bundle builder for MEV transaction submission._
- `class Instruction` — A single Solana instruction (simplified representation).
    - `def size_bytes(self) -> int`
- `class Bundle` — A Jito bundle — a sequence of transactions submitted atomically.
    - `def num_instructions(self) -> int`
    - `def estimated_size(self) -> int`
    - `def tip_sol(self) -> float`
- `class BundleBuilder` — Builds Jito bundles from a series of instructions.
    - `def __init__(self, signer: str='', default_tip: int=1000) -> None`
    - `def add_instruction(self, ix: Instruction) -> 'BundleBuilder'`
    - `def clear(self) -> 'BundleBuilder'`
    - `def build(self, tip_lamports: Optional[int]=None) -> Bundle`
    - `def validate(bundle: Bundle) -> List[str]` — Validate a bundle. Returns list of error strings (empty = valid).
    - `def estimate_tip(expected_profit_lamports: int, tip_fraction: float=0.5) -> int` — Suggest a tip as a fraction of expected profit.

### `flint.mev.clmm`
_CLMMPool — concentrated liquidity model for tick-range AMM pools._
- imports: `..models`
- `class TickRange` — A liquidity range in a concentrated liquidity pool.
- `class CLMMPool` — Concentrated liquidity pool with tick-range price impact.
    - `def __init__(self, pool_address: str, dex: str, token_a_mint: str, token_b_mint: str, tick_ranges: List[TickRange], current_tick: int, tick_spacing: int, fee_rate: float, sqrt_price: float)`
    - `def price_at_tick(self, tick: int) -> float`
    - `def output_amount(self, amount_in: float, a_to_b: bool) -> float` — Compute output by walking through active tick ranges.
    - `def to_pool_state(self) -> PoolState`

### `flint.mev.jit`
_JIT (Just-In-Time) liquidity detection for Drift protocol._
- imports: `..models`
- `class JitOpportunity` — A detected JIT fill opportunity.
- `class JitDetector` — Detects JIT liquidity opportunities from orderbook state.
    - `def __init__(self, maker_rebate: float=0.0002, min_spread_bps: float=5.0, min_size: float=0.1) -> None`
    - `def scan_orderbook(self, snapshot: OrderbookSnapshot, vamm_price: float) -> List[JitOpportunity]` — Check if there are JIT opportunities given current orderbook and vAMM price.

### `flint.mev.liquidation`
_Liquidation opportunity scanner for Drift/Mango positions._
- imports: `..models`
- `def compute_liquidation_price(entry_price: float, size: float, collateral: float, maintenance_margin_ratio: float=0.05, is_long: bool=True) -> float` — Compute the oracle price at which a position gets liquidated.
- `class LiquidationScanner` — Scans positions for liquidation opportunities.
    - `def __init__(self, maintenance_margin_ratio: float=0.05, liquidation_fee: float=0.01, min_profit: float=0.0) -> None`
    - `def scan_positions(self, positions: List[Dict], oracle_prices: Dict[str, float]) -> List[LiquidationOpportunity]` — Check a list of positions against current oracle prices.

### `flint.mev.simulator`
_MEV simulator — replay historical data and estimate extractable value._
- imports: `..models`, `.arb`, `.liquidation`
- `class MevSnapshot` — MEV state at a single point in time.
    - `def total_profit(self) -> float`
- `class SimulationResult` — Aggregated results from a historical MEV simulation.
    - `def total_profit(self) -> float`
    - `def avg_arb_profit_per_period(self) -> float`
- `class MevSimulator` — Simulates historical MEV extraction opportunities.
    - `def __init__(self, arb_min_profit_bps: float=10.0, arb_amount: float=1.0, liq_maintenance_margin: float=0.05, liq_fee: float=0.01) -> None`
    - `def run(self, pool_snapshots: Dict[int, List[PoolState]], position_snapshots: Dict[int, List[Dict]], oracle_prices: Dict[int, Dict[str, float]], start_token: str) -> SimulationResult` — Run the simulation over time-keyed snapshots.
