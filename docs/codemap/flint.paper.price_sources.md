# Codemap: `flint.paper.price_sources`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.paper.price_sources.__init__`
_Pluggable price-source layer for the live PnL ticker._
- imports: `.base`, `.drift_dlob`, `.duckdb_cache`, `.hyperliquid`
- `def default_chain(store=None)` — Recommended source priority. Pass `store` to enable the

### `flint.paper.price_sources.base`
_Price-source ABC + small dataclasses._
- `class PriceQuote` — One price observation. `ts` is the source-side time when
- `class SourceHealth` — Per-source running stats. Surfaced via
    - `def record_success(self) -> None`
    - `def record_error(self, err: str) -> None`
    - `def to_dict(self) -> dict`
- `class PriceSource(abc.ABC)` — One backing source of mid prices.
    - `def __init__(self) -> None`
    - `def fetch(self, markets: Iterable[str]) -> Dict[str, PriceQuote]`
    - `def supports(self, market: str) -> bool` — Optional pre-filter — sources can hint the chain about

### `flint.paper.price_sources.drift_dlob`
_Drift DLOB price source._
- imports: `...precision`, `.base`
- `class DriftDLOBSource(PriceSource)`
    - `def __init__(self, timeout_s: float=5.0) -> None`
    - `def fetch(self, markets: Iterable[str]) -> Dict[str, PriceQuote]`

### `flint.paper.price_sources.duckdb_cache`
_DuckDB last-candle price source._
- imports: `.base`
- `class LocalDuckDBSource(PriceSource)`
    - `def __init__(self, store) -> None`
    - `def fetch(self, markets: Iterable[str]) -> Dict[str, PriceQuote]`

### `flint.paper.price_sources.hyperliquid`
_Hyperliquid /info allMids price source._
- imports: `.base`
- `class HyperliquidInfoSource(PriceSource)`
    - `def __init__(self, timeout_s: float=5.0) -> None`
    - `def supports(self, market: str) -> bool`
    - `def fetch(self, markets: Iterable[str]) -> Dict[str, PriceQuote]`
