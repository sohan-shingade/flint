# Codemap: `flint.strategy.templates`

> source git hash: `a4a80d9fd30c790134780969a2a392088c28d23d`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.strategy.templates.__init__`
_strategy.templates — the built-in strategies, one registry (§8.4)._
- imports: `.funding_svd`, `.ml_template`, `.perp`, `.registry`, `.technical`

### `flint.strategy.templates._base`
_Shared scaffolding for the built-in templates (§8.4)._
- imports: `..base`, `flint.core.models`
- `def template_params(**extra: Any) -> dict[str, Any]` — The common template knobs (``venue``, ``notional_usd``) plus a template's own.
- `def rebalance_signals(ctx: Any, market: str, venue: str, desired: str, notional_usd: float) -> list[Signal]` — Return the minimal signals to move ``(venue, market)`` to ``desired`` state.
- `class TemplateStrategy(Strategy)` — Base for the non-ML built-in templates: ``venue``/``notional`` + a rebalance seam.

### `flint.strategy.templates.funding_svd`
_Cross-sectional funding factor template: rank-1 SVD on normalized funding (§8.4)._
- imports: `._base`, `flint.core.models`
- `class FundingSvdStrategy(TemplateStrategy)` — Trade residual funding vs the SVD market-wide funding factor (§8.4).
    - `def __init__(self, **overrides: Any) -> None`
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`

### `flint.strategy.templates.indicators`
_Pure-Python indicators for the built-in templates — no heavy deps (§8.4)._
- imports: `flint.core.models`
- `def closes(history: Sequence['Candle']) -> list[float]` — The close series of ``history`` (oldest→newest).
- `def sma(values: Sequence[float], n: int) -> float | None` — Simple moving average of the last ``n`` values, or ``None`` if too few.
- `def ema(values: Sequence[float], n: int) -> float | None` — Exponential moving average (SMA-seeded) of ``values``, or ``None`` if too few.
- `def rsi(values: Sequence[float], n: int) -> float | None` — Wilder's RSI over the last ``n`` deltas, or ``None`` if too few values.
- `def stdev(values: Sequence[float], n: int) -> float | None` — Population standard deviation of the last ``n`` values, or ``None`` if too few.
- `def highest(values: Sequence[float], n: int) -> float | None` — Highest of the last ``n`` values, or ``None`` if too few.
- `def lowest(values: Sequence[float], n: int) -> float | None` — Lowest of the last ``n`` values, or ``None`` if too few.

### `flint.strategy.templates.ml_template`
_The built-in ML template — a LightGBM trend classifier (§8.4, §8.5)._
- imports: `.`, `..ml`, `._base`, `flint.core.models`
- `class LightGbmTrendStrategy(MLStrategy)` — LightGBM classifier on bounded trend/momentum features (§8.4 ML template).
    - `def features(self, market: str, history: list['Candle'], ctx: Any) -> dict[str, float]`
    - `def target(self, market: str, history: list['Candle'], future_window: 'FutureWindow') -> int` — 1 if the forward window closed up, else 0 (§8.5 — closed windows only).
    - `def train(self, X: list[dict[str, Any]], y: list[Any], ctx: Any) -> None`
    - `def decide(self, candle: 'Candle', features: dict[str, Any], model: Any, ctx: Any) -> list['Signal']`

### `flint.strategy.templates.perp`
_Perp-native templates: funding harvest, funding dislocation, basis, OI momentum (§8.4)._
- imports: `._base`, `flint.core.models`
- `class FundingHarvestStrategy(TemplateStrategy)` — Collect funding by holding the side that *receives* it (§8.4, §6.4).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`
- `class FundingDislocationStrategy(TemplateStrategy)` — Trade the HL leg when its funding dislocates from a benchmark rate (§8.4).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`
- `class BasisTradeStrategy(TemplateStrategy)` — Fade the perp premium: short when perp trades rich to index, long when cheap (§8.4).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`
- `class OiMomentumStrategy(TemplateStrategy)` — Open-interest momentum: rising OI confirms the price move to trade with (§8.4).
    - `def __init__(self, **overrides: Any) -> None`
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`

### `flint.strategy.templates.registry`
_The built-in template registry — the single source of truth (§8.4)._
- imports: `..base`
- `class TemplateNotFound(KeyError)` — No built-in template registered under this name (§8.4).
- `class TemplateSpec` — One registered template: its class plus the metadata surfaces read (§8.4).
    - `def param_spec(self) -> dict` — The template's declared knobs (name → default).
- `def register(spec: TemplateSpec) -> TemplateSpec` — Register ``spec``; raise on a duplicate name (a registry is not a merge).
- `def get_template(name: str) -> TemplateSpec` — Return the spec registered under ``name`` or raise :class:`TemplateNotFound`.
- `def list_templates() -> list[TemplateSpec]` — Every registered template, sorted by name (stable for UIs/CLIs).
- `def template_names() -> list[str]` — The registered template names, sorted.

### `flint.strategy.templates.technical`
_The classic technical templates: MA/EMA cross, RSI, Bollinger, breakout (§8.4)._
- imports: `.`, `._base`, `flint.core.models`
- `class MaCrossStrategy(TemplateStrategy)` — Trend-following moving-average cross: long above, short below (§8.4).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`
- `class RsiReversionStrategy(TemplateStrategy)` — Mean-reversion on RSI: long when oversold, short when overbought (§8.4).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`
- `class BollingerStrategy(TemplateStrategy)` — Bollinger-band mean reversion: long below the lower band, short above upper (§8.4).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`
- `class BreakoutStrategy(TemplateStrategy)` — Donchian breakout: long above the prior ``lookback`` high, short below its low (§8.4).
    - `def on_candle(self, candle: 'Candle', history: list['Candle'], ctx: Any) -> list['Signal']`
