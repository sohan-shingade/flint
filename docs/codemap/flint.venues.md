# Codemap: `flint.venues`

> source git hash: `a4a0c748dd23cd0acf997413f917bc2df1504795`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.venues.__init__`
_venues — one adapter per exchange, keyed by market structure (§7)._
- imports: `.spec`, `.structure`

### `flint.venues.spec`
_``VenueSpec`` — a venue's hard numbers, each with a primary source (D14)._
- imports: `.structure`
- `class LatencyProfile` — One-way decision→eligible latency: a lognormal (median, p95) in ms (§6.3).
- `class MaintTier` — One size band of a venue's maintenance-margin schedule (§6.5, D14).
    - `def maint_frac(self) -> float` — Maintenance fraction for this band = half the initial margin at max lev.
- `class LiquidationSpec` — What happens at and after a liquidation on one venue (§6.5, D14).
    - `def maint_frac(self, notional: float) -> float` — The maintenance fraction for a position of ``notional`` USD (§6.5).
- `class VenueSpec` — The cited, testable constants for one venue's fills (§6.3, D14).

### `flint.venues.structure`
_Market structure — how a venue actually matches trades (§2.6, §7)._
- `class MarketStructure(StrEnum)` — How a venue matches orders — selects the fill model (§2.6, §6.3).
