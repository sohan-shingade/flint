# Codemap: `flint.connectors.drift`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.connectors.drift.__init__`
- imports: `.client`, `.types`

### `flint.connectors.drift.client`
_Drift protocol connector._
- imports: `...models`, `...providers.drift_api`, `..base`, `.types`
- `class DriftConnector(BaseConnector)` — Drift connector backed by public REST APIs.
    - `def __init__(self, client: Optional[httpx.Client]=None) -> None`
    - `def protocol(self) -> str`
    - `async def get_markets(self) -> List[MarketInfo]`
    - `async def get_orderbook(self, market: str) -> OrderbookSnapshot`
    - `async def get_price(self, market: str) -> float`
    - `async def get_funding_rates(self, market: str='SOL-PERP', limit: int=100) -> List[FundingRate]`
    - `async def place_order(self, order: Order) -> Order`
    - `async def cancel_order(self, order_id: str) -> bool`
    - `async def get_positions(self) -> List[Dict]`
    - `async def get_balances(self) -> Dict[str, float]`
    - `def get_market_info(self, symbol: str) -> Optional[DriftPerpMarket]`
    - `async def close(self) -> None`

### `flint.connectors.drift.stream`
_Drift event stream — polls the DLOB and Data APIs for live updates._
- imports: `...models`, `...providers.drift_api`
- `class DriftPoller` — Polls Drift APIs at configurable intervals.
    - `def __init__(self, provider: Optional[DriftDataProvider]=None) -> None`
    - `def poll_once(self, market: str='SOL-PERP', depth: int=10) -> OrderbookSnapshot` — Fetch orderbook once and fire callback if set.
    - `def poll_funding(self, market: str='SOL-PERP', limit: int=10) -> List[FundingRate]` — Fetch latest funding rates and fire callback if set.
    - `def run_loop(self, market: str='SOL-PERP', interval_s: float=2.0, max_iterations: Optional[int]=None) -> None` — Blocking poll loop. Use ``max_iterations`` for testing.
    - `def stop(self) -> None`
    - `def close(self) -> None`

### `flint.connectors.drift.types`
_Drift-specific types._
- `class DriftPerpMarket`
- `class DriftUserPosition`
    - `def entry_price(self) -> float`
    - `def is_long(self) -> bool`
    - `def size(self) -> float`
    - `def unrealised_pnl(self, oracle_price: float) -> float`
