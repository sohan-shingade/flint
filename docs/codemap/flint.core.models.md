# Codemap: `flint.core.models`

> source git hash: `60c07da262788921210b200ec4eb53f58ad550e6`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.core.models.__init__`
_core.models — frozen dataclasses: Candle, MarkSnapshot, FundingRate, OrderbookSnapshot, BorrowSnapshot, Order, Fill, Position, the Side/OrderType/TimeInForce enums, and Signal (§5, §8.1)._
- imports: `.enums`, `.market`, `.signal`, `.trading`

### `flint.core.models.enums`
_Trading enumerations (§5, §8.1)._
- `class Side(StrEnum)` — Direction of an order, fill, or position (one enum, shared — §5).
    - `def sign(self) -> int` — +1 for LONG, -1 for SHORT — the exposure/PnL multiplier.
    - `def opposite(self) -> 'Side'` — The direction that reduces a position of this side.
- `class OrderType(StrEnum)` — Order kinds the engine understands (§5, §8.1).
- `class TimeInForce(StrEnum)` — Order lifetime policy (§5, §8.1).

### `flint.core.models.market`
_Market-data models — the recorded/observed facts an engine reads (§5)._
- `class Candle` — One OHLCV bar. ``ts`` is the bar START (unix ms).
- `class MarkSnapshot` — The 'three prices' recorded together (§5, §6.1).
    - `def basis_bps(self) -> float` — Perp premium/discount of mark vs index, in basis points.
- `class FundingRate` — A single published funding observation (§5, §2.2).
- `class OrderbookSnapshot` — One moment of recorded/archived CLOB depth (§5).
- `class QuoteTick` — One top-of-book quote — the tick lane's ``on_quote`` datum (§8.6, §9).
    - `def mid(self) -> float` — The bid/ask midpoint (0.0 if a side is missing — never invented).
    - `def spread_bps(self) -> float` — The bid/ask spread in basis points of the mid (0.0 if unquotable).
- `class BookDelta` — One L2 incremental book update — the tick lane's ``on_book`` datum (§8.6, §9).
    - `def is_delete(self) -> bool`
- `class BorrowSnapshot` — Jupiter/GMX carrying cost — NOT a funding analog (§5, §6.3).

### `flint.core.models.signal`
_The Signal — the strategy's declarative output (§8.1)._
- imports: `.enums`
- `class Signal` — A declared trading intent (§8.1).
    - `def is_close(self) -> bool`
    - `def long(cls, market: str, venue: str, size_usd: float=0.0, size: float=0.0, **kwargs) -> 'Signal'`
    - `def short(cls, market: str, venue: str, size_usd: float=0.0, size: float=0.0, **kwargs) -> 'Signal'`
    - `def close(cls, market: str, venue: str, **kwargs) -> 'Signal'` — Reduce-only close of the full current position (§8.1 rule 3).

### `flint.core.models.trading`
_Trading models — orders, fills, positions (§5)._
- imports: `.enums`
- `class Order` — An instruction to trade. Mutable — the engine advances its state (§6.2).
- `class Fill` — An immutable record of an executed trade (§5).
- `class Position` — An open position (§5).
    - `def signed_size(self) -> float` — Size with direction: +size for LONG, -size for SHORT.
    - `def notional(self, mark_price: float) -> float` — Absolute position value at ``mark_price``.
    - `def unrealized_pnl(self, mark_price: float) -> float` — Mark-to-market PnL against ``mark_price`` (§5, §6.5).
