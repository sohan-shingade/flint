# Codemap: `flint.api.routes`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.api.routes.__init__`

### `flint.api.routes.backtest`
_Backtest API routes._
- imports: `...analytics.monte_carlo`, `...analytics.tearsheet`, `...backtest.calibration`, `...backtest.engine`, `...backtest.parity`, `...config`, `...data.quality`, `...execution.capital`, `...execution.fill_models`, `...execution.margin`, `...execution.venue_config`, `...journal.storage`, `...models`, `...providers.drift_candles`, `...providers.drift_s3`, `...regimes`, `...services.strategies`, `...store`, `...strategy.loader`, `...strategy.sandbox`, `.strategies`
- `def configure_concurrency(max_concurrent: int)` — Called at startup to set concurrency from config.
- `class BacktestRequest(BaseModel)`
- `def run_backtest(req: BacktestRequest, request: Request)` — Submit a backtest. Returns an ID to poll for results.
- `def list_backtests()` — List all tracked backtest runs with their statuses.
- `def list_regimes()` — Return all available regime definitions.
- `def run_regime_backtest(request: Request, body: dict)` — Run a separate backtest per regime. Returns an ID to poll for results.
- `def get_status(run_id: str)`
- `def get_results(run_id: str)`
- `def cancel_backtest(run_id: str)` — Mark a backtest as cancelled. The thread will continue but results are discarded.
- `def compare_backtests(ids: str)` — Compare multiple backtests. Pass comma-separated IDs.
- `def run_calibration(req: dict, request: Request)` — Run slippage calibration (read-only, does not write to config).
- `def run_parity(req: dict, request: Request)` — Run backtest-vs-paper parity test.

### `flint.api.routes.collector`
_Collector status and trigger API routes._
- imports: `...collector.tasks`
- `class TriggerRequest(BaseModel)`
- `def get_collector_status(request: Request)`
- `async def trigger_collection(req: TriggerRequest, request: Request)`
- `def get_collector_config()`

### `flint.api.routes.data`
_Data query API — candles, funding rates, OI, liquidations, whale transfers,_
- imports: `...analytics.correlation`, `...models`, `...providers`, `...services.data`, `...store`
- `def get_ohlcv(request: Request, market: str=Query(..., description='Market symbol, e.g. SOL-PERP'), resolution_s: int=Query(3600, description='Candle width in seconds'), start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None), limit: int=Query(1000, le=10000), venue: Optional[str]=Query(None, description='Filter by venue'))`
- `def get_volume(request: Request, market: str=Query('SOL-PERP'), resolution_s: int=Query(3600), start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None))` — Get per-venue volume data for a market.
- `def get_funding(request: Request, market: str=Query('SOL-PERP'), start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None))` — Get funding rates for a market, grouped by venue.
- `def get_borrow_rates(request: Request, market: str=Query('SOL-PERP'), start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None))` — Get Jupiter borrow rate history for a market.
- `def delete_market_data(market: str, request: Request)` — Delete all data for a specific market (candles, funding, OI, etc.).
- `def list_markets(request: Request)` — List markets with data in the store.
- `def check_data(request: Request, market: Optional[str]=Query(None), markets: Optional[str]=Query(None), resolution_s: int=Query(3600), start_ts: int=Query(...), end_ts: int=Query(...))` — Check if data exists for a given market/timeframe/date range.
- `def check_markets_data(request: Request, body: dict)` — Check data availability for multiple markets at once.
- `def list_provider_status()` — List all registered providers and their availability status.
- `def get_open_interest(request: Request, market: str, start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None), limit: int=Query(1000, le=10000))` — Query open interest snapshots for a market.
- `def get_liquidations(request: Request, market: str, start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None), limit: int=Query(500, le=5000))` — Query liquidation events for a market.
- `def get_whale_transfers(request: Request, token: Optional[str]=Query(None, description='Token mint address'), wallet: Optional[str]=Query(None, description='Wallet address'), start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None), limit: int=Query(500, le=5000))` — Query whale transfer events, filterable by token mint and/or wallet.
- `def get_dex_volume(request: Request, market: str, dex: Optional[str]=Query(None, description='Filter by DEX name'), start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None), limit: int=Query(1000, le=10000))` — Query DEX volume snapshots for a market.
- `def get_data_freshness(request: Request)` — Data freshness report across all providers and markets.
- `def get_correlation(request: Request, markets: str=Query(..., description='Comma-separated market symbols, e.g. SOL-PERP,BTC-PERP,ETH-PERP'), resolution_s: int=Query(3600, description='Candle resolution in seconds'), start_ts: Optional[int]=Query(None), end_ts: Optional[int]=Query(None))` — Compute pairwise correlation matrix for the requested markets.
- `def download_market_data(request: Request, body: dict)` — Download market data for a specific market and date range.
- `def start_async_download(request: Request, body: dict)` — Start an async download for one or more markets. Returns immediately with a download ID.
- `def get_download_status(dl_id: str)` — Poll async download progress.
- `def available_markets()` — List all markets available for download from Drift (perp + spot) + CoinGecko.

### `flint.api.routes.journal`
_Journal API — persistent backtest history._
- imports: `...services`
- `def list_runs(request: Request, limit: int=50)`
- `def get_run(run_id: str, request: Request)`
- `def delete_run(run_id: str, request: Request)`
- `def compare_runs(ids: str, request: Request)`

### `flint.api.routes.live`
_Live trading data API endpoints._
- `def get_live_fills(request: Request, session_id: str='', venue: str='', market: str='')` — Query live fills with optional filters.
- `def get_live_equity(request: Request, session_id: str='')` — Query equity history for a session.
- `def get_live_sessions(request: Request)` — List live trading sessions.

### `flint.api.routes.mev`
_MEV API routes._
- imports: `...mev.arb`, `...mev.liquidation`, `...models`
- `class PoolInput(BaseModel)`
- `class ArbScanRequest(BaseModel)`
- `class PositionInput(BaseModel)`
- `class LiqScanRequest(BaseModel)`
- `def scan_arb(req: ArbScanRequest)`
- `def scan_liquidations(req: LiqScanRequest)`

### `flint.api.routes.optimization`
_Optimization API routes._
- imports: `...backtest.engine`, `...execution.capital`, `...execution.fill_models`, `...execution.margin`, `...execution.venue_config`, `...optimization.ga_optimizer`, `...optimization.optimizer`, `...optimization.walk_forward`, `...providers.drift_candles`, `...store`, `...strategy.loader`
- `class OptimizeRequest(BaseModel)`
- `class WalkForwardRequest(BaseModel)`
- `def run_optimization(req: OptimizeRequest, request: Request)` — Submit an optimization job.
- `def run_walk_forward_analysis(req: WalkForwardRequest, request: Request)` — Run walk-forward analysis to detect overfitting.
- `def get_results(run_id: str)`

### `flint.api.routes.paper`
_Paper trading API routes._
- imports: `...config`, `...paper.risk_guard`, `...paper.session_store`, `...services.strategies`, `...strategy.loader`, `.backtest`
- `class StartRequest(BaseModel)`
- `class StopRequest(BaseModel)`
- `async def start_paper(req: StartRequest, request: Request)`
- `async def stop_paper(req: StopRequest, request: Request)`
- `async def kill_paper(req: StopRequest, request: Request)`
- `async def paper_status(session_id: str, request: Request)`
- `async def list_sessions(request: Request)`
- `def get_portfolio(request: Request)` — Aggregate portfolio view across all paper sessions.
- `def deploy_strategy(body: dict, request: Request)` — Deploy a strategy from BacktestLab with replay-forward execution.
- `def redeploy_session(body: dict, request: Request)` — Redeploy a session from a new start date.
- `def redeploy_all_sessions(body: dict, request: Request)` — Redeploy all active sessions from a new start date.
- `async def paper_trades(session_id: str, request: Request)`
- `def get_equity_history(session_id: str, request: Request)` — Get full equity curve for a session from DuckDB.
- `def update_risk_config(session_id: str, body: dict, request: Request)` — Update risk configuration for a running session.
- `def get_reconciliation(session_id: str, request: Request, ts_window_s: int=60)` — Reconcile engine-recorded fills vs venue-reported fills.
- `async def post_reconciliation(session_id: str, request: Request, file: UploadFile=File(...), ts_window_s: int=60)` — Match engine fills against a user-uploaded venue fill CSV.

### `flint.api.routes.replay`
_Replay API — query the portfolio event log + reconstruct state._
- imports: `...portfolio.event_log`, `...portfolio.replay`, `...portfolio.snapshots`
- `def list_events(session_id: str, request: Request, since: Optional[int]=Query(None, description='Return events with seq > since'), limit: int=Query(200, ge=1, le=_MAX_EVENTS_PER_PAGE))`
- `def get_state(session_id: str, request: Request, target_ts: int=Query(..., description='Replay up to this epoch second'), initial_capital: float=Query(10000.0), use_snapshot: bool=Query(True, description='Fast-forward via snapshot when available'))`
- `def get_summary(session_id: str, request: Request)` — Cheap polling target — counts only, no full payload reads.

### `flint.api.routes.strategies`
_Strategy listing API — generated dynamically from backtest builders._
- imports: `.backtest`
- `def list_strategies()`
- `def get_strategy(name: str)`

### `flint.api.routes.system`
_System API — status check, configuration, and strategy registry._
- imports: `...backtest.rust_capabilities`, `...execution.venue_config`, `...regimes`
- `class SystemStatus(BaseModel)`
- `class ConfigRequest(BaseModel)`
- `class ConfigResponse(BaseModel)`
- `def system_status(request: Request)`
- `def get_capabilities(request: Request)` — Feature matrix for UI + MCP clients.
- `def save_config(body: ConfigRequest)` — Save optional API keys to .env file.
- `def list_venues()` — List all supported execution venues with their configurations.
- `def list_regimes()` — List all market regime definitions.
- `class StrategyCreate(BaseModel)`
- `def list_registered_strategies(request: Request)` — List all registered strategies with lifecycle stats.
- `def register_strategy(body: StrategyCreate, request: Request)` — Register a new strategy or update an existing one.
- `def get_registered_strategy(name: str, request: Request)` — Get a specific registered strategy by name.
- `def delete_registered_strategy(name: str, request: Request)` — Delete a registered strategy.
- `def price_sources_health(request: Request)` — Per-source health for the live PnL ticker chain.

### `flint.api.routes.user_strategies`
_User strategy CRUD API routes._
- imports: `...strategy.loader`
- `class SaveStrategyRequest(BaseModel)`
- `class ValidateRequest(BaseModel)`
- `def save_strategy(req: SaveStrategyRequest)`
- `def list_strategies()`
- `def load_strategy(name: str)`
- `def delete_strategy(name: str)`
- `def validate_strategy(req: ValidateRequest)`
