# Codemap: `flint.store`

> source git hash: `bf2d38593d105c40890d8198e2f3307e4e934e9a`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.store.__init__`
_DuckDB-backed store package._
- imports: `.store`

### `flint.store.schema`
_DDL constants and migration logic for the DuckDB-backed store._
- `def create_tables(conn: duckdb.DuckDBPyConnection) -> None` — Create all tables and run legacy migrations.

### `flint.store.store`
_DuckDB-backed candle store._
- imports: `.`, `..models`
- `class FlintStore` — Thread-safe DuckDB store.
    - `def __init__(self, path: str='')`
    - `def upsert_candles(self, candles: List[Candle]) -> int`
    - `def query_candles(self, market: str, resolution_s: int, start_ts: Optional[int]=None, end_ts: Optional[int]=None, limit: Optional[int]=None, venue: Optional[str]=None) -> List[Candle]`
    - `def count_candles(self, market: str, resolution_s: int, venue: Optional[str]=None) -> int`
    - `def has_candles(self) -> bool` — Check if any candle data exists.
    - `def get_markets_needing_pyth_migration(self) -> list` — Return market names that have non-Pyth candles but no Pyth candles.
    - `def get_market_date_range(self, market: str)` — Return (min_ts, max_ts) for all candles of a market, or None if no data.
    - `def query_candles_with_fallback(self, market: str, resolution_s: int, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> List[Candle]` — Query candles preferring venue='pyth'; falls back to any venue if none found.
    - `def upsert_funding_rates(self, rates: List[FundingRate]) -> int` — Insert funding rates into venue_funding_rates table.
    - `def query_funding_rates(self, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None, venue: Optional[str]=None) -> List[FundingRate]` — Query funding rates from venue_funding_rates.
    - `def query_funding_by_venue(self, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> dict` — Query funding rates grouped by venue.
    - `def upsert_borrow_rates(self, snapshots: List[BorrowSnapshot]) -> int` — Upsert Jupiter Perps borrow rate snapshots.
    - `def query_borrow_rates(self, market: str, start_ts: int, end_ts: int) -> List[BorrowSnapshot]` — Query borrow rate snapshots for a market within [start_ts, end_ts].
    - `def query_borrow_cumulative(self, market: str, ts: int) -> Optional[float]` — Return the cumulative_rate at or before ts, or None if no data exists.
    - `def upsert_oracle_prices(self, prices: List[OraclePrice]) -> int`
    - `def query_oracle_prices(self, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> List[OraclePrice]`
    - `def count_oracle_prices(self, market: str) -> int`
    - `def upsert_orderbook_snapshots(self, snapshots: list) -> int`
    - `def query_orderbook_snapshots(self, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> List['OrderbookSnapshot']` — Query orderbook snapshots for a market within a time range.
    - `def query_nearest_orderbook(self, venue: str, market: str, ts: int) -> Optional['OrderbookSnapshot']` — Return the most recent orderbook snapshot for (venue, market) at or before ts.
    - `def upsert_pool_snapshots(self, snapshots: list) -> int`
    - `def upsert_venue_funding(self, snapshots: list) -> int` — Insert cross-venue funding rate snapshots.
    - `def query_venue_funding(self, venue: str, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> list` — Query funding rates for a specific venue + market.
    - `def list_venues(self, market: Optional[str]=None) -> list` — List venues that have funding data, optionally filtered by market.
    - `def upsert_open_interest(self, records: List['OpenInterest']) -> int`
    - `def query_open_interest(self, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None, venue: Optional[str]=None) -> list`
    - `def upsert_liquidations(self, records: List['Liquidation']) -> int`
    - `def query_liquidations(self, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> list`
    - `def upsert_whale_transfers(self, records: List['WhaleTransfer']) -> int`
    - `def query_whale_transfers(self, token_mint: Optional[str]=None, wallet: Optional[str]=None, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> list`
    - `def upsert_dex_volume(self, records: List['DexVolume']) -> int`
    - `def query_dex_volume(self, market: str, dex: Optional[str]=None, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> list`
    - `def upsert_token_unlocks(self, records: list) -> int`
    - `def upsert_sync_metadata(self, meta: 'SyncMetadata') -> None`
    - `def get_sync_metadata(self, provider: str, market: str, data_type: str) -> Optional['SyncMetadata']`
    - `def is_range_synced(self, market: str, resolution_s: int, start_ts: int, end_ts: int, max_age_s: int=3600) -> bool` — Check if a candle range is already covered in the local DB.
    - `def list_sync_metadata(self, provider: Optional[str]=None) -> list`
    - `def get_data_freshness(self) -> list` — Return freshness info for all tracked provider/market pairs.
    - `def create_live_session(self, session_id: str, strategy_name: str, market: str, network: str, venue: str, initial_capital: float, config_snapshot: str) -> None`
    - `def update_live_session_status(self, session_id: str, status: str, stopped_at: Optional[int]=None) -> None`
    - `def get_live_session(self, session_id: str) -> Optional[dict]`
    - `def upsert_live_order(self, order_id: str, session_id: str, venue_order_id: Optional[int], market: str, side: str, order_type: str, size: float, price: float, state: str, retry_count: int, tx_sig: Optional[str], created_at: int, updated_at: int, state_history: str) -> None`
    - `def get_live_orders(self, session_id: str, state: Optional[str]=None) -> list`
    - `def insert_live_fill(self, fill_id: str, order_id: str, session_id: str, market: str, side: str, price: float, size: float, fee: float, tx_sig: str, venue: str, is_partial: bool, ts: int) -> None`
    - `def get_live_fills(self, session_id: str, market: Optional[str]=None) -> list`
    - `def query_live_fills_by_venue(self, venue: str, market: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> list` — Query fills from live_fills by venue and market.
    - `def insert_live_equity(self, session_id: str, ts: int, equity: float, cash: float, unrealized_pnl: float) -> None`
    - `def get_live_equity_history(self, session_id: str) -> list`
    - `def upsert_tick_snapshot(self, pool_address: str, ts: int, dex: str, token_a_mint: str, token_b_mint: str, current_tick: int, tick_spacing: int, fee_rate: float, sqrt_price: float, tick_data_json: str) -> None`
    - `def query_tick_snapshots(self, pool_address: str, start_ts: Optional[int]=None, end_ts: Optional[int]=None) -> list`
    - `def upsert_strategy(self, strategy_id: str, name: str, code: str='', params_json: str='{}', category: str='custom', status: str='draft', notes: str='') -> None` — Save or update a strategy in the registry.
    - `def update_strategy_status(self, name: str, status: str) -> None` — Update the lifecycle status of a strategy by name.
    - `def list_strategies(self) -> list` — List all registered strategies with lifecycle stats.
    - `def get_strategy(self, name: str) -> dict | None` — Get a single strategy by name.
    - `def delete_strategy(self, name: str) -> bool` — Delete a strategy by name.
    - `def list_live_sessions(self, limit: int=20) -> list` — List recent live trading sessions (most recent first).
    - `def list_markets_with_data(self) -> list` — List every (market, resolution) pair that has candle data.
    - `def list_venues_for_market(self, market: str, exclude_venues: Optional[List[str]]=None) -> List[str]` — Distinct venues that have candle data for a given market.
    - `def delete_market_data(self, market: str) -> dict` — Delete candles + funding + oracle + orderbook + OI + liquidations
    - `def count_funding_rates(self, market: str, start_ts: int, end_ts: int) -> int`
    - `def count_orderbook_snapshots(self, market: str, start_ts: int, end_ts: int) -> int`
    - `def count_open_interest(self, market: str, start_ts: int, end_ts: int) -> int`
    - `def mark_running_live_sessions_interrupted(self, stopped_at: int) -> list` — Startup-recovery helper: any live_session row stuck at 'running'
    - `def count_venue_candles(self, market: str, start_ts: int, end_ts: int, exclude_venues: Optional[List[str]]=None) -> int` — Count candles for a market across non-excluded venues within ts range.
    - `def close(self) -> None`
