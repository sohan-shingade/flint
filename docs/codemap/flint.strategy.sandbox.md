# Codemap: `flint.strategy.sandbox`

> source git hash: `14b81bdae6eae18cd604bead255e45aad876a0af`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.strategy.sandbox.__init__`
_strategy.sandbox — the OS-isolated strategy runner (D25, §8.3)._
- imports: `.errors`, `.oslayer`, `.runner`, `.screen`

### `flint.strategy.sandbox._child`
_The sandbox child process (§8.3). Run as ``python -I -m flint.strategy.sandbox._child``._
- imports: `flint.strategy.sandbox.policy`, `flint.strategy.sandbox.protocol`
- `def main() -> None`

### `flint.strategy.sandbox._child_backtest`
_The sandbox child for a FULL user-source backtest (§8.3, D25 closure)._
- imports: `._child`, `.policy`, `flint.adapters`, `flint.core.models.market`, `flint.engine`, `flint.engine.portfolio`, `flint.live`, `flint.ports`, `flint.strategy.ml`, `flint.strategy.templates.registry`, `flint.venues`
- `def main() -> None`

### `flint.strategy.sandbox._spawn`
_Shared child-process spawn/scrub/limits machinery (§8.3)._
- imports: `.errors`, `.oslayer`, `flint.ports`
- `def spawn_child(child_module: str, job: dict[str, Any], quota: ResourceQuota) -> dict[str, Any]` — Run ``child_module`` on ``job`` in an isolated subprocess; return its envelope.

### `flint.strategy.sandbox.backtest_runner`
_``run_backtest_in_sandbox`` — a full user-source backtest inside the sandbox._
- imports: `._spawn`, `.errors`, `flint.core.models.market`, `flint.ports`
- `class SandboxBacktestResult` — What the sandboxed engine run produced, as inert data for the parent.
- `def run_backtest_in_sandbox(source: str, class_name: str, *, candles: list[Candle], funding: dict[str, list[FundingRate]], marks: dict[str, list[MarkSnapshot]], seed: int=0, initial_capital: str='100000', fund_venue: str='hyperliquid', run_id: str='sandbox', overrides: dict[str, Any] | None=None, quota: ResourceQuota | None=None) -> SandboxBacktestResult` — Run ``source``'s ``class_name`` backtest over the given inert inputs, isolated.

### `flint.strategy.sandbox.errors`
_Sandbox failure taxonomy (§8.3)._
- `class SandboxError(Exception)` — Base class for every sandbox outcome that isn't a clean result.
- `class StrategyError(SandboxError)` — The sandboxed strategy raised (or a denied import failed inside it).
    - `def __init__(self, error_type: str, message: str) -> None`
- `class SandboxTimeout(SandboxError)` — A CPU (RLIMIT_CPU) or wall-clock limit killed the run.
- `class SandboxViolation(SandboxError)` — The child crashed, exceeded the output cap, or broke the protocol.

### `flint.strategy.sandbox.oslayer`
_OS-isolation layer — stubbed hooks for nsjail/bubblewrap + seccomp (§8.3)._
- imports: `flint.ports`
- `def os_layer_available() -> bool` — Whether an OS jail (nsjail/bwrap) is wired up. False until Linux hardening.
- `def wrap_command(cmd: Sequence[str], quota: ResourceQuota) -> list[str]` — Wrap ``cmd`` in the OS jail if available; otherwise pass it through.

### `flint.strategy.sandbox.policy`
_Import allowlist + minimal ``__builtins__`` (§8.3)._
- `def build_safe_builtins() -> dict[str, Any]` — Return the restricted ``__builtins__`` dict handed to strategy code.

### `flint.strategy.sandbox.protocol`
_The parent<->child wire codec (§8.3)._
- `def encode_value(obj: Any) -> dict[str, Any]` — Encode a value to a ``{"kind", "data"}`` envelope. Raises on unsupported.
- `def decode_value(env: dict[str, Any]) -> Any` — Decode a ``{"kind", "data"}`` envelope. Uses JSON/Arrow only, never pickle.

### `flint.strategy.sandbox.runner`
_``run_strategy_sandboxed`` — the ONE way strategy code ever executes (§8.3)._
- imports: `._spawn`, `.errors`, `.protocol`, `.screen`, `flint.ports`
- `def run_strategy_sandboxed(source: str, entry: str='run', input_value: Any=None, *, quota: ResourceQuota | None=None, screen: bool=False) -> Any` — Run ``source``'s ``entry(input_value)`` in the sandbox; return its result.

### `flint.strategy.sandbox.screen`
_Static AST pre-screen for strategy source — lint-grade UX, NOT the boundary (§8.3)._
- imports: `.errors`, `.policy`
- `class ScreenViolation` — One line-precise reason the source failed the screen (§8.3).
- `class StrategyScreenError(SandboxError)` — The static screen rejected the source (a UX failure, raised pre-run).
    - `def __init__(self, violations: list[ScreenViolation]) -> None`
- `def screen_source(source: str, filename: str='<strategy>') -> list[ScreenViolation]` — Return every screen violation in ``source``, sorted by (line, col).
- `def screen_or_raise(source: str, filename: str='<strategy>') -> None` — Raise :class:`StrategyScreenError` if ``source`` has any screen violations.
