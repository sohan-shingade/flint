# Codemap: `flint.engine`

> source git hash: `94ecc280684030330ed470ccd79edd74b5dfc5d4`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.engine.__init__`
_engine — the simulator/executor shared by backtest and paper._
- imports: `.api`, `.context`, `.loop`, `.money`, `.orders`, `.portfolio`, `.select`, `.state`, `.tearsheet`

### `flint.engine.api`
_The engine seam — the substrate-agnostic run contract (§6.0, D29, plan §A1)._
- imports: `.context`, `.fills`, `.loop`, `.portfolio`, `.state`, `flint.core.models`, `flint.venues`
- `class EngineFeed` — All recorded market data an engine run consumes (§6.0, plan §A1).
- `class EngineRunSpec` — The run's config + funding + policy knobs, substrate-agnostic (§6.0).
- `class SimulationEngine(Protocol)` — The one contract every simulation substrate satisfies (§6.0, D29).
    - `def run(self, feed: EngineFeed, strategy: object, *, event_log: EventLog, spec: EngineRunSpec) -> PortfolioState`

### `flint.engine.loop`
_The per-bar backtest loop — the honest engine's spine (§6.1)._
- imports: `.context`, `.fills`, `.funding.settlement`, `.liquidation.cascade`, `.money`, `.orders`, `.signals`, `.state`, `flint.core.models`, `flint.core.time`, `flint.engine.portfolio`, `flint.engine.portfolio.events`, `flint.venues`
- `class EngineConfig` — The engine's locked-but-calibratable defaults for a run.
- `class Strategy(Protocol)` — The minimal strategy seam the loop calls (slice 3.6 formalises it).
    - `def on_candle(self, candle: Candle, ctx: 'EngineContext') -> list[Signal]`
- `class NoopStrategy` — A strategy that never trades — the default for engine-mechanics tests.
    - `def on_candle(self, candle: Candle, ctx: 'EngineContext') -> list[Signal]`
- `class EngineContext` — The read-only window a strategy sees, under the §8.2 visibility contract.
    - `def position(self, market: str, venue: str | None=None) -> Position | None` — The open position on ``(venue or default, market)``, or ``None``.
    - `def account(self, venue: str | None=None) -> AccountView` — A per-venue equity snapshot + the §8.1 sizing helpers (§8.2, §6.5/§6.6).
    - `def funding_rate(self, market: str, venue: str | None=None) -> FundingRate | None` — Last **published predicted** funding rate knowable at bar start (§6.4).
    - `def basis_bps(self, market: str, venue: str | None=None) -> float | None` — Perp premium vs index from the last MarkSnapshot before bar start (§8.2).
    - `def open_interest(self, market: str, venue: str | None=None) -> float | None` — Last open-interest reading before bar start, or ``None`` (§8.2).
    - `def orderbook(self, market: str, venue: str | None=None) -> OrderbookSnapshot | None` — Last L2 snapshot before bar start, **within** the staleness threshold (§8.2/§6.3).
    - `def candles(self, market: str, lookback: int, venue: str | None=None) -> list[Candle]` — The last ``lookback`` **closed** bars for ``market`` (bar_end ≤ now, §8.2).
    - `def submit_order(self, market: str, venue: str, side: Side, size: float, *, type: OrderType=OrderType.MARKET, price: float=0.0, tif: TimeInForce | None=None, margin_mode: str='cross', reduce_only: bool=False, client_order_id: str='') -> object` — The imperative escape hatch (§8.1, D21): submit an order directly.
- `class BacktestEngine` — Walks candles through the locked per-bar sequence (§6.1).
    - `def __init__(self, event_log: EventLog, *, config: EngineConfig | None=None, fill_model: FillModel | None=None, state: PortfolioState | None=None, venue_spec: VenueSpec | None=None) -> None`
    - `def run(self, candles: list[Candle], *, marks: dict[str, list[MarkSnapshot]] | None=None, funding: dict[str, list[FundingRate]] | None=None, books: dict[str, list[OrderbookSnapshot]] | None=None, trades: dict[str, list[TradePrint]] | None=None, oi: dict[str, list[OpenInterestSnapshot]] | None=None, strategy: Strategy | None=None) -> PortfolioState` — Run the loop over ``candles`` and return the final portfolio state.

### `flint.engine.money`
_The monetary type and the one conversion into it (§5 numeric policy)._
- `def money(value: float | int | str | Decimal) -> Money` — Convert ``value`` to the monetary ``Decimal`` type via its string form.

### `flint.engine.orders`
_The persisted order state machine (§6.2)._
- imports: `flint.core.models`
- `class OrderStatus(StrEnum)` — The states an order occupies over its life (§6.2).
- `class OrderTransitionError(Exception)` — An illegal state transition was attempted — always an engine bug (§6.2).
- `class OrderRecord` — One order's live position in the state machine (§6.2).
    - `def is_terminal(self) -> bool`
    - `def remaining(self) -> float`
    - `def transition(self, to: OrderStatus, *, reason: str='') -> None` — Advance to ``to``, rejecting any transition the §6.2 machine forbids.
    - `def apply_fill(self, size: float) -> None` — Record ``size`` base units filled, advancing placed/partial → filled.

### `flint.engine.select`
_Engine dispatch — pick the simulation substrate by name (§6.0, D29, plan §A1)._
- imports: `.api`, `.loop`, `.money`, `.nautilus`, `.portfolio`, `.state`
- `class UnknownEngineError(ValueError)` — An engine name that this build cannot provide (unbuilt or unrecognized, §6.0).
- `def resolve_engine_name(name: str) -> str` — Map a requested engine name to the substrate that will actually run (§6.0).
- `def installed_nautilus_version() -> str` — The exact ``nautilus_trader`` version this process runs (§19.4/§19.6).
- `class LegacyBarEngine` — The legacy per-bar loop as a :class:`SimulationEngine` — zero behavior change.
    - `def run(self, feed: EngineFeed, strategy: object, *, event_log: EventLog, spec: EngineRunSpec) -> PortfolioState`
- `def engine_for(name: str) -> type` — Return the :class:`SimulationEngine` factory for ``name`` (§6.0, D29).

### `flint.engine.signals`
_Signal→order conversion + size_usd materialization — the bar lane's pure routing (§8.1)._
- imports: `flint.core.models`
- `class SignalValidationError(ValueError)` — A strategy emitted an invalid Signal batch (§8.1 conversion rules).
- `class OrderRequest` — A resolved order the caller will build (assigning a coid) and submit.
    - `def as_order_kwargs(self) -> dict`
- `def route_signals(signals: list[Signal], default_venue: str, position_of: Callable[[str, str], Position | None]) -> list[OrderRequest | _UsdIntent]` — Convert a bar's signals to order requests / deferred USD intents (§8.1).
- `def round_lot(size_base: float, size_decimals: int) -> tuple[float, float]` — Floor ``size_base`` to the venue lot; return (lot_size, sub-lot residual).
- `def materialize_usd_intent(intent: _UsdIntent, open_price: float, size_decimals: int) -> tuple[OrderRequest, float]` — Size a deferred USD intent to base units at the execution bar's OPEN (§8.1).

### `flint.engine.state`
_Engine state — the account and position book the loop mutates (§6.1, §6.6)._
- imports: `.money`, `flint.core.models`
- `class Account` — One venue's margin account: ``Decimal`` cash plus running counters.
    - `def credit(self, amount: Money) -> None` — Add ``amount`` (signed) to free cash.
- `class PortfolioState` — The whole simulated wallet: per-venue accounts + a position book.
    - `def account(self, venue: str) -> Account` — The account for ``venue``, created with zero cash on first touch.
    - `def fund(self, venue: str, cash: float | Money) -> Account` — Seed ``venue``'s account with starting ``cash`` (test/setup helper).
    - `def position(self, venue: str, market: str) -> Position | None` — The open position on ``(venue, market)``, or ``None``.
    - `def equity(self, venue: str, marks: dict[str, float]) -> float` — Venue equity = cash + unrealized PnL of that venue's positions.
    - `def cross_margin_available(self, venue: str, marks: dict[str, float]) -> float` — The shared cross pool on ``venue`` = equity − locked isolated margin (§6.5).

### `flint.engine.tearsheet`
_The full-cost tearsheet data block + run invariants (§6.3, §19.2)._
- imports: `.money`, `.orders`, `.portfolio.events`
- `class InvariantError(AssertionError)` — A run-end conservation invariant was violated — always an engine bug (§19.2).
- `class Tearsheet` — The decomposed full-cost picture of a run (§6.3, §19.2).
- `def build_tearsheet(events: Iterable[Event]) -> Tearsheet` — Fold ``events`` into the full-cost tearsheet data block (§6.3).
- `def check_invariants(tearsheet: Tearsheet, orders: dict[str, object], *, expected_funding_settlements: int | None=None) -> None` — Assert the §19.2 run-end conservation invariants, raising on any violation.
