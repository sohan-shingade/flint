# Codemap: `flint.agent`

> source git hash: `ee75cc464d14a74074400df252597a9de9113bc4`
> generated by `scripts/codemap.py` — do not edit by hand

### `flint.agent.__init__`
_agent — the agentic development loop over the MCP tool surface (§13.1)._
- imports: `.loop`

### `flint.agent.loop`
_The agentic development loop — author → validate → backtest → revise (§13.1)._
- imports: `flint.mcp_srv.tools`
- `class Feedback` — The structured signal handed back to the author to drive the next revision.
- `class AgentIteration` — One turn of the loop: the source tried and every structured response to it.
    - `def failure_reasons(self) -> list[dict[str, Any]]`
- `class AgentSession` — The transcript of a loop run — every iteration, and whether it converged.
    - `def n_iterations(self) -> int`
    - `def final(self) -> AgentIteration | None`
- `def sequence_author(sources: Sequence[str]) -> StrategyAuthor` — An author that yields ``sources`` in order, ignoring feedback (fixtures, D26).
- `def run_agent_loop(tools: AgentTools, author: StrategyAuthor, *, backtest_kwargs: dict[str, Any] | None=None, max_iterations: int=10) -> AgentSession` — Drive the author↔Flint loop until it converges or runs out of turns (§13.1).
