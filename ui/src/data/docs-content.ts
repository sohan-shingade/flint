// AUTO-GENERATED from docs/guides/*.md and docs/tutorials/*.md
// Run: python scripts/build_docs.py
// Do not edit this file directly — edit the markdown sources instead.

export interface DocCodeBlock {
  language: string
  code: string
}

export interface DocTopic {
  id: string
  title: string
  content: string
  codeBlocks?: DocCodeBlock[]
}

export interface DocSection {
  id: string
  title: string
  topics: DocTopic[]
}

export const docsContent: DocSection[] = 
[
  {
    "id": "getting-started",
    "title": "GETTING STARTED",
    "topics": [
      {
        "id": "quickstart",
        "title": "Quickstart",
        "content": "\n<p>Get from zero to a validated, paper-trading strategy in under 30 minutes.</p>\n\n<h4>Prerequisites</h4>\n\n<ul>\n<li>Python 3.10 or higher</li>\n<li>Git</li>\n</ul>\n\n<p>---</p>\n\n<h4>Step 1: Install</h4>\n\n\n<p><code>flint init</code> downloads a few days of SOL-PERP candle data from Drift and runs a sample momentum backtest to confirm the install works. <code>flint serve</code> starts the API and UI at <a href=\"http://localhost:8000\">localhost:8000</a>.</p>\n\n<p>Verify the install:</p>\n\n\n<p>For development (separate API and UI with hot reload):</p>\n\n\n<p>---</p>\n\n<h4>Step 2: Download Data</h4>\n\n<p>The sample data from <code>flint init</code> covers only a few days. Before running real backtests, download historical data for the markets you want to test.</p>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>Open <a href=\"http://localhost:8000\">localhost:8000</a> and go to the <strong>Data Explorer</strong> tab.</li>\n<li>Select a market (e.g., <strong>SOL-PERP</strong>).</li>\n<li>Choose a date range. 90-180 days is a good starting point.</li>\n<li>Click <strong>Download</strong>.</li>\n</ol>\n\n<p>The download fetches OHLCV candles from Drift and funding rates from all enabled venues (Drift, Binance, Hyperliquid, and others). All data providers are free and require no API keys.</p>\n\n<h4>Via the API</h4>\n\n\n<p>Check what markets are available to download:</p>\n\n\n<p>Check what data you already have locally:</p>\n\n\n<p>---</p>\n\n<h4>Step 3: Run Your First Backtest</h4>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>Go to <strong>BacktestLab</strong>.</li>\n<li>Select <strong>Momentum</strong> from the strategy dropdown. (There are 20 built-in strategies -- Momentum is a good starting point.)</li>\n<li>Set the market to <strong>SOL-PERP</strong>.</li>\n<li>Set a date range that falls within your downloaded data.</li>\n<li>Set initial capital to <strong>10000</strong>.</li>\n<li>Click <strong>Run Backtest</strong>.</li>\n</ol>\n\n<h4>Via the API</h4>\n\n\n<p>The response returns a <code>backtest_id</code>. Poll for results:</p>\n\n\n<p>The <code>status</code> field will be <code>running</code> (with a <code>progress</code> percentage), <code>complete</code>, or <code>error</code>.</p>\n\n<p>---</p>\n\n<h4>Step 4: Read the Results</h4>\n\n<p>Once the backtest completes, the results include metrics, an equity curve, a trade list, and a full tearsheet. Here is what the key metrics mean and how to interpret them.</p>\n\n<h4>Sharpe Ratio</h4>\n\n<p>The Sharpe ratio measures risk-adjusted return. It answers: \"How much return did I get per unit of risk?\"</p>\n\n<ul>\n<li><strong>Above 2.0</strong> -- Excellent. The strategy generates strong returns relative to its volatility.</li>\n<li><strong>1.0 to 2.0</strong> -- Good. Worth investigating further with walk-forward validation.</li>\n<li><strong>0.5 to 1.0</strong> -- Marginal. May not survive transaction costs and slippage in live trading.</li>\n<li><strong>Below 0</strong> -- The strategy lost money. It performed worse than holding cash.</li>\n</ul>\n\n<h4>Max Drawdown</h4>\n\n<p>The largest peak-to-trough decline in equity during the backtest. A max drawdown of 15% means the account dropped 15% from its highest point before recovering.</p>\n\n<ul>\n<li><strong>Under 10%</strong> -- Conservative. Suitable for most risk tolerances.</li>\n<li><strong>10-20%</strong> -- Moderate. Typical for trend-following strategies.</li>\n<li><strong>20-30%</strong> -- Aggressive. Requires conviction and a long time horizon.</li>\n<li><strong>Over 30%</strong> -- Dangerous. Most traders will abandon the strategy before it recovers.</li>\n</ul>\n\n<h4>Win Rate</h4>\n\n<p>The percentage of trades that were profitable.</p>\n\n<ul>\n<li><strong>Above 60%</strong> -- High win rate. Common in mean-reversion strategies.</li>\n<li><strong>40-60%</strong> -- Normal range for most strategies.</li>\n<li><strong>Below 40%</strong> -- The strategy relies on large winners to compensate for frequent small losses. Check the profit factor.</li>\n</ul>\n\n<h4>Profit Factor</h4>\n\n<p>Total gross profit divided by total gross loss. A profit factor of 1.5 means the strategy made $1.50 for every $1.00 it lost.</p>\n\n<ul>\n<li><strong>Above 1.5</strong> -- Strong.</li>\n<li><strong>1.0 to 1.5</strong> -- Marginal after accounting for execution costs.</li>\n<li><strong>Below 1.0</strong> -- The strategy is net negative.</li>\n</ul>\n\n<h4>Other Metrics</h4>\n\n<ul>\n<li><strong>Total Return</strong> -- Percentage gain or loss on initial capital.</li>\n<li><strong>Sortino Ratio</strong> -- Like Sharpe, but only penalizes downside volatility. More relevant for strategies with asymmetric returns.</li>\n<li><strong>Monte Carlo</strong> -- On backtests with 5+ trades, Flint runs a 500-iteration bootstrap to estimate confidence intervals on the results.</li>\n</ul>\n\n<h4>What &quot;Good&quot; Looks Like</h4>\n\n<p>For SOL-PERP over the past 6 months, realistic expectations for a single-asset strategy:</p>\n\n<ul>\n<li>Sharpe above 1.0 with max drawdown under 20% is a solid result.</li>\n<li>Sharpe above 2.0 on historical data often indicates overfitting. Validate with walk-forward before trusting it.</li>\n<li>In a bear market (2025-2026), even a well-designed strategy may have negative total returns. The goal is to lose less than buy-and-hold and to control drawdown.</li>\n</ul>\n\n<p>---</p>\n\n<h4>Step 5: Optimize</h4>\n\n<p>Once you have a strategy that shows promise, optimize its parameters to find the best configuration.</p>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>In BacktestLab, after running a backtest, click the <strong>OPTIMIZE</strong> button.</li>\n<li>Set the number of <strong>trials</strong> (50 is a good default; 200 for thorough search).</li>\n<li>Choose the <strong>metric</strong> to optimize (Sharpe ratio is the default).</li>\n<li>Click <strong>Run</strong>.</li>\n</ol>\n\n<p>Flint uses Optuna (a Bayesian hyperparameter optimizer) to search the parameter space defined by the strategy's <code>parameters()</code> method. Each trial runs a full backtest with different parameter values.</p>\n\n<h4>Via the API</h4>\n\n\n<p>Poll for results the same way as backtests:</p>\n\n\n<p>The results include the best parameters found, convergence curve, parameter importance rankings, and the top 20 trials with their metrics.</p>\n\n<h4>Interpreting Optimization Results</h4>\n\n<ul>\n<li><strong>Convergence curve</strong> -- If the best value plateaus early (within the first 20-30 trials), the search has likely found the optimum. If it is still improving at 50 trials, consider running more.</li>\n<li><strong>Parameter importance</strong> -- Shows which parameters have the most impact on the metric. If a parameter has near-zero importance, it can be fixed at its default.</li>\n<li><strong>Best params</strong> -- The parameter combination that produced the highest metric value. Use these as a starting point, not a final answer.</li>\n</ul>\n\n<p>---</p>\n\n<h4>Step 6: Walk-Forward Validation</h4>\n\n<p>> <strong>Warning</strong>: Backtests are not predictions. A strategy that looks excellent on historical data may fail completely in live trading. Overfitting is the most common reason strategies fail after deployment. Always validate with walk-forward analysis before paper trading or going live.</p>\n\n<p>Walk-forward analysis is the single most important step in strategy validation. It works by splitting your data into multiple windows, optimizing on each training window, and testing on the subsequent out-of-sample window. This simulates what would have happened if you had optimized and deployed the strategy in real time.</p>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>In BacktestLab, after running an optimization, click the <strong>WALK-FORWARD</strong> button.</li>\n<li>Set the number of <strong>windows</strong> (5 is the default).</li>\n<li>Set the <strong>train/test split</strong> (70/30 is the default -- 70% of each window for training, 30% for testing).</li>\n<li>Click <strong>Run</strong>.</li>\n</ol>\n\n<h4>Via the API</h4>\n\n\n<h4>Interpreting Walk-Forward Results</h4>\n\n<p>The key output is the <strong>overfitting ratio</strong>: the ratio of out-of-sample performance to in-sample performance.</p>\n\n<ul>\n<li><strong>Overfitting ratio near 1.0</strong> -- The strategy performs about the same out-of-sample as in-sample. This is the best case.</li>\n<li><strong>Overfitting ratio 0.5-0.8</strong> -- Some performance degradation out-of-sample, which is normal. The strategy is likely viable.</li>\n<li><strong>Overfitting ratio below 0.3</strong> -- Severe overfitting. The in-sample results do not generalize. Do not deploy.</li>\n<li><strong>Negative out-of-sample Sharpe</strong> -- The strategy loses money on unseen data. Go back to Step 5 and try different parameters or a different strategy.</li>\n</ul>\n\n<p>Also check <strong>parameter stability</strong> across windows. If the optimal parameters change drastically from one window to the next, the strategy is fragile and unlikely to work going forward.</p>\n\n<p>---</p>\n\n<h4>Step 7: Deploy to Paper Trading</h4>\n\n<p>Paper trading runs your strategy against live market prices with simulated execution. No real money is at risk.</p>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>On any completed backtest result, click <strong>Deploy to Paper</strong>.</li>\n<li>The strategy starts running immediately against live Drift prices.</li>\n</ol>\n\n<h4>Via the API</h4>\n\n\n<p>The response includes a <code>session_id</code> for monitoring and control.</p>\n\n<h4>What Paper Trading Tests</h4>\n\n<p>Paper trading validates aspects that backtesting cannot:</p>\n\n<ul>\n<li><strong>Execution timing</strong> -- Can the strategy generate signals fast enough to act on them?</li>\n<li><strong>Data availability</strong> -- Does the strategy depend on data that arrives late or is missing?</li>\n<li><strong>Regime sensitivity</strong> -- How does the strategy behave in current market conditions, which may differ from the backtest period?</li>\n</ul>\n\n<p>---</p>\n\n<h4>Step 8: Monitor Paper Trading</h4>\n\n<h4>Via the UI</h4>\n\n<p>Go to the <strong>Paper Trading</strong> page. Active sessions show:</p>\n\n<ul>\n<li><strong>Equity curve</strong> -- Live equity over time, updated on each bar.</li>\n<li><strong>Open positions</strong> -- Current holdings with unrealized PnL.</li>\n<li><strong>Trade log</strong> -- Every fill with timestamp, price, and PnL.</li>\n<li><strong>Session status</strong> -- Running or stopped.</li>\n</ul>\n\n<h4>When to Stop</h4>\n\n<ul>\n<li><strong>Equity drops below a threshold</strong> -- If the strategy draws down more than its historical max drawdown, stop it. The market conditions may have changed.</li>\n<li><strong>No trades for an extended period</strong> -- If the strategy goes several days without trading when it should be active, check whether data is flowing correctly.</li>\n<li><strong>Consistent underperformance vs. backtest</strong> -- If paper results are significantly worse than backtest results after 2-4 weeks, the strategy may be overfit to historical data.</li>\n</ul>\n\n<p>A reasonable paper trading trial is 2-4 weeks. Less than that is too short to draw conclusions. More than 8 weeks without deploying live means the strategy is gathering dust.</p>\n\n<p>---</p>\n\n<h4>Step 9: MCP Integration (Optional)</h4>\n\n<p>Flint includes an MCP (Model Context Protocol) server that lets AI tools like Claude Code interact with Flint directly -- running backtests, querying data, and optimizing strategies through natural language.</p>\n\n<h4>Add to Claude Code</h4>\n\n\n<p>That single command registers Flint as an MCP server. After adding it, you can ask Claude Code to do things like:</p>\n\n<ul>\n<li>\"Run a momentum backtest on SOL-PERP for the last 90 days\"</li>\n<li>\"Download BTC-PERP data from Drift\"</li>\n<li>\"Optimize my RSI strategy with 100 trials\"</li>\n<li>\"What is the current funding rate for SOL-PERP across venues?\"</li>\n</ul>\n\n<h4>Available MCP Tools</h4>\n\n<p><code>run_backtest</code>, <code>optimize_strategy</code>, <code>get_candles</code>, <code>download_market_data</code>, <code>list_available_markets</code>, <code>list_local_markets</code>, <code>list_strategies</code>, <code>get_funding_rates</code>, <code>get_open_interest</code>, <code>get_correlation</code>, <code>get_data_freshness</code></p>\n\n<h4>Run Standalone</h4>\n\n\n<p>For full details, see the <a href=\"../../flint/mcp_server.py\">MCP server source</a>.</p>\n\n<p>---</p>\n\n<h4>Next Steps</h4>\n\n<ul>\n<li><a href=\"strategy-authoring.md\">Strategy Authoring Guide</a> -- Write custom strategies, understand the v1/v2 API, configure optimization parameters.</li>\n<li><a href=\"../tutorials/first-strategy.md\">First Strategy Tutorial</a> -- Step-by-step walkthrough of building, optimizing, and deploying an RSI-MACD strategy.</li>\n<li><a href=\"data-providers.md\">Data Provider Guide</a> -- All 15 data providers, multi-venue downloads, API key setup for Birdeye/Helius.</li>\n<li><a href=\"architecture.md\">Architecture Overview</a> -- Execution pipeline, fill models, margin engine, data flow.</li>\n<li><a href=\"slippage-models.md\">Slippage Models Guide</a> -- 4-tier impact model, calibration, transaction cost analysis.</li>\n<li><a href=\"live-deployment.md\">Live Deployment Guide</a> -- Deploy to Drift or Hyperliquid (devnet first, then mainnet).</li>\n</ul>\n\n<h4>CLI Quick Reference</h4>\n\n",
        "codeBlocks": [
          {
            "language": "bash",
            "code": "git clone https://github.com/sohan-shingade/flint.git && cd flint\npip install -e .\nflint init\nflint serve"
          },
          {
            "language": "bash",
            "code": "flint --help"
          },
          {
            "language": "bash",
            "code": "flint serve --dev          # API only at localhost:8000\ncd ui && npm run dev       # UI with hot reload at localhost:5173"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/data/download \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"market\": \"SOL-PERP\",\n    \"resolution\": \"1m\",\n    \"start_ts\": 1727740800,\n    \"end_ts\": 1743465600\n  }'"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/data/available-markets | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/data/freshness | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/backtest/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"strategy\": \"momentum\",\n    \"market\": \"SOL-PERP\",\n    \"start_ts\": 1727740800,\n    \"end_ts\": 1743465600,\n    \"initial_capital\": 10000,\n    \"fee_rate\": 0.0006\n  }' | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/backtest/<backtest_id>/results | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/optimize/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"code\": \"<your strategy code here>\",\n    \"market\": \"SOL-PERP\",\n    \"start_ts\": 1727740800,\n    \"end_ts\": 1743465600,\n    \"initial_capital\": 10000,\n    \"metric\": \"sharpe_ratio\",\n    \"trials\": 50\n  }' | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/optimize/<run_id>/results | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/optimize/walk-forward \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"code\": \"<your strategy code here>\",\n    \"market\": \"SOL-PERP\",\n    \"start_ts\": 1727740800,\n    \"end_ts\": 1743465600,\n    \"initial_capital\": 10000,\n    \"metric\": \"sharpe_ratio\",\n    \"n_windows\": 5,\n    \"train_pct\": 0.7,\n    \"trials_per_window\": 30\n  }' | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/paper/start \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"strategy\": \"momentum\",\n    \"market\": \"SOL-PERP\",\n    \"initial_capital\": 10000,\n    \"venue\": \"drift\"\n  }' | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "claude mcp add flint -- python -m flint.mcp_server"
          },
          {
            "language": "bash",
            "code": "pip install flint[mcp]\npython -m flint.mcp_server"
          },
          {
            "language": "bash",
            "code": "flint init                                          # download sample data + run demo backtest\nflint serve                                         # build UI + start API at localhost:8000\nflint serve --dev                                   # API only (run UI separately with npm run dev)\nflint backtest <strategy.py>                        # run backtest from CLI\nflint optimize <strategy.py>                        # hyperparameter search\nflint data download --market SOL-PERP --days 180    # download market data\nflint data status                                   # show what data you have\nflint new my_strategy                               # scaffold a new strategy file\nflint live --paper                                  # paper trade with live prices"
          }
        ]
      },
      {
        "id": "first-strategy",
        "title": "Tutorial: First Strategy",
        "content": "\n<p>This tutorial walks through building a custom RSI-MACD confluence strategy from scratch, optimizing it with Optuna, validating it with walk-forward analysis, and deploying it to paper trading. The entire process uses SOL-PERP as the example market.</p>\n\n<h4>Prerequisites</h4>\n\n<ul>\n<li>Flint installed and running (<code>flint serve</code> at <a href=\"http://localhost:8000\">localhost:8000</a>)</li>\n<li>At least 90 days of SOL-PERP data downloaded (see the <a href=\"../guides/quickstart.md\">Quickstart Guide</a>)</li>\n</ul>\n\n<p>---</p>\n\n<h4>Step 1: Start with the Template</h4>\n\n<p>Every Flint strategy inherits from the <code>Strategy</code> base class and implements three things: a <code>name</code> property, an <code>on_candle</code> method, and a <code>reset</code> method. Here is the minimal skeleton:</p>\n\n\n<p>The <code>on_candle</code> method is called once for every candle bar during a backtest. It receives the current candle, the full history of previous candles, and an optional execution context (<code>ctx</code>). We will use the v2 context-based API, which gives full control over order placement.</p>\n\n<p>---</p>\n\n<h4>Step 2: Understand the Indicators</h4>\n\n<p>Our strategy combines two indicators: RSI and MACD. Each measures something different, and the combination filters out false signals that either indicator alone would produce.</p>\n\n<h4>RSI (Relative Strength Index)</h4>\n\n<p>RSI measures the speed and magnitude of recent price changes on a scale from 0 to 100.</p>\n\n<ul>\n<li><strong>RSI below 30</strong>: The asset is \"oversold\" -- it has dropped quickly and may be due for a bounce.</li>\n<li><strong>RSI above 70</strong>: The asset is \"overbought\" -- it has risen quickly and may be due for a pullback.</li>\n<li><strong>RSI between 30 and 70</strong>: No strong signal either way.</li>\n</ul>\n\n<p>RSI is a mean-reversion indicator. It works well in ranging markets but generates false signals during strong trends.</p>\n\n<h4>MACD (Moving Average Convergence Divergence)</h4>\n\n<p>MACD tracks the relationship between two exponential moving averages (a fast EMA and a slow EMA). It produces three values:</p>\n\n<ul>\n<li><strong>MACD line</strong>: Fast EMA minus slow EMA.</li>\n<li><strong>Signal line</strong>: A smoothed average of the MACD line.</li>\n<li><strong>Histogram</strong>: MACD line minus signal line. When the histogram crosses from negative to positive, it signals bullish momentum. The reverse signals bearish momentum.</li>\n</ul>\n\n<p>MACD is a trend-following indicator. It catches sustained moves but lags behind sharp reversals.</p>\n\n<h4>Why Combine Them</h4>\n\n<p>RSI catches oversold/overbought extremes. MACD confirms the momentum direction. By requiring both indicators to agree before entering a trade, we filter out most of the false signals from either indicator alone. The tradeoff is fewer trades, but higher conviction on each one.</p>\n\n<p>---</p>\n\n<h4>Step 3: Define Entry and Exit Logic</h4>\n\n<p>The logic:</p>\n\n<ol>\n<li><strong>Buy</strong> when RSI is oversold (below 30) AND the MACD histogram crosses from negative to positive.</li>\n<li><strong>Sell</strong> when RSI is overbought (above 70) AND the MACD histogram crosses from positive to negative.</li>\n<li><strong>Exit</strong> any open position when the opposite confluence signal fires.</li>\n</ol>\n\n<p>Here is the full strategy:</p>\n\n\n<p>Key decisions in this code:</p>\n\n<ul>\n<li><strong>Position sizing</strong>: We use 90% of available cash on each entry, leaving a 10% buffer for fees and slippage.</li>\n<li><strong>Exit logic</strong>: We close on any opposite confluence signal. This is simple but effective -- the strategy does not try to hold through adverse signals.</li>\n<li><strong>v1/v2 dual support</strong>: When <code>ctx</code> is provided (backtest or paper trading), the strategy uses direct order placement. When <code>ctx</code> is None (rare, signal mode only), it falls back to returning Signal.BUY/SELL.</li>\n</ul>\n\n<p>---</p>\n\n<h4>Step 4: Add Optimization Parameters</h4>\n\n<p>To enable Optuna optimization, add a <code>parameters()</code> classmethod that defines the search space for each tunable parameter. Each parameter specifies its type (<code>int</code> or <code>float</code>) and the range to search.</p>\n\n<p>Add this method to the strategy class:</p>\n\n\n<p>Each key in the returned dictionary must match a constructor parameter name exactly. Optuna will sample values within the <code>low</code>/<code>high</code> range, construct the strategy with those values, run a full backtest, and record the result.</p>\n\n<p>The <code>default</code> field is optional but recommended. It tells the optimizer what value to use as a starting point and documents the \"no optimization\" baseline.</p>\n\n<p>---</p>\n\n<h4>Step 5: Backtest It</h4>\n\n<p>Paste the full strategy code (including the <code>parameters()</code> method) into BacktestLab's code editor, or save it as a file and submit via the API.</p>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>Go to <strong>BacktestLab</strong>.</li>\n<li>Switch to the <strong>Custom Code</strong> tab.</li>\n<li>Paste the strategy code.</li>\n<li>Set market to <strong>SOL-PERP</strong>, date range to the last 90 days, capital to <strong>10000</strong>.</li>\n<li>Click <strong>Run Backtest</strong>.</li>\n</ol>\n\n<h4>Via the API</h4>\n\n\n<p>Check the results. With default parameters on SOL-PERP, expect a modest result -- the defaults are not tuned for any specific market regime. A Sharpe between 0.3 and 1.0 with the default parameters is normal and indicates the strategy has something to work with. If the Sharpe is negative with defaults, the confluence logic may not suit the current SOL-PERP regime.</p>\n\n<p>---</p>\n\n<h4>Step 6: Optimize with Optuna</h4>\n\n<p>Now let Optuna find better parameters.</p>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>After the backtest finishes, click <strong>OPTIMIZE</strong>.</li>\n<li>Set trials to <strong>100</strong> (more trials means a more thorough search, but takes longer).</li>\n<li>Keep the metric as <strong>sharpe_ratio</strong>.</li>\n<li>Click <strong>Run</strong>.</li>\n</ol>\n\n<h4>Via the API</h4>\n\n\n<p>Poll for results:</p>\n\n\n<h4>Reading Optimization Output</h4>\n\n<p>The results include:</p>\n\n<ul>\n<li><strong>best_params</strong> -- The parameter values that produced the highest Sharpe ratio.</li>\n<li><strong>best_value</strong> -- The Sharpe ratio achieved with those parameters.</li>\n<li><strong>convergence</strong> -- An array of <code>[trial_number, best_value_so_far]</code> pairs. If the curve flattens early, the optimizer has converged.</li>\n<li><strong>param_importance</strong> -- Ranking of which parameters mattered most. If <code>rsi_period</code> has importance 0.45 and <code>macd_signal</code> has 0.02, the RSI period is far more influential.</li>\n<li><strong>top_trials</strong> -- The 20 best trials with full metrics (Sharpe, PnL, drawdown, win rate, trade count).</li>\n</ul>\n\n<p>A realistic optimized result on SOL-PERP might look like: Sharpe of 1.2-1.8, max drawdown of 12-18%, win rate of 45-55%, and 30-80 trades over 90 days. These numbers vary significantly depending on the market regime during the test period.</p>\n\n<p>> <strong>Warning</strong>: Do not trust optimized results at face value. A Sharpe of 3.0+ from optimization almost always indicates overfitting. The next step is critical.</p>\n\n<p>---</p>\n\n<h4>Step 7: Walk-Forward Validation</h4>\n\n<p>Walk-forward analysis tests whether the optimized parameters generalize to unseen data.</p>\n\n<h4>Via the UI</h4>\n\n<ol>\n<li>After optimization completes, click <strong>WALK-FORWARD</strong>.</li>\n<li>Set windows to <strong>5</strong>, train/test split to <strong>70/30</strong>.</li>\n<li>Click <strong>Run</strong>.</li>\n</ol>\n\n<h4>Via the API</h4>\n\n\n<h4>Interpreting the Results</h4>\n\n<p>Each window shows an in-sample Sharpe (from the training period) and an out-of-sample Sharpe (from the test period). The key numbers:</p>\n\n<ul>\n<li><strong>avg_oos_sharpe</strong> -- Average out-of-sample Sharpe across all windows. This is the most honest estimate of future performance. If it is positive, the strategy has predictive value.</li>\n<li><strong>overfitting_ratio</strong> -- Out-of-sample performance divided by in-sample performance. A value near 1.0 means minimal overfitting. Below 0.3 means the in-sample results do not generalize.</li>\n<li><strong>parameter_stability</strong> -- How much the optimal parameters vary across windows. High stability means the strategy is robust; high variance means it is fragile.</li>\n</ul>\n\n<h4>Decision Framework</h4>\n\n<table class='docs-table'>\n<tr><th>avg_oos_sharpe</th><th>overfitting_ratio</th><th>Decision</th></tr>\n<table class='docs-table'>\n<tr><td>Above 0.5</td><td>Above 0.5</td><td>Deploy to paper trading</td></tr>\n<table class='docs-table'>\n<tr><td>Above 0.5</td><td>Below 0.5</td><td>Simplify the strategy (fewer parameters) and re-validate</td></tr>\n<table class='docs-table'>\n<tr><td>Below 0.5</td><td>Any</td><td>Rethink the strategy logic or try a different market</td></tr>\n<table class='docs-table'>\n<tr><td>Negative</td><td>Any</td><td>Do not deploy. The strategy does not work out of sample</td></tr>\n</table>\n\n<p>In a bear market (2025-2026), even a good strategy may have a modest positive or slightly negative total return. The goal is risk-adjusted performance, not absolute returns. A strategy with a 0.7 Sharpe that limits drawdowns to 15% is more valuable than one that returned 50% but had a 40% drawdown along the way.</p>\n\n<p>---</p>\n\n<h4>Step 8: Deploy to Paper Trading</h4>\n\n<p>If walk-forward validation passes, deploy the strategy to paper trading.</p>\n\n<h4>Via the UI</h4>\n\n<p>Click <strong>Deploy to Paper</strong> on the backtest result page. The strategy starts running against live Drift prices immediately.</p>\n\n<h4>Via the API</h4>\n\n\n<p>Replace the params with the best values from your optimization.</p>\n\n<h4>Monitoring</h4>\n\n<p>Go to the <strong>Paper Trading</strong> page in the UI. Watch for:</p>\n\n<ul>\n<li><strong>Equity curve</strong> -- Is it trending in the expected direction?</li>\n<li><strong>Trade frequency</strong> -- Does it match the backtest? If the backtest made 50 trades in 90 days, paper trading should average roughly 1 trade every 2 days.</li>\n<li><strong>Drawdown</strong> -- If it exceeds the max drawdown from walk-forward testing, stop the session and investigate.</li>\n</ul>\n\n<p>Run paper trading for at least 2 weeks before drawing conclusions. Market regimes shift, and a few days of data is not enough to validate a strategy.</p>\n\n<p>---</p>\n\n<h4>Summary</h4>\n\n<p>The full workflow:</p>\n\n<ol>\n<li>Write the strategy with clear entry/exit logic.</li>\n<li>Add <code>parameters()</code> for optimization.</li>\n<li>Backtest with defaults to confirm the logic works.</li>\n<li>Optimize with Optuna (50-100 trials).</li>\n<li>Validate with walk-forward (5 windows, 70/30 split).</li>\n<li>If avg_oos_sharpe > 0.5 and overfitting_ratio > 0.5, deploy to paper.</li>\n<li>Monitor paper trading for 2-4 weeks.</li>\n</ol>\n\n<p>The most common mistake is skipping step 5. Optimization always produces impressive-looking numbers. Walk-forward is what separates strategies that work from strategies that were overfit to historical noise.</p>\n",
        "codeBlocks": [
          {
            "language": "python",
            "code": "from flint.strategy.base import Strategy\nfrom flint.models import Candle, Signal, Side\n\n\nclass MyStrategy(Strategy):\n    @property\n    def name(self) -> str:\n        return \"my_strategy\"\n\n    def on_candle(self, candle, history, ctx=None):\n        return Signal.HOLD\n\n    def reset(self):\n        pass"
          },
          {
            "language": "python",
            "code": "from flint.strategy.base import Strategy\nfrom flint.models import Candle, Signal, Side\n\nimport numpy as np\n\n\nclass RSIMACDStrategy(Strategy):\n    def __init__(\n        self,\n        rsi_period=14,\n        macd_fast=12,\n        macd_slow=26,\n        macd_signal=9,\n        rsi_oversold=30.0,\n        rsi_overbought=70.0,\n    ):\n        if macd_fast >= macd_slow:\n            raise ValueError(\"macd_fast must be less than macd_slow\")\n        self.rsi_period = rsi_period\n        self.macd_fast = macd_fast\n        self.macd_slow = macd_slow\n        self.macd_signal = macd_signal\n        self.rsi_oversold = rsi_oversold\n        self.rsi_overbought = rsi_overbought\n\n        # Internal state for MACD\n        self._fast_ema = 0.0\n        self._slow_ema = 0.0\n        self._signal_ema = 0.0\n        self._prev_histogram = 0.0\n        self._macd_initialized = False\n        self._macd_history = []\n\n    @property\n    def name(self):\n        return f\"RSI-MACD({self.rsi_period}, {self.macd_fast}/{self.macd_slow})\"\n\n    def reset(self):\n        self._fast_ema = 0.0\n        self._slow_ema = 0.0\n        self._signal_ema = 0.0\n        self._prev_histogram = 0.0\n        self._macd_initialized = False\n        self._macd_history = []\n\n    def _compute_rsi(self, history):\n        \"\"\"Compute RSI from recent close prices.\"\"\"\n        closes = np.array([c.close for c in history[-(self.rsi_period + 1):]])\n        deltas = np.diff(closes)\n        gains = np.where(deltas > 0, deltas, 0)\n        losses = np.where(deltas < 0, -deltas, 0)\n        avg_gain = float(np.mean(gains))\n        avg_loss = float(np.mean(losses))\n        if avg_loss == 0:\n            return 100.0\n        rs = avg_gain / avg_loss\n        return 100 - 100 / (1 + rs)\n\n    def _update_macd(self, price, history):\n        \"\"\"Update MACD state and return the histogram, or None if not ready.\"\"\"\n        if not self._macd_initialized:\n            if len(history) < self.macd_slow:\n                return None\n            closes = [c.close for c in history[-self.macd_slow:]]\n            self._fast_ema = sum(closes[-self.macd_fast:]) / self.macd_fast\n            self._slow_ema = sum(closes) / self.macd_slow\n            self._macd_initialized = True\n            macd_val = self._fast_ema - self._slow_ema\n            self._macd_history.append(macd_val)\n            self._signal_ema = macd_val\n            self._prev_histogram = 0.0\n            return None\n\n        fast_mult = 2 / (self.macd_fast + 1)\n        slow_mult = 2 / (self.macd_slow + 1)\n        self._fast_ema = price * fast_mult + self._fast_ema * (1 - fast_mult)\n        self._slow_ema = price * slow_mult + self._slow_ema * (1 - slow_mult)\n\n        macd_val = self._fast_ema - self._slow_ema\n        self._macd_history.append(macd_val)\n        if len(self._macd_history) > self.macd_signal * 2:\n            self._macd_history = self._macd_history[-self.macd_signal:]\n\n        if len(self._macd_history) < self.macd_signal:\n            return None\n\n        if len(self._macd_history) == self.macd_signal:\n            self._signal_ema = sum(self._macd_history) / self.macd_signal\n            self._prev_histogram = macd_val - self._signal_ema\n            return None\n\n        signal_mult = 2 / (self.macd_signal + 1)\n        self._signal_ema = (\n            macd_val * signal_mult + self._signal_ema * (1 - signal_mult)\n        )\n\n        histogram = macd_val - self._signal_ema\n        prev = self._prev_histogram\n        self._prev_histogram = histogram\n        return histogram if prev != 0 else None\n\n    def on_candle(self, candle, history, ctx=None):\n        min_needed = max(self.rsi_period + 1, self.macd_slow)\n        if len(history) < min_needed:\n            return Signal.HOLD\n\n        # Compute indicators\n        rsi = self._compute_rsi(history)\n        prev_hist = self._prev_histogram\n        histogram = self._update_macd(candle.close, history)\n\n        if histogram is None:\n            return Signal.HOLD\n\n        # Confluence signals\n        rsi_bullish = rsi < self.rsi_oversold\n        rsi_bearish = rsi > self.rsi_overbought\n        macd_bullish = prev_hist <= 0 and histogram > 0\n        macd_bearish = prev_hist >= 0 and histogram < 0\n\n        buy_signal = rsi_bullish and macd_bullish\n        sell_signal = rsi_bearish and macd_bearish\n\n        # v2: use execution context for order placement\n        if ctx is not None:\n            pos = ctx.position(candle.market)\n            if pos is None:\n                if buy_signal:\n                    size = (ctx.account.cash * 0.9) / candle.close\n                    if size > 0:\n                        ctx.market_order(candle.market, Side.LONG, size)\n                elif sell_signal:\n                    size = (ctx.account.cash * 0.9) / candle.close\n                    if size > 0:\n                        ctx.market_order(candle.market, Side.SHORT, size)\n            else:\n                if buy_signal or sell_signal:\n                    ctx.close_position(candle.market)\n            return Signal.HOLD\n\n        # v1 fallback for signal-based mode\n        if buy_signal:\n            return Signal.BUY\n        elif sell_signal:\n            return Signal.SELL\n        return Signal.HOLD"
          },
          {
            "language": "python",
            "code": "    @classmethod\n    def parameters(cls):\n        return {\n            \"rsi_period\": {\"type\": \"int\", \"low\": 5, \"high\": 30, \"default\": 14},\n            \"macd_fast\": {\"type\": \"int\", \"low\": 5, \"high\": 20, \"default\": 12},\n            \"macd_slow\": {\"type\": \"int\", \"low\": 15, \"high\": 50, \"default\": 26},\n            \"macd_signal\": {\"type\": \"int\", \"low\": 5, \"high\": 15, \"default\": 9},\n            \"rsi_oversold\": {\"type\": \"float\", \"low\": 15, \"high\": 40, \"default\": 30},\n            \"rsi_overbought\": {\"type\": \"float\", \"low\": 60, \"high\": 85, \"default\": 70},\n        }"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/backtest/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"code\": \"<paste your strategy code here>\",\n    \"market\": \"SOL-PERP\",\n    \"start_ts\": 1735689600,\n    \"end_ts\": 1743465600,\n    \"initial_capital\": 10000,\n    \"fee_rate\": 0.0006\n  }' | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/optimize/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"code\": \"<paste your strategy code here>\",\n    \"market\": \"SOL-PERP\",\n    \"start_ts\": 1735689600,\n    \"end_ts\": 1743465600,\n    \"initial_capital\": 10000,\n    \"metric\": \"sharpe_ratio\",\n    \"trials\": 100\n  }' | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/optimize/<run_id>/results | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/optimize/walk-forward \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"code\": \"<paste your strategy code here>\",\n    \"market\": \"SOL-PERP\",\n    \"start_ts\": 1735689600,\n    \"end_ts\": 1743465600,\n    \"initial_capital\": 10000,\n    \"metric\": \"sharpe_ratio\",\n    \"n_windows\": 5,\n    \"train_pct\": 0.7,\n    \"trials_per_window\": 30\n  }' | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/paper/start \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"code\": \"<paste your strategy code here>\",\n    \"market\": \"SOL-PERP\",\n    \"initial_capital\": 10000,\n    \"params\": {\n      \"rsi_period\": 10,\n      \"macd_fast\": 8,\n      \"macd_slow\": 21,\n      \"macd_signal\": 7,\n      \"rsi_oversold\": 25,\n      \"rsi_overbought\": 72\n    },\n    \"venue\": \"drift\"\n  }' | python3 -m json.tool"
          }
        ]
      }
    ]
  },
  {
    "id": "guides",
    "title": "GUIDES",
    "topics": [
      {
        "id": "web-ui",
        "title": "Web UI",
        "content": "\n<p>Flint's web interface runs at <code>http://localhost:8000</code> after starting the server with <code>flint serve</code>. It provides visual tools for every stage of the quant workflow: data exploration, backtesting, optimization, paper trading, and live monitoring.</p>\n\n<h4>Starting the UI</h4>\n\n\n<p>In production mode, the built React app is served directly by FastAPI. In dev mode, use Vite's dev server for hot reload during UI development.</p>\n\n<h4>Pages</h4>\n\n<p>The sidebar navigation lists all pages. Here's what each one does and when to use it.</p>\n\n<h4>BacktestLab</h4>\n\n<p>The primary workspace. Write strategies, run backtests, optimize, validate, and deploy -- all in one page.</p>\n\n<p><strong>Left panel -- Code Editor:</strong></p>\n<ul>\n<li>Monaco-based editor with Python syntax highlighting</li>\n<li>Load built-in templates from the dropdown (20 strategies)</li>\n<li>Save/load custom strategies with names</li>\n<li>Ctrl+Enter to run, Ctrl+S to save</li>\n</ul>\n\n<p><strong>Right panel -- Configuration:</strong></p>\n<ul>\n<li>Market selector (SOL-PERP, BTC-PERP, ETH-PERP, etc.)</li>\n<li>Venue preset (Drift, Hyperliquid, Binance, OKX, Bybit) -- sets default fee rates</li>\n<li>Date range with presets (1M, 3M, 6M, 1Y, 3Y) or custom dates</li>\n<li>Capital and fee rate inputs</li>\n<li>Advanced settings: fill model, slippage, latency, margin tracking, capital allocation</li>\n</ul>\n\n<p><strong>Run + Optimize buttons:</strong></p>\n<ul>\n<li>RUN: executes the backtest. Shows progress bar during execution.</li>\n<li>OPTIMIZE: runs Optuna hyperparameter search. Select metric (Sharpe, PnL, Calmar, Win Rate) and trial count (5-500).</li>\n<li>After optimization: WALK-FORWARD button appears for overfitting validation. Set fold count (2-20).</li>\n</ul>\n\n<p><strong>Data staleness warning:</strong></p>\n<p>A yellow banner appears above the run button if the selected market's data is older than 24 hours, prompting you to download fresh data from the Data tab.</p>\n\n<p><strong>Results panel (below editor):</strong></p>\n<ul>\n<li>Metrics summary: PnL, return %, Sharpe, max drawdown, win rate, profit factor, Sortino, Calmar</li>\n<li>Monte Carlo confidence intervals (500 simulations): Sharpe CI, max drawdown CI, ruin probability</li>\n<li>Equity curve chart with buy-and-hold benchmark overlay</li>\n<li>Drawdown chart</li>\n<li>Trade table: entry/exit timestamps, prices, size, PnL per trade</li>\n<li>PnL histogram</li>\n<li>Instrument exposure timeline (for multi-market strategies)</li>\n</ul>\n\n<p><strong>Export buttons:</strong></p>\n<ul>\n<li>JSON: full backtest result as JSON file</li>\n<li>TRADES CSV: trade-by-trade export (timestamp, side, entry/exit price, PnL, holding hours)</li>\n<li>EQUITY CSV: equity curve with drawdown (timestamp, equity, drawdown %)</li>\n</ul>\n\n<p><strong>Regime testing:</strong></p>\n<p>Switch to REGIMES range preset to backtest across multiple market conditions:</p>\n<ul>\n<li>8 regimes from Dec 2023 to Apr 2026 (bull, bear, crash, sideways, high-vol)</li>\n<li>Select which regimes to test, runs separate backtests for each</li>\n<li>Results show per-regime metrics side by side</li>\n</ul>\n\n<p><strong>Optimization results:</strong></p>\n<ul>\n<li>Best parameters with metric value</li>\n<li>Top 10 trials table (metric, PnL, Sharpe, max DD, win rate, params)</li>\n<li>BACKTEST BEST PARAMS button to run the winner</li>\n<li>WALK-FORWARD button with fold selector for overfitting detection</li>\n<li>Walk-forward results: per-fold in-sample vs out-of-sample Sharpe, degradation %, overfit ratio, parameter stability</li>\n</ul>\n\n<p><strong>Deploy:</strong></p>\n<ul>\n<li>DEPLOY button opens deploy panel to start a paper trading session with current strategy and params</li>\n</ul>\n\n<p><strong>Journal:</strong></p>\n<ul>\n<li>JOURNAL button shows all saved backtest runs</li>\n<li>Checkbox selection on rows to compare 2-4 runs</li>\n<li>COMPARE button shows side-by-side metrics diff table with winner highlighting per metric</li>\n</ul>\n\n<h4>Dashboard</h4>\n\n<p>Overview page showing system health and strategy performance.</p>\n\n<p><strong>Strategy Leaderboard:</strong></p>\n<ul>\n<li>Ranked table of all strategies from journal runs</li>\n<li>Sort by: avg Sharpe, avg PnL, best Sharpe, win rate, max drawdown, run count</li>\n<li>Groups runs by strategy name, computes per-strategy averages</li>\n<li>Color-coded: positive PnL in green, negative in red, Sharpe > 1 in green</li>\n</ul>\n\n<p><strong>Data coverage:</strong></p>\n<ul>\n<li>Shows which markets have data and their date ranges</li>\n<li>Quick-download buttons for common markets</li>\n</ul>\n\n<h4>DataExplorer</h4>\n\n<p>Browse and download market data.</p>\n\n<p><strong>Market browser:</strong></p>\n<ul>\n<li>Select market and resolution from dropdowns</li>\n<li>View candle chart with volume bars</li>\n<li>Date range picker for filtering</li>\n</ul>\n\n<p><strong>Download panel:</strong></p>\n<ul>\n<li>Download OHLCV candles from Drift</li>\n<li>Download funding rates from 7 venues simultaneously</li>\n<li>Progress indicator during download</li>\n<li>Data coverage visualization showing gaps</li>\n</ul>\n\n<p><strong>Multi-venue view:</strong></p>\n<ul>\n<li>Select venue to view venue-specific data</li>\n<li>Correlation matrix between markets</li>\n<li>Volume comparison across venues</li>\n</ul>\n\n<h4>Paper Trading</h4>\n\n<p>Manage paper trading sessions.</p>\n\n<p><strong>Sidebar -- Session list:</strong></p>\n<ul>\n<li>All active and stopped sessions with status indicators</li>\n<li>Green dot = live, amber = replaying, red = stopped</li>\n<li>Click to select and view details</li>\n</ul>\n\n<p><strong>Main panel -- Session detail:</strong></p>\n<ul>\n<li>Strategy name, market, deployment time</li>\n<li>Metrics grid: equity, realized PnL, unrealized PnL, total trades, fees, leverage</li>\n<li>Equity curve chart with buy-and-hold overlay</li>\n<li>Trade history table</li>\n<li>Action buttons: STOP (graceful), KILL (immediate)</li>\n<li>PARITY TEST button: compares paper results against a backtest on the same data</li>\n<p>  - Shows PnL divergence, fill price MAE, equity correlation, signal timing match</p>\n<p>  - Pass/fail indicator (< 2% divergence = pass)</p>\n\n<p><strong>Deploy panel:</strong></p>\n<ul>\n<li>Deploy a new strategy from code or built-in template</li>\n<li>Set market, capital, and venue</li>\n</ul>\n\n<h4>LiveMonitor</h4>\n\n<p>Monitor live trading sessions on Drift and Hyperliquid.</p>\n\n<ul>\n<li>Session selector for multi-venue trading</li>\n<li>Real-time equity curve</li>\n<li>Recent fills table</li>\n<li>Position summary</li>\n</ul>\n\n<h4>FundingHeatmap</h4>\n\n<p>Cross-venue funding rate visualization.</p>\n\n<ul>\n<li>Heatmap showing funding rates across markets and venues</li>\n<li>Color scale from negative (green, shorts pay longs) to positive (red, longs pay shorts)</li>\n<li>Useful for identifying funding arbitrage opportunities</li>\n</ul>\n\n<h4>FillAnalysis</h4>\n\n<p>Analyze fill quality and slippage.</p>\n\n<ul>\n<li>Per-venue fill analysis</li>\n<li>Slippage distribution charts</li>\n<li>Compare actual fills vs expected prices</li>\n</ul>\n\n<h4>MevDashboard</h4>\n\n<p>MEV opportunity detection and monitoring.</p>\n\n<ul>\n<li>Arbitrage route scanning</li>\n<li>Cross-venue price discrepancy alerts</li>\n<li>Historical MEV opportunity log</li>\n</ul>\n\n<h4>Setup</h4>\n\n<p>Initial configuration wizard.</p>\n\n<ul>\n<li>API key setup (Birdeye, Helius)</li>\n<li>Venue configuration (Drift keypair, Hyperliquid key, CCXT credentials)</li>\n<li>Data preset selection (Starter: SOL-PERP, Full: all markets)</li>\n<li>Risk configuration defaults</li>\n</ul>\n\n<h4>Docs</h4>\n\n<p>Links to documentation. Redirects to the guides in the docs/ directory.</p>\n\n<h4>Keyboard Shortcuts</h4>\n\n<table class='docs-table'>\n<tr><th>Shortcut</th><th>Action</th><th>Where</th></tr>\n<table class='docs-table'>\n<tr><td>Ctrl+Enter</td><td>Run backtest</td><td>BacktestLab</td></tr>\n<table class='docs-table'>\n<tr><td>Ctrl+S</td><td>Save strategy</td><td>BacktestLab</td></tr>\n<table class='docs-table'>\n<tr><td>Ctrl+Shift+O</td><td>Open strategy</td><td>BacktestLab</td></tr>\n</table>\n\n<h4>URL Routes</h4>\n\n<table class='docs-table'>\n<tr><th>Path</th><th>Page</th></tr>\n<table class='docs-table'>\n<tr><td><code>/</code></td><td>Dashboard</td></tr>\n<table class='docs-table'>\n<tr><td><code>/backtest</code></td><td>BacktestLab</td></tr>\n<table class='docs-table'>\n<tr><td><code>/data</code></td><td>DataExplorer</td></tr>\n<table class='docs-table'>\n<tr><td><code>/paper</code></td><td>Paper Trading</td></tr>\n<table class='docs-table'>\n<tr><td><code>/live</code></td><td>LiveMonitor</td></tr>\n<table class='docs-table'>\n<tr><td><code>/funding</code></td><td>FundingHeatmap</td></tr>\n<table class='docs-table'>\n<tr><td><code>/fills</code></td><td>FillAnalysis</td></tr>\n<table class='docs-table'>\n<tr><td><code>/mev</code></td><td>MevDashboard</td></tr>\n<table class='docs-table'>\n<tr><td><code>/setup</code></td><td>Setup</td></tr>\n<table class='docs-table'>\n<tr><td><code>/docs</code></td><td>Docs</td></tr>\n</table>\n\n<h4>Tech Stack</h4>\n\n<p>The UI is built with React 19, Vite, and Tailwind CSS. Key libraries:</p>\n<ul>\n<li><strong>lightweight-charts v5</strong>: candlestick and equity curve charts</li>\n<li><strong>Monaco Editor</strong>: strategy code editor (same engine as VS Code)</li>\n<li><strong>React Router</strong>: client-side routing</li>\n</ul>\n\n<p>To modify the UI, edit files in <code>ui/src/</code>. Run <code>cd ui && npm run dev</code> for hot reload. Production build: <code>npm run build</code> outputs to <code>ui/dist/</code> which FastAPI serves automatically.</p>\n",
        "codeBlocks": [
          {
            "language": "bash",
            "code": "flint serve                   # production: API + UI at localhost:8000\nflint serve --dev             # dev mode: API only at localhost:8000\ncd ui && npm run dev          # dev UI at localhost:5173 (hot reload, proxies to API)"
          }
        ]
      },
      {
        "id": "strategy-authoring",
        "title": "Strategy Authoring",
        "content": "\n<p>This guide covers writing, testing, and optimizing trading strategies in Flint, from simple signal-based strategies to multi-market cross-venue arbitrage.</p>\n\n<p>---</p>\n\n<h4>Overview</h4>\n\n<p>All strategies inherit from the <code>Strategy</code> abstract base class in <code>flint/strategy/base.py</code>. Flint ships with 20 built-in strategy templates covering momentum, mean reversion, funding rate harvesting, cross-venue arbitrage, basis trading, and MEV monitoring. User strategies live in <code>strategies/user/</code> (gitignored) and are loaded via the API or the BacktestLab code editor.</p>\n\n<p>There are two strategy styles:</p>\n\n<ul>\n<li><strong>v1 (Signal-based)</strong>: <code>on_candle()</code> returns <code>Signal.BUY</code>, <code>Signal.SELL</code>, or <code>Signal.HOLD</code>. The engine handles position sizing and order placement.</li>\n<li><strong>v2 (Context-based)</strong>: <code>on_candle()</code> calls <code>ctx.market_order()</code>, <code>ctx.stop_order()</code>, etc. directly and returns <code>Signal.HOLD</code>. Full control over order logic.</li>\n</ul>\n\n<p>---</p>\n\n<h4>Strategy ABC</h4>\n\n<p>Every strategy must implement three members defined in <code>flint/strategy/base.py</code>:</p>\n\n\n<ul>\n<li><code>name</code> -- unique identifier shown in the UI and results.</li>\n<li><code>on_candle(candle, history, ctx)</code> -- called on every bar. <code>candle</code> is the current bar; <code>history</code> is all bars up to (but not including) the current one. <code>ctx</code> is the <code>ExecutionContext</code> (None in v1-only mode).</li>\n<li><code>reset()</code> -- called before every backtest run to clear accumulated state (counters, EMAs, etc.).</li>\n<li><code>parameters()</code> -- optional classmethod for Optuna optimization (see the Optimization section below).</li>\n</ul>\n\n<p>---</p>\n\n<h4>v1: Signal-Based Strategy</h4>\n\n<p>Return <code>Signal.BUY</code>, <code>Signal.SELL</code>, or <code>Signal.HOLD</code>. The engine opens and closes positions automatically using the configured fee rate and position sizing.</p>\n\n\n<p>Available indicators in <code>flint.indicators</code>: <code>sma</code>, <code>ema</code>, <code>wma</code>, <code>rsi</code>, <code>stochastic</code>, <code>macd</code>, <code>bollinger</code>, <code>bollinger_width</code>, <code>atr</code>, <code>volatility</code>, <code>vwap</code>, <code>volume_ratio</code>, <code>roc</code>, <code>adx</code>, <code>z_score</code>, <code>highest_high</code>, <code>lowest_low</code>. All take <code>(history, period)</code> and return floats.</p>\n\n<p>---</p>\n\n<h4>v2: Context-Based Strategy</h4>\n\n<p>Use the <code>ExecutionContext</code> (<code>ctx</code>) to place orders directly. Always return <code>Signal.HOLD</code> since the engine ignores signal values when you manage orders manually.</p>\n\n\n<p>Multi-venue backtests require <code>capital_allocation</code> in the request:</p>\n\n\n<p>---</p>\n\n<h4>ExecutionContext API</h4>\n\n<p>The <code>ctx</code> parameter is an instance of <code>ExecutionContext</code> (defined in <code>flint/execution/context.py</code>). All implementations -- <code>BacktestContext</code>, <code>PaperBroker</code>, <code>LiveDriftContext</code>, <code>LiveHyperliquidContext</code>, <code>MultiVenueLiveContext</code> -- share the same interface. Strategies deploy to any execution environment without modification.</p>\n\n<h4>State Properties</h4>\n\n<table class='docs-table'>\n<tr><th>Property</th><th>Type</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td><code>ctx.account</code></td><td><code>AccountState</code></td><td>Equity, cash, unrealized PnL (aggregated across venues)</td></tr>\n<table class='docs-table'>\n<tr><td><code>ctx.positions</code></td><td><code>list[PositionInfo]</code></td><td>All open positions (across all venues)</td></tr>\n<table class='docs-table'>\n<tr><td><code>ctx.pending_orders</code></td><td><code>list[Order]</code></td><td>Unfilled orders</td></tr>\n<table class='docs-table'>\n<tr><td><code>ctx.current_candle</code></td><td><code>Candle</code> or <code>None</code></td><td>The bar being processed</td></tr>\n<table class='docs-table'>\n<tr><td><code>ctx.timestamp</code></td><td><code>int</code></td><td>Current unix timestamp (seconds)</td></tr>\n</table>\n\n<h4>Order Methods</h4>\n\n<p>All order methods accept an optional <code>venue</code> parameter. In single-venue backtests, venue defaults to <code>\"default\"</code>. In multi-venue backtests and live trading, specify the venue explicitly.</p>\n\n\n<h4>Convenience Methods</h4>\n\n\n<h4>Market Data Methods</h4>\n\n\n<p>---</p>\n\n<h4>Multi-Market Strategies</h4>\n\n<p>Use <code>ctx.get_candles()</code> to read data from markets other than the one being iterated over. The UI auto-detects <code>ctx.get_candles(\"MARKET\")</code> calls in the code editor and prompts you to download data for each referenced market.</p>\n\n\n<p>The engine accepts <code>Dict[str, List[Candle]]</code> for multi-market mode. No additional configuration needed beyond downloading the data.</p>\n\n<p>---</p>\n\n<h4>All 20 Built-In Strategy Templates</h4>\n\n<table class='docs-table'>\n<tr><th>File</th><th>Style</th><th>Category</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td><code>momentum.py</code></td><td>v1</td><td>trend</td><td>Rate-of-change momentum</td></tr>\n<table class='docs-table'>\n<tr><td><code>ema_crossover.py</code></td><td>v1</td><td>trend</td><td>Fast/slow EMA crossover</td></tr>\n<table class='docs-table'>\n<tr><td><code>ma_crossover.py</code></td><td>v1</td><td>trend</td><td>Simple MA crossover</td></tr>\n<table class='docs-table'>\n<tr><td><code>rsi.py</code></td><td>v1</td><td>mean-rev</td><td>RSI overbought/oversold</td></tr>\n<table class='docs-table'>\n<tr><td><code>bollinger.py</code></td><td>v1</td><td>mean-rev</td><td>Bollinger Band mean reversion</td></tr>\n<table class='docs-table'>\n<tr><td><code>mean_reversion.py</code></td><td>v1</td><td>mean-rev</td><td>Z-score mean reversion</td></tr>\n<table class='docs-table'>\n<tr><td><code>macd_divergence.py</code></td><td>v1</td><td>trend</td><td>MACD histogram divergence</td></tr>\n<table class='docs-table'>\n<tr><td><code>rsi_macd_combo.py</code></td><td>v2</td><td>multi-signal</td><td>RSI + MACD combined signals</td></tr>\n<table class='docs-table'>\n<tr><td><code>atr_breakout.py</code></td><td>v2</td><td>trend</td><td>ATR-based breakout with stops</td></tr>\n<table class='docs-table'>\n<tr><td><code>breakout_momentum.py</code></td><td>v2</td><td>trend</td><td>Breakout momentum with confirmation</td></tr>\n<table class='docs-table'>\n<tr><td><code>momentum_breakout.py</code></td><td>v2</td><td>trend</td><td>Volume-confirmed momentum breakout</td></tr>\n<table class='docs-table'>\n<tr><td><code>dual_timeframe.py</code></td><td>v2</td><td>trend</td><td>Dual timeframe trend alignment</td></tr>\n<table class='docs-table'>\n<tr><td><code>vwap_reversion.py</code></td><td>v2</td><td>mean-rev</td><td>VWAP reversion with stops</td></tr>\n<table class='docs-table'>\n<tr><td><code>grid_trader.py</code></td><td>v2</td><td>defi</td><td>Grid trading with limit orders</td></tr>\n<table class='docs-table'>\n<tr><td><code>funding_harvest.py</code></td><td>v2</td><td>defi</td><td>Single-venue funding collection</td></tr>\n<table class='docs-table'>\n<tr><td><code>funding_arb.py</code></td><td>v2</td><td>defi</td><td>Cross-venue funding rate arbitrage</td></tr>\n<table class='docs-table'>\n<tr><td><code>multi_venue_funding.py</code></td><td>v2</td><td>defi</td><td>Multi-venue delta-neutral harvest</td></tr>\n<table class='docs-table'>\n<tr><td><code>funding_mean_reversion.py</code></td><td>v2</td><td>defi</td><td>Bollinger bands on funding rates</td></tr>\n<table class='docs-table'>\n<tr><td><code>basis_trade.py</code></td><td>v2</td><td>defi</td><td>Cross-venue basis trade</td></tr>\n<table class='docs-table'>\n<tr><td><code>mev_arb_monitor.py</code></td><td>v2</td><td>monitor</td><td>MEV arbitrage opportunity scanner (no trades)</td></tr>\n</table>\n\n<p>Strategy source files are in <code>flint/strategy/</code>. Per-strategy documentation is in <code>docs/strategies/</code>.</p>\n\n<p>---</p>\n\n<h4>Optimization with Optuna</h4>\n\n<p>Override the <code>parameters()</code> classmethod to declare which constructor arguments the optimizer should search:</p>\n\n\n<p>Supported types:</p>\n\n<table class='docs-table'>\n<tr><th>Type</th><th>Fields</th><th>Example</th></tr>\n<table class='docs-table'>\n<tr><td><code>int</code></td><td><code>low</code>, <code>high</code>, <code>default</code></td><td><code>{\"type\": \"int\", \"low\": 5, \"high\": 100}</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>float</code></td><td><code>low</code>, <code>high</code>, <code>default</code></td><td><code>{\"type\": \"float\", \"low\": 0.01, \"high\": 0.5}</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>categorical</code></td><td><code>choices</code>, <code>default</code></td><td><code>{\"type\": \"categorical\", \"choices\": [\"ema\", \"sma\"]}</code></td></tr>\n</table>\n\n<p>The optimizer calls <code>parameters()</code>, samples values via Optuna, instantiates the strategy with those values, runs a full backtest, and records the chosen metric as the objective. All 20 built-in strategies support optimization.</p>\n\n<h4>Running Optimization</h4>\n\n<p>Via the API:</p>\n\n\n<p>Via the UI: after any backtest run, click \"Optimize\" to launch a parameter search. Results show a ranked table of all trials with one-click \"backtest with best params.\"</p>\n\n<p>Supports Bayesian search (default, TPE sampler), grid search, and random search. Metrics: <code>sharpe_ratio</code>, <code>total_pnl</code>, <code>win_rate</code>, <code>max_drawdown</code>, <code>sortino</code>.</p>\n\n<h4>Walk-Forward Validation</h4>\n\n<p>For more robust results, Flint supports walk-forward optimization. This splits the data into sequential train/test windows, optimizes on each training window, then validates on the following out-of-sample test window. This guards against overfitting to a single in-sample period.</p>\n\n<p>---</p>\n\n<h4>Strategy Loader and Security</h4>\n\n<p>User strategies are loaded dynamically by <code>flint/strategy/loader.py</code>. The loader uses AST validation to block unsafe code before execution.</p>\n\n<h4>Allowed Imports</h4>\n\n<p>Any import outside this list is rejected at load time:</p>\n\n\n<h4>Blocked Builtins</h4>\n\n<p>These builtins are blocked in user strategy code:</p>\n\n\n<p>Dangerous attribute accesses (<code>system</code>, <code>popen</code>, <code>spawn</code>, <code>call</code>, <code>check_output</code>, <code>getenv</code>, <code>environ</code>, <code>listdir</code>, <code>remove</code>, <code>rmdir</code>, <code>unlink</code>) are also blocked.</p>\n\n<p>The backtest engine enforces a 300-second timeout and allows a maximum of 5 concurrent runs.</p>\n\n<h4>Saving User Strategies</h4>\n\n<p>Via the API:</p>\n\n\n<p>Or paste directly into the Monaco code editor in BacktestLab.</p>\n\n<p>---</p>\n\n<h4>Common Pitfalls</h4>\n\n<h4>Not Enough History</h4>\n\n<p>If your strategy uses a 50-period SMA but there are only 20 candles in <code>history</code>, the indicator will either fail or return a meaningless value. Always guard with a length check:</p>\n\n\n<h4>Look-Ahead Bias</h4>\n\n<p>Never peek at future data. The <code>history</code> list contains only candles strictly before the current one. The <code>candle</code> parameter is the current bar. Using <code>candle.close</code> to make a decision is fine (the engine fills at the close price). Reading from a future index in history is not.</p>\n\n<h4>Overfitting</h4>\n\n<p>High Sharpe ratios on in-sample data do not guarantee out-of-sample performance. Use walk-forward validation and keep the parameter count low. A strategy with 2--3 parameters is usually more robust than one with 10.</p>\n\n<h4>State Leaking Between Runs</h4>\n\n<p>If your strategy accumulates state (counters, rolling windows, position flags), make sure <code>reset()</code> clears all of it. The optimizer runs many trials, and leftover state from a previous trial will corrupt the next one.</p>\n",
        "codeBlocks": [
          {
            "language": "python",
            "code": "from flint.strategy.base import Strategy\nfrom flint.models import Candle, Signal\n\nclass MyStrategy(Strategy):\n    @property\n    def name(self) -> str:\n        return \"my_strategy\"\n\n    def on_candle(self, candle: Candle, history: list[Candle], ctx=None) -> Signal:\n        # Your logic here\n        return Signal.HOLD\n\n    def reset(self) -> None:\n        # Clear any internal state (called before each backtest run)\n        pass"
          },
          {
            "language": "python",
            "code": "from flint.strategy.base import Strategy\nfrom flint.models import Candle, Signal\nfrom flint import indicators\n\nclass RSIStrategy(Strategy):\n    def __init__(self, period: int = 14, oversold: float = 30.0, overbought: float = 70.0):\n        self.period = period\n        self.oversold = oversold\n        self.overbought = overbought\n\n    @property\n    def name(self) -> str:\n        return f\"RSI({self.period})\"\n\n    def reset(self) -> None:\n        pass\n\n    def on_candle(self, candle: Candle, history: list[Candle], ctx=None) -> Signal:\n        if len(history) < self.period:\n            return Signal.HOLD\n\n        closes = [c.close for c in history] + [candle.close]\n        rsi_val = indicators.rsi(closes, self.period)\n\n        if rsi_val < self.oversold:\n            return Signal.BUY\n        elif rsi_val > self.overbought:\n            return Signal.SELL\n        return Signal.HOLD\n\n    @classmethod\n    def parameters(cls) -> dict:\n        return {\n            \"period\":     {\"type\": \"int\",   \"low\": 5,   \"high\": 100, \"default\": 14},\n            \"oversold\":   {\"type\": \"float\", \"low\": 10.0, \"high\": 40.0, \"default\": 30.0},\n            \"overbought\": {\"type\": \"float\", \"low\": 60.0, \"high\": 90.0, \"default\": 70.0},\n        }"
          },
          {
            "language": "python",
            "code": "from flint.strategy.base import Strategy\nfrom flint.models import Candle, Signal, Side\nfrom flint import indicators\n\nclass MultiMarketFundingArb(Strategy):\n    \"\"\"Cross-venue funding rate arbitrage.\n\n    Compares funding between Drift and Hyperliquid. When the spread is\n    wide enough, goes long on the venue paying less funding and short\n    on the venue paying more.\n    \"\"\"\n\n    def __init__(self, entry_threshold: float = 0.0005, size: float = 0.1):\n        self.entry_threshold = entry_threshold\n        self.size = size\n\n    @property\n    def name(self) -> str:\n        return \"funding_arb\"\n\n    def reset(self) -> None:\n        pass\n\n    def on_candle(self, candle: Candle, history: list[Candle], ctx=None) -> Signal:\n        if ctx is None:\n            return Signal.HOLD\n\n        # Compare funding rates across venues\n        funding = ctx.get_funding_by_venue(\"SOL-PERP\", lookback=1)\n        drift_rates = funding.get(\"drift\", [])\n        hyper_rates = funding.get(\"hyperliquid\", [])\n\n        if not drift_rates or not hyper_rates:\n            return Signal.HOLD\n\n        drift_rate = drift_rates[-1][1]\n        hyper_rate = hyper_rates[-1][1]\n        spread = hyper_rate - drift_rate\n\n        if spread > self.entry_threshold:\n            # Hyperliquid paying more: long Drift, short Hyperliquid\n            ctx.market_order(\"SOL-PERP\", Side.LONG, self.size, venue=\"drift\")\n            ctx.market_order(\"SOL-PERP\", Side.SHORT, self.size, venue=\"hyperliquid\")\n        elif spread < -self.entry_threshold:\n            # Drift paying more: long Hyperliquid, short Drift\n            ctx.market_order(\"SOL-PERP\", Side.LONG, self.size, venue=\"hyperliquid\")\n            ctx.market_order(\"SOL-PERP\", Side.SHORT, self.size, venue=\"drift\")\n\n        return Signal.HOLD\n\n    @classmethod\n    def parameters(cls) -> dict:\n        return {\n            \"entry_threshold\": {\"type\": \"float\", \"low\": 0.0001, \"high\": 0.005, \"default\": 0.0005},\n            \"size\":            {\"type\": \"float\", \"low\": 0.01, \"high\": 1.0, \"default\": 0.1},\n        }"
          },
          {
            "language": "json",
            "code": "{\n  \"strategy\": \"funding_arb\",\n  \"market\": \"SOL-PERP\",\n  \"capital_allocation\": {\"drift\": 5000, \"hyperliquid\": 5000},\n  \"margin_tracking\": true\n}"
          },
          {
            "language": "python",
            "code": "# Market order -- fills at close price (backtest) or market price (live)\nctx.market_order(market, side, size, reduce_only=False, tag=\"\", venue=\"default\") -> str\n\n# Limit order -- resting order at a specific price\nctx.limit_order(market, side, size, price, reduce_only=False, tag=\"\", venue=\"default\") -> str\n\n# Stop-loss order -- triggers a market order at trigger_price\nctx.stop_order(market, side, size, trigger_price, tag=\"\", venue=\"default\") -> str\n\n# Take-profit order -- triggers a market order when price reaches target\nctx.take_profit_order(market, side, size, trigger_price, tag=\"\", venue=\"default\") -> str\n\n# Cancel a specific order by ID\nctx.cancel(order_id) -> bool\n\n# Cancel all orders (optionally for one market)\nctx.cancel_all(market=None) -> int"
          },
          {
            "language": "python",
            "code": "# Close entire position for a market on a venue\nctx.close_position(market, venue=\"default\") -> str | None\n\n# Get a single position\nctx.position(market, venue=\"default\") -> PositionInfo | None\n\n# All positions on a specific venue\nctx.venue_positions(venue) -> list[PositionInfo]\n\n# Venue cash balance\nctx.venue_balance(venue) -> float\nctx.venue_balances() -> dict[str, float]\n\n# Transfer capital between venues (arrives after configurable delay)\nctx.transfer(from_venue, to_venue, amount) -> bool\n\n# Pre-trade cost estimation\nctx.estimate_cost(market, size_usd) -> CostEstimate"
          },
          {
            "language": "python",
            "code": "# Multi-market candle history\nctx.get_candles(market, lookback=50) -> list[Candle]\n\n# Funding rates (hourly)\nctx.get_funding_rate(market=None) -> float | None\nctx.get_funding_rates(market=None, lookback=24) -> list[tuple[int, float]]\nctx.get_funding_by_venue(market=None, lookback=24) -> dict[str, list[tuple[int, float]]]\n\n# Orderbook\nctx.get_orderbook(market=None) -> OrderbookSnapshot | None\nctx.get_impact_price(market, side, size) -> float | None\n\n# Open interest\nctx.get_open_interest(market=None) -> tuple[float, float] | None\nctx.get_open_interest_history(market=None, lookback=24) -> list[tuple]"
          },
          {
            "language": "python",
            "code": "def on_candle(self, candle: Candle, history: list[Candle], ctx=None) -> Signal:\n    if ctx is None:\n        return Signal.HOLD\n\n    # Read BTC candles as a leading indicator\n    btc = ctx.get_candles(\"BTC-PERP\", lookback=24)\n    if len(btc) < 2:\n        return Signal.HOLD\n\n    btc_momentum = (btc[-1].close - btc[-2].close) / btc[-2].close\n\n    if btc_momentum > 0.005:\n        ctx.market_order(\"SOL-PERP\", Side.LONG, 0.1)\n    elif btc_momentum < -0.005:\n        ctx.close_position(\"SOL-PERP\")\n\n    return Signal.HOLD"
          },
          {
            "language": "python",
            "code": "@classmethod\ndef parameters(cls) -> dict:\n    return {\n        \"period\":    {\"type\": \"int\",   \"low\": 5,   \"high\": 100, \"default\": 14},\n        \"oversold\":  {\"type\": \"float\", \"low\": 10.0, \"high\": 40.0, \"default\": 30.0},\n        \"overbought\":{\"type\": \"float\", \"low\": 60.0, \"high\": 90.0, \"default\": 70.0},\n    }"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/optimize/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"strategy\": \"rsi\", \"market\": \"SOL-PERP\", \"trials\": 100,\n       \"start_ts\": 1709251200, \"end_ts\": 1743465600}'"
          },
          {
            "language": "text",
            "code": "flint, numpy, math, statistics, collections, dataclasses,\ntyping, enum, abc, functools, itertools, operator, __future__"
          },
          {
            "language": "text",
            "code": "eval, exec, compile, __import__, breakpoint,\nglobals, locals, vars, open"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/user-strategies \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\": \"my_rsi\", \"code\": \"...\"}'"
          },
          {
            "language": "python",
            "code": "if len(history) < self.period:\n    return Signal.HOLD"
          }
        ]
      },
      {
        "id": "data-providers",
        "title": "Data Providers",
        "content": "\n<p>Flint ships with 15 data providers covering Solana on-chain data, CEX candles, oracle prices, DEX pools, and cross-venue funding rates from 7 venues. All providers are managed by <code>flint/providers/registry.py</code>.</p>\n\n<p>---</p>\n\n<h4>Provider Overview</h4>\n\n<table class='docs-table'>\n<tr><th>Provider</th><th>File</th><th>Auth</th><th>Data</th></tr>\n<table class='docs-table'>\n<tr><td>Drift Data API</td><td><code>providers/drift_candles.py</code></td><td>None</td><td>OHLCV candles (48 perp markets)</td></tr>\n<table class='docs-table'>\n<tr><td>Drift S3</td><td><code>providers/drift_s3.py</code></td><td>None</td><td>Historical trade records, funding</td></tr>\n<table class='docs-table'>\n<tr><td>Drift OI</td><td><code>providers/open_interest.py</code></td><td>None</td><td>Open interest (long/short)</td></tr>\n<table class='docs-table'>\n<tr><td>Drift Funding</td><td><code>providers/drift_api.py</code></td><td>None</td><td>Funding rates, L2/L3 orderbook</td></tr>\n<table class='docs-table'>\n<tr><td>Hyperliquid Candles</td><td><code>providers/hyperliquid_candles.py</code></td><td>None</td><td>OHLCV candles for Hyperliquid perps</td></tr>\n<table class='docs-table'>\n<tr><td>Birdeye</td><td><code>providers/birdeye.py</code></td><td><code>FLINT_BIRDEYE_API_KEY</code></td><td>Any Solana SPL token OHLCV</td></tr>\n<table class='docs-table'>\n<tr><td>Helius</td><td><code>providers/helius.py</code></td><td><code>FLINT_HELIUS_API_KEY</code></td><td>Liquidations, whale transfers</td></tr>\n<table class='docs-table'>\n<tr><td>Pyth</td><td><code>providers/pyth.py</code></td><td>None</td><td>Oracle prices (20 pairs)</td></tr>\n<table class='docs-table'>\n<tr><td>Raydium</td><td><code>providers/raydium.py</code></td><td>None</td><td>AMM/CLMM pool data</td></tr>\n<table class='docs-table'>\n<tr><td>Orca</td><td><code>providers/orca.py</code></td><td>None</td><td>Whirlpool pool data</td></tr>\n<table class='docs-table'>\n<tr><td>GeckoTerminal</td><td><code>providers/gecko.py</code></td><td>None</td><td>DEX pool OHLCV</td></tr>\n<table class='docs-table'>\n<tr><td>Jupiter</td><td><code>providers/jupiter.py</code></td><td>None</td><td>Swap quotes</td></tr>\n<table class='docs-table'>\n<tr><td>CoinGecko</td><td><code>providers/coingecko.py</code></td><td>None</td><td>Spot candles (BTC, ETH, etc.)</td></tr>\n<table class='docs-table'>\n<tr><td>CCXT</td><td><code>providers/ccxt_provider.py</code></td><td>None (bundled)</td><td>100+ CEX exchanges, volume data</td></tr>\n<table class='docs-table'>\n<tr><td>Cross-venue funding</td><td><code>providers/funding_rates.py</code></td><td>None</td><td>7 venues normalized to 1h</td></tr>\n</table>\n\n<p>All Drift, Pyth, Raydium, Orca, GeckoTerminal, Jupiter, CoinGecko, and CCXT providers are free and require no API keys. Only Birdeye and Helius need keys (both free tier, no credit card).</p>\n\n<p>---</p>\n\n<h4>Funding Venues</h4>\n\n<p>The cross-venue funding provider (<code>providers/funding_rates.py</code>) collects funding rates from 7 perpetual exchanges and normalizes them to a common hourly schema. All are free with no API keys.</p>\n\n<table class='docs-table'>\n<tr><th>Venue</th><th>Source</th><th>Symbol Format</th></tr>\n<table class='docs-table'>\n<tr><td>Drift</td><td><code>DriftFundingProvider</code> (native)</td><td>SOL-PERP</td></tr>\n<table class='docs-table'>\n<tr><td>Hyperliquid</td><td>Public API (<code>api.hyperliquid.xyz</code>)</td><td>SOL</td></tr>\n<table class='docs-table'>\n<tr><td>OKX</td><td>Public API</td><td>SOL-USDT-SWAP</td></tr>\n<table class='docs-table'>\n<tr><td>Bybit</td><td>Public API</td><td>SOLUSDT</td></tr>\n<table class='docs-table'>\n<tr><td>dYdX</td><td>Public API</td><td>SOL-USD</td></tr>\n<table class='docs-table'>\n<tr><td>Gate.io</td><td>Public API</td><td>SOL_USDT</td></tr>\n<table class='docs-table'>\n<tr><td>Bitget</td><td>Public API</td><td>SOLUSDT</td></tr>\n</table>\n\n<p>All exchanges report funding at 8-hour intervals. Flint forward-fills these to hourly resolution so strategies can react to funding changes within each payment window rather than only at settlement times.</p>\n\n<p>Funding data is stored in the <code>venue_funding_rates</code> DuckDB table. Access from strategy code:</p>\n\n\n<p>---</p>\n\n<h4>API Keys</h4>\n\n<h4>Birdeye (`FLINT_BIRDEYE_API_KEY`)</h4>\n\n<p>Birdeye provides OHLCV candles for any SPL token on Solana, not just the 48 markets Drift tracks. Get a free key at <a href=\"https://birdeye.so/developers\">birdeye.so</a> (no credit card required).</p>\n\n<h4>Helius (`FLINT_HELIUS_API_KEY`)</h4>\n\n<p>Helius provides on-chain liquidation events and whale transfer tracking. Get a free key at <a href=\"https://helius.dev\">helius.dev</a> (no credit card required).</p>\n\n<h4>CCXT (optional)</h4>\n\n<p>CCXT connects to 100+ CEX exchanges. Most support public candle data without keys. Authenticated endpoints require per-exchange credentials.</p>\n\n<h4>Setting Keys</h4>\n\n<p>Add keys to a <code>.env</code> file in the project root (never commit this file):</p>\n\n\n<p>Or export as environment variables with the <code>FLINT_</code> prefix:</p>\n\n\n<p>Config is loaded by <code>flint/config.py</code> via Pydantic settings. It merges <code>flint.yaml</code>, <code>.env</code>, and environment variables. YAML keys flatten: <code>db.path</code> becomes <code>db_path</code>.</p>\n\n<p>Check which providers are enabled and reachable:</p>\n\n\n<p>---</p>\n\n<h4>Downloading Data</h4>\n\n<h4>Quick Start</h4>\n\n\n<p>Downloads a sample set of SOL-PERP candles from Drift and runs a sanity-check backtest. This is the fastest way to verify your installation.</p>\n\n<h4>Via the UI</h4>\n\n<p>The Data Explorer page has a download manager with:</p>\n\n<ul>\n<li><strong>Venue selector</strong> -- choose Drift, Hyperliquid, or both</li>\n<li><strong>Market picker</strong> -- select individual markets or use presets</li>\n<li><strong>Presets</strong> -- \"Starter Pack\" (top 5 markets), \"Everything\" (all 48 Drift markets + Hyperliquid equivalents + funding from all venues)</li>\n<li><strong>Date range selector</strong> -- pick start/end dates</li>\n</ul>\n\n<p>Download warnings are surfaced in the UI if any venue encounters errors (rate limits, 503/403 responses, connectivity issues).</p>\n\n<h4>Via the CLI</h4>\n\n\n<h4>Via the API</h4>\n\n\n<p><strong>Parameters:</strong></p>\n\n<table class='docs-table'>\n<tr><th>Field</th><th>Type</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td><code>market</code></td><td>string</td><td>Market symbol, e.g. <code>\"SOL-PERP\"</code>, <code>\"BTC-PERP\"</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>resolution</code></td><td>string</td><td>Candle resolution: <code>\"1m\"</code>, <code>\"5m\"</code>, <code>\"1h\"</code>, <code>\"1d\"</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>start_ts</code></td><td>int</td><td>Start time as unix timestamp (seconds)</td></tr>\n<table class='docs-table'>\n<tr><td><code>end_ts</code></td><td>int</td><td>End time as unix timestamp (seconds)</td></tr>\n<table class='docs-table'>\n<tr><td><code>venues</code></td><td>list[string]</td><td>Optional. Venues to download from (default: all enabled)</td></tr>\n</table>\n\n<p>For multi-market strategies, download all referenced markets before running a backtest. The UI's bulk download with presets handles this automatically.</p>\n\n<p>---</p>\n\n<h4>Checking Data Coverage</h4>\n\n<p>Before running a backtest, verify that candle data exists for your chosen date range:</p>\n\n\n<p>Returns a coverage summary including the number of candles available, gaps detected, and the actual date range stored. The BacktestLab UI also checks coverage before running and prompts you to download missing data from the Data tab.</p>\n\n<p>---</p>\n\n<h4>Data Freshness</h4>\n\n<p>Check how stale the data is for each market and table:</p>\n\n\n<p>The response shows, per market and data type, the timestamp of the most recent record and how many hours ago it was fetched.</p>\n\n<p>---</p>\n\n<h4>Available and Local Markets</h4>\n\n<p>List all markets that can be downloaded (Drift's 48 perp markets, Drift spot, Hyperliquid, CoinGecko):</p>\n\n\n<p>List markets already downloaded and stored locally in DuckDB:</p>\n\n\n<p>---</p>\n\n<h4>Multi-Venue Candle Storage</h4>\n\n<p>Candle data is stored per-venue in DuckDB. The <code>candles</code> table uses a composite primary key:</p>\n\n\n<p>This means you can store SOL-PERP candles from both Drift and Hyperliquid simultaneously, each reflecting the price action on that specific venue. In the Data Explorer, you can overlay candle data from multiple venues on the same chart (overlay mode) or view them in separate panels (split mode) to spot price dislocations.</p>\n\n<p>---</p>\n\n<h4>Cross-Venue Funding Analysis</h4>\n\n<p>The Data Explorer lets you overlay funding rates from all venues on one chart. When one venue's funding diverges from the rest, there is a potential arbitrage opportunity.</p>\n\n<p>Query funding rates by venue:</p>\n\n\n<p>Get the cross-market correlation matrix:</p>\n\n\n<p>---</p>\n\n<h4>Jupiter Perps Limitations</h4>\n\n<p>Jupiter Perps has no historical borrow rate or volume API. Historical backfill is not currently available:</p>\n\n<ul>\n<li><strong>Borrow rates</strong>: Jupiter's <code>perps-api.jup.ag</code> provides current rates only. Forward collection via <code>JupiterBorrowCollector</code> works but only accumulates going forward.</li>\n<li><strong>Volume</strong>: Approximated from Helius Enhanced Transaction USDC transfers (collateral proxy, not notional). Limited to recent data on the free tier.</li>\n<li><strong>No historical OHLCV</strong>: Jupiter Perps has no candle endpoint. Use Pyth oracle prices instead.</li>\n</ul>\n\n<p>Strategies that depend on Jupiter Perps borrow rate history or volume should not be backtested beyond the data that has been forward-collected.</p>\n\n<p>---</p>\n\n<h4>Orca and Raydium (Spot DEXes)</h4>\n\n<p>Orca and Raydium are spot DEXes. They have no funding rates (that is a perpetuals concept). Available data:</p>\n\n<ul>\n<li><strong>Current pool data</strong>: TVL, reserves, fee rates -- via native APIs</li>\n<li><strong>Historical OHLCV + volume</strong>: Via GeckoTerminal (free, no key) for any Solana pool</li>\n<li><strong>Tick-level liquidity</strong>: Orca Whirlpools via <code>OrcaTickFetcher</code> (on-chain RPC)</li>\n</ul>\n\n<p>---</p>\n\n<h4>Adding a Custom Provider</h4>\n\n<ol>\n<li><strong>Create the provider file</strong>: <code>flint/providers/my_provider.py</code></li>\n</ol>\n\n\n<ol>\n<li><strong>Register the provider</strong>: Add it to <code>flint/providers/__init__.py</code>.</li>\n</ol>\n\n<ol>\n<li><strong>Add config</strong> (optional): Add an enable/disable entry in <code>flint.yaml</code> under the <code>providers</code> key:</li>\n</ol>\n\n\n<p>The registry (<code>registry.py</code>) reads this config and checks provider availability at startup. Disabled or unavailable providers are skipped silently.</p>\n\n<p>---</p>\n\n<h4>DuckDB Tables</h4>\n\n<p>All fetched data is stored in the local DuckDB file (path configured in <code>flint.yaml</code>). The 12 tables are:</p>\n\n<table class='docs-table'>\n<tr><th>Table</th><th>Data</th></tr>\n<table class='docs-table'>\n<tr><td><code>candles</code></td><td>OHLCV bars with venue column (PK: venue, market, resolution_s, ts)</td></tr>\n<table class='docs-table'>\n<tr><td><code>venue_funding_rates</code></td><td>Hourly funding rates by venue</td></tr>\n<table class='docs-table'>\n<tr><td><code>oracle_prices</code></td><td>Pyth oracle price snapshots</td></tr>\n<table class='docs-table'>\n<tr><td><code>orderbook_snapshots</code></td><td>L2 orderbook depth</td></tr>\n<table class='docs-table'>\n<tr><td><code>pool_snapshots</code></td><td>Raydium/Orca AMM pool state</td></tr>\n<table class='docs-table'>\n<tr><td><code>open_interest</code></td><td>Long/short OI by market</td></tr>\n<table class='docs-table'>\n<tr><td><code>liquidations</code></td><td>On-chain liquidation events</td></tr>\n<table class='docs-table'>\n<tr><td><code>whale_transfers</code></td><td>Large wallet movements</td></tr>\n<table class='docs-table'>\n<tr><td><code>dex_volume</code></td><td>DEX trading volume</td></tr>\n<table class='docs-table'>\n<tr><td><code>token_unlocks</code></td><td>Token vesting unlock events</td></tr>\n<table class='docs-table'>\n<tr><td><code>tick_snapshots</code></td><td>CLMM tick data for concentrated liquidity pools</td></tr>\n<table class='docs-table'>\n<tr><td><code>sync_metadata</code></td><td>Last-fetched timestamps per source</td></tr>\n</table>\n\n<p>Access data via the <code>FlintStore</code> singleton (<code>app.state.store</code>). Never create a new DuckDB connection directly. All store methods use <code>threading.Lock</code> for thread safety.</p>\n",
        "codeBlocks": [
          {
            "language": "python",
            "code": "# All venue funding for a market (last 24 hours)\nctx.get_funding_by_venue(\"SOL-PERP\", lookback=24)\n# Returns: {\"drift\": [(ts, rate), ...], \"hyperliquid\": [(ts, rate), ...], ...}\n\n# Aggregate funding rate for the primary venue\nctx.get_funding_rate(\"SOL-PERP\")"
          },
          {
            "language": "bash",
            "code": "FLINT_BIRDEYE_API_KEY=your_key_here\nFLINT_HELIUS_API_KEY=your_key_here"
          },
          {
            "language": "bash",
            "code": "export FLINT_BIRDEYE_API_KEY=your_key_here"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/data/providers | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "flint init"
          },
          {
            "language": "bash",
            "code": "flint data download --market SOL-PERP --days 180"
          },
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/data/download \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"market\": \"SOL-PERP\",\n    \"resolution\": \"1m\",\n    \"start_ts\": 1709251200,\n    \"end_ts\": 1743465600,\n    \"venues\": [\"drift\", \"hyperliquid\"]\n  }'"
          },
          {
            "language": "bash",
            "code": "curl -s \"http://localhost:8000/api/v1/data/check?market=SOL-PERP&start_ts=1709251200&end_ts=1743465600\" \\\n  | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/data/freshness | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/data/available-markets | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/data/markets | python3 -m json.tool"
          },
          {
            "language": "sql",
            "code": "PRIMARY KEY (venue, market, resolution_s, ts)"
          },
          {
            "language": "bash",
            "code": "curl -s \"http://localhost:8000/api/v1/data/funding?market=SOL-PERP&venue=drift&lookback=168\" \\\n  | python3 -m json.tool"
          },
          {
            "language": "bash",
            "code": "curl -s http://localhost:8000/api/v1/data/correlation | python3 -m json.tool"
          },
          {
            "language": "python",
            "code": "from .base import CandleProvider\n\nclass MyProvider(CandleProvider):\n    def fetch_candles(\n        self,\n        market: str,\n        resolution_s: int,\n        start_ts: int,\n        end_ts: int,\n    ) -> list:\n        # Fetch and return a list of Candle objects\n        ..."
          },
          {
            "language": "yaml",
            "code": "providers:\n  my_provider:\n    enabled: true"
          }
        ]
      },
      {
        "id": "live-deployment",
        "title": "Live Deployment",
        "content": "\n<p>This guide covers the full path from paper trading to mainnet execution on Drift, Hyperliquid, and centralized exchanges via CCXT. It includes venue setup, risk configuration, parity testing, and monitoring.</p>\n\n<p>---</p>\n\n<h4>1. Paper Trading</h4>\n\n<p>Paper trading runs your strategy on real-time price data with simulated execution. No keys, no real orders, no risk. Use it to validate behavior before committing capital.</p>\n\n<h4>Start via the UI</h4>\n\n<p>In BacktestLab, click <strong>Deploy to Paper</strong> on any backtest result. The paper session starts immediately with:</p>\n\n<ul>\n<li><strong>Venue selection</strong> -- choose Drift, Hyperliquid, CEX, or multiple venues</li>\n<li><strong>Replay-forward execution</strong> -- replays up to 30 days of history, then transitions to live candle processing via WebSocket feeds</li>\n<li><strong>Realistic fills</strong> -- 5bps slippage, venue-specific fee schedule, optional order latency</li>\n<li><strong>Funding rate payments</strong> -- applied hourly from real multi-venue data</li>\n<li><strong>Live PnL updates</strong> -- mid-price polling every 5 seconds</li>\n<li><strong>Equity curve with buy-and-hold baseline</strong> -- strategy performance vs holding the asset</li>\n<li><strong>Trade markers</strong> -- entry/exit dots on the equity chart</li>\n<li><strong>Session persistence</strong> -- survives server restarts and resumes automatically</li>\n<li><strong>Per-venue capital allocation</strong> -- allocate capital per venue with simulated transfer delays</li>\n</ul>\n\n<h4>Start via the API</h4>\n\n\n<h4>Start via the CLI</h4>\n\n\n<h4>Session status</h4>\n\n<p>A paper session is either <strong>running</strong> (actively processing candles and generating orders) or <strong>stopped</strong> (closed out, no longer ticking). Monitor running sessions in the Paper Trading page of the UI or via the API:</p>\n\n\n<p>The Paper Trading page shows live equity curves, open positions, recent trades, and cumulative PnL for each running session.</p>\n\n<p>---</p>\n\n<h4>2. Venue Setup</h4>\n\n<p>Flint supports live trading on three venue types: <strong>Drift</strong> (Solana-native), <strong>Hyperliquid</strong> (Arbitrum L2), and <strong>CEX</strong> (Binance, OKX, Bybit, and any CCXT-compatible exchange). You can trade on one or more simultaneously.</p>\n\n<h4>Drift</h4>\n\n<p>Drift uses a Solana keypair for signing transactions on-chain via the driftpy SDK.</p>\n\n<p><strong>Environment variables:</strong></p>\n\n\n<p><code>FLINT_PRIVATE_KEY</code> must be the base58 representation of a 64-byte Solana keypair (the format output by <code>solana-keygen new</code>). Never use a hardware wallet address -- Flint needs the raw private key to sign transactions.</p>\n\n<p><strong>flint.yaml:</strong></p>\n\n\n<p>Use <code>devnet</code> while testing. When <code>devnet</code> is set, Flint connects to Solana devnet via driftpy. Switch to <code>mainnet</code> only after completing the mainnet checklist below.</p>\n\n<p><strong>Getting test SOL on devnet:</strong></p>\n\n\n<h4>Hyperliquid</h4>\n\n<p>Hyperliquid uses an Ethereum-style private key (hex, without the <code>0x</code> prefix) for EIP-712 order signing.</p>\n\n<p><strong>Environment variable:</strong></p>\n\n\n<p><strong>Use an API wallet.</strong> Hyperliquid supports trade-only API wallets that cannot initiate withdrawals. This is the safest option for automated trading. Create one at <a href=\"https://app.hyperliquid.xyz\">app.hyperliquid.xyz</a> under Settings, then API Wallets. Fund it by transferring from your main account via the Hyperliquid web UI.</p>\n\n<p>Withdrawals must always be initiated through the Hyperliquid web UI -- Flint does not implement withdrawal logic. This is by design for security.</p>\n\n<p>When <code>live_network</code> is <code>devnet</code>, Flint connects to Hyperliquid's testnet. When <code>mainnet</code>, it connects to production.</p>\n\n<h4>CEX (Binance, OKX, Bybit, and others)</h4>\n\n<p>Flint supports live trading on centralized exchanges via the CCXT library. The <code>LiveCCXTContext</code> provides the same interface as <code>LiveDriftContext</code> and <code>LiveHyperliquidContext</code> -- strategies deploy to any CCXT-compatible exchange with zero code changes.</p>\n\n<p><strong>Environment variables:</strong></p>\n\n\n<p>Market symbols are mapped automatically. Flint's <code>SOL-PERP</code> maps to <code>SOL/USDT:USDT</code> on CCXT exchanges, <code>BTC-PERP</code> maps to <code>BTC/USDT:USDT</code>, and so on for 18+ supported pairs. Any <code>-PERP</code> market not in the explicit mapping falls back to <code>{BASE}/USDT:USDT</code>.</p>\n\n<p><strong>Exchange-specific notes:</strong></p>\n\n<table class='docs-table'>\n<tr><th>Exchange</th><th>Password required</th><th>Sandbox</th><th>Notes</th></tr>\n<table class='docs-table'>\n<tr><td>Binance</td><td>No</td><td>Yes (testnet.binancefuture.com)</td><td>Largest CEX, deepest liquidity</td></tr>\n<table class='docs-table'>\n<tr><td>OKX</td><td>Yes (<code>FLINT_CCXT_PASSWORD</code>)</td><td>Yes (aws.okx.com sandbox)</td><td>Passphrase set during API key creation</td></tr>\n<table class='docs-table'>\n<tr><td>Bybit</td><td>No</td><td>Yes (testnet.bybit.com)</td><td>Good perp liquidity</td></tr>\n</table>\n\n<p>Always start with <code>FLINT_CCXT_SANDBOX=true</code> and switch to <code>false</code> only after completing the mainnet checklist.</p>\n\n<p>---</p>\n\n<h4>3. Risk Configuration</h4>\n\n<p>Risk settings apply in <code>flint.yaml</code> and are enforced across backtests, paper trading, and live trading.</p>\n\n<h4>Kill switch</h4>\n\n<p>The <code>EquityMonitor</code> tracks equity continuously in live sessions. When equity drops below the configured threshold from its peak, it cancels all open orders and closes all positions immediately across all venues.</p>\n\n\n<p>The live session will not restart automatically after a kill switch event. You must restart <code>flint live</code> manually after investigating.</p>\n\n<h4>Risk guard chain</h4>\n\n<p><code>RiskManager</code> runs a chain of six guards before each order is submitted. An order must pass all guards to be executed.</p>\n\n<table class='docs-table'>\n<tr><th>Guard</th><th>What it does</th><th>Config key</th></tr>\n<table class='docs-table'>\n<tr><td><code>MaxPositionSize</code></td><td>Rejects orders when cumulative USD notional on a market exceeds a cap</td><td><code>max_position_notional</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>MaxOpenPositions</code></td><td>Rejects new entries when too many positions are open (does not block closes)</td><td><code>max_open_positions</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>MaxDrawdownCircuitBreaker</code></td><td>Rejects all orders once cumulative drawdown from peak exceeds threshold; must be manually reset</td><td><code>max_drawdown_pct</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>DailyLossLimit</code></td><td>Rejects orders after daily realized loss exceeds a USD threshold; resets at day boundary</td><td><code>daily_loss_limit_usd</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>MaxOrdersPerMinute</code></td><td>Sliding-window rate limiter preventing runaway strategy loops</td><td><code>live_max_orders_per_minute</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>PerMarketPositionLimit</code></td><td>Hard per-market USD notional cap, configured as a JSON map</td><td><code>live_per_market_position_limits</code></td></tr>\n</table>\n\n<p><strong>Example configuration:</strong></p>\n\n\n<h4>Dry-run mode</h4>\n\n<p>Dry-run mode runs the full live pipeline -- WebSocket feeds, strategy ticks, order construction, risk checks -- but does not submit transactions. Every intended order is logged with <code>tx_sig=\"DRY_RUN\"</code>.</p>\n\n\n<p>Or via the CLI:</p>\n\n\n<p>Dry-run fills are simulated at the mid-price with the configured fee rate. Both venues support dry-run. When running multi-venue, dry-run applies to all venues simultaneously.</p>\n\n<p>---</p>\n\n<h4>4. Parity Testing</h4>\n\n<p>Before going live, verify that your strategy produces consistent results across the backtest engine and the paper/live engine on the same historical window.</p>\n\n<h4>What it measures</h4>\n\n<p>The parity test runs both engines on the same candle data and compares:</p>\n\n<table class='docs-table'>\n<tr><th>Metric</th><th>Description</th><th>Pass threshold</th></tr>\n<table class='docs-table'>\n<tr><td>PnL divergence</td><td>Percentage difference in total PnL between engines</td><td>< 2%</td></tr>\n<table class='docs-table'>\n<tr><td>Fill price MAE</td><td>Mean absolute error of fill prices vs backtest fills</td><td>Low is better</td></tr>\n<table class='docs-table'>\n<tr><td>Equity correlation</td><td>Pearson correlation between the two equity curves</td><td>Close to 1.0</td></tr>\n<table class='docs-table'>\n<tr><td>Signal timing match</td><td>Percentage of signals that occur on the same bar</td><td>High is better</td></tr>\n</table>\n\n<p>A failing parity test (> 2% PnL divergence) usually indicates a fill model mismatch, a fee rate discrepancy, or a funding rate included in one engine but not the other.</p>\n\n<h4>Run via the UI</h4>\n\n<p>In the Paper Trading page, click the <strong>PARITY TEST</strong> button. Select the strategy, market, and date range. Results display inline.</p>\n\n<h4>Run via the API</h4>\n\n\n<h4>Run via the CLI</h4>\n\n\n<p>---</p>\n\n<h4>5. Recommended Progression</h4>\n\n<p>Follow this sequence to move from idea to live trading safely:</p>\n\n<ol>\n<li><strong>Backtest</strong> -- Validate the strategy on historical data in BacktestLab. Test across multiple regimes (bull, bear, crash, sideways, high_vol).</li>\n</ol>\n\n<ol>\n<li><strong>Optimize</strong> -- Run Optuna optimization (1-500 trials) to find robust parameters. Use walk-forward validation to check for overfitting.</li>\n</ol>\n\n<ol>\n<li><strong>Walk-forward validation</strong> -- Confirm out-of-sample performance holds across regime boundaries.</li>\n</ol>\n\n<ol>\n<li><strong>Paper trade (1+ week)</strong> -- Deploy to paper trading on the target venue. Monitor equity curve, trade frequency, and drawdown behavior on real market data.</li>\n</ol>\n\n<ol>\n<li><strong>Parity test</strong> -- Run a parity test comparing backtest and paper results on overlapping data. Ensure < 2% PnL divergence.</li>\n</ol>\n\n<ol>\n<li><strong>Devnet / testnet</strong> -- Deploy with real transaction submission on devnet (Drift) or testnet (Hyperliquid, CEX sandboxes). Verify fills, reconnections, and funding-rate ticks over at least a few hours.</li>\n</ol>\n\n<ol>\n<li><strong>Dry-run on mainnet</strong> -- Set <code>live_network: mainnet</code> with <code>live_dry_run: true</code>. Confirms RPC connectivity and order construction without submitting real orders.</li>\n</ol>\n\n<ol>\n<li><strong>Mainnet with small capital</strong> -- Switch <code>live_dry_run: false</code> with conservative position limits and a tight kill switch. Scale up gradually.</li>\n</ol>\n\n<h4>Mainnet checklist</h4>\n\n<p>Before switching to <code>live_network: mainnet</code> with <code>live_dry_run: false</code>:</p>\n\n<ul>\n<li>[ ] Wallet funded with enough SOL for gas + collateral (minimum 0.1 SOL gas buffer for Drift)</li>\n<li>[ ] Hyperliquid API wallet funded via web UI transfer (if applicable)</li>\n<li>[ ] CEX API keys created with trade-only permissions, no withdrawal access (if applicable)</li>\n<li>[ ] <code>FLINT_RPC_URL</code> set to a reliable mainnet endpoint (Helius, QuickNode, or your own)</li>\n<li>[ ] <code>live_kill_switch_drawdown_pct</code> set to an acceptable loss threshold</li>\n<li>[ ] <code>live_per_market_position_limits</code> sized to your actual capital</li>\n<li>[ ] <code>live_max_orders_per_minute</code> reviewed for the strategy's expected signal frequency</li>\n<li>[ ] Parity test passed (< 2% PnL divergence)</li>\n<li>[ ] Devnet/testnet session completed with no unexpected errors</li>\n<li>[ ] Dry-run on mainnet verified (RPC connectivity + order construction)</li>\n</ul>\n\n<p>---</p>\n\n<h4>6. Multi-Venue Live Execution</h4>\n\n<p>To run a strategy across multiple venues simultaneously, Flint uses <code>MultiVenueLiveContext</code>. This wraps <code>LiveDriftContext</code>, <code>LiveHyperliquidContext</code>, and/or <code>LiveCCXTContext</code>, maintaining separate margin, position state, and connections per venue.</p>\n\n<h4>Setup</h4>\n\n<p>Provide keys for all target venues in your <code>.env</code>:</p>\n\n\n<h4>Tick modes</h4>\n\n\n<table class='docs-table'>\n<tr><th>Mode</th><th>Behavior</th><th>Use case</th></tr>\n<table class='docs-table'>\n<tr><td><code>primary</code></td><td>Ticks only on primary venue candle close</td><td>Latency-sensitive strategies, single authoritative clock</td></tr>\n<table class='docs-table'>\n<tr><td><code>any</code></td><td>Ticks on any venue candle close</td><td>Cross-venue monitoring, arb detection</td></tr>\n</table>\n\n<h4>Routing orders</h4>\n\n<p>In your strategy, route orders to specific venues explicitly:</p>\n\n\n<h4>Position tracking</h4>\n\n<p>Positions are keyed by <code>(venue, market)</code>. Each venue maintains independent margin and position state.</p>\n\n\n<p><code>ctx.account</code> reflects total equity across all venues.</p>\n\n<h4>Paired leg submission</h4>\n\n<p>For cross-venue strategies (e.g., funding arb), <code>MultiVenueLiveContext</code> supports paired leg submission:</p>\n\n<ul>\n<li>Both legs are submitted simultaneously</li>\n<li>A configurable timeout waits for both fills</li>\n<li>If one leg fills and the other fails, auto-unwind logic closes the filled leg to prevent unhedged exposure</li>\n</ul>\n\n<p>---</p>\n\n<h4>7. Monitoring</h4>\n\n<h4>Equity and trade monitoring</h4>\n\n<p>The LiveMonitor page in the UI shows:</p>\n\n<ul>\n<li>Real-time equity curves per venue and aggregate</li>\n<li>Open positions with unrealized PnL</li>\n<li>Recent trade log with fill prices and fees</li>\n<li>Risk guard rejection history</li>\n</ul>\n\n<h4>Status endpoint</h4>\n\n\n<p>Returns server status, active live sessions, WebSocket connection health, and store connectivity.</p>\n\n<h4>Alerts</h4>\n\n<p>Flint pushes notifications to Telegram and/or Discord when configured:</p>\n\n\n<p><strong>Events that fire alerts:</strong></p>\n\n<table class='docs-table'>\n<tr><th>Event</th><th>Severity</th></tr>\n<table class='docs-table'>\n<tr><td>Fill received (open/close)</td><td>Info</td></tr>\n<table class='docs-table'>\n<tr><td>Order rejected by risk guard</td><td>Warning</td></tr>\n<table class='docs-table'>\n<tr><td>Drawdown warning threshold breached</td><td>Warning</td></tr>\n<table class='docs-table'>\n<tr><td>Kill switch triggered</td><td>Critical</td></tr>\n<table class='docs-table'>\n<tr><td>WebSocket disconnected / reconnected</td><td>Info</td></tr>\n<table class='docs-table'>\n<tr><td>RPC error / transaction retry</td><td>Warning</td></tr>\n<table class='docs-table'>\n<tr><td>Uncaught strategy exception</td><td>Critical</td></tr>\n<table class='docs-table'>\n<tr><td>Paired leg failure / auto-unwind</td><td>Critical</td></tr>\n</table>\n\n<h4>WebSocket feeds</h4>\n\n<p>All live data arrives through venue-specific WebSocket feeds with automatic reconnection:</p>\n\n\n<p>Reconnection uses exponential backoff (1s, 2s, 4s, up to 60s max). On reconnect, missed candles are backfilled from the REST provider. A health check forces reconnect if no message is received for 30 seconds.</p>\n\n<p>Paper trading uses the same WebSocket feeds as live trading -- it ticks on real candle closes, not stored historical candles.</p>\n",
        "codeBlocks": [
          {
            "language": "bash",
            "code": "curl -s -X POST http://localhost:8000/api/v1/paper/start \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"strategy\": \"momentum\",\n    \"market\": \"SOL-PERP\",\n    \"initial_capital\": 10000,\n    \"venue\": \"drift\"\n  }'"
          },
          {
            "language": "bash",
            "code": "flint live --paper --strategy momentum --market SOL-PERP"
          },
          {
            "language": "bash",
            "code": "# List all sessions\ncurl http://localhost:8000/api/v1/paper/sessions\n\n# Get detailed status of a session (equity, positions, trades)\ncurl http://localhost:8000/api/v1/paper/{session_id}/status"
          },
          {
            "language": "bash",
            "code": "FLINT_PRIVATE_KEY=<base58-encoded-solana-keypair>\nFLINT_RPC_URL=https://api.mainnet-beta.solana.com"
          },
          {
            "language": "yaml",
            "code": "live_network: devnet   # devnet | mainnet"
          },
          {
            "language": "bash",
            "code": "solana airdrop 2 <your-wallet-address> --url devnet"
          },
          {
            "language": "bash",
            "code": "FLINT_HYPERLIQUID_PRIVATE_KEY=<64-char-hex-private-key>"
          },
          {
            "language": "bash",
            "code": "FLINT_CCXT_EXCHANGE=binance       # Exchange name: \"binance\", \"okx\", \"bybit\", etc.\nFLINT_CCXT_API_KEY=<your-api-key>\nFLINT_CCXT_SECRET=<your-api-secret>\nFLINT_CCXT_PASSWORD=<passphrase>  # Required for OKX; optional for others\nFLINT_CCXT_SANDBOX=true           # \"true\" for testnet (default), \"false\" for production"
          },
          {
            "language": "yaml",
            "code": "# Kill switch -- flatten everything if equity drops this much from peak\nlive_kill_switch_drawdown_pct: 0.15\n\n# Early warning before kill switch fires\nlive_drawdown_warning_pct: 0.075"
          },
          {
            "language": "yaml",
            "code": "max_open_positions: 5\nmax_drawdown_pct: 0.20\ndaily_loss_limit_usd: 500\nlive_max_orders_per_minute: 30\nlive_per_market_position_limits: '{\"SOL-PERP\": 10000, \"BTC-PERP\": 50000}'"
          },
          {
            "language": "yaml",
            "code": "live_dry_run: true"
          },
          {
            "language": "bash",
            "code": "flint live --strategy momentum --market SOL-PERP --dry-run"
          },
          {
            "language": "bash",
            "code": "curl -X POST http://localhost:8000/api/v1/backtest/parity \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"strategy\": \"momentum\",\n    \"market\": \"SOL-PERP\",\n    \"start_ts\": 1735689600,\n    \"end_ts\": 1740873600,\n    \"capital\": 10000,\n    \"fee_rate\": 0.0005\n  }'"
          },
          {
            "language": "bash",
            "code": "flint parity --strategy momentum --market SOL-PERP --start 2026-01-01 --end 2026-03-01"
          },
          {
            "language": "bash",
            "code": "# Drift\nFLINT_PRIVATE_KEY=<solana-keypair>\nFLINT_RPC_URL=https://api.mainnet-beta.solana.com\n\n# Hyperliquid\nFLINT_HYPERLIQUID_PRIVATE_KEY=<hex-private-key>\n\n# CEX (e.g., Binance)\nFLINT_CCXT_EXCHANGE=binance\nFLINT_CCXT_API_KEY=<key>\nFLINT_CCXT_SECRET=<secret>\nFLINT_CCXT_SANDBOX=false"
          },
          {
            "language": "yaml",
            "code": "live_multi_venue_primary: drift   # which venue's candle closes drive strategy ticks\nlive_tick_mode: primary           # primary | any"
          },
          {
            "language": "python",
            "code": "ctx.market_order(\"SOL-PERP\", \"long\", size, venue=\"drift\")\nctx.market_order(\"SOL-PERP\", \"short\", size, venue=\"hyperliquid\")\nctx.market_order(\"BTC-PERP\", \"long\", size, venue=\"binance\")"
          },
          {
            "language": "python",
            "code": "ctx.venue_balance(\"drift\")          # Cash on Drift\nctx.venue_balance(\"hyperliquid\")    # Cash on Hyperliquid\nctx.venue_balance(\"binance\")        # Cash on Binance\nctx.venue_positions(\"drift\")        # Positions on Drift only"
          },
          {
            "language": "bash",
            "code": "curl http://localhost:8000/api/v1/health"
          },
          {
            "language": "yaml",
            "code": "# Telegram\ntelegram_bot_token: \"123456:ABC-your-bot-token\"\ntelegram_chat_id: \"-100your-chat-id\"\n\n# Discord\ndiscord_webhook_url: \"https://discord.com/api/webhooks/...\""
          },
          {
            "language": "text",
            "code": "WebSocketFeed (ABC)              -- reconnection logic, health checks, REST fallback\n  +-- DriftWebSocketFeed         -- trade streaming + funding rate subscription\n  +-- HyperliquidWebSocketFeed   -- candle, L2 book, orderUpdates channels\n  +-- PythWebSocketFeed          -- sub-second oracle prices (Hermes)"
          }
        ]
      },
      {
        "id": "mcp-integration",
        "title": "MCP Integration",
        "content": "\n<p>Flint exposes an MCP (Model Context Protocol) server that lets AI models interact with the platform directly -- running backtests, querying market data, managing paper trading sessions, and optimizing strategies through natural language.</p>\n\n<p>---</p>\n\n<h4>Setup</h4>\n\n<h4>Install the MCP dependency</h4>\n\n\n<h4>Add to Claude Code</h4>\n\n\n<h4>Run standalone (stdio transport)</h4>\n\n\n<p>The MCP server accesses DuckDB directly for data tools (candles, funding rates, open interest, correlation, freshness). For paper trading and journal tools, it connects to the Flint API server, which must be running separately:</p>\n\n\n<p>---</p>\n\n<h4>Tools</h4>\n\n<p>The MCP server exposes 17 tools organized by category.</p>\n\n<h4>Backtesting</h4>\n\n<table class='docs-table'>\n<tr><th>Tool</th><th>Description</th><th>Key Args</th></tr>\n<table class='docs-table'>\n<tr><td><code>run_backtest</code></td><td>Run a backtest on Solana market data. Returns PnL, Sharpe, win rate, trades.</td><td><code>market</code>, <code>strategy</code>, <code>start_date</code>, <code>end_date</code>, <code>initial_capital</code>, <code>fee_rate</code>, <code>code</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>list_strategies</code></td><td>List all 20 built-in strategies with parameters and categories.</td><td>--</td></tr>\n</table>\n\n<h4>Paper Trading</h4>\n\n<table class='docs-table'>\n<tr><th>Tool</th><th>Description</th><th>Key Args</th></tr>\n<table class='docs-table'>\n<tr><td><code>start_paper_trading</code></td><td>Start a paper trading session with simulated fills on live data.</td><td><code>market</code>, <code>strategy</code>, <code>initial_capital</code>, <code>code</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>stop_paper_trading</code></td><td>Stop a session gracefully. Closes positions and saves state.</td><td><code>session_id</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>get_paper_sessions</code></td><td>List all sessions with status, equity, PnL, trade count.</td><td>--</td></tr>\n<table class='docs-table'>\n<tr><td><code>get_paper_status</code></td><td>Detailed status of one session including positions and equity history.</td><td><code>session_id</code></td></tr>\n</table>\n\n<h4>Data</h4>\n\n<table class='docs-table'>\n<tr><th>Tool</th><th>Description</th><th>Key Args</th></tr>\n<table class='docs-table'>\n<tr><td><code>get_candles</code></td><td>Get OHLCV candle data for a market.</td><td><code>market</code>, <code>resolution_s</code>, <code>start_date</code>, <code>end_date</code>, <code>limit</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>download_market_data</code></td><td>Download candles from Drift + funding from 7 venues.</td><td><code>market</code>, <code>days</code>, <code>resolution_s</code>, <code>funding_venues</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>list_available_markets</code></td><td>List all downloadable markets (Drift perps, spot, CoinGecko).</td><td>--</td></tr>\n<table class='docs-table'>\n<tr><td><code>list_local_markets</code></td><td>Show what data is cached in the local DuckDB.</td><td>--</td></tr>\n<table class='docs-table'>\n<tr><td><code>get_funding_rates</code></td><td>Get funding rate history grouped by venue.</td><td><code>market</code>, <code>venue</code>, <code>limit</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>get_open_interest</code></td><td>Get open interest for a Drift perp market.</td><td><code>market</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>get_correlation</code></td><td>Compute cross-market correlation matrix from returns.</td><td><code>markets</code>, <code>resolution_s</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>get_data_freshness</code></td><td>Check how fresh data is across providers and markets.</td><td>--</td></tr>\n</table>\n\n<h4>Optimization</h4>\n\n<table class='docs-table'>\n<tr><th>Tool</th><th>Description</th><th>Key Args</th></tr>\n<table class='docs-table'>\n<tr><td><code>optimize_strategy</code></td><td>Run Optuna hyperparameter search for best strategy params.</td><td><code>market</code>, <code>strategy</code>, <code>start_date</code>, <code>end_date</code>, <code>metric</code>, <code>trials</code></td></tr>\n</table>\n\n<h4>Journal</h4>\n\n<table class='docs-table'>\n<tr><th>Tool</th><th>Description</th><th>Key Args</th></tr>\n<table class='docs-table'>\n<tr><td><code>list_journal_runs</code></td><td>List past backtest runs with strategy, PnL, Sharpe, win rate.</td><td><code>limit</code></td></tr>\n<table class='docs-table'>\n<tr><td><code>compare_runs</code></td><td>Compare multiple backtest runs side-by-side.</td><td><code>run_ids</code> (comma-separated)</td></tr>\n</table>\n\n<p>---</p>\n\n<h4>Resources</h4>\n\n<p>The MCP server exposes two resources that AI models can read for context:</p>\n\n<table class='docs-table'>\n<tr><th>URI</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td><code>flint://guide</code></td><td>Platform overview, typical workflow, strategy list, funding venues, paper trading usage</td></tr>\n<table class='docs-table'>\n<tr><td><code>flint://markets</code></td><td>Current list of available markets with types and counts</td></tr>\n</table>\n\n<p>---</p>\n\n<h4>Example Workflow</h4>\n\n<p>A typical conversation with an AI model using Flint's MCP tools follows this progression:</p>\n\n<p><strong>1. Download data</strong></p>\n\n<p>> \"Download 90 days of SOL-PERP data\"</p>\n\n<p>The model calls <code>download_market_data(market=\"SOL-PERP\", days=90)</code>. This fetches candles from Drift and funding rates from all 7 venues (Drift, Hyperliquid, OKX, Bybit, dYdX, Gate.io, Bitget).</p>\n\n<p><strong>2. Run a backtest</strong></p>\n\n<p>> \"Run a backtest with the RSI strategy\"</p>\n\n<p>The model calls <code>run_backtest(market=\"SOL-PERP\", strategy=\"rsi\")</code> and returns PnL, Sharpe ratio, max drawdown, win rate, and trade count.</p>\n\n<p><strong>3. Optimize parameters</strong></p>\n\n<p>> \"Optimize it with 50 trials\"</p>\n\n<p>The model calls <code>optimize_strategy(market=\"SOL-PERP\", strategy=\"rsi\", trials=50)</code> and returns the best parameters found (period, oversold/overbought thresholds) along with the top trial scores.</p>\n\n<p><strong>4. Deploy to paper trading</strong></p>\n\n<p>> \"Paper trade with the best params\"</p>\n\n<p>The model calls <code>start_paper_trading(market=\"SOL-PERP\", strategy=\"rsi\")</code> (or passes custom code with the optimized parameters) and returns a session ID.</p>\n\n<p><strong>5. Monitor performance</strong></p>\n\n<p>> \"Check how it is doing\"</p>\n\n<p>The model calls <code>get_paper_sessions()</code> to see current equity, PnL, and trade count across all active sessions.</p>\n\n<p><strong>6. Review history</strong></p>\n\n<p>> \"Show me my last 10 backtests\"</p>\n\n<p>The model calls <code>list_journal_runs(limit=10)</code> to display past runs, then optionally <code>compare_runs(run_ids=\"abc,def\")</code> for side-by-side comparison.</p>\n\n<p>---</p>\n\n<h4>Notes</h4>\n\n<ul>\n<li>The MCP server runs on stdio transport by default. It is designed for local use with Claude Code or other MCP-compatible clients.</li>\n<li>Data tools (<code>get_candles</code>, <code>list_local_markets</code>, <code>get_funding_rates</code>, <code>get_open_interest</code>, <code>get_correlation</code>, <code>get_data_freshness</code>, <code>download_market_data</code>, <code>list_available_markets</code>) access DuckDB directly and do not require the API server.</li>\n<li>Paper trading tools (<code>start_paper_trading</code>, <code>stop_paper_trading</code>, <code>get_paper_sessions</code>, <code>get_paper_status</code>) and journal tools (<code>list_journal_runs</code>, <code>compare_runs</code>) make HTTP requests to <code>http://127.0.0.1:8000</code> and require <code>flint serve</code> to be running.</li>\n<li>The <code>run_backtest</code> and <code>optimize_strategy</code> tools access DuckDB directly and run the backtest engine in-process. They do not require the API server.</li>\n<li>All market data is free. No API keys are needed for core functionality (candles, funding, backtesting, optimization, paper trading).</li>\n</ul>\n",
        "codeBlocks": [
          {
            "language": "bash",
            "code": "pip install flint[mcp]"
          },
          {
            "language": "bash",
            "code": "claude mcp add flint -- python -m flint.mcp_server"
          },
          {
            "language": "bash",
            "code": "python -m flint.mcp_server"
          },
          {
            "language": "bash",
            "code": "flint serve   # start API server at localhost:8000"
          }
        ]
      },
      {
        "id": "slippage-models",
        "title": "Slippage Models",
        "content": "\n<p>Flint uses a four-tier impact model to simulate execution costs in backtests, plus per-venue transaction cost models, a calibration engine, and concentrated liquidity (CLMM) modeling for MEV research. The engine automatically selects the highest-fidelity model available for each order based on what data is present in FlintStore.</p>\n\n<p>---</p>\n\n<h4>1. Four-Tier Impact Model</h4>\n\n<p>The <code>ImpactStage</code> in the fill pipeline selects a model in priority order:</p>\n\n<table class='docs-table'>\n<tr><th>Tier</th><th>Model</th><th>Condition</th></tr>\n<table class='docs-table'>\n<tr><td>0</td><td>vAMM curve (<code>VammCurve</code>)</td><td><code>vamm_enabled: true</code> and market has a <code>vamm_configs</code> entry</td></tr>\n<table class='docs-table'>\n<tr><td>1</td><td>Orderbook walk (<code>OrderbookFillModel</code>)</td><td>L2 snapshot present in <code>orderbook_snapshots</code> for the bar timestamp</td></tr>\n<table class='docs-table'>\n<tr><td>2</td><td>Sqrt participation</td><td>Volume data available in candles, no L2 snapshot</td></tr>\n<table class='docs-table'>\n<tr><td>3</td><td>Flat bps fallback</td><td>No market depth data; uses <code>fee_rate</code> as a constant impact</td></tr>\n</table>\n\n<p>The tier selection is per-order, per-bar. A strategy running on SOL-PERP with vAMM enabled uses Tier 0; if vAMM is disabled but an L2 snapshot was fetched, it uses Tier 1 -- no config change required.</p>\n\n<p>---</p>\n\n<h4>2. vAMM Curve (Tier 0)</h4>\n\n<p><code>flint/execution/vamm.py</code> -- <code>VammCurve</code> implements a constant-product AMM anchored to an oracle price. This is the most accurate fill model for Drift Protocol backtests, as Drift itself uses a vAMM for price discovery.</p>\n\n<h4>Model</h4>\n\n<p>The curve maintains base and quote reserves such that:</p>\n\n\n<p>where <code>K = sqrt_k^2</code>. The peg multiplier anchors the curve center to the current oracle price:</p>\n\n\n<h4>Fill price formula</h4>\n\n<p>For a <strong>long</strong> (buying base):</p>\n\n\n<p>For a <strong>short</strong> (selling base):</p>\n\n\n<p>The peg multiplier is the ratio of the current oracle price to the curve's reference price, keeping the curve centered on fair value.</p>\n\n<h4>Impact in basis points</h4>\n\n\n<h4>K and depth</h4>\n\n<p>Larger K means deeper liquidity and lower price impact for a given size. Each major Drift market has a <code>DEFAULT_SQRT_K</code> calibrated to approximate historical AMM depth:</p>\n\n<table class='docs-table'>\n<tr><th>Market</th><th>DEFAULT_SQRT_K</th></tr>\n<table class='docs-table'>\n<tr><td>SOL-PERP</td><td>2,000,000</td></tr>\n<table class='docs-table'>\n<tr><td>BTC-PERP</td><td>500,000</td></tr>\n<table class='docs-table'>\n<tr><td>ETH-PERP</td><td>800,000</td></tr>\n</table>\n\n<h4>Configuration</h4>\n\n\n<p>In backtest requests, pass <code>vamm_configs</code> per market to override the default K.</p>\n\n<p>---</p>\n\n<h4>3. Orderbook Walk (Tier 1)</h4>\n\n<p><code>flint/execution/fill_models.py</code> -- <code>OrderbookFillModel</code> walks the L2 orderbook snapshot to compute a volume-weighted fill price.</p>\n\n<p>When an L2 snapshot is present in <code>orderbook_snapshots</code> for the current bar, the model:</p>\n\n<ol>\n<li>Sorts the relevant side (asks for buys, bids for sells) by price.</li>\n<li>Walks levels, accumulating size until the order is fully filled.</li>\n<li>Computes the volume-weighted average fill price across all consumed levels.</li>\n<li>If the order size exceeds available depth, the remaining size fills at the worst level plus the flat bps fallback.</li>\n</ol>\n\n<p>This model is most useful for venues where you have fetched L2 orderbook data (Drift, Hyperliquid).</p>\n\n<p>---</p>\n\n<h4>4. Sqrt Participation Model (Tier 2)</h4>\n\n<p>When no vAMM or orderbook data is available but volume is known, impact is estimated via a square-root participation model:</p>\n\n\n<p><strong>Variables:</strong></p>\n\n<ul>\n<li><code>fill_size</code> -- order size in base units</li>\n<li><code>bar_volume</code> -- total traded volume on the bar (from candle data)</li>\n<li><code>impact_coefficient</code> -- venue-specific constant from <code>VenueConfig</code></li>\n</ul>\n\n<p><strong>What <code>impact_coefficient</code> means</strong>: a coefficient of <code>0.1</code> means that filling 1% of bar volume incurs 1% of that percentage as slippage: <code>0.1 * sqrt(0.01) = 0.01</code> = 1 bps. Higher coefficients mean less liquid markets.</p>\n\n<p><strong>Defaults per venue</strong> (from <code>VenueConfig</code>):</p>\n\n<table class='docs-table'>\n<tr><th>Venue</th><th>impact_coefficient</th></tr>\n<table class='docs-table'>\n<tr><td>Drift</td><td>0.10</td></tr>\n<table class='docs-table'>\n<tr><td>Hyperliquid</td><td>0.08</td></tr>\n<table class='docs-table'>\n<tr><td>Binance</td><td>0.05</td></tr>\n<table class='docs-table'>\n<tr><td>OKX</td><td>0.06</td></tr>\n<table class='docs-table'>\n<tr><td>Bybit</td><td>0.06</td></tr>\n<table class='docs-table'>\n<tr><td>dYdX</td><td>0.09</td></tr>\n</table>\n\n<p>---</p>\n\n<h4>5. Flat BPS Fallback (Tier 3)</h4>\n\n<p>When no market depth or volume data is available, impact is a constant percentage of the fill price:</p>\n\n\n<p>This is the fastest and least accurate model. Use it for quick feasibility checks during strategy development, then switch to a higher tier before sizing up capital.</p>\n\n<p>---</p>\n\n<h4>6. Transaction Cost Models</h4>\n\n<p><code>flint/execution/tx_costs.py</code> -- <code>TxCostModel</code> ABC with per-venue implementations.</p>\n\n<p>Each fill can include a network-level cost on top of exchange fees. This separates exchange maker/taker fees from blockchain transaction costs.</p>\n\n<h4>Per-venue implementations</h4>\n\n<table class='docs-table'>\n<tr><th>Venue</th><th>Model Class</th><th>Components</th></tr>\n<table class='docs-table'>\n<tr><td>Drift</td><td><code>SolanaTxCostModel</code></td><td>Priority fee (p50/p90 of recent fees) + Jito bundle tip</td></tr>\n<table class='docs-table'>\n<tr><td>Hyperliquid</td><td><code>HyperliquidTxCostModel</code></td><td>Negligible L1 settlement cost</td></tr>\n<table class='docs-table'>\n<tr><td>CEX (Binance, OKX, etc.)</td><td><code>CexTxCostModel</code></td><td>Zero network cost</td></tr>\n</table>\n\n<h4>Solana cost breakdown</h4>\n\n\n<p>Priority fees are modeled from historical percentile distributions. Use <code>p50</code> for normal conditions, <code>p90</code> for urgent orders.</p>\n\n<h4>Pre-trade cost estimation</h4>\n\n<p>Use <code>ctx.estimate_cost()</code> in your strategy to check costs before placing an order:</p>\n\n\n<p>This is especially useful for small trades on Solana where network fees can be a significant percentage of trade value.</p>\n\n<h4>Configuration</h4>\n\n\n<p>---</p>\n\n<h4>7. Calibration Engine</h4>\n\n<p><code>flint/backtest/calibration.py</code> -- <code>CalibrationEngine</code> fits impact model coefficients from observed live fills. This is the bridge between simulated and real execution.</p>\n\n<h4>Power-law model</h4>\n\n\n<p><strong>Variables:</strong></p>\n\n<ul>\n<li><code>a</code> -- amplitude coefficient (fitted)</li>\n<li><code>sigma</code> -- rolling realized volatility (normalization for regime robustness)</li>\n<li><code>Q</code> -- fill size in USD notional</li>\n<li><code>ADV</code> -- average daily volume in USD</li>\n<li><code>b</code> -- exponent (fitted; <code>b = 0.5</code> is the sqrt model)</li>\n</ul>\n\n<p>The sigma and ADV normalization makes coefficients comparable across different volatility regimes and liquidity periods.</p>\n\n<h4>Sqrt model (special case)</h4>\n\n<p>The sqrt model is the power-law with <code>b</code> fixed at <code>0.5</code>:</p>\n\n\n<h4>Model selection</h4>\n\n<p>The calibration engine fits both models (sqrt fixed at <code>b=0.5</code>, and free power-law) and selects between them via 5-fold cross-validation on held-out fills. Power-law is chosen when it reduces out-of-sample MAE by more than 10%.</p>\n\n<h4>Drift detection</h4>\n\n<p>After each calibration, the engine computes the relative change in <code>a</code> from the previous calibration. If the change exceeds <strong>15%</strong>, a drift alert fires suggesting recalibration. This detects liquidity regime changes -- for example, if a market becomes significantly more or less liquid over time.</p>\n\n<h4>Running calibration</h4>\n\n\n<p>This reads live fills from <code>FlintStore</code>, fits the model, prints a report, and writes calibrated coefficients back to <code>VenueConfig</code>. Add <code>--dry-run</code> to print the report without saving.</p>\n\n<p>Also available via API:</p>\n\n\n<p>The API endpoint is read-only and never writes to config.</p>\n\n<p>---</p>\n\n<h4>8. CLMM Pool Model</h4>\n\n<p><code>flint/mev/clmm.py</code> -- <code>CLMMPool</code> models concentrated liquidity pools (Orca Whirlpool, Raydium CLMM) for MEV and arbitrage research.</p>\n\n<h4>What it does</h4>\n\n<ul>\n<li>Computes swap outputs given tick data and input amounts</li>\n<li>Detects arbitrage opportunities between CLMM price and oracle/perp price</li>\n<li>Models price impact within concentrated liquidity ranges</li>\n</ul>\n\n<h4>Tick data</h4>\n\n<p><code>flint/providers/orca_ticks.py</code> -- <code>OrcaTickFetcher</code> fetches CLMM tick data from Orca's on-chain accounts. Tick data is stored in the <code>tick_snapshots</code> table:</p>\n\n\n<p>Each snapshot contains: current tick, tick spacing, fee rate, sqrt price, and the full tick data array.</p>\n\n<h4>Usage</h4>\n\n<p>The <code>MevArbMonitor</code> strategy uses <code>CLMMPool</code> to scan for arbitrage routes between DEX pools and perp markets. The MEV Dashboard in the UI visualizes detected opportunities.</p>\n\n\n<p>---</p>\n\n<h4>9. Configuration Reference</h4>\n\n<table class='docs-table'>\n<tr><th>Key</th><th>Default</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td><code>vamm_enabled</code></td><td><code>false</code></td><td>Enable vAMM fill model (Tier 0)</td></tr>\n<table class='docs-table'>\n<tr><td><code>vamm_default_sqrt_k</code></td><td><code>1000000</code></td><td>K for markets without explicit config</td></tr>\n<table class='docs-table'>\n<tr><td><code>impact_coefficient</code></td><td>per-venue</td><td>Sqrt model amplitude (in <code>VenueConfig</code>)</td></tr>\n<table class='docs-table'>\n<tr><td><code>tx_cost_priority_fee_lamports</code></td><td><code>100000</code></td><td>Solana priority fee</td></tr>\n<table class='docs-table'>\n<tr><td><code>tx_cost_jito_tip_lamports</code></td><td><code>50000</code></td><td>Jito bundle tip</td></tr>\n<table class='docs-table'>\n<tr><td><code>tx_cost_sol_price_usd</code></td><td><code>140.0</code></td><td>SOL price fallback</td></tr>\n</table>\n\n<p>---</p>\n\n<h4>10. When to Use Which Model</h4>\n\n<table class='docs-table'>\n<tr><th>Situation</th><th>Recommended tier</th></tr>\n<table class='docs-table'>\n<tr><td>Drift perp backtest, high accuracy needed</td><td>Tier 0 (vAMM) -- enable <code>vamm_enabled: true</code></td></tr>\n<table class='docs-table'>\n<tr><td>Any venue with L2 snapshot data</td><td>Tier 1 (orderbook) -- download orderbook data first</td></tr>\n<table class='docs-table'>\n<tr><td>CEX backtest with volume data</td><td>Tier 2 (sqrt) -- default for Binance/OKX/Bybit</td></tr>\n<table class='docs-table'>\n<tr><td>Rough feasibility check</td><td>Tier 3 (flat bps) -- fast, low accuracy</td></tr>\n<table class='docs-table'>\n<tr><td>Post-live calibration available</td><td>Tier 2 with calibrated <code>impact_coefficient</code></td></tr>\n<table class='docs-table'>\n<tr><td>Cross-venue arb strategies</td><td>Enable both vAMM (Drift) and sqrt (Hyperliquid)</td></tr>\n</table>\n\n<h4>Recommended workflow</h4>\n\n<ol>\n<li><strong>Development</strong>: Start with Tier 3 (flat bps) to confirm signal logic works.</li>\n<li><strong>Refinement</strong>: Switch to Tier 0 (vAMM for Drift) or Tier 2 (sqrt for other venues) to test with realistic market impact.</li>\n<li><strong>Validation</strong>: Download L2 orderbook data and run with Tier 1 for the most accurate backtests.</li>\n<li><strong>Post-live</strong>: After accumulating live fills, run <code>flint calibrate</code> to fit coefficients from real data.</li>\n<li><strong>Monitoring</strong>: Watch for drift alerts (15% coefficient change) and recalibrate periodically.</li>\n</ol>\n\n<p>A strategy that is profitable under flat-bps but not under vAMM is not viable on Drift. Always validate with higher-fidelity models before committing real capital.</p>\n",
        "codeBlocks": [
          {
            "language": "text",
            "code": "base_reserve * quote_reserve = K"
          },
          {
            "language": "text",
            "code": "base_reserve  = sqrt_k / sqrt(oracle_price)\nquote_reserve = sqrt_k * sqrt(oracle_price)"
          },
          {
            "language": "text",
            "code": "fill_price = quote_reserve / (base_reserve - fill_size) * peg_multiplier"
          },
          {
            "language": "text",
            "code": "fill_price = quote_reserve / (base_reserve + fill_size) * peg_multiplier"
          },
          {
            "language": "text",
            "code": "impact_bps = abs(fill_price - oracle_price) / oracle_price * 10_000"
          },
          {
            "language": "yaml",
            "code": "vamm_enabled: true\nvamm_default_sqrt_k: 1000000   # fallback for markets without explicit config"
          },
          {
            "language": "text",
            "code": "impact_pct = impact_coefficient * sqrt(fill_size / bar_volume)"
          },
          {
            "language": "text",
            "code": "impact_pct = fee_rate  (in bps / 10_000)"
          },
          {
            "language": "text",
            "code": "total_cost_usd = (priority_fee_lamports + jito_tip_lamports) / LAMPORTS_PER_SOL * sol_price_usd"
          },
          {
            "language": "python",
            "code": "cost = ctx.estimate_cost(\"SOL-PERP\", size_usd=1000)\n# cost.exchange_fee_usd  -- maker/taker fees\n# cost.network_fee_usd   -- Solana priority fee + Jito tip\n# cost.total              -- exchange_fee_usd + network_fee_usd\n\nif cost.total > max_acceptable_cost:\n    return  # skip this bar"
          },
          {
            "language": "yaml",
            "code": "tx_cost_priority_fee_lamports: 100000   # default priority fee\ntx_cost_jito_tip_lamports: 50000        # default Jito tip\ntx_cost_sol_price_usd: 140.0            # used when live SOL price unavailable"
          },
          {
            "language": "text",
            "code": "impact_bps = a * sigma * (Q / ADV)^b"
          },
          {
            "language": "text",
            "code": "impact_bps = a * sigma * sqrt(Q / ADV)"
          },
          {
            "language": "bash",
            "code": "flint calibrate --venue drift --market SOL-PERP"
          },
          {
            "language": "bash",
            "code": "curl -X POST http://localhost:8000/api/v1/calibrate \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"venue\": \"drift\", \"market\": \"SOL-PERP\"}'"
          },
          {
            "language": "sql",
            "code": "PRIMARY KEY (pool_address, ts)"
          },
          {
            "language": "bash",
            "code": "# Scan for arb opportunities via API\ncurl -X POST http://localhost:8000/api/v1/mev/scan/arb"
          }
        ]
      }
    ]
  },
  {
    "id": "reference",
    "title": "REFERENCE",
    "topics": [
      {
        "id": "architecture",
        "title": "Architecture",
        "content": "\n<p>Flint is a local-first algorithmic trading, backtesting, and MEV research platform for Solana. This document describes how the major subsystems connect, where data flows, and where to plug in extensions.</p>\n\n<p>---</p>\n\n<h4>1. Directory Structure</h4>\n\n\n<p>---</p>\n\n<h4>2. Execution Hierarchy</h4>\n\n<p><code>ExecutionContext</code> is the abstract base class that every strategy receives. It exposes a uniform order API regardless of whether the strategy is running in a backtest, paper session, or live on-chain. All order methods (<code>market_order</code>, <code>limit_order</code>, <code>stop_order</code>, <code>cancel</code>, <code>cancel_all</code>) accept an optional <code>venue</code> parameter. Strategies written against <code>ExecutionContext</code> deploy to any venue without modification.</p>\n\n\n<p><code>BacktestContext</code> replays candle data and fills orders through the FillPipeline. <code>PaperBroker</code> uses the same fill models but ticks on real WebSocket candle closes rather than stored history. The three <code>Live*Context</code> classes submit real orders to their respective venues and poll for fill confirmations.</p>\n\n<p>Each <code>LiveExecutionContext</code> subclass implements: <code>place_order</code>, <code>cancel_order</code>, <code>modify_order</code>, <code>get_positions</code>, <code>get_account</code>, <code>poll_fills</code>, and <code>sync_on_startup</code> (reconciles local state with venue state on restart).</p>\n\n<p>---</p>\n\n<h4>3. Backtest Engine</h4>\n\n<p><code>flint/backtest/engine.py</code> -- event-driven, single-threaded candle replay.</p>\n\n<p><strong>Input formats:</strong></p>\n\n<ul>\n<li><code>List[Candle]</code> for single-market backtests</li>\n<li><code>Dict[str, List[Candle]]</code> for multi-market backtests (engine auto-detects <code>ctx.get_candles(\"MARKET\")</code> calls in strategy code)</li>\n</ul>\n\n<p><strong>Per-bar cycle:</strong> feed candle(s) to strategy, collect orders, run FillPipeline, update positions and equity.</p>\n\n<p><strong>Strategy versions:</strong> v1 strategies return <code>Signal.BUY/SELL/HOLD</code>. v2 strategies use context-based orders (<code>ctx.market_order()</code>, <code>ctx.stop_order()</code>, <code>ctx.get_candles(\"BTC-PERP\")</code>). Both are supported.</p>\n\n<p><strong>Constraints:</strong> 300-second timeout per backtest. Maximum 5 concurrent backtest runs.</p>\n\n<p><strong>Multi-venue:</strong> Position keys are <code>(venue, market)</code>. Margin and positions are isolated per venue. Each venue uses its own fill model and fee schedule.</p>\n\n<h4>Rust engine (flint_core)</h4>\n\n<p>When the <code>flint_core</code> Rust package is installed, the backtest engine auto-dispatches to it for 10-50x faster execution. The Rust engine exposes the same <code>RustEngine</code> interface via PyO3 bindings and produces identical results -- verified by parity tests in <code>tests/test_rust_parity_benchmark.py</code>.</p>\n\n\n<p>The Rust engine implements the full backtest loop in <code>rust/src/runner.rs</code>, including fill models, venue-specific pipelines, margin tracking, capital allocation, and metric computation. The Python engine remains the fallback when <code>flint_core</code> is not installed.</p>\n\n<p>---</p>\n\n<h4>4. Fill Pipeline</h4>\n\n<p>The fill pipeline runs inside <code>BacktestContext</code> on every order. It is a chain of stages applied in sequence:</p>\n\n\n<h4>4-tier impact model</h4>\n\n<p>ImpactStage selects the highest-fidelity model available for each order, per bar:</p>\n\n<table class='docs-table'>\n<tr><th>Tier</th><th>Model</th><th>When used</th></tr>\n<table class='docs-table'>\n<tr><td>0</td><td><code>VammCurve</code> (constant-product vAMM)</td><td><code>vamm_enabled: true</code> and market has a configured vAMM</td></tr>\n<table class='docs-table'>\n<tr><td>1</td><td>Orderbook walk (<code>OrderbookFillModel</code>)</td><td>L2 snapshot present in FlintStore for the bar</td></tr>\n<table class='docs-table'>\n<tr><td>2</td><td>Sqrt participation impact</td><td>Volume data available, no orderbook snapshot</td></tr>\n<table class='docs-table'>\n<tr><td>3</td><td>Flat bps fallback</td><td>No market depth data available</td></tr>\n</table>\n\n<p>Tier selection is per-order, per-bar. A strategy running on SOL-PERP with vAMM enabled uses Tier 0; if vAMM is disabled but an L2 snapshot was fetched, it falls to Tier 1 automatically.</p>\n\n<h4>Per-venue fill configs</h4>\n\n<p>Venue-specific fill logic lives in dedicated modules (<code>fill_drift.py</code>, <code>fill_hyperliquid.py</code>, <code>fill_cex.py</code>, <code>fill_jupiter.py</code>). The <code>fill_registry.py</code> dispatches to the correct venue pipeline based on order routing. The Rust engine mirrors this with <code>venue_fills.rs</code> covering Drift 3-tier, Hyperliquid CLOB, Jupiter, and 10 CEX venues.</p>\n\n<p>---</p>\n\n<h4>5. Margin Engine</h4>\n\n<p><code>flint/execution/margin.py</code> -- <code>MarginEngine</code> with per-venue configs.</p>\n\n<p>Position key is <code>(venue, market)</code>, so margin is isolated per venue. On each bar the engine recomputes unrealized PnL, checks maintenance margin, and triggers liquidation if the margin ratio drops below the maintenance threshold.</p>\n\n<p>Venue configs define fees, initial margin, maintenance margin, leverage limits, and liquidation penalties. Presets are defined in <code>flint/execution/venue_config.py</code>:</p>\n\n<table class='docs-table'>\n<tr><th>Venue</th><th>Taker fee</th><th>Maker fee</th><th>Max leverage</th><th>Base latency</th></tr>\n<table class='docs-table'>\n<tr><td>Drift</td><td>10 bps</td><td>-2 bps (rebate)</td><td>10x</td><td>8.0s</td></tr>\n<table class='docs-table'>\n<tr><td>Hyperliquid</td><td>3.5 bps</td><td>1 bps</td><td>20x</td><td>1.0s</td></tr>\n<table class='docs-table'>\n<tr><td>Binance</td><td>4.5 bps</td><td>2 bps</td><td>50x</td><td>0.2s</td></tr>\n<table class='docs-table'>\n<tr><td>OKX</td><td>5 bps</td><td>2 bps</td><td>50x</td><td>0.3s</td></tr>\n<table class='docs-table'>\n<tr><td>Bybit</td><td>5.5 bps</td><td>2 bps</td><td>50x</td><td>0.3s</td></tr>\n<table class='docs-table'>\n<tr><td>dYdX</td><td>5 bps</td><td>1 bps</td><td>20x</td><td>2.0s</td></tr>\n<table class='docs-table'>\n<tr><td>Jupiter</td><td>6 bps</td><td>6 bps</td><td>100x</td><td>12.0s</td></tr>\n</table>\n\n<p>Enable in backtest requests with <code>margin_tracking: true</code>.</p>\n\n<p>---</p>\n\n<h4>6. Capital Allocator</h4>\n\n<p><code>flint/execution/capital.py</code> -- <code>VenueAllocator</code> with per-venue balances.</p>\n\n<ul>\n<li>Maintains separate cash accounts per venue, independent of unrealized PnL.</li>\n<li>Models transfer delays and costs between venues for cross-venue strategy backtests.</li>\n<li><code>ctx.transfer(from_venue, to_venue, amount)</code> models inter-venue capital movement with configurable delays.</li>\n<li><code>fragmentation_metrics()</code> reports capital utilization and idle cash per venue.</li>\n</ul>\n\n<p>Enable in backtest requests with <code>capital_allocation: {\"drift\": 5000, \"hyperliquid\": 3000, \"binance\": 2000}</code>.</p>\n\n<p>---</p>\n\n<h4>7. Regime System</h4>\n\n<p><code>flint/regimes.py</code> defines 8 curated market regimes spanning Dec 2023 to Apr 2026, covering five regime types:</p>\n\n<table class='docs-table'>\n<tr><th>Regime</th><th>Type</th><th>Period</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td>Pre-ETF Consolidation</td><td>sideways</td><td>Dec 2023 - Jan 2024</td><td>Low volatility, awaiting ETF catalyst</td></tr>\n<table class='docs-table'>\n<tr><td>ETF Bull Run</td><td>bull</td><td>Jan 2024 - May 2024</td><td>BTC spot ETF approval drives +70% rally</td></tr>\n<table class='docs-table'>\n<tr><td>Summer Correction</td><td>bear</td><td>Jun 2024 - Aug 2024</td><td>Profit-taking, BTC -12%</td></tr>\n<table class='docs-table'>\n<tr><td>Recovery Rally</td><td>bull</td><td>Sep 2024 - Dec 2024</td><td>Re-acceleration to new ATHs</td></tr>\n<table class='docs-table'>\n<tr><td>Peak & Distribution</td><td>high_vol</td><td>Jan 2025 - Mar 2025</td><td>Topping pattern with funding spikes</td></tr>\n<table class='docs-table'>\n<tr><td>Extended Decline</td><td>bear</td><td>Apr 2025 - Sep 2025</td><td>Slow grind lower</td></tr>\n<table class='docs-table'>\n<tr><td>Crash Phase 1</td><td>crash</td><td>Oct 2025 - Dec 2025</td><td>Accelerating sell-off, capitulation</td></tr>\n<table class='docs-table'>\n<tr><td>Crash Phase 2</td><td>crash</td><td>Jan 2026 - Apr 2026</td><td>Continued decline, dead cat bounces</td></tr>\n</table>\n\n<p>Regimes are used in BacktestLab for multi-regime testing -- run a strategy across all regimes to evaluate robustness. The UI keeps a synced copy in <code>ui/src/constants/regimes.ts</code>.</p>\n\n<p>---</p>\n\n<h4>8. FlintStore (DuckDB)</h4>\n\n<p><code>flint/store.py</code> -- single shared DuckDB connection wrapped in a <code>threading.Lock</code>.</p>\n\n<p><strong>Thread safety rule:</strong> every method uses <code>with self._lock:</code>. Batched upserts are wrapped in <code>BEGIN TRANSACTION / COMMIT</code> for atomicity. Never open a second DuckDB connection or access <code>_conn</code>/<code>_lock</code> directly from outside the class.</p>\n\n<h4>12 tables</h4>\n\n<table class='docs-table'>\n<tr><th>Table</th><th>Primary key</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td><code>candles</code></td><td><code>(venue, market, resolution_s, ts)</code></td><td>OHLCV bars, per-venue</td></tr>\n<table class='docs-table'>\n<tr><td><code>venue_funding_rates</code></td><td>venue + market + ts</td><td>Hourly funding rates by venue</td></tr>\n<table class='docs-table'>\n<tr><td><code>oracle_prices</code></td><td>market + ts</td><td>Pyth oracle price snapshots</td></tr>\n<table class='docs-table'>\n<tr><td><code>orderbook_snapshots</code></td><td>market + ts</td><td>L2 orderbook depth</td></tr>\n<table class='docs-table'>\n<tr><td><code>pool_snapshots</code></td><td>pool_address + ts</td><td>Raydium/Orca AMM pool state</td></tr>\n<table class='docs-table'>\n<tr><td><code>open_interest</code></td><td>market + ts</td><td>Long/short OI by market</td></tr>\n<table class='docs-table'>\n<tr><td><code>liquidations</code></td><td>tx_sig</td><td>On-chain liquidation events</td></tr>\n<table class='docs-table'>\n<tr><td><code>whale_transfers</code></td><td>tx_sig</td><td>Large wallet movements</td></tr>\n<table class='docs-table'>\n<tr><td><code>dex_volume</code></td><td>market + ts</td><td>DEX trading volume</td></tr>\n<table class='docs-table'>\n<tr><td><code>token_unlocks</code></td><td>token + ts</td><td>Token vesting unlock events</td></tr>\n<table class='docs-table'>\n<tr><td><code>tick_snapshots</code></td><td><code>(pool_address, ts)</code></td><td>CLMM tick data for concentrated liquidity</td></tr>\n<table class='docs-table'>\n<tr><td><code>sync_metadata</code></td><td>source + key</td><td>Last-fetched timestamps per source</td></tr>\n</table>\n\n<p>Live trading adds four more tables: <code>live_sessions</code>, <code>live_orders</code>, <code>live_fills</code>, <code>live_equity_history</code>.</p>\n\n<p>---</p>\n\n<h4>9. Risk Guards</h4>\n\n<p><code>flint/risk/guards.py</code> -- <code>RiskManager</code> chains 6 guard types. Every order must pass all guards before execution. The same guards apply in backtests, paper trading, and live trading.</p>\n\n<table class='docs-table'>\n<tr><th>Guard</th><th>Behavior</th></tr>\n<table class='docs-table'>\n<tr><td><code>MaxPositionSize</code></td><td>Rejects orders when cumulative USD notional on a market exceeds a cap</td></tr>\n<table class='docs-table'>\n<tr><td><code>MaxOpenPositions</code></td><td>Rejects new entries when too many positions are open (does not block closes)</td></tr>\n<table class='docs-table'>\n<tr><td><code>MaxDrawdownCircuitBreaker</code></td><td>Rejects all orders once cumulative drawdown exceeds threshold; latches until manually reset</td></tr>\n<table class='docs-table'>\n<tr><td><code>DailyLossLimit</code></td><td>Rejects orders after daily loss exceeds USD threshold; auto-resets at day boundary</td></tr>\n<table class='docs-table'>\n<tr><td><code>MaxOrdersPerMinute</code></td><td>Sliding-window rate limiter (default 30/min)</td></tr>\n<table class='docs-table'>\n<tr><td><code>PerMarketPositionLimit</code></td><td>Hard per-market USD notional cap from a configured JSON map</td></tr>\n</table>\n\n<p><code>EquityMonitor</code> (<code>flint/risk/monitor.py</code>) runs continuously in live sessions. When equity drops below <code>live_kill_switch_drawdown_pct</code> from peak, it cancels all open orders and closes all positions across all venues, then fires a critical alert. A warning alert fires at <code>live_drawdown_warning_pct</code>.</p>\n\n<p>---</p>\n\n<h4>10. MCP Server</h4>\n\n<p><code>flint/mcp_server.py</code> exposes 17 tools for AI model integration via the Model Context Protocol:</p>\n\n\n<p><strong>Tools:</strong> <code>run_backtest</code>, <code>list_strategies</code>, <code>start_paper_trading</code>, <code>stop_paper_trading</code>, <code>get_paper_sessions</code>, <code>get_paper_status</code>, <code>list_journal_runs</code>, <code>optimize_strategy</code>, <code>get_candles</code>, <code>download_market_data</code>, <code>list_available_markets</code>, <code>list_local_markets</code>, <code>get_funding_rates</code>, <code>get_open_interest</code>, <code>get_correlation</code>, <code>get_data_freshness</code>, <code>get_provider_status</code>.</p>\n\n<p><strong>Resources:</strong> <code>flint://guide</code> (usage overview), <code>flint://markets</code> (market list).</p>\n\n<p>All tools route through the same FlintStore and engine code paths used by the API and CLI.</p>\n\n<p>---</p>\n\n<h4>11. React UI</h4>\n\n<p>10 pages built with React 19, Vite, and Tailwind CSS. Production build is served by FastAPI from <code>ui/dist/</code>. Development server runs at <code>localhost:5173</code> and proxies API calls to <code>localhost:8000</code>.</p>\n\n<table class='docs-table'>\n<tr><th>Page</th><th>Purpose</th></tr>\n<table class='docs-table'>\n<tr><td>BacktestLab</td><td>Monaco code editor, run/compare backtests, regime selection, deploy to paper</td></tr>\n<table class='docs-table'>\n<tr><td>PaperTrading</td><td>Paper session management, live equity, parity test button</td></tr>\n<table class='docs-table'>\n<tr><td>DataExplorer</td><td>Browse markets, download data, check coverage and freshness</td></tr>\n<table class='docs-table'>\n<tr><td>Dashboard</td><td>Overview of equity curves, recent runs, system health</td></tr>\n<table class='docs-table'>\n<tr><td>LiveMonitor</td><td>Real-time equity, positions, trade log, alert history</td></tr>\n<table class='docs-table'>\n<tr><td>MevDashboard</td><td>Arb opportunity scanner, CLMM pool analysis</td></tr>\n<table class='docs-table'>\n<tr><td>FundingHeatmap</td><td>Cross-venue funding rate visualization</td></tr>\n<table class='docs-table'>\n<tr><td>FillAnalysis</td><td>Fill quality analysis, slippage breakdown per venue</td></tr>\n<table class='docs-table'>\n<tr><td>Docs</td><td>Embedded documentation</td></tr>\n<table class='docs-table'>\n<tr><td>Setup</td><td>Initial configuration, data download, provider status</td></tr>\n</table>\n\n<p>Charts use lightweight-charts v5 (<code>InteractiveChart</code> component). The code editor uses Monaco (<code>CodeEditor</code> component). Data hooks (<code>useBacktest</code>, <code>useStrategies</code>, <code>useOptimize</code>, <code>useJournal</code>) manage API communication and polling.</p>\n\n<p>---</p>\n\n<h4>12. Data Flow</h4>\n\n<p>End-to-end path from source to result:</p>\n\n\n<p>For live sessions, the path from Providers to Strategy runs on every WebSocket candle close. Fills go to <code>live_fills</code> in FlintStore and trigger alert notifications.</p>\n\n<p>For backtests, the Rust engine (when installed) replaces the Python BacktestEngine in this pipeline with identical logic running 10-50x faster.</p>\n",
        "codeBlocks": [
          {
            "language": "text",
            "code": "flint/\n  strategy/            # Strategy ABC, 20 built-in templates, AST-validated loader\n  execution/           # ExecutionContext ABC and all concrete implementations\n    context.py           # ExecutionContext ABC (market_order, limit_order, stop_order)\n    backtest_context.py  # BacktestContext -- candle-replay fills via FillPipeline\n    paper_broker.py      # PaperBroker -- same fill models, no on-chain transactions\n    live_base.py         # LiveExecutionContext ABC\n    drift_live.py        # LiveDriftContext -- driftpy SDK, Solana RPC\n    hyperliquid_live.py  # LiveHyperliquidContext -- REST + EIP-712 signing\n    ccxt_live.py         # LiveCCXTContext -- Binance, OKX, Bybit, any CCXT exchange\n    multi_venue_live.py  # MultiVenueLiveContext -- routes by venue param\n    fill_models.py       # FillModel ABC, ClosePriceFill, OrderbookFillModel, FillPipeline\n    impact.py            # ImpactStage (4-tier model)\n    latency.py           # LatencyStage\n    partial_fill.py      # PartialFillStage\n    order_tracker.py     # OrderTracker state machine (PENDING -> FILLED/CANCELLED/REJECTED)\n    margin.py            # MarginEngine with per-venue configs\n    capital.py           # VenueAllocator with per-venue balances\n    venue_config.py      # VenueConfig presets (Drift, Hyperliquid, Binance, OKX, Bybit, dYdX, Jupiter)\n    vamm.py              # VammCurve (constant-product vAMM fill model)\n    tx_costs.py          # TxCostModel ABC + per-venue implementations\n    fill_drift.py        # Drift-specific fill logic\n    fill_hyperliquid.py  # Hyperliquid-specific fill logic\n    fill_cex.py          # CEX-specific fill logic\n    fill_jupiter.py      # Jupiter-specific fill logic\n    fill_registry.py     # Fill model registry for venue dispatch\n    synthetic_depth.py   # Synthetic orderbook generation\n  backtest/            # Event-driven engine, parity test, calibration\n    engine.py            # BacktestEngine\n    parity.py            # ParityTest -- backtest vs paper engine comparison\n    calibration.py       # CalibrationEngine (slippage calibration from live fills)\n  regimes.py           # 8 market regime definitions (Dec 2023 - Apr 2026)\n  optimization/        # Optuna optimizer, walk-forward validation\n  paper/               # Paper trading engine + broker (ticks on live WebSocket feeds)\n  risk/                # RiskGuard chain, EquityMonitor kill switch\n    guards.py            # RiskManager + 6 guard types\n    monitor.py           # EquityMonitor (live kill switch)\n  portfolio/           # Multi-strategy engine, equal-weight + inverse-vol allocators\n  providers/           # 14 data providers, 10 funding venues, WebSocket feeds\n    registry.py          # DataProvider ABC, ProviderRegistry\n    drift_candles.py     # DriftCandleProvider (48 markets)\n    drift_s3.py          # DriftS3Provider (archival trade records)\n    drift_api.py         # DriftDataProvider (funding, orderbook)\n    funding_rates.py     # 10-venue funding rate collection\n    birdeye.py           # Birdeye (any Solana token OHLCV)\n    helius.py            # Helius (liquidations, whale tracking)\n    pyth.py              # Pyth oracle prices (20 pairs)\n    raydium.py           # Raydium AMM/CLMM pool data\n    orca.py              # Orca Whirlpool pool data\n    gecko.py             # GeckoTerminal DEX pool OHLCV\n    jupiter.py           # Jupiter swap quotes\n    coingecko.py         # CoinGecko spot candles\n    ccxt_provider.py     # CCXT (100+ CEX exchanges, volume data)\n    hyperliquid_candles.py  # HyperliquidCandleProvider\n    websocket.py         # WebSocketFeed ABC\n    drift_ws.py          # DriftWebSocketFeed\n    hyperliquid_ws.py    # HyperliquidWebSocketFeed\n    pyth_ws.py           # PythWebSocketFeed\n    candle_aggregator.py # CandleAggregator (raw trades -> OHLCV bars)\n    orca_ticks.py        # OrcaTickFetcher (CLMM tick data)\n  connectors/          # Drift (driftpy), Hyperliquid (native REST + EIP-712)\n  analytics/           # Metrics, tearsheet, Monte Carlo, correlation\n  mev/                 # Arb detection, CLMM tick model\n    clmm.py              # CLMMPool (concentrated liquidity modeling)\n  indicators.py        # 20 technical indicators (sma, ema, rsi, macd, bollinger, atr, vwap, adx...)\n  precision.py         # Fixed-point math for Solana (Decimal at boundaries, int on-chain)\n  store.py             # Thread-safe DuckDB store -- single shared connection, 12 tables\n  config.py            # Pydantic settings (flint.yaml + .env + FLINT_ env prefix)\n  cli.py               # Typer CLI (8 commands)\n  api/main.py          # FastAPI app (30+ endpoints, serves built UI from ui/dist/)\n  models.py            # Shared dataclasses: Candle, Order, Fill, Signal, FundingRate, etc.\n  mcp_server.py        # MCP server for AI integration (17 tools)\n\nrust/                  # Rust backtesting engine (PyO3 bindings -> flint_core)\n  src/lib.rs             # PyO3 Python bindings (RustEngine class)\n  src/runner.rs          # Main backtest loop orchestration\n  src/types.rs           # Shared type definitions\n  src/engine/\n    fills.rs             # Generic fill models (close, slippage, sqrt impact)\n    venue_fills.rs       # Per-venue pipelines (Drift 3-tier, HL CLOB, Jupiter, 10 CEX)\n    orders.rs            # Order processing with venue dispatch\n    positions.rs         # Position state machine\n    fees.rs              # Fee computation\n    margin.rs            # Margin/liquidation engine\n    capital.rs           # Multi-venue capital allocation\n    venue_config.rs      # Per-venue fee/margin/latency configs\n    synthetic_depth.rs   # Synthetic orderbook generation\n    metrics.rs           # Performance metric computation\n\nui/                    # React 19 + Vite + Tailwind CSS\n  src/pages/\n    BacktestLab.tsx      # Strategy editor (Monaco), run + compare backtests\n    PaperTrading.tsx     # Paper session management, parity test button\n    DataExplorer.tsx     # Browse and download market data\n    Dashboard.tsx        # Overview: equity curves, recent runs, system health\n    LiveMonitor.tsx      # Live session monitoring, equity, positions, alerts\n    MevDashboard.tsx     # Arb opportunity scanner, CLMM analysis\n    FundingHeatmap.tsx   # Cross-venue funding rate heatmap\n    FillAnalysis.tsx     # Fill quality analysis, slippage breakdown\n    Docs.tsx             # Embedded documentation\n    Setup.tsx            # Initial configuration and data download\n  src/components/        # InteractiveChart (lightweight-charts v5), CodeEditor (Monaco)\n  src/hooks/             # useBacktest, useStrategies, useOptimize, useJournal"
          },
          {
            "language": "text",
            "code": "ExecutionContext (ABC)\n+-- BacktestContext              -- candle-replay, fills via FillPipeline\n+-- PaperBroker                  -- same fill models, ticks on live WebSocket feeds\n+-- LiveExecutionContext (ABC)\n    +-- LiveDriftContext          -- driftpy SDK, Solana RPC\n    +-- LiveHyperliquidContext    -- native REST + EIP-712 signing\n    +-- LiveCCXTContext           -- Binance, OKX, Bybit, any CCXT exchange\n    +-- MultiVenueLiveContext     -- wraps multiple venue contexts, routes by venue param"
          },
          {
            "language": "bash",
            "code": "pip install maturin\ncd rust && maturin develop"
          },
          {
            "language": "text",
            "code": "Order\n  +-- LatencyStage          -- configurable execution latency (ms)\n        +-- ImpactStage     -- fill price with market impact (4-tier)\n              +-- PartialFillStage -- probabilistic partial fill based on volume\n                    +-- Fill"
          },
          {
            "language": "bash",
            "code": "python -m flint.mcp_server                          # run standalone (stdio transport)\nclaude mcp add flint -- python -m flint.mcp_server   # add to Claude Code"
          },
          {
            "language": "text",
            "code": "External APIs / WebSockets\n        |\n        v\n   Providers (14 REST + 3 WS feeds + 10 funding venues)\n        |  fetch / stream\n        v\n   FlintStore (DuckDB, thread-safe)\n        |  query\n        v\n   BacktestEngine / PaperBroker / LiveExecutionContext\n        |  candles + funding\n        v\n   Strategy.on_candle()\n        |  orders\n        v\n   RiskManager (6-guard chain)\n        |  approved orders\n        v\n   FillPipeline (latency -> impact -> partial fill)\n        |  fills\n        v\n   TxCostModel (per-venue network costs)\n        |  costed fills\n        v\n   BacktestResult / live_fills table"
          }
        ]
      },
      {
        "id": "safety-rails",
        "title": "Safety Rails",
        "content": "\n<p>Read this before deploying any strategy beyond backtesting.</p>\n\n<p>---</p>\n\n<h4>1. Kill Switch (EquityMonitor)</h4>\n\n<p>The <code>EquityMonitor</code> (<code>flint/risk/monitor.py</code>) runs as a background async task</p>\n<p>alongside the strategy tick loop, checking equity every <code>check_interval_s</code></p>\n<p>seconds (default <strong>5s</strong>) against two thresholds.</p>\n\n<table class='docs-table'>\n<tr><th>Parameter</th><th>Config key</th><th>Default</th></tr>\n<table class='docs-table'>\n<tr><td>Kill switch threshold</td><td><code>live_kill_switch_drawdown_pct</code></td><td><strong>0.15</strong> (15%)</td></tr>\n<table class='docs-table'>\n<tr><td>Warning threshold</td><td><code>live_drawdown_warning_pct</code></td><td><strong>0.075</strong> (7.5%)</td></tr>\n<table class='docs-table'>\n<tr><td>Check interval</td><td><code>live_kill_switch_check_interval_s</code></td><td><strong>5.0s</strong></td></tr>\n</table>\n\n<p><strong>What happens when it triggers:</strong></p>\n\n<ol>\n<li>A <code>CRITICAL</code> log is emitted: <code>KILL SWITCH: drawdown X% >= Y%. Flattening all positions.</code></li>\n<li><code>cancel_all()</code> is called -- every open and pending order across all venues is cancelled.</li>\n<li><code>close_position()</code> is called for every open position, per-market and per-venue.</li>\n<li>The strategy tick loop is halted (<code>_running = False</code>).</li>\n<li>If a <code>NotificationManager</code> is configured (Telegram, Discord, webhook), an alert is sent with equity details.</li>\n<li><strong>Manual restart is required.</strong> The monitor sets <code>_tripped = True</code> and will not resume automatically. You must restart the <code>flint live</code> process after investigating.</li>\n</ol>\n\n<p>The warning threshold fires a <code>WARNING</code> log and notification at 7.5% drawdown</p>\n<p>(default), giving you a chance to intervene before the kill switch trips.</p>\n\n<p>---</p>\n\n<h4>2. Risk Guards</h4>\n\n<p>Risk guards are pre-trade checks chained in a <code>RiskManager</code> -- an order must</p>\n<p>pass <strong>all</strong> guards to be submitted. If any guard rejects, the order is dropped</p>\n<p>and logged. See <code>flint/risk/guards.py</code>.</p>\n\n<h4>MaxPositionSize</h4>\n\n<p>Rejects orders whose cumulative notional (existing + new) on a market exceeds</p>\n<p>a USD cap. Configured via <code>RiskManager</code> construction. On violation: order</p>\n<p>rejected with <code>INFO</code> log. The order never reaches the venue.</p>\n\n<h4>PerMarketPositionLimit</h4>\n\n<p>Hard USD notional cap per individual market, configured as <code>{market: limit}</code>.</p>\n\n<ul>\n<li><strong>Config key</strong>: <code>live_per_market_position_limits</code> (comma-separated <code>MARKET:LIMIT</code> pairs).</li>\n<li><strong>On violation</strong>: Order rejected with <code>INFO</code> log.</li>\n</ul>\n\n<h4>MaxOpenPositions</h4>\n\n<p>Rejects new-position orders when the cap is reached. Orders that reduce an</p>\n<p>existing position pass through. Config: <code>max_open_trades</code> (default <strong>5</strong>).</p>\n\n<h4>MaxDrawdownCircuitBreaker</h4>\n\n<p>Stops all trading once cumulative drawdown from peak exceeds the threshold.</p>\n<p>Unlike the kill switch, this does <strong>not</strong> flatten positions -- it only rejects</p>\n<p>new orders. Once tripped, stays tripped until explicitly reset.</p>\n<p>Config: <code>max_drawdown_pct</code> (default <strong>0.20</strong> / 20%).</p>\n\n<h4>DailyLossLimit</h4>\n\n<p>Stops trading for the rest of the calendar day after daily PnL drops below a</p>\n<p>USD threshold. Resets automatically at the start of a new day.</p>\n<p>Config: <code>DailyLossLimit(max_daily_loss=500, initial_capital=10000)</code>.</p>\n\n<h4>MaxOrdersPerMinute</h4>\n\n<p>Rejects orders if too many have been placed in the last 60 seconds.</p>\n<p>Config: <code>live_max_orders_per_minute</code> (default <strong>30</strong>).</p>\n\n<p>---</p>\n\n<h4>3. Dry-Run Mode</h4>\n\n<p>Dry-run mode executes the full live pipeline -- strategy ticks, risk guard</p>\n<p>evaluation, order tracking, fill logging -- without submitting real</p>\n<p>transactions to any venue.</p>\n\n<p><strong>How to enable:</strong></p>\n\n\n<p>Or in <code>flint.yaml</code>:</p>\n\n\n<p><strong>What you see:</strong> Orders flow through the entire pipeline. Fills are recorded</p>\n<p>with <code>tx_sig=\"DRY_RUN\"</code> and priced at the current candle close. Fees are</p>\n<p>simulated at 5bps. All logging, notifications, and risk guard checks work</p>\n<p>identically to real execution.</p>\n\n<p><strong>Why to use it:</strong> Validate strategy behavior under mainnet market conditions</p>\n<p>(real prices, real funding rates) without financial risk. Run dry-run for at</p>\n<p>least a full trading session before switching to live.</p>\n\n<p>See <code>flint/execution/live_base.py</code>, <code>submit_pending_orders()</code> method.</p>\n\n<p>---</p>\n\n<h4>4. Order Lifecycle Safety</h4>\n\n<p>The <code>OrderTracker</code> (<code>flint/execution/order_tracker.py</code>) manages every order</p>\n<p>from creation to terminal state with built-in safety mechanisms.</p>\n\n<h4>Rate Limiting</h4>\n\n<table class='docs-table'>\n<tr><th>Parameter</th><th>Config key</th><th>Default</th></tr>\n<table class='docs-table'>\n<tr><td>Max orders/sec</td><td><code>live_rate_limit_orders_per_sec</code></td><td><strong>10</strong></td></tr>\n<table class='docs-table'>\n<tr><td>Max concurrent tx</td><td><code>live_rate_limit_concurrent_tx</code></td><td><strong>2</strong></td></tr>\n</table>\n\n<p>If either limit is hit, remaining orders are deferred to the next tick.</p>\n\n<h4>Retry Logic</h4>\n\n<table class='docs-table'>\n<tr><th>Parameter</th><th>Config key</th><th>Default</th></tr>\n<table class='docs-table'>\n<tr><td>Max retries</td><td><code>live_max_retries</code></td><td><strong>3</strong></td></tr>\n<table class='docs-table'>\n<tr><td>On failure after retries</td><td><code>live_on_order_failure</code></td><td><strong>\"drop\"</strong></td></tr>\n</table>\n\n<p>On submission failure, the order returns to PENDING and retries next tick.</p>\n<p>After <code>max_retries</code>: <strong>\"drop\"</strong> (default) marks FAILED and continues;</p>\n<p><strong>\"halt\"</strong> marks FAILED and stops the strategy loop (manual restart required).</p>\n\n<h4>Order Timeout</h4>\n\n<ul>\n<li><strong>Submission timeout</strong> (30s): SUBMITTED orders with no on-chain confirmation trigger a retry.</li>\n<li><strong>Limit order timeout</strong> (<code>live_limit_order_timeout_bars</code>, default <strong>10 bars</strong>): Unfilled limit orders are marked EXPIRED and cancelled.</li>\n</ul>\n\n<p>---</p>\n\n<h4>5. Network Failure Handling</h4>\n\n<h4>RPC Failures</h4>\n\n<p>Order submission failures trigger the retry logic described above (exponential</p>\n<p>back-off via tick-based retry, up to <code>max_retries</code>). Position sync failures are</p>\n<p>logged but do not halt the strategy -- the next sync interval retries</p>\n<p>automatically.</p>\n\n<h4>WebSocket Disconnects</h4>\n\n<p>When the event-driven tick loop (<code>on_candle_close</code> mode) does not receive a</p>\n<p>WebSocket candle within <code>tick_interval_s * 2</code>, it falls back to REST candle</p>\n<p>fetch. WebSocket feeds reconnect automatically on the next cycle.</p>\n\n<h4>Transaction Dropped</h4>\n\n<p>If a submitted transaction is not confirmed within the 30-second submission</p>\n<p>timeout, the <code>OrderTracker</code> retries submission (up to <code>max_retries</code>). Orders</p>\n<p>are not duplicated -- each retry reuses the same Flint order ID.</p>\n\n<p>---</p>\n\n<h4>6. &quot;What Happens When Things Go Wrong&quot;</h4>\n\n<h4>Scenario: Your strategy enters a runaway loop</h4>\n\n<p>The <code>MaxOrdersPerMinute</code> guard (default 30/min) rejects excess orders before</p>\n<p>they reach the venue. The <code>OrderTracker</code> rate limiter (default 10/sec, 2</p>\n<p>concurrent) provides a second layer. If the strategy somehow floods beyond</p>\n<p>both, the kill switch monitors equity and will flatten everything if drawdown</p>\n<p>hits the threshold.</p>\n\n<h4>Scenario: Market crashes 20% in 5 minutes</h4>\n\n<p>The <code>EquityMonitor</code> checks equity every 5 seconds. When drawdown from peak</p>\n<p>exceeds 15% (default), it cancels all orders, closes all positions across all</p>\n<p>venues, halts the strategy, and sends a notification. The</p>\n<p><code>MaxDrawdownCircuitBreaker</code> (20% default) provides a secondary guard that</p>\n<p>blocks any new orders even if the monitor is delayed.</p>\n\n<h4>Scenario: Solana RPC goes down</h4>\n\n<p>Order submissions fail and enter the retry queue (up to 3 retries). The</p>\n<p>WebSocket tick loop falls back to REST polling. Position sync logs errors but</p>\n<p>continues retrying each interval. No orders are submitted to a dead RPC -- they</p>\n<p>queue locally until connectivity is restored or retries are exhausted.</p>\n\n<h4>Scenario: You deploy to mainnet by accident</h4>\n\n<p>The default <code>live_network</code> is <code>\"devnet\"</code> and <code>live_hyperliquid_network</code> is</p>\n<p><code>\"testnet\"</code>. Mainnet requires explicitly setting <code>--network mainnet</code> on the CLI</p>\n<p>or <code>live_network: mainnet</code> in <code>flint.yaml</code>. There is no path to mainnet</p>\n<p>without deliberate configuration.</p>\n\n<h4>Scenario: A strategy bug opens a huge position</h4>\n\n<p><code>PerMarketPositionLimit</code> rejects the order before it reaches the venue if the</p>\n<p>resulting notional would exceed the configured cap. <code>MaxPositionSize</code> provides</p>\n<p>a global cap. If no position limits are configured and the order goes through,</p>\n<p>the <code>EquityMonitor</code> will catch the resulting equity impact.</p>\n\n<p>---</p>\n\n<h4>7. Recommended Safety Configuration</h4>\n\n<p>A conservative <code>flint.yaml</code> for a first-time live deployment:</p>\n\n\n<p><strong>Recommended progression:</strong></p>\n\n<ol>\n<li>Backtest your strategy with realistic fill models and funding.</li>\n<li>Paper trade for at least 1 week with live data.</li>\n<li>Deploy on devnet/testnet with real venue connectivity but fake funds.</li>\n<li>Run <code>--dry-run</code> on mainnet to verify order flow against real conditions.</li>\n<li>Go live on mainnet with conservative position limits and the kill switch enabled.</li>\n</ol>\n",
        "codeBlocks": [
          {
            "language": "bash",
            "code": "flint live --strategy my_strategy.py --venue drift --dry-run"
          },
          {
            "language": "yaml",
            "code": "live_dry_run: true"
          },
          {
            "language": "yaml",
            "code": "# Start on devnet -- switch to mainnet only after validation\nlive_network: devnet\nlive_hyperliquid_network: testnet\n\n# Enable dry-run for mainnet testing (disable when ready for real orders)\nlive_dry_run: true\n\n# Kill switch: flatten everything at 10% drawdown, warn at 5%\nlive_kill_switch_drawdown_pct: 0.10\nlive_drawdown_warning_pct: 0.05\nlive_kill_switch_check_interval_s: 5.0\n\n# Rate limiting\nlive_max_orders_per_minute: 20\nlive_rate_limit_orders_per_sec: 5\nlive_rate_limit_concurrent_tx: 1\n\n# Order failure: halt the strategy (don't silently continue)\nlive_on_order_failure: halt\nlive_max_retries: 3\nlive_limit_order_timeout_bars: 5\n\n# Position limits (adjust to your capital)\nmax_open_trades: 3\nmax_drawdown_pct: 0.15\nlive_per_market_position_limits: \"SOL-PERP:5000,BTC-PERP:10000,ETH-PERP:7500\"\n\n# Notifications (at least one -- you want to know when the kill switch fires)\n# telegram_bot_token: \"your-bot-token\"\n# telegram_chat_id: \"your-chat-id\"\n# discord_webhook_url: \"https://discord.com/api/webhooks/...\""
          }
        ]
      },
      {
        "id": "devnet-testing-guide",
        "title": "Devnet Testing",
        "content": "\n<p>How to validate Flint's execution pipeline against Drift devnet before going live.</p>\n\n<h4>1. Getting a Devnet Wallet</h4>\n\n<p>Generate a new Solana keypair dedicated to devnet testing:</p>\n\n\n<p>Fund it with devnet SOL:</p>\n\n\n<p>Deposit devnet USDC into your Drift account via the Drift devnet UI:</p>\n<p><https://app.drift.trade/?env=devnet></p>\n\n<p>You need at least 100 USDC deposited as collateral to run the validation script.</p>\n\n<h4>2. Setting Up Environment</h4>\n\n<p>Export the required environment variables:</p>\n\n\n<p>Optional overrides (with their defaults):</p>\n\n<table class='docs-table'>\n<tr><th>Variable</th><th>Default</th><th>Description</th></tr>\n<table class='docs-table'>\n<tr><td><code>FLINT_MARKET</code></td><td><code>SOL-PERP</code></td><td>Drift market to trade</td></tr>\n<table class='docs-table'>\n<tr><td><code>FLINT_NUM_BARS</code></td><td><code>30</code></td><td>Number of ticks to run</td></tr>\n<table class='docs-table'>\n<tr><td><code>FLINT_CAPITAL</code></td><td><code>100.0</code></td><td>Starting capital (USDC)</td></tr>\n<table class='docs-table'>\n<tr><td><code>FLINT_TICK_INTERVAL</code></td><td><code>60</code></td><td>Seconds between ticks</td></tr>\n<table class='docs-table'>\n<tr><td><code>FLINT_MIN_FILLS</code></td><td><code>10</code></td><td>Minimum fills for calibration</td></tr>\n</table>\n\n<p>Install dependencies if you haven't already:</p>\n\n\n<h4>3. Running the Validation</h4>\n\n\n<p>The script runs six steps:</p>\n\n<ol>\n<li><strong>Initialize</strong> -- Creates a DuckDB store and configures the strategy (momentum breakout).</li>\n<li><strong>Connect</strong> -- Connects to Drift devnet via driftpy, checks collateral balance.</li>\n<li><strong>Execute</strong> -- Runs the strategy tick loop for <code>NUM_BARS</code> ticks, submitting real orders to devnet.</li>\n<li><strong>Collect</strong> -- Queries all fill data from the store (prices, sizes, fees, tx signatures).</li>\n<li><strong>Calibrate</strong> -- Fits a market impact model (<code>impact_bps = a <em> sigma </em> (Q/ADV)^b</code>) from the fills, if enough data.</li>\n<li><strong>Parity test</strong> -- Replays the same candle data through the backtest engine and paper broker, comparing PnL and fill prices against the live results.</li>\n</ol>\n\n<p>Progress is logged to stdout. The full report is saved to <code>reports/devnet_validation_<timestamp>.json</code>.</p>\n\n<p>Estimated runtime: <code>NUM_BARS * TICK_INTERVAL</code> seconds (default: ~30 minutes).</p>\n\n<h4>4. Reading the Report</h4>\n\n<p>The JSON report contains a <code>steps</code> object. Key metrics to check:</p>\n\n<p><strong>Calibration</strong> (<code>steps.calibration.report</code>):</p>\n\n<table class='docs-table'>\n<tr><th>Metric</th><th>Good</th><th>Concerning</th></tr>\n<table class='docs-table'>\n<tr><td><code>r_squared</code></td><td>> 0.5</td><td>< 0.3</td></tr>\n<table class='docs-table'>\n<tr><td><code>mae_bps</code></td><td>< 5 bps</td><td>> 15 bps</td></tr>\n<table class='docs-table'>\n<tr><td><code>cv_r_squared</code></td><td>> 0.4</td><td>< 0.2</td></tr>\n<table class='docs-table'>\n<tr><td><code>recommended_impact_coeff</code></td><td>Within 2x of current</td><td>> 5x divergence</td></tr>\n</table>\n\n<p><strong>Parity test</strong> (<code>steps.parity.report</code>):</p>\n\n<table class='docs-table'>\n<tr><th>Metric</th><th>Good</th><th>Concerning</th></tr>\n<table class='docs-table'>\n<tr><td><code>pnl_divergence_pct</code></td><td>< 2%</td><td>> 5%</td></tr>\n<table class='docs-table'>\n<tr><td><code>equity_correlation</code></td><td>> 0.95</td><td>< 0.85</td></tr>\n<table class='docs-table'>\n<tr><td><code>fill_price_mae</code></td><td>< $0.10</td><td>> $1.00</td></tr>\n<table class='docs-table'>\n<tr><td><code>trade_count_match</code></td><td><code>true</code></td><td><code>false</code></td></tr>\n</table>\n\n<h4>5. Calibrating from Results</h4>\n\n<p>If the calibration step produced a <code>recommended_impact_coeff</code>, apply it:</p>\n\n\n<p>Then update your <code>flint.yaml</code> or use the CLI:</p>\n\n\n<p>Re-run backtests with the updated coefficient to confirm improvement. Compare backtest results before and after via <code>/api/v1/backtest/compare</code>.</p>\n\n<h4>6. When to Move to Mainnet</h4>\n\n<p>Complete this checklist before deploying with real funds:</p>\n\n<ul>\n<li>[ ] Parity test passes (< 2% PnL divergence)</li>\n<li>[ ] Kill switch tested (triggered and recovered on devnet)</li>\n<li>[ ] Dry-run on mainnet shows expected behavior (<code>dry_run=True</code> in LiveDriftContext)</li>\n<li>[ ] Impact coefficients calibrated from devnet fills (not just defaults)</li>\n<li>[ ] At least 50 fills collected for statistical significance</li>\n<li>[ ] Risk guards configured and tested (max drawdown, position limits, daily loss cap)</li>\n<li>[ ] Notification channels working (fills, errors, risk events)</li>\n<li>[ ] Wallet funded with only the intended capital (no excess)</li>\n</ul>\n\n<p>To do a mainnet dry-run (logs orders but does not submit them):</p>\n\n\n<p>Only proceed to live mainnet execution after all checklist items are confirmed.</p>\n",
        "codeBlocks": [
          {
            "language": "bash",
            "code": "solana-keygen new --outfile ~/.config/solana/devnet.json\nsolana config set --url devnet --keypair ~/.config/solana/devnet.json"
          },
          {
            "language": "bash",
            "code": "solana airdrop 2 --url devnet"
          },
          {
            "language": "bash",
            "code": "# Required: your base58-encoded private key\nexport FLINT_PRIVATE_KEY=$(solana-keygen pubkey ~/.config/solana/devnet.json --outfile /dev/stdout 2>/dev/null)\n# For the private key specifically, extract from your keypair JSON:\nexport FLINT_PRIVATE_KEY=$(python3 -c \"\nimport json, base58\nwith open('$HOME/.config/solana/devnet.json') as f:\n    key_bytes = bytes(json.load(f))\nprint(base58.b58encode(key_bytes).decode())\n\")\n\n# Required: RPC endpoint\nexport FLINT_RPC_URL=https://api.devnet.solana.com"
          },
          {
            "language": "bash",
            "code": "pip install -e .\npip install driftpy"
          },
          {
            "language": "bash",
            "code": "python examples/devnet_validation.py"
          },
          {
            "language": "bash",
            "code": "# View the recommendation\npython -c \"\nimport json\nwith open('reports/devnet_validation_<timestamp>.json') as f:\n    r = json.load(f)\ncal = r['steps']['calibration']['report']\nprint(f'Current:     {cal[\\\"current_impact_coeff\\\"]:.6f}')\nprint(f'Recommended: {cal[\\\"recommended_impact_coeff\\\"]:.6f}')\n\""
          },
          {
            "language": "bash",
            "code": "flint calibrate --venue drift --market SOL-PERP --lookback-days 30"
          },
          {
            "language": "bash",
            "code": "export FLINT_RPC_URL=https://api.mainnet-beta.solana.com\nexport FLINT_ALLOW_MAINNET=true\n# Then in your strategy runner, pass dry_run=True to LiveDriftContext"
          }
        ]
      }
    ]
  }
]
